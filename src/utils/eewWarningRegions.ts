// 緊急地震速報の警報対象地方の列挙。
//
// **`eew.ts` から分けてあるのは、外部依存を持たせないため。** あちらは React のフック
// （`hooks/usePsWaveCalc`）や通信層（`services/kyoshin`）を引くので Node から import できない。
// 読み上げのアクセントを監査するスクリプト（`scripts/audit-accent-unidic.ts`）はこの列挙を
// 対象語に含める必要があり、同じ場所に置くと列挙を実装から取れず、字面を書き写すことになる
// （そうすると気象庁が地方予報区を増やしたとき、監査だけが古い集合で回り続ける）。
// 助詞の列挙（`ttsTrailingParticles.ts`）を分けてあるのと同じ理由。

/**
 * 地方予報区の標準順（気象庁のコード 9910〜9960 の並び。おおむね北から南）。
 *
 * **電文の文書順に依存しない。** 実配信では同じ地震の続報で並びが入れ替わる
 * （2024-01-01 能登本震で「北陸 / 甲信 / 東海 / 関東 / 東北 / 近畿」→「… / 東北 / 関東 / …」）。
 * 気象庁が `Item` を「既に警報だった地方」「新たに加わった地方」の 2 つへ分けるため、
 * 文書順は「いつ警報対象になったか」を反映していて、地理的な並びではない。
 *
 * **地図の地方名ラベル（`utils/regions.ts`）とは別物。** あちらは一般の地方区分で、
 * 中部がひとまとめ・島嶼を持たない。名前が重なるものがあっても流用しないこと。
 */
export const EEW_WARNING_REGION_ORDER: readonly string[] = [
  '北海道', '東北', '関東', '伊豆諸島', '小笠原', '北陸', '甲信',
  '東海', '近畿', '中国', '四国', '九州', '奄美', '沖縄',
]
