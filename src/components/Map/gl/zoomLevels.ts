// 複数レイヤーで共有する表示ズーム閾値。
// 各レイヤー固有の閾値（地名ラベルの粒度切替など）は各コンポーネント側に置く。

/**
 * 国内の細かい線（活断層・県境・一次細分区域の境界）を描画する下限ズーム。
 *
 * 遠地地震を選ぶとカメラは震源と日本全体の両方を収めるため世界規模まで引く（実測 zoom 1.9 前後）。
 * その倍率ではこれらの線が潰れ、日本列島が塗り潰された塊にしか見えなくなるため描画しない。
 * 陸地の塗り（`land-fill`）とプレート境界は閾値の対象外で、引いた画でも列島の位置は分かる。
 *
 * 3.5 という値は「通常の日本全体表示では従来どおり描画される」ことを基準に決めた
 * （日本全体フィットのズームは地図の幅で決まり、スマホ幅 375px で約 3.8・デスクトップで約 5.0）。
 */
export const DETAIL_MIN_ZOOM = 3.5
