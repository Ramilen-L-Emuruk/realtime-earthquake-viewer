# 地図描画 MapLibre 一本化 移行 進め方ドキュメント

> **【アーカイブ】本ドキュメントは 2026-07 の MapLibre 移行 PoC 期の記録。**
> 実装本体（MapLibre 一本化・feature-state 差分更新・カスタムレイヤー FBO 二層合成・EEW 予報円 GL）は
> リリース済み（v4.x）。§8「テキスト描画」の「起動時ウォーム採用・実機確認済み」および SW `**/*.pbf`
> precache は方針として決着したが **実装コードには存在しない**（対応する PoC コードは
> `worktree/feat/webgl-poc` ブランチに残り、`main` には未マージ）。実機で問題が顕在化した場合は本節の
> 議論と実測データを参照して再着手する。最新のランタイム仕様は `docs/spec/map-rendering-spec.md` を参照。

> 対象コード: `src/App.tsx`, `src/components/Map/`（全レイヤー）, `src/hooks/useKyoshinRealtime.ts` ほか, `scripts/build-*.mjs`
> 最終更新: 2026-07-25
> ステータス: **段階0 完了**（`KyoshinPoints` 差分更新化・開発機/実機（Surface Go 2）計測完了）／**層 B・検証項目1（律速切り分け）決着**（実機3回計測。塗り面積 4 倍・線幅 10 倍まで上げても vsync 天井を破らず＝**律速に到達しない**。間引き廃止は描画コストの観点で青信号。§6 結果記録）／**検証項目5（非加算合成・最難関）クリア**（カスタムレイヤーの FBO 二層合成で現行と等価に再現。実機で 59〜60fps・longtask 0・追加コストは測定限界以下）／**検証項目6（毎秒更新）クリア**（実機3回計測。本番相当ベースマップ 40,917 頂点＋観測点 1,725 の毎秒全点更新でも `repaint-only` の収束 6.6〜12ms・`feature-state` 16〜36ms・実機目視でカクつきなし。本設計は `feature-state` 採用。§6 結果記録）／**検証項目5×6 の交差点（カスタムレイヤーの毎秒更新）クリア**（実機 ①1,725点3回・③17,250点2回＝更新モード計15証跡は fps 59.5〜60.0（baseline 5証跡はウォームアップ残りで58.25〜59.92・モード比較には使わない）・全20証跡で longtask 0・`settleTimeouts` 0（該当するもの）。**インデックスバッファ方式を採用**＝素朴実装と描画結果はピクセル差 0・更新 CPU は 2〜5 倍軽い。副産物として **`KyoshinSubThreshold` の再描画が標準 circle の 1.3〜2.7 倍重い**ことが判明。§6 結果記録・[レビュー綴り](webgl-poc-review-b5d8bae.md)）。／**検証項目7（EEW 複合負荷・最悪局面）クリア**（実機2スイート＝8証跡。`flyTo` 多段＋予報円 100ms 更新＋強震点毎秒更新＋`setData` を同時に走らせても **fps 59.9〜60.0・longtask 0・`flyTo` 区間 p95 17.5〜19.5ms**。**唯一恐れていた `setData` の 356ms は刺さらなかった**——実際に 67.2〜547.2ms の収束が起きたが `apply` は 0〜0.9ms で、worker 実行のためメインスレッドを止めない。副産物として **本番の緩和策「`flyTo` 中は予報円を止める」は MapLibre では不要**と判明。§6 結果記録・[レビュー綴り](webgl-poc-review-b4524cd.md)）。**これで移行判断の主要項目（律速・非加算合成・毎秒更新・交差点・複合負荷）が全て通った。** ／**検証項目2（Leaflet 比較）決着 — 一本化は GO**（実機・**両エンジンを同一セッションで計測し vsync 天井 16.7ms の一致を証跡で確認**・82証跡。毎フレーム全画面再描画で **Leaflet 150.0ms（9フレーム）対 MapLibre 16.7ms（1フレーム）＝ 9 倍以上**（MapLibre 側は飽和のため下限）。**間引き廃止のコストは Leaflet +2フレーム／MapLibre ±0** ＝ 本プロジェクトの出発点だったフル解像度化が MapLibre なら無償。**頂点数を 1/3 にすると Leaflet だけ 2 フレーム軽くなり MapLibre は動かない**＝ジオメトリ量がメインスレッドに現れるかどうかが両者の分かれ目（機序は未測定・§6 の注記参照）。§6 結果記録・[レビュー綴り](webgl-poc-review-82d4d39.md)）。**これで移行判断の全項目（律速・非加算合成・毎秒更新・交差点・複合負荷・Leaflet 比較）が通った。GO/NO-GO は GO。** ／**検証項目4（当たり判定）決着**（`poc/hittest.ts` で bbox 方式を採用し開発機検証。r=8px でオフセット8px以内=必ずヒット・9px以上=必ず非ヒットの境界一致をzoom5/8/11の3水準で確認、遠方点は誤検知なし。本番tolerance:8相当としてr=8採用。フレーム性能ではなく判定ロジック検証のため実機計測は不要と判断）。／**テキスト描画（§8）描画は可能・ただし実機でコスト問題が顕在化**（`poc/label.ts` で glyphs 未設定・`text-font` に OS日本語フォント名指定のみの構成を検証。地方/県/区域名（漢字5〜7文字含む）・震度ラベル("5弱"等の漢字混じり含む)の全パターンで正しく描画されることを開発機で確認・日本語グリフPBFの自前生成は不要と判明。**しかし実機Surface Go2でグリフ初回生成が blockMaxMs 最大262.7ms・longtask常発と判明——現行DOMラベルには無い新規コストで、地震検知時の自動ズームと重なる最悪局面がありうる。「描ける」は正しいが「解消」ではなく本設計で対処方式(起動時事前生成等)を決める要対応事項。§8末尾参照**）。／**モバイル（§6）はユーザー判断によりバックグラウンド復帰判別の事前PoC検証を行わず、リリース後に問題が発覚してから対応する方針に確定**（描画互換性は一次確認済み・GO/NO-GOには含めない）。／**実行時メモリ推定ツール（§8）を開発機検証**（`poc/webglMemoryTracker.ts` でWebGLコンテキストをプロキシしbufferData/texStorage2D等から確保量を推定。DPR軸1.0→2.0でテクスチャ推定値が約1.89倍（DPR¹に近い線形）で単調に反応することを確認、`poc/main.ts`のstat・計測レポートに統合。セルフレビュー対応でcontext lost時のリセット処理も追加）。／**レビュー(8c75e49)対応**（HIGH1: 当たり判定の「境界一致」が円vs正方形の差が出ない軸だけの検証だった問題をナローフェーズ導入で解消。HIGH2: カメラ計測にvsync実測が欠落していた問題を`__runCameraSuite`先頭のstatic計測で解消。MEDIUM: テキスト描画のグリフ生成コスト計測フックを追加、実機計測は残作業。LOW: webglMemoryTrackerに`tracked`フラグ追加）。／**レビュー(4ecdb6e)でHIGH1・HIGH2・LOWは対応確認済み（HIGH1は独立検証で決着）だが、MEDIUM対応（ラベルのグリフ計測）に新規HIGHを指摘**——ズーム帯切替方式では視野が狭まり県47件・区域192件が一度も描画されず、全ズーム帯でグリフ生成数が40種のまま不変だった（`longtask0`は「安い」でなく「生成していない」の誤読）。日本全体のビューを固定し`visibility`切替＋`renderedSymbols`/`glyphsGenerated`を結果に含める方式に作り直した。／**セルフレビューで、intensity-labelが常時表示（initial visible）だったため`load`時点で既にグリフ生成済みという同型の事故（4番目のデータ点が汚染）を追加発見・解消**（初期`visibility:'none'`＋手動トグルボタンに変更、glyphManagerアクセスの不要なunknownキャストも除去）。最終的にグリフ生成数が`15→84→209→218`と全4段で単調増加することを確認。／**レビュー(f5d8de5)が新規HIGHを指摘: `elapsedMs`がグリフ生成コストに無反応**（生成量が60倍以上変わっても差0.7ms・約612msで一定。フレーム時間`max`には明確に出る=6.3ms→17.6ms）。`measureLabelLayer`にrAFフレーム計測（`measure.ts`の`summarizeFrames`を共有）を追加し`frame`（p50/p95/max）を結果に含めて解消。副産物として**開発機の実vsyncは16.7ms(60Hz)ではなく6.1ms(≒164Hz)と判明**（Playwright/Chromiumのheadless実行はvsync16.7ms固定になるため、本セッションの計測値と実際のChromeでの値が異なっていた。実機計測はブラウザ直接操作のため影響なし）。 →**【2026-07-27 訂正・§8末尾／レビュー(3598d26)参照】** この副産物の記述は誤り。本セッションの Playwright は **headed**（`webdriver` false・UA に Headless なし）で、16.7ms は Playwright が固定した値ではなく実測値。レビュー機（6.1ms、後の追加検証では5.9ms）も headed 申告。**双方 headed にも関わらず vsync が一致しない原因は未特定**（別モニタか別実行コンテキストか他要因かは未検証。「機差」と断定していたのも確かめずに名付けた誤り）。グリフ生成コストは vsync 非依存の `blockMaxMs`（`measureLabelLayer` に新設）で計測すれば **60Hz 開発機でも subregions≈20〜24ms として σ~0.5ms で再現性高く検出できる**ことを確認した（「開発機では検出できない」は計測器の誤選択だった。両環境でほぼ同値を返すため vsync 差の原因を追う必要もない）。（各検証項目の状態は上記「検証項目 状態一覧」を正とする。以下は当時の残作業メモ）次は実装上の詰め: **①項目3 カメラ操作＝実機計測済みだが Leaflet 側カメラ計測が無く判定保留（比較対象の作成が残作業）②真の実行時メモリ＝実機で WebGL 推定は取得したが VRAM 観測不可・本番比較の定義から要再検討で未完③本番ビルドでの実機再計測（層 A 残差の実寸）** ／**【2026-07-28 追加決着】検証項目3（カメラ）は MapLibre 優位で決着**（両エンジン `-v2` 実機再計測68件。着地コスト Leaflet 188ms 対 MapLibre 36ms・5.2倍／longtask 32/32 対 7/32。第1回「Leaflet優位」は fps が着地を捉えず誤判定していたのを反転。§6検証項目3）。／**本番アプリの移動中ベースラインを実機取得**（`measure-moving-baseline.js`。[webgl-production-baseline-2026-07-27.md](webgl-production-baseline-2026-07-27.md)）：静止は健全（fps57・DOM31/秒）だが**移動中は fps13〜25・blockMaxMs最大1,173ms・DOM書き込みが31→12,563/秒(405倍)＝段階0の差分更新は移動中に効かない**（動くと全点座標が変わり全点書き換えに戻る・依頼§1④仮説を実測確認）。主犯は静的パス再描画＋観測点1725の二つで毎秒更新対策だけでは届かず、**「移行せずに直せる余地は静止時より小さい」＝GO を裏付け**。**【計測環境注記】本日の実機計測2本（項目3・移動中）は dev サーバー（`vite dev`）取得で本番ビルドではない**（React development モードは production より重い）。相対結論（項目3の優劣・段階0が移動中に効かない・主犯の比率・domWrites 405倍＝アプリのコードが発行する回数でビルドモード非依存）は不変だが、**絶対 fps 値（13〜25 等）は本番ビルドで改善しうる（未検証・残作業D＝本番ビルド実機再計測）**。／**【2026-07-28 残作業D 決着】本番ビルド（`build:dmdss`→`preview:dmdss`）で実機再計測完了**（[結果記録](webgl-production-build-measure-result-2026-07-27.md)。証跡12件・`swControlled:false`＝実機はLAN経由HTTPでセキュアコンテキスト外のためSW非介入、計測の本旨には非影響）。**核心（段階0が移動中に効かない）は本番ビルドでも不変**（DOM書き込みは pan/zoom とも静止時比200〜500倍の桁のまま）。**改善度は区間で大きく異なる**：pan は dev の過大評価が判明（fps 約2倍・`blockMaxMs`約2.5〜3倍改善）が、**zoom はほぼ改善せず**（fps 13〜20→15〜21・`blockMaxMs` 590〜910ms→540〜653ms）＝**ズームの重さは dev アーティファクトでなくアプリの実コストそのもの**と判明。これは検証項目3（MapLibre着地コスト5.2倍優位）と符合し、**一本化の必要性を追加で裏付け**。残作業Dはこれで完了。
> 方針: **地図描画を MapLibre GL JS に一本化する全面リライト**（Leaflet を廃し、地図基盤・オーバーレイ・当たり判定を単一の GPU エンジンへ統一）

### 検証項目 状態一覧（**状態はこの表を唯一の正とする**・2026-07-27 更新）

> 各項目の詳細な数値・経緯は §6「結果記録」に集約し、状態（済／未・判定）はこの表だけを更新する。§2.4／§5 の進捗メモや各節の要約に状態を重複記載しない（同じ事実が複数箇所に散り、結果が出ても一部が「未実施」のまま取り残される事故を防ぐため）。証跡は `scripts/perf/results/`。

| 検証項目 | 開発機 | 実機(Surface Go 2) | 判定 |
|---|---|---|---|
| 段階0（層A 差分更新） | 済 | 済 | **完了**（v3.23.1 リリース済） |
| 1 律速切り分け | 済 | 済（19計測） | **決着**＝律速に到達しない |
| 2 Leaflet 比較 | 済 | 済（`leafletB` 44＋`layerB-vs` 38） | **決着**＝一本化 GO |
| 3 カメラ操作 | 済 | 両エンジン `-v2` 再計測済（各34件・新実装） | **決着＝MapLibre 優位**（着地コスト Leaflet 188ms 対 MapLibre 36ms・5.2倍／longtask 32/32 対 7/32。前回「Leaflet優位」を反転） |
| 4 当たり判定 | 済（bbox＋ナロー r=8） | 不要 | **決着** |
| 5 非加算合成 | 済 | 済（`sub-` 9） | **決着**＝FBO 二層合成で等価 |
| 6 毎秒更新 | 済 | 済（`rt` 12） | **決着**＝feature-state 採用 |
| 5×6 交差点 | 済 | 済（`subrt` 20） | **決着**＝インデックスバッファ方式 |
| 7 EEW 複合負荷 | 済 | 済（`eew` 8） | **決着**＝天井内 |
| §8 テキスト描画 | 済 | 済（`label` 4＋B案A/B各1＋ウォーム検証2） | **決着（B案＋起動時ウォーム採用・実機確認済み）**＝県47は起動時ウォームで12.7倍改善しlongtask消滅。区域192はウォームでlongtaskは消えるが`blockMaxMs`63.8ms残存し追加緩和策の検討余地あり。区域coldの計測条件に未解決の論点も残る。[結果](webgl-glyph-warm-mitigation-surface-go2-2026-07-27.md) |
| §8 実行時メモリ | 済（推定ツール＋安定性観測ハーネス） | WebGL 推定（`memory` 1）＋**安定性観測 決着（PASS・実機30分完走）** | **決着（PASS）**＝実機（Surface Go 2）で `__runStabilitySuite` 30分完走・`contextLost:0`・メモリは初期立ち上がり後プラトー（41〜44MB帯で振動）。§8末尾／[結果記録](webgl-poc-surface-go2-stability-2026-07-27.md)参照（2026-07-28） |
| モバイル | 描画一次確認 | — | リリース後対応（GO/NO-GO 外） |

**総合: GO/NO-GO は GO。** §8 実行時メモリの安定性観測は PASS で決着（2026-07-28）。本番ビルドでの実機再計測（残作業D）も決着（2026-07-28・pan は dev の過大評価と判明、**zoom はアプリの実コストで一本化の必要性を追加で裏付け**）。実機待りの項目は無し。§8 テキスト描画は要対処だが方式選択で回避可能なため GO は不変。

本ドキュメントは「非力な PC で地図描画が重い」問題に対し、描画基盤を MapLibre GL JS へ一本化する取り組みの調査結果・方式判断・保つべき仕様・進め方を記録する。実装はまだ行っていない。進め方は **「まず問題を二層（静止時負荷／フル解像度負荷）に分解 → 安価な中間改善（層 A の差分更新化）と律速（CPU / GPU フィルレート）の切り分けを先に実測 → その結果で全面リライトの是非を確定 → PoC で残る致命的リスクを潰す → 本設計」** の順とする（詳細は §2.4・§6）。律速がフィルレートだと判明した場合は、間引き廃止を保留し DPR 抑制を軸に対策を組み替える分岐を持つ（一本化の是非は DPR 抑制の実装手段の観点から再判断する。§6「フィルレート律速だった場合の分岐」）。

## 目次

1. 背景と目的
2. 現状分析（調査結果）
3. 方式判断の経緯
4. 採用アーキテクチャ（MapLibre 一本化）
5. 保つべき挙動（回帰防止チェックリスト）
6. PoC 計画（律速分解を軸に）
7. PoC 後の本設計・移行戦略
8. リスクと未確定事項
9. 付録: 移行対象レイヤーと参照コード

---

## 1. 背景と目的

- 非力な PC でアプリの動作が重いという報告がある。
- 負荷軽減のため、**ビルド時に地図・活断層などの頂点を間引いている**（`scripts/build-*.mjs`）。本来はフル解像度で表示したいが、描画負荷のために削らざるを得ない状況。
- **目標**: 地図描画を GPU 基盤へ一本化し、間引きをやめてフル解像度で表示しても軽く動くようにする。見た目・挙動は現行と完全に同一を維持する。
- **一本化の意図**: 「Leaflet(CPU 土台) + WebGL オーバーレイ」の二重構造は、両者の投影・アニメを毎フレーム同期させる弱点を抱える。中途半端な二重管理を避け、地図基盤ごと単一の GPU エンジン（MapLibre GL JS）へ統一する。

## 2. 現状分析（調査結果）

### 2.1 実測（Playwright + Performance 計測）

計測環境: dev（DMDSS 版）、`devicePixelRatio=1`、viewport 1837×965。**このマシンは非力ではない**点に留意（＝定常コストが 60fps に吸収されて見えている）。

| 局面 | 平均 FPS | ロングタスク | DOM 属性更新 | メモリ |
|---|---|---|---|---|
| リアルタイムモード・静止 12 秒 | 59.2（最低 56） | 0 件 | **527 回/秒**（追加・削除 0） | 63.9→66.5MB（安定） |
| 設定タブ・静止（点描画なし） | 60 | 0 件 | 0 | 安定 |

- **静止時の定常コスト**として毎秒 527 回の属性更新が発生。高性能機では 60fps に吸収されるが、非力機では体感の重さに直結すると考えられる。
- **【注意】「527 回/秒」は計測手順が未記録**（何をどうカウントしたか不明）で、このままでは再現・比較に使えない。桁の検算にも疑義がある: Leaflet の SVG レンダラは `setStyle` で `fill`/`fill-opacity`/`fill-rule`/`stroke`、`setRadius` で `d` を毎回 `setAttribute` するため、約1,725点の無条件全点更新なら素朴には数千回/秒の属性更新になるはず。「主因 = `KyoshinPoints` の無条件全点更新」という因果づけ自体は §2.2 のコード読解と整合するが、数値は鵜呑みにせず、段階0 の再計測（§6）で計測スクリプトと数え方を記録した上で改修前（before）から測り直す。
  - **【2026-07-25 再計測済み】実際は約 10,370 回/秒**（1,725 点 × 6 属性/秒）で「527 回/秒」は桁誤りだった。属性 6 の内訳と before/after の全数値は §6「結果記録」参照。
- **CPU スロットル検証（6 倍）＋ 自動マウスドラッグ ＋ CDP セッション**の三重掛けでレンダラがクラッシュ。これは**計測器側の事故の公算が高く、アプリの欠陥とは断定しない**。PoC では倍率を 4 倍程度に緩め、操作も穏やかにする。
- **パン/ズーム操作時の実測は未取得**（上記クラッシュで中断）。PoC で取得する。

### 2.2 描画方式の現状

**強震モニタの点（`mode === 'kyoshin'`）** — 4 レイヤーすべて共有ペイン `kyoshin-points`・`interactive:false`・`pointerEvents:none`（クリック・ホバー不使用）:

| レイヤー | 対象 | 実装 | 毎秒の更新 |
|---|---|---|---|
| `KyoshinPoints` | 震度1以上（index 7+） | `L.circleMarker` 約1725点・SVG レンダラ | **全点を無条件に `setRadius`+`setStyle`（差分なし）** |
| `KyoshinSubThreshold` | 震度0以下（index 1-6） | 生 SVG `<circle>`・レベル別 `<g opacity>` で非加算合成 | **変化した点だけ**グループ付け替え（差分更新済み） |
| `KyoshinDetectedPoints` | 確定検知点 | 生 SVG・数十点 | 毎回フル作り直し（点数が少なく軽い） |
| `KyoshinMaxEffect` | 最大震度の波紋 | 生 SVG `<circle>`・rAF | 同時 0〜1 個の短命アニメ（600ms） |

- **SVG を使う理由（コード内コメント）**: 過去に canvas 実装で flyTo/ズームアニメ中に巨大バッファの再合成が重く撤退した経緯があり、SVG＋`transform` 追従に移行済み。
- 毎秒の定常コストの主因は `KyoshinPoints` の無条件全点更新（他レイヤーは差分更新・少数で軽い）。

**活断層（`ActiveFaultsLayer`）**:

- 約 580 セグメント（3,400 本超のライン）を `L.polyline`（SVG レンダラ）で描画。
- **クリックでポップアップ（地名表示）を持つ** — 可視線は `interactive:false`、当たり判定は同一形状の透明 `L.polyline`（Canvas レンダラ・tolerance 付き）に `bindPopup` して委譲。線から数 px ずれてもポップアップが開く。
- マウント時に 1 度だけ Leaflet レイヤーを構築し、以降は表示/非表示の切替のみ。

**ベースマップ（`BaseMap` / `JapanMap`）**: 背景ラスタータイル（`tile-pane` に img。GPU 合成され元々軽い）、県境・一次細分区域・海岸線・ラベル。詳細は本設計時に精査する。

### 2.3 間引きの実態と理由

- ビルド時に Douglas-Peucker で頂点を削減: `build-active-faults.mjs`（`EPSILON=0.002` 度 ≒ 220m）、`build-prefectures.mjs`、`build-subregions.mjs`、`build-tsunami-zones.mjs`。
- **理由は「描画負荷」**（データ量・転送ではない）。`ActiveFaultsLayer` のコメント（毎秒再レンダーで全本数の差分計算が重い → レイヤー数・頂点数を削減）が根拠。`build-prefectures.mjs` にも「拡大時の崩れを抑えるため詳細を残す」とあり、負荷制約下で本意でなく削っていることが読み取れる。
- 生成 JSON（`public/data/*.json`）は初回 1 回の fetch/parse のみで、継続的な負荷ではない。
- **結論**: 描画負荷が理由である以上、GPU 描画に移せば間引きをやめられる見込みが高い。

#### 【2026-07-28 追記】間引き撤廃の実施と、一次細分区域の高精細化（調査完了・高精細版 `_1` を採用）

- **間引き撤廃を実施**（MapLibre 一本化後）: `build-subregions.mjs` / `build-prefectures.mjs` の `EPSILON` を `0` にし、Douglas-Peucker を撤廃（全頂点保持・座標は小数4桁 ≈11m 丸めのみ）。§2.3 の結論「GPU 化で間引きをやめられる」を実行に移したもの。
  - 実測: 一次細分区域 16,029→21,058 頂点（+31%・314KB→408KB）、都道府県 12,444→15,570 頂点（+25%・237KB→296KB）。**描画性能は不変**（PoC の「フル解像度化は実質無償」を裏付け）。生成 JSON は遅延読込のため、増えるのはダウンロード量のみ（計 +約150KB）。
- **判明した律速**: 撤廃後もズーム 10.5 で海岸線・区域境界が角張ったまま。**律速は間引き（DP 220m）ではなく、元データ（気象庁 予報区等 GIS・[Ichihai1415/JMA-GIS-GeoJSON](https://github.com/Ichihai1415/JMA-GIS-GeoJSON)）自体が generalized な行政界で解像度が粗いこと**。DP はソースの約 24% を削っていただけで、真の律速はソースの粗さだった。
- **調査タスク（2026-07-28 着手・進行中）**: 一次細分区域について、より高精細な境界データが **(a) 既存で入手可能か**、**(b) 作成できないか** を調査する。
  - **必須制約**: 境界は気象庁の公式「予報区（一次細分区域）」定義と一致していなければならない（アプリが震度をこの区域コード単位で集約し、区域名ラベルも同一定義に依存するため）。汎用の高精細海岸線（国土数値情報・Natural Earth 等）への単純差し替えは不可。
  - 調査対象の候補: ① 予報区 GIS のより高精細な版・別ソースの有無 ② 予報区ポリゴンを高精細海岸線でスナップ/クリップして境界を精緻化する生成手法 ③ 陸シルエットのみ高精細海岸線で描き、震度集約は従来ポリゴンで行う二層化。
- **調査結果（2026-07-28 完了）**: 候補① で決着。現ソース Ichihai1415/JMA-GIS-GeoJSON は**簡素化率違いの2版**を提供しており（`_01`＝きつい簡素化・約865KB / `_1`＝緩い簡素化・約5.6MB＝6.5倍）、**従来は `_01` を使っていた**。`_1` は同じ 194 feature・properties(`code`/`name`/`namekana`)で**区域定義・区域コードが不変**（震度集約・区域名ラベルはコード改変なしで動く）＝**ドロップイン**。原始頂点は一次細分区域 142,787。②③（自前スナップ生成・二層化）は不要と判断。
- **採用（2026-07-28）**: ソース URL を `_01`→`_1` に切替え、EPSILON=0（撤廃・全頂点保持）で再生成。一次細分区域 142,471頂点・都道府県 115,778頂点。三陸リアス海岸をズーム10.5で実機確認し、角張りが解消してなめらかに描画されることを確認。
  - **サイズ**: 基図計 非圧縮 ~4.64MB（区域2.56/県2.08）だが**本番 gzip 配信で ~1.22MB**（座標JSONは約26%に圧縮）。遅延読込（初回の地図表示時1回）＋SW キャッシュのため、増えるのは初回転送のみで地震時のダウンロードは0。描画性能は WebGL で不変。現代回線で許容と判断し採用。
    - 注: 都道府県の陸塗りが海岸線シルエットを作るため、区域だけでなく都道府県も同時に `_1` 高精細化した（片方だけだと海岸線がズレて見える）。
- **県境と細分区分のズレ解消（2026-07-28）**: 高精細化後、極拡大で県境（`prefectures.json`）と細分区分（`subregions.json`）の共有境界が数十m ずれて二重線に見える問題が判明。原因は両者が**別々の気象庁 GIS データを独立に digitize/簡素化したもの**で、共有すべき境界の頂点が一致しないこと（すべての県境は細分区分の境界でもあるのに別ソース）。
  - **解法（単一ソース化）**: `build-prefectures.mjs` を「府県予報区ポリゴンから直接生成」→「**一次細分区域を県ごとに融合(dissolve)して県ポリゴンを作る**」方式に変更。県境が細分区分と同一頂点を共有するためズレが原理的に消える。区域→県の割当は府県予報区ポリゴンとの**最大重複（mapshaper largest-overlap・devDep 追加）**で機械的に行い、コード対応表は不要。府県予報区ポリゴンに重ならない区域は最近傍県へフォールバック（geometry を持たない岩礁＝鷹島(甑島南方)等は描画対象外で除外）。
  - **結果**: 県 47・頂点 115,778→**97,562**（内部共有エッジ除去でむしろ削減・2.18MB→1.84MB）。**県境頂点の 97.15% が細分区分頂点と完全一致**（融合前はほぼ0%）、残り 2.85% も融合スナップ＋11m 丸め差でサブ11m＝視覚上一致。ズーム13.5 で県境が単線化することを実機確認。
- **残りの線も間引き撤廃（2026-07-28）**: 基図境界に続き、他の線データも `EPSILON=0` に撤廃してフル解像度化した。
  - **活断層線**（`build-active-faults.mjs`・産総研 活断層DB）: 31,436頂点（gzip 0.17MB）。近畿の密集域をズーム11で滑らかに描画確認。
  - **津波海岸線**（`build-tsunami-zones.mjs`・気象庁 津波予報区）: ソースは既に高精細版 `_1` だったため撤廃のみで最高精細に。86,846頂点（gzip 0.28MB）。大警報テストで三陸リアス海岸が高精細描画されることを確認。
  - **プレート境界線**（PB2002/fraxen）: 間引きは元々なし。原典が粗い全球モデルのため、これがソースの上限（別データに替えない限り高精細化不可・据え置き）。
  - 津波海岸線は細分区分とは別の気象庁データセットのため、細分区分の海岸線とは極拡大でわずかにズレうるが、津波発報時のみ太い等級色で描く event 用のため実害小（厳密一致まで求めるなら別途 dissolve 的処理が必要）。
- **高精細化後の描画性能の実機計測ハーネス（2026-07-28）**: 上記の高精細化で地図上の主要な線・塗りの総頂点数が
  約 358,000（一次細分区域142,471＋都道府県97,562＋活断層31,436＋津波86,846）＝**PoC 実機検証基準の 40,917 頂点の
  約8.75倍**になった（レビュー `docs/webgl-migration-review-89188bd.md` の MEDIUM 指摘）。PoC 項目1 は合成スケーリング
  （塗り面積4倍・線幅10倍）で律速不到達を確認したもので、実ジオメトリの生頂点増（頂点シェーダ・ライン結合数）は
  性質が異なりうるため、**非力機（Surface Go 2）での実機計測で裏取りする**。
  - ツール: `scripts/perf/measure-app-render.js`（PoC `poc/measure.ts` の実証済みプリミティブを実アプリ向けに移植）。
    実アプリの `window.__mapGL` を駆動し 6 シナリオを計測: static/pan-maxzoom/zoom を quake・kyoshin 各モードで、
    加えて **`maxload-eew`（防災アプリ最悪ケース）＝リアルタイム稼働中に EEW 特別警報テストを発火し、予想震度塗り＋
    震源＋カメラ flyTo＋毎秒更新＋高精細ベースマップを同時に走らせる**。
  - 主指標は **blockMaxMs（MessageChannel ping-pong の vsync 非依存ブロック検出）**——開発機は vsync 天井に張り付き
    frame では差が出ないが、非力機の「カクッと引っかかる」実コストはこれで捉えられる（PoC の教訓）。fps は実 elapsed 割り、
    各シナリオに `detect`（queryRenderedFeatures 数）を持たせ「狙った負荷を実際に踏んだか」を結果自身で保証する。
  - 実機運用: `npm run dev:dmdss -- --host` → 実機ブラウザで DevTools から1行注入
    `document.head.appendChild(Object.assign(document.createElement('script'),{src:'/__perf-script?file=measure-app-render.js&auto=1&label=surface-go2-hires'}))`
    → 全シナリオ自動計測 → `scripts/perf/results/` へ証跡保存（perf-report プラグインを `?file=` で新スクリプト配信可に拡張）。
  - 開発機スモークテスト済み（detect フラグで負荷計上を確認・`maxload-eew` が blockMax 最大＝設計通り）。
  - **実機計測結果（2026-07-28・Surface Go 2・n=3）**: `docs/webgl-migration-hires-perf-surface-go2-2026-07-28.md`。
    静止/パン/ズームの5シナリオは温まると健全（blockMax 41〜113ms）だが、**`maxload-eew`（防災最悪ケース）だけ
    3回とも fps17〜19・longtask 計約2秒（12秒の15〜21%）と劣化が再現**＝EEW 発報時特有の負荷。PoC 検証項目7
    （fps59.9〜60・longtask 0）からの実質的な劣化として **HIGH で確認**された（頂点数8.75倍増の懸念が実機で顕在化）。
  - **原因切り分けのため分離シナリオを追加（`flyto-widezoom-quake`＝広ズームのカメラ×高精細ベース単体／
    `eew-static`＝EEW 複合をカメラ静止で＝毎秒更新+波+EEW塗り×ベースの持続コスト単体）＋`detectMid`（窓の途中でも
    detect を採り、終端の海上ビューで全0になる問題を解消）**。次の実機計測で「ベース×広ズーム×カメラ」か
    「EEW 複合の持続再描画」かの支配源を確定し、盲目的な対処を避けてから狙って直す方針（レビュー
    `webgl-migration-review-89188bd.md` の警告に沿う）。

### 2.4 問題の二層構造（重要な前提）

現状の「重さ」は性質の異なる 2 つの負荷に分解できる。方式判断と PoC 設計はこの分解を前提にする。**両者を分けずに測ると、全面リライトの効果を過大評価する。**

| 層 | 症状 | 主因 | 解の粒度 |
|---|---|---|---|
| **A. 静止時の定常負荷** | 無操作でも毎秒約 10,370 回（2026-07-25 再計測・§6 結果記録）の DOM 属性更新 | `KyoshinPoints` の無条件全点 `setRadius`+`setStyle`（差分なし）。ほかに毎秒の React 再レンダー（第二成分・§6 結果記録） | **安価** — Leaflet のまま差分更新化。隣の `KyoshinSubThreshold` が既に実装済みの方式を移植するだけ（数十行）→ **実施済み（§6 結果記録）** |
| **B. フル解像度の線/面の描画負荷** | 間引きをやめると線・面が増え重くなる | 多頂点の SVG ペイント | **高価** — GPU 化（＝MapLibre）が要る。ただし律速がフィルレートなら GPU 化でも解決しない恐れ（§8） |

- **層 A は MapLibre 一本化を待たずに単独で潰せる。** 全面リライトの投資判断の前に、まず層 A を差分更新化して実機で測り、「目的（非力機の軽量化）の何割が層 A の解消で埋まるか」を把握する（→ §6 段階0）。
- **層 B こそが MapLibre 一本化の本来の動機。** ただし層 B の律速が「頂点処理・ドローコール」なら GPU 化で効くが、「フィルレート（画面を塗るピクセル速度）」なら描画 API を変えても効かない。間引き（Douglas-Peucker）は形状保存のため、廃止で増えるのは頂点数であって塗り面積はほぼ不変 — つまりフィルレート律速だと間引き廃止は効きも悪化もせず、当初目的（フル解像度化）自体が効果対象外になる。加えて描画モデルの差も逆風になりうる: Leaflet のパンは CSS transform のコンポジタ合成のみで再ペイントがほぼ走らないのに対し、WebGL 地図はカメラ操作中に毎フレーム全ビューポートを描き直す（→ §6 検証項目1 で切り分け、判明時は §6「フィルレート律速だった場合の分岐」へ）。

## 3. 方式判断の経緯

判断は 3 段階で行った。すべての経緯を記録する。

### 第 1 段階: 描画技術（canvas / WebGL / SVG 差分）

| 観点 | 自前 canvas | WebGL | 現行 SVG を差分更新 |
|---|---|---|---|
| 静止時の毎秒負荷 | 極小 | 極小 | 小（変化点のみ） |
| パン/ズーム操作時 | 軽い | 最軽 | 要素数の壁が残る |
| フル解像度の線/面 | 要素増で徐々に重い | GPU で軽い | 重い（不可に近い） |
| 依存追加 | なし | あり | なし |
| 実装量/リスク | 中 | 大 | 小 |

- **層 A（静止時負荷）** は「現行 SVG を差分更新」で十分に潰せる（§2.4）。安価なので全面リライトの前にまず実施・実測する（→ §6 段階0）。
- **層 B（フル解像度の線/面）** は canvas/SVG では要素数・ペイントの壁に当たる → GPU（**WebGL**）が要る。真の要件「**地図・活断層をフル解像度で軽く**」はこちらで、以降の一本化議論は層 B を対象とする。

### 第 2 段階: WebGL の実装形態（二本立て / 一本化）

| | MapLibre 一本化（採用） | Leaflet + deck.gl 二本立て |
|---|---|---|
| CPU/GPU 同期問題 | **消える（単一エンジン）** | 残る（要検証） |
| 間引き廃止 | 完全 | 完全 |
| 描画の軽さ | GPU で統一 | GPU（オーバーレイのみ） |
| flyTo/当たり判定 | ネイティブで綺麗 | Leaflet 流用 |
| 将来性（ベクタータイル/3D） | ◎ | △ |
| 作業量/回帰リスク | 全面リライト | オーバーレイのみ |
| バンドルサイズ | 重い（実行時は軽い） | 中 |

- 二本立ては Leaflet(CPU 投影・アニメ) と deck.gl(GPU) を毎フレーム同期させる中途半端さが残る。これを嫌い、**地図基盤ごと MapLibre GL JS へ一本化する**方針を採用。deck.gl は不要になる（MapLibre 単体で点・線・面・当たり判定・GPU 描画・flyTo が揃うため）。

### 第 3 段階: 描画バックエンド（WebGL2 / WebGPU）— ターゲット実機 Surface Go 2

- **ターゲット実機は Surface Go 2**: Pentium Gold 4425Y / Core m3-8100Y、GPU **Intel UHD Graphics 615**（2018 年世代・最下位クラスの統合 GPU）、10.5" 1920×1280・220ppi（Windows スケーリング 150% 前提で実効 DPR ≒ 1.5）、RAM 4〜8GB。**「弱い統合 GPU × 高精細画面」が負荷特性の核**。
- **WebGPU は不採用**。理由:
  1. **動作保証がない** — Chrome 113+ で既定有効だが、クラッシュ・不正描画・脆弱性のあるドライバを外す**ブロックリスト**があり、UHD 615 のような旧世代の弱 GPU は対象になりやすい。無効化されると WebGL2 フォールバックに落ちるため、**WebGPU 前提で作っても実機では WebGL2 で動く公算**が高い。
  2. **動いても効果が限定的** — UHD 615 は生の GPU 性能が低く、WebGPU の利点（ドローコール削減・計算シェーダ）の恩恵が小さい。
  3. **真の律速に効かない** — 220ppi を弱 GPU で毎フレーム塗る**フィルレート**が律速になりうる。描画 API を WebGPU に変えても解決しない（DPR 抑制など別対策の領域）。
- **採用は WebGL2（＝ MapLibre）**。UHD 615 でも枯れた WebGL2 は安定動作し、ブロックリストのリスクも WebGPU より低い。
- **将来性は MapLibre 経由で獲得する** — MapLibre の WebGPU バックエンド対応が実用化すれば、エンジン更新で自動的に恩恵を受けられる（自前 WebGPU 実装は不要）。これにより「一本化」と「将来性」を両立する。

## 4. 採用アーキテクチャ（MapLibre 一本化）

**MapLibre GL JS を唯一の地図・描画エンジンとする。** Leaflet / react-leaflet / deck.gl は廃止する。

- 背景タイル → MapLibre のラスタータイルソース（現行の地理院系タイルを移設）
- 点（強震点・観測点）→ circle レイヤー。色は index の data-driven styling、**更新は `feature-state` を採用**（2026-07-26 実機計測で確定・§6 結果記録 検証項目6）。各 feature に top-level `id` を振り、paint 式を `['coalesce', ['feature-state','lvl'], ['get','lvl'], 0]` として毎秒 `setFeatureState` で変化点のみ差し替える。**`setData` は再タイル化が走り 10〜22 倍のコスト**（実機 3 回計測: `setData` 356〜378ms vs `feature-state` 16〜36ms）のためフォールバック扱い
- 線（活断層・海岸線）→ line レイヤー（フル解像度でも GPU で軽い）
- 面（県境・一次細分区域塗り）→ fill レイヤー
- 当たり判定（活断層ポップアップ等）→ `queryRenderedFeatures`
- カメラ操作（flyTo・パン・ズーム）→ MapLibre ネイティブ（`flyToLite` の自前実装は廃止）
- 特殊合成・アニメ（`KyoshinSubThreshold` の非加算合成、波紋、予報円拡大）→ 標準レイヤーで表現できない場合は MapLibre のカスタムレイヤー（WebGL 直書き）で実装
  - **`KyoshinSubThreshold` はカスタムレイヤー確定**（2026-07-26 実機計測で成立・§6 結果記録 検証項目5）。方式は**レベルごとにオフスクリーン FBO へ不透明描画 → `opacity` で premultiplied over 合成**（現行 SVG の `<g opacity>` と等価）
  - **【注意】このレイヤーには `feature-state` が効かない**（paint 式を経由せず自前の頂点バッファを持つため）。毎秒のレベル更新は別途設計が要る → **インデックスバッファ方式を採用（実機検証済み・2026-07-26）**。座標は静的な頂点バッファ 1 本、毎秒はレベル順 index をカウンティングソート O(n) して `Uint16Array`（1,725点で 3.4KB）を `bufferSubData`、レベルごとに `drawElements(POINTS, count, UNSIGNED_SHORT, rangeStart*2)` でレンジ描画する。実機で 1,725点・17,250点ともに vsync 天井内（更新モードは fps 59.5〜60.0。baseline のみウォームアップ残りで58.25〜59.92）・longtask 0。素朴実装（レベル別バッファを毎秒再構築）より更新 CPU が 2〜5 倍軽い。詳細は §6「検証項目5×6 の交差点」
  - **【注意】再描画そのものが標準 circle より重い**（`repaint-only` の収束が 15.8〜17.9ms・項目6 の標準 circle は 6.6〜12.0ms）。FBO 二層合成の代償であり、項目7（EEW 複合負荷）で効く余地がある

**投影の追い風**: MapLibre も Leaflet も Web メルカトル（EPSG:3857）。既存の `public/data/*.json` の座標はそのまま使える見込み（GeoJSON は `[lng, lat]` 順のため並び順の変換は要る）。

**React 統合**: MapLibre GL JS はバニラ JS。統合方式（`react-map-gl` を使う / `maplibre-gl` を直接 `useRef`+`useEffect` で制御）は本設計着手時に Context7 で最新事情を確認して確定する。現行レイヤーが命令的（`useMap`+`useEffect` で Leaflet API 直叩き）なので、`maplibre-gl` 直制御とも親和性はある。

## 5. 保つべき挙動（回帰防止チェックリスト）

移行後も以下を現行と同一に保つ（括弧内は MapLibre での実現手段の当たり）:

- **強震点**: 気象庁配色 / `SHINDO0_COLOR`、半径 `BASE_RADIUS(2.5) × iconScale`、index 別表示規則（0＝非表示 / 1-6＝半透明合成 / 7+＝不透明 0.85）、カメラ追従、非インタラクティブ。
- **`KyoshinSubThreshold`**: 指数カーブの不透明度（index 0→0、index 6→0.35）と二層の合成規則 — **①同一レベル内は重なっても濃くならない**（レベルごとの `<g opacity>` に circle を格納し、グループを一枚に合成してから opacity を掛ける）／**②異なるレベル間の重なりは素直に濃くなる**（別グループのため）。（→ MapLibre の circle は同一レイヤー内でも通常のアルファブレンド（over 合成）で重なりが蓄積して濃くなるため、「レベルごとに別レイヤー化＋各レイヤー内の重なりを潰す」再現が要る。標準スタイルでは不可でカスタムレイヤー行きの公算 = PoC 項目5）
- **`KyoshinDetectedPoints`**: 震度に応じた可変サイズ、確定点による上書き、低→高震度の順の重ね。
- **`KyoshinMaxEffect`**: 最大震度更新時の波紋アニメ（600ms、index 7 以上でトリガ）。
- **活断層**: 色 `#c2410c` / 太さ 1.2 / 不透明度 0.65、**クリックで地名ポップアップ**（線近傍の tolerance 相当）。（→ `queryRenderedFeatures`）
- **ベースマップ**: 背景タイル、県境・一次細分区域・海岸線・ラベルの見た目。（ラベルは現状 DOM = `L.marker`+`divIcon`。MapLibre での実現方式は §8「テキスト描画」参照）
- **その他の地図連携機能**（付録参照）: EEW 震源マーカー・予報円、津波海岸線、地震マーカー・震度集約、ヒートマップ、検知点、自動フィット（`FitToDetection`/`FitToCandidate`）、flyTo。
- 2 バリアント（standard / DMDSS）双方で成立すること。

## 6. PoC 計画（律速分解を軸に）

### 目的

全面リライトに踏み込む前に、**(a) 層 A の安価な改善でどこまで軽くなるか**・**(b) 層 B の律速が CPU かフィルレートか**を先に確定し、その上で **MapLibre 一本化の致命的リスクを薄い縦切りの PoC で潰す**。使い捨てブランチでの最小実装で本体への統合は不要（ただし段階0 の差分更新化は有効なら本採用してよい）。

### 段階0: 層 A の安価な中間改善を先に実測（全面リライトの前段）

- `KyoshinPoints` の毎秒更新を、隣の `KyoshinSubThreshold` と同じ**差分更新**（前回 index を保持し、変化した点だけ `setStyle`/`setRadius`）に書き換える。Leaflet のまま・数十行。
  - **実装注意**: 差分経路だけでは `iconScale` 変更時に index 据え置きの点が取り残される。`KyoshinSubThreshold` と同様、「indices の差分更新」と「iconScale 変更時の全点再適用」の二経路に分ける（同ファイルの drawFn コメント参照）。
- 静止時の DOM 属性更新（層 A）がどこまで下がるかを、**開発機の CPU スロットルと実機（Surface Go 2）の両方で**実測する。
  - **計測方法を記録する**: §2.1 の「527 回/秒」は手順未記録で再現できない（§2.1 注記）。改修前（before）から同一手順で測り直し、計測スクリプトと数え方を残す。
  - **主指標はフレーム時間**: DOM 属性更新回数は中間指標にとどめ、判断は体感に直結するフレーム時間の分布（p95）とロングタスクで行う。
- **判断**: 層 A の解消だけで実機の体感が十分軽くなるなら、全面リライト（層 B 対応）の緊急度は下がる。層 B が実機で明確なボトルネックだと確認できてから全面リライトへ進む。

### 検証項目（この順で潰す）

1. **律速の切り分け（最優先）**: 層 B の重さの正体が **CPU（頂点処理・JS）** か **GPU フィルレート（塗り面積）** かを分解する。ここを外すと PoC 結果を誤読する（切り分け方法は下記「測定方法」参照）。
   - **フィルレート律速だった場合、間引き廃止は効かない**（形状保存の間引きを廃止しても塗り面積はほぼ不変。§2.4）。判明した場合は下記「フィルレート律速だった場合の分岐」に従い対策を組み替える。
   - **【2026-07-25 実機3回計測・決着済み】結論: 律速に到達しない。** 塗り面積を 0.98M→**3.91M px（4.0 倍）**・線幅を 1.2→**12（10 倍）**・頂点を 9,874↔31,646 と振り、両者を重ねた `overload`（3.91M px ＋ 線幅 12）まで負荷を上げても、**全 19 計測が p95 16.9〜17.0ms・fps 58〜60** で vsync 天井（16.7ms）を破らなかった。フィルレート律速でも CPU/頂点律速でもなく、**実配置の 1.8 倍以上の負荷まで律速に届かない**（＝下記「フィルレート律速だった場合の分岐」は発動しない）。**間引き廃止は描画コストの観点で青信号**（フル 31,646 頂点と間引き後 9,874 頂点で p95 の差が測定不能）。
     - 1・2回目は**判定不成立**だった。pan・zoom とも飽和した baseline から負荷を**下げて**振っていたため何も測れておらず、さらに 1回目に zoom で見えた劣化（33.3ms）は**タイル初回読み込みのウォームアップ交絡**だった（3回目の `baseline-warm` 対照 16.9ms で確定）。経緯と撤回した主張は下記「結果記録」、スイート改訂のレビューは [webgl-poc-review-4b0517c.md](webgl-poc-review-4b0517c.md)。
     - **残る論点**（層 B の外・当時のメモ。現在の状態は冒頭の状態一覧表を正とする）: 検証項目2 の Leaflet 比較（→実機計測済み・決着）・検証項目5 非加算合成（→決着）・毎秒更新（→決着）・EEW 複合負荷（→決着）・真の実行時メモリ（→未完）。観点は [webgl-layerb-verification-points.md](webgl-layerb-verification-points.md)。
2. **フル解像度描画の軽さ**: MapLibre で 背景タイル＋県境＋**間引き前フル解像度の活断層**＋強震点を描画し、非力機相当（CPU 4 倍スロットル・実機）でも現行以上に軽い（パン/ズーム時のロングタスク減・FPS 同等以上）。
   - **【2026-07-27 実機計測済み・決着＝一本化GO（状態は冒頭の状態一覧表を正とする）】** 層B（項目1）は「MapLibreは現行負荷でvsync天井を破らない」ことを確定させたが、「Leafletより軽いか」には答えていない（両者とも60fpsに張り付く場合、メインスレッドの仕事量差はフレーム時間だけでは区別できない可能性がある）。`poc/leaflet-main.ts` / `poc/leaflet-measure.ts` / `poc/leaflet-index.html` として、`poc/main.ts`（MapLibre版）と条件を揃えた最小構成のLeaflet版PoCを作成した。負荷軸は活断層full/thin・線幅・観測点on/off（DPR軸はLeafletに対応APIが無いため対象外）。パンは実ドラッグと同じ経路（`_rawPanBy`+`'move'`のみを毎フレーム、`'moveend'`はフェーズ終了時に1回）で駆動し、ズームは`zoom-native`（本番同様のアニメーションズーム）と`zoom-forced`（MapLibre同様に毎フレーム強制再描画）の両方を測れるようにした。
   - **レビュー対応済み**: `panBy(...,{animate:false})`が内部で`'move'`と`'moveend'`を同一発火し実ドラッグでは
     起きないフル再描画を毎フレーム注入していた問題（比較の前提を壊す重大な罠）を`_rawPanBy`+`'move'`のみの
     駆動に修正済み（コミット済み・レビューでも「比較の成立を救った修正」と評価）。その後のレビューで、
     ドラッグ終了時の一括再描画コストが計測窓の外に落ちて`pan`が実際より軽く出てしまう問題（HIGH1）と
     `fps`算出の定義不一致（MEDIUM2）を追加指摘され、`moveendMs`として名指し記録・実測経過時間ベースの
     `fps`に修正した。さらに独立検証で、Leaflet側の証跡に天井（vsync間隔）の手がかりが無く、実際に
     「同じ開発機」と記録された2測定が天井の違う環境（60Hz対170Hz）で取られ数値が食い違う事故が
     判明した（HIGH4）。`estimatedVsyncMs`をLeaflet側だけでなく比較相手の`poc/measure.ts`（MapLibre側）
     にも追加し、両者を同じ天井で測ったか証跡から判別できるようにした。詳細は
     [レビュー綴り](webgl-poc-review-82d4d39.md)。
   - **【完了・2026-07-27】** 実機（Surface Go 2）で `__runLeafletBSuite` を実行済み（`surface-go2-leafletB*` 44件＋比較相手 `surface-go2-layerB-vs*` 38件）。**毎フレーム全画面再描画で Leaflet 150.0ms 対 MapLibre 16.7ms＝9倍以上・一本化は GO で決着**（§6 検証項目2・冒頭の状態一覧表）。この残作業は完了しているため再実行は不要。
3. **カメラ操作の滑らかさ**: flyTo・パン・ズームがスロットル下・実機で滑らか。
   - **【2026-07-27 実機計測済み・判定は保留（状態は冒頭の状態一覧表を正とする。実機結果は本項目末尾）】** 既存の層B PoC（`poc/main.ts`・`poc/measure.ts`）を拡張。本番の flyTo パターン（JapanMap.tsx 調査・`flyToLite.ts` 経由の全呼び出しを集計）は「単一点への flyTo は目標 zoom=MAX_ZOOM(8) 固定」「複数点への flyToBounds は padding [60,60]（EEW検知系）／[48,48]（津波・観測点フィット）／[20,20]（JAPAN_BOUNDS 全体復帰）の3パターン」「duration は 0.8秒（EEW系）・1.0秒（それ以外）の二値」。MapLibre の duration は**ミリ秒**単位（Leaflet は秒単位）のため取り違え防止で `durationSec * 1000` と明示した。
   - 既存 `measureOnce`（固定durationで回してframeDeltaを集計）とは計測の性質が異なるため、`measureOneFly` を新設: flyTo/fitBounds 呼び出し〜`moveend` 発火までを1計測単位とし、実測経過時間（`elapsedMs`）ベースでフレーム時間p50/p95/max・longtaskを集計する。単発計測 `__measureCameraFly` とスイート `__runCameraSuite`（単一点flyTo⇔flyToBounds を duration/padding を本番同様にローテーションしながら往復）を追加。padding=60/48 は本番同様「局所的な点クラスタへの maxZoom 付きズームイン」を再現し、padding=20 のみ JAPAN_BOUNDS 全体復帰にする（セルフレビュー HIGH: 当初は全 padding で同じ JAPAN_BOUNDS を使い回しており、最も負荷の高い局面が測れていなかった）。
   - **セルフレビューで発見・修正した不具合**: fitBounds の options に `maxZoom: undefined` をキーごと渡す（=padding20 のケースで maxZoom を指定しない実装だった）と、MapLibre 内部のデフォルト値マージが効かず `Invalid LngLat object: (NaN, NaN)` で例外が飛ぶ挙動を実機投入前に踏んだ。`fitBoundsOptions()` ヘルパーで maxZoom が無いときはキー自体を省く形に直した。実機計測前に潰せた点で価値のある発見。
   - 開発機での動作確認（tsc型チェック・Playwrightでコンソールエラー0件・単発/スイート双方の計測値が期待通り: flyTo 0.8s指定で elapsedMs≈814ms・fitBounds(padding20) 1.0s指定で≈1008ms・padding60/48も局所クラスタへの飛行としてエラー無く動作）は完了。全計測が vsync天井付近に張り付き longtask 0件で、負荷の有無自体はこの環境では判別できない（**天井の値そのものが実測より16.7ms=60Hzと誤って書かれていた。訂正は下記「レビュー(f5d8de5)対応」参照**）。
   - **【2026-07-27 レビュー(8c75e49) HIGH2 対応】** 新設の`measureOneFly`に`estimatedVsyncMs`が無く、`__runCameraSuite`も`static`フェーズを一度も踏まないため、カメラ計測の証跡群に vsync周期の実測が一件も含まれていなかった（検証項目2 HIGH4 で対策したはずのパターンが新設パスで再発）。実際、計画書は開発機を「vsync天井16.7ms（60Hz）」としているが、レビュー`b5d8bae`は同じ開発機を「170Hz」と記録しており矛盾していた。`__runCameraSuite`の先頭で`measureOnce(deps,'static',...)`を一度踏ませ、その`frame.p50`を`lastEstimatedVsyncMs`としてモジュールスコープにキャッシュ、以降の全`measureOneFly`証跡に含めるよう修正した。実機投入前に開発機で動作確認済み（スイート実行後は`estimatedVsyncMs:16.7`が正しく付与される）。
   - **【2026-07-27 レビュー(f5d8de5) で原因判明】** 上記の矛盾（16.7ms対170Hz）の原因は**計測環境の違い**だった。本セッションのPlaywright（Chromiumをheadless/CDP経由で駆動）は vsync が **16.7ms(60Hz)** に固定されるのに対し、レビュー側が実際のChromeブラウザで直接計測したところ vsync は **6.1ms(≒164Hz)** だった（同一マシンでも実行コンテキストでvsyncが変わる）。したがって本PoCの開発機計測値（`estimatedVsyncMs:16.7`等、Playwright経由）は、実際にChromeで開いたときの天井とは異なることに注意（実機Surface Go 2は通常のブラウザ操作で計測するため、この差異は実機計測では発生しない見込み）。 →**【2026-07-27 訂正・レビュー(3598d26)＆§8追加検証参照】** この説明（「Playwright＝headless＝60Hz固定 / 実Chrome＝164Hz / 同一マシンでも実行コンテキストでvsyncが変わる」）は**誤り**。本セッションの Playwright は headed（`navigator.userAgent` に Headless なし・`webdriver` false）で直接計測しており、16.7ms は Playwright が固定した値ではなく実測値。レビュー側は同じく headed 申告で 6.1ms（レビュー(61776b0)の追加検証では 5.9ms）。**双方 headed にも関わらず vsync 実測が16.7ms対5.9〜6.1msで一致しない原因は未特定**（別モニタか別実行コンテキストか他要因かは未検証。以前この場所で「別セッションの別の物理機のモニタ差」と断定したのは、確かめずに原因を名付けた誤りだった）。本セッションの `estimatedVsyncMs:16.7` は誤記ではなくこの環境での実測値であり、実機Surface Go 2（60Hz）とも整合する。`blockMaxMs`（`poc/label.ts`）は両環境でほぼ同値を返し vsync 非依存と実証済みのため、vsync 差の原因を追う実務上の必要はない。
   - **【2026-07-27 実機計測（Surface Go 2・2スイート各17計測）・判定は保留】** 詳細は [webgl-poc-surface-go2-2026-07-27.md](webgl-poc-surface-go2-2026-07-27.md)。**静止は健全（fps 59〜60）だが、飛行中に初めて vsync 天井を破った（fps 28.2〜52.7・`frame.max` 最大183.3ms・longtask 0〜2件/最大95ms）。** `frame.p50` は全32計測で 16.7ms＝過半のフレームは間に合っており、p95/max が2〜11フレームに間欠的に跳ねる形。**ウォームアップ仮説は否定**（2スイート目が1スイート目より悪化＝fps下限52.7→28.2。連続稼働の熱影響が疑われるが未検証）。**ただし Leaflet 側の同条件（実機・同じ flyTo/fitBounds）計測が無く、優劣は判定できない**——検証項目2 では毎フレーム全画面再描画で Leaflet が MapLibre の9倍以上重かったため、飛行中も再描画が走る Leaflet の方が悪い可能性は十分ある。**現時点の数字だけで「MapLibre は実機で不足」と結論してはならない。**
   - **【2026-07-27 比較用の Leaflet カメラ計測スイートを実装（資材作成済み・実機計測待ち）】** 実機報告は「本番アプリ側に計測フックを仕込め」としていたが、本番はレイヤー内容が MapLibre PoC と異なり交絡が入るため、**既存の Leaflet PoC（`poc/leaflet-measure.ts`／`leaflet-main.ts`・MapLibre 版 PoC と同一の GEBCO＋活断層＋観測点構成）に MapLibre 版 `__runCameraSuite` を apples-to-apples でミラーした** `__runLeafletCameraSuite` を追加（PoC 対 PoC の同条件比較にする方が筋がよい）。座標順（Leaflet は `[lat,lng]`）と duration 単位（Leaflet は秒）だけ Leaflet 仕様に合わせ、flyTo パターン（単一点 zoom8・padding 60/48/20・duration 0.8/1.0秒）・ラベル形式・往復数・先頭 static での vsync 証跡は完全に揃えた。開発機で動作確認済み（point/bounds とも moveend まで計測・duration が秒で正しく効く・estimatedVsyncMs 16.7 がキャッシュ添付・コンソールエラー0）。
   - **残作業**: 実機（Surface Go 2）で `await window.__runLeafletCameraSuite('surface-go2-leaflet-camera')` を実行し、MapLibre 側 `surface-go2-camera*`（34件・fps 28.2〜52.7）と fps・`frame.p95`・`frame.max`・longtask を突き合わせて項目3 の GO/NO-GO を判定する（実機セッション担当）。
   - **【結果の読み方・計測前に共有（2026-07-27 レビュー(8657812) の MEDIUM 2件）】**
     - **① 飛行先の乱数は両エンジンで独立**（`randomPointInJapan` 等を実行時に引くため、MapLibre 側の既計測とは別の飛行列になる）＝厳密な1対1ペア比較ではない。**まず現状のまま計測し、差が明確なら（項目2 の 9 倍のように）平均・分布比較で判定できる。差が微妙（≒1.5倍以内）で乱数ばらつきに埋もれる場合に限り**、乱数シードを固定して同一飛行列を両エンジンで再現するペア比較へ切り替える（その場合は MapLibre 側も再計測が必要）。
     - **② Leaflet PoC は本番より軽い**（本番 `ActiveFaultsLayer.tsx` は可視 SVG 線に加え当たり判定用の透明 Canvas 線を重ねて持つが、PoC は MapLibre 版に対応物が無く省略している。[webgl-poc-review-82d4d39.md](webgl-poc-review-82d4d39.md) 情報3）。よって **Leaflet PoC が MapLibre より遅ければ結論は強まる（本番はさらに遅いはず＝安全側）**。逆に **Leaflet PoC が MapLibre より速い場合はこのバイアスを差し引く必要があり、そのまま結論にできない**（当たり判定線を足した条件での再計測を検討）。
   - **【2026-07-27 Leaflet 側 実機計測 第1回・判定は保留のまま／計測漏れを修正】** 実機で `__runLeafletCameraSuite` を2スイート実行（`surface-go2-leaflet-camera*` 34件）。frame 統計・longtask 上は **Leaflet が明確に優った**（longtask 0 対 MapLibre 13件・2スイート目 p95 中央 17.1ms 対 49.9ms）。**だが結論にできない**——[webgl-poc-surface-go2-camera-leaflet-2026-07-27.md](webgl-poc-surface-go2-camera-leaflet-2026-07-27.md) が指摘したとおり、**Leaflet は飛行中 CSS transform でサボり着地（moveend）で一括再描画する性質のため、その着地コストが `measureOneFlyLeaflet` の frame 統計から漏れていた**（measureOnce の moveendMs と同じ穴の再発）。→ **`measureOneFlyLeaflet` に MessageChannel ブロック検出器を追加し着地コストを `landingBlockMaxMs` として名指しで捕捉するよう修正済み**（開発機ですら frame p95 16.8ms の裏で着地ブロック 20〜24.9ms を検出＝隠れコストの存在を確認）。`landingBlockMaxMs` が小さければ Leaflet 優位は本物、大きければ「飛行中は滑らかだが着地で固まる」体感評価へ切り替える。
   - **【2026-07-27 レビュー(b46f058) MEDIUM2 対応・両エンジンを対称化】** 当初「MapLibre 側は毎フレーム描画で着地コストが無いため再計測不要」としたが、これは**未検証の仮定**だった（`measure.ts` に MessageChannel は0箇所で MapLibre の着地ブロックは一度も測っていなかった）。同じブロック検出器を MapLibre 版 `measureOneFly` にも入れて対称化（`landingBlockMaxMs` 追加）。**開発機実測で MapLibre の着地ブロックは point 13.3ms・bounds 5.8ms＝≈0 ではなかった**（Leaflet の 24.9/20ms より小さいが存在する。仮定を実データに置換できた）。**残作業: 両エンジンとも新実装（`landingBlockMaxMs` 込み）で実機再計測**し、frame 統計＋着地ブロックの両面で優劣判定する。longtask 観測窓が settle まで広がったため実機第1回とは longtask を単純比較しない（観測窓の変更として説明する・MEDIUM1）。`LANDING_SETTLE_MS=300` の妥当性は `landingBlockTop3Ms` の2/3番手が settle 端に張り付いていないかで結果確認する。
   - **【再計測の形（2026-07-27 レビュー(ec74bef) ガイダンス）】** 既存証跡 `surface-go2-camera*`/`surface-go2-leaflet-camera*` 各34件は**旧実装**（着地検出なし）。混同回避のため**新ラベル `-v2`**（`surface-go2-camera-v2`/`surface-go2-leaflet-camera-v2`）で測り直す。両エンジンとも**2スイート連続**（第1回同様。MapLibre は2スイート目が熱で悪化した疑いがあり片側1スイートでは条件が揃わない）＝計4スイート・実行順は第1回と揃えるか逆順も取る。読む点: ①`landingBlockMaxMs` の左右差（着地コストが Leaflet に偏るか＝判定の核心）②longtask の左右差（新実装同士で初めて公平比較）③`frame.p95`/`fps` は定義不変で第1回とも比較可（`elapsedMs` に settle を含めていない）。
   - **【2026-07-27 決着＝MapLibre 優位・第1回の判定を反転】** 実機で両エンジン `-v2` を各2スイート再計測（計68件）。[webgl-poc-surface-go2-camera-final-2026-07-27.md](webgl-poc-surface-go2-camera-final-2026-07-27.md)。**着地コスト（飛行中が軽い `frame.p95<20ms` の回に限定＝着地由来と判定できる回）が Leaflet 188.1ms 対 MapLibre 36.1ms＝5.2倍（最悪14倍）・longtask 発火 32/32 対 7/32**。**fps はほぼ同値（51.67 対 52.25）で着地コストを捉えないため、第1回(旧実装・fps/frame.p95/longtask=飛行中のみ)は「Leaflet 優位」と誤判定していた**。体感対応: Leaflet は飛行中滑らかだが着地で最大0.57秒停止・MapLibre は飛行やや粗いが着地23〜41ms＝後者が優る。前報 [webgl-poc-surface-go2-camera-leaflet-2026-07-27.md](webgl-poc-surface-go2-camera-leaflet-2026-07-27.md) の「Leaflet 優位」は本結果で覆った（削除せず経緯として残す）。副産物: (a) **MapLibre の着地コストも 23〜41ms で ≈0 ではなかった**（対称化＝MEDIUM2 対応で判明。仮定を検証可能にしたから気づけた）(b) 第1回の「2スイート目悪化」は**熱**（冷却を挟むと消失＝実機連続計測はスイート間に冷却を挟む運用知見）。残る留保: Leaflet PoC は本番より軽い（当たり判定線省略）が、MapLibre 優位を強める向きのため追加検証の実益は低い。**計測は dev サーバー取得だが、PoC は React 非使用＋両エンジン同一環境のため優劣判定はビルドモード非依存**（絶対値 188/36ms 等は本番ビルドで変わりうる・未検証）。
4. **当たり判定**: 活断層クリックで `queryRenderedFeatures` によりポップアップが線近傍で開く（現行 tolerance 相当）。
   - **【2026-07-27 進捗・開発機検証済み・決着】** 本番（ActiveFaultsLayer.tsx・JapanMap.tsx 調査: 可視線は SVG・`interactive:false`、当たり判定は同一形状を重ねた透明 Canvas 線 `L.canvas({ tolerance: 8 })`。プレート境界線・津波海岸線も同一パターン）に対し、MapLibre の `queryRenderedFeatures` には tolerance 相当のオプションが無いため、**クリック点中心の正方形 bbox（1辺 2r px）でヒット判定する「bbox方式」を採用**（transparent hit-layer 方式との2択のうち、AskUserQuestion で確定）。`poc/hittest.ts`／`poc/hittest.html` を新規作成した。
   - オフセット距離を厳密に制御するため、地図中心の緯度を通る既知の直線（テスト線）を追加し、`map.project()` で得たスクリーン座標を基準にPlaywrightで px オフセットを作って自動検証。**r=8px のとき、線からのオフセットが8px以内は必ずヒット・9px以上は必ず非ヒット**という境界一致を zoom 5/8/11 の3水準で確認した（bbox方式はスクリーンピクセル座標系で判定するため、期待どおりズーム非依存）。300px超の遠方点は全r値で非ヒット（誤検知なし）。**本番の `tolerance:8` 相当として r=8 を初期値に採用する**。
   - 型チェック・Playwright（実クリックイベント経由の動作も確認）ともにコンソールエラー0件。当たり判定は判定ロジックの正しさの検証が主目的のため、フレーム性能計測（実機計測）は不要と判断し本項目は開発機検証のみで決着とする。
   - **【2026-07-27 レビュー(8c75e49) HIGH1 対応】** 上記「境界一致」は、Leafletの実際の判定（`pointToSegmentDistance`によるユークリッド距離＝**円**、半径tolerance）に対し、bbox方式は**正方形**（1辺2r）であるにも関わらず、検証がテスト線=水平・オフセット=垂直方向のみだったため「円と正方形が恒等的に一致する軸」だけを見ていたという指摘を受けた。斜め方向では正方形が最大√2倍（8px→11.3px）まで拾ってしまい、活断層は任意角度に走るため無視できない差（面積比約1.27倍・最大距離約1.41倍）だった。対処として `hitTest` を「bboxで候補を粗く絞る（ブロードフェーズ）→ 候補に対し実際の点-線分距離をユークリッドで計算し円判定する（ナローフェーズ）」の2段構成に修正し、本番の`tolerance:8`（円）とそのまま一致させた。円は正方形bboxに必ず内接するため、ブロードフェーズが候補を取りこぼすことはない。Playwrightで(a)水平線での従来境界（回帰確認、7.9px以内=ヒット・8px以上=非ヒット。旧8pxちょうどの境界がタイル量子化誤差で微小に動いた点は誤差として許容）、(b)線の端点付近の斜め45度オフセット（ユークリッド距離10.18px>r=8）で、修正前(bboxのみ)なら誤ヒットしていたところ修正後は正しく非ヒットになることを確認した。
   - **【2026-07-27 追加指摘対応】ホバー時のカーソル変更が本番では自動、MapLibreでは自前実装が必要**。本番（Leaflet）は当たり判定用の透明線が `interactive:true` のため、Leaflet が `leaflet-interactive` クラス経由でホバー時に自動的に `cursor:pointer` を当てる。MapLibre にはこの自動処理が無く、当たり判定（bbox方式）を実装しただけではクリックは機能してもホバー時にカーソルが変わらず「クリックできる場所が分からない」使用感になる（実機確認前にユーザー指摘で発覚）。`poc/hittest.ts` に `mousemove` ハンドラを追加し、同じ `hitTest`（bbox判定）をホバーのたびに呼んで `map.getCanvas().style.cursor` を `pointer`⇔`''` に手動で切り替える実装で対応。実際の DOM `mousemove` イベント経由（`dispatchEvent`）で線上→`pointer`・遠方→通常カーソル・再度線上→`pointer` と正しく往復することを確認した。**本設計でも「当たり判定を実装するレイヤーは全て mousemove でのカーソル切替を対で実装する」ことを申し送る**（活断層・プレート境界・津波海岸線の3レイヤー共通）。
5. **非加算合成の再現**: `KyoshinSubThreshold` の **①同一レベル内は重なっても濃くならない／②レベル間は素直に濃くなる** の二層挙動を再現できるか（**WebGL 共通の最難関。抜けると見た目が変わる**）。標準 circle レイヤーは同一レイヤー内でも通常のアルファブレンド（over 合成）で重なりが濃くなるため、レベルごとに別レイヤー化＋各レイヤー内の重なり潰し（ステンシル/デプス等のカスタムレイヤー）が要る公算。
   - **【2026-07-25 実機検証済み・クリア】** カスタムレイヤーの**オフスクリーン FBO 二層合成**（レベルごとに不透明で描いてから opacity で over 合成）で再現できた。開発機で解析期待値と±1で一致（①②とも成立）、**実機（UHD 615）で 59〜60fps・longtask 0・`ERROR:` なし・描画崩れなし**、標準 circle 比の追加コストは測定限界以下。数値・留保は下記「結果記録」、レビュー経緯は [webgl-poc-review-3c2ddaf.md](webgl-poc-review-3c2ddaf.md)。
6. **毎秒更新のコスト**: 強震点の index 更新を feature-state か `setData` で軽く回せるか。**注意: WebGL は 1 点の変化でも全ビューポート再描画になる**（Leaflet+SVG の静止時は変化領域のみ再ペイント）。層 A の「毎秒の属性更新」が MapLibre 化後は「毎秒 1 回の全画面描画」へ置き換わるため、フィルレート律速の実機で毎秒のカクつきとして体感されないかを確認する。
   - **【2026-07-26 実機計測済み（3 回）・クリア】** 本番相当のベースマップ（40,917 頂点）＋観測点 1,725 を毎秒全点更新しても、**全画面再描画の収束は 6.6〜12ms**（`repaint-only`）・**`feature-state` 16〜36ms**・fps 59.2〜60・longtask 0 件。**実機目視でもカクつきなし**。上記の「毎秒 1 回の全画面描画」への懸念は**否定された**。`setData` も 356〜378ms（周期の 36〜38%）で収束するため素朴実装も許容だが `feature-state` 比 10〜22 倍のため**本設計は `feature-state` を採用**。なお **10ms 台の測定は実機で 2 倍程度ばらつく**（下記「実機のばらつき」）。数値・判断は下記「結果記録」、レビュー経緯は [webgl-poc-review-9b97a37.md](webgl-poc-review-9b97a37.md)。
7. **EEW 発報シナリオの複合負荷**: flyTo＋予報円アニメ＋毎秒更新が同時に走る、最も性能が問われる瞬間をシナリオ化して測る（設定タブの EEW テストで再現可）。PoC 実装に EEW 系が無い段階ではカメラ操作＋点更新＋円アニメの同時実行で代替し、レイヤー移植後の回帰検証で本シナリオを必須にする。
   - **【2026-07-27 実機計測済み・クリア（状態は冒頭の状態一覧表を正とする）】** `poc/eew-composite.ts` / `poc/eew-composite.html` として PoC 化した。本番の flyTo（JapanMap.tsx 調査: 新規発報・予報円成長フィット・解除の3系統が断続的に発火）・予報円（PsWaveLayer.tsx 調査: 100ms周期・zoomstart〜zoomend間は再描画を止める設計）・強震点毎秒更新（項目5×6のインデックスバッファ方式カスタムレイヤーを流用・最悪ケース）・EEW領域の`setData`（フィーチャ数が変わる、唯一`setData`が効く局面）を同一タイムライン（約30秒・本番のEEWテスト自動解除に合わせる）で駆動する。
   - **計測モード**: `baseline`／`flyto-only`（カメラ操作単独）／`composite-pause`（本番同様flyTo中は予報円更新を一時停止）／`composite-nopause`（止めない最悪ケース）の4種。開発機での動作確認（点数・pauseOnZoomの効果・二重起動防止・停止の即時性・型チェック）は完了。
   - **レビュー対応済み**: `flyto-only`/`baseline`で強震点カスタムレイヤーが全点index0（非表示）になっており
     基準線から描画コストが抜けていた問題（HIGH1）と、`wave.update()`がrenderサイクル外でGL呼び出しを
     行い`waveApply`の定義が項目5×6と揃わない問題（HIGH2）を修正し、実機投入可の状態にした。詳細は
     [レビュー綴り](webgl-poc-review-b4524cd.md)。
   - **【完了・2026-07-27】** 実機で `__runEewSuite` を実行済み（`surface-go2-eew*` 8件）。**複合負荷下でも fps 59.9〜60.0・longtask 0・flyTo 区間 p95 17.5〜19.5ms でクリア**（§6 検証項目7・冒頭の状態一覧表）。この残作業は完了しているため再実行は不要。

### 検証項目5×6 の交差点: カスタムレイヤーの毎秒更新（**クリア**）

> **2026-07-26 決着**: `b5d8bae`〜`c471303` で PoC 化（`poc/subthreshold-rt.ts` / `poc/subthreshold-rt.html`）し、
> 実機（Surface Go 2）で **①1,725点を3回・③17,250点を2回**計測して決着した。
> **`repaint-only`/`custom-update`/`naive-rebuild` の計15証跡は fps 59.5〜60.0。baseline 5証跡はウォームアップ
> 残りの影響で 58.25〜59.92（下記「計測上の注意」参照・モード比較には使わない）。全20証跡で longtask 0・
> `settleTimeouts` 0（該当するもの）。カスタムレイヤーの毎秒全点更新は成立する。**
> インデックスバッファ方式は素朴実装と**ピクセル完全一致**（開発機・全3水準で差0）かつ更新 CPU が**2〜5倍軽い**。
> **本設計は インデックスバッファ方式を採用する。**
> レビュー・再現手順・開発機での等価性検証は綴り [webgl-poc-review-b5d8bae.md](webgl-poc-review-b5d8bae.md)。

項目5・項目6 はいずれもクリアしたが、**両者が交わる一点だけが未検証のまま残っていた**。

| PoC | 確認した内容 | 使用レイヤー |
|---|---|---|
| 検証項目5（`poc/subthreshold.ts`） | 非加算合成の正しさ（**静的な点**） | カスタムレイヤー（FBO 二層合成） |
| 検証項目6（`poc/realtime.ts`） | 毎秒更新の軽さ（**標準 circle レイヤー**） | 標準レイヤー |
| **交差点**（`poc/subthreshold-rt.ts`） | **カスタムレイヤーを毎秒更新する** | **クリア（2026-07-26）** |

**なぜ穴になったか**: 項目6 の解決手段である `feature-state` は **MapLibre の paint 式を通る仕組み**であり、
**カスタムレイヤーには効かない**（自前の頂点バッファを持ち paint 式を経由しないため）。
一方 項目5 の PoC は `gl.bufferData(…, gl.STATIC_DRAW)` で**一度だけ構築し更新経路を持たなかった**
（テスト点が固定だったため必要が無かった）。

`KyoshinSubThreshold`（index 1〜6）は**毎秒レベルが変わるレイヤー**であり、本番実装では必ず通る道である。

**検討した案**: 座標は不変で**レベルだけが毎秒変わる**という性質を使う。

| 案 | 毎秒のコスト | 難点 |
|---|---|---|
| レベル別バッファを毎秒作り直す（PoC の `naive-rebuild`） | 座標を 6 本分リパック＋アップロード（約 13.8KB/秒） | CPU のリパックが毎秒発生 |
| 全点 1 バッファ＋点ごとの level 属性、シェーダで `discard` | 属性のみ（約 1.7KB/秒） | **頂点処理が 6 倍**（全点 × 6 レベル）。PoC 未実装 |
| **インデックスバッファ（採用）** | **約 3.4KB/秒＋O(n) カウンティングソート** | 実装がやや込み入る |

採用案の構成:

- **頂点バッファ（座標）**: 静的。1 回アップロードして以降触らない
- **インデックスバッファ**: 毎秒更新。レベル順に点 index を並べ替える（カウンティングソート O(n)）
- **描画**: レベルごとに `drawElements(POINTS, count, UNSIGNED_SHORT, rangeStart*2)` でレンジ描画
- **index 型の上限**: `Uint16Array` は 65,535点まで。本番の全観測点 4,372 なら余裕。超える場合は
  WebGL2 標準の `UNSIGNED_INT` へ（offset 換算が `*2` → `*4` に変わる）

1,725 点なら `Uint16Array` で **3.4KB/秒**。座標の再アップロードもリパックも不要。

#### 実機計測結果（Surface Go 2 / DPR 1.5 / canvas 1908×1152 / vsync 16.7ms / ベースマップ 40,917頂点）

`baseline`（更新なし）・`repaint-only`（更新なしで全画面再描画のみ＝基準線）・`custom-update`（採用案）・
`naive-rebuild`（素朴実装の対照）の4モード。毎秒 1 回、**全点にランダムなレベルを割り当てる**最悪ケース。

**① 1,725点（本番相当・3回）**

| mode | `apply` p50 | `settle` p50 | `updateFrame` p50 | `updateFrame` max | fps |
|---|---|---|---|---|---|
| `baseline` | — | — | — | 283.3 / 17.2 / 17.6 | 58.25 / 59.92 / 59.92 |
| `repaint-only` | **0.2 / 0.2 / 0.2** | 17.9 / 15.8 / 14.4 | 16.7 / 16.7 / 16.6 | 29.4 / 17.3 / 16.7 | 59.92 / 59.92 / 59.92 |
| `custom-update` | **0.2 / 0.2 / 0.1** | 12.6 / 15.4 / 12.7 | 16.6 / 16.7 / 16.7 | 17.3 / 16.9 / 33.0 | 59.58 / 59.83 / 59.75 |
| `naive-rebuild` | **0.4 / 0.5 / 0.4** | 12.3 / 13.3 / 11.7 | 16.7 / 16.7 / 16.6 | 17.0 / 17.0 / 17.0 | 59.92 / 59.92 / 59.83 |

**③ 17,250点（現行の10倍・方式比較専用・2回）**

| mode | `apply` p50 | `apply` p95 | `settle` p50 | `updateFrame` max | fps |
|---|---|---|---|---|---|
| `baseline` | — | — | — | 250 / 73.1 | 58.67 / 59.50 |
| `repaint-only` | **0.2 / 0.2** | 0.4 / 0.6 | 13.1 / 13.4 | 35.5 / 17.0 | 59.83 / 59.92 |
| `custom-update` | **0.4 / 0.5** | 2.4 / 0.6 | 14.3 / 11.4 | 17.0 / 17.0 | 60.00 / 59.92 |
| `naive-rebuild` | **2.1 / 1.4** | 2.8 / 3.3 | 15.2 / 15.7 | 17.0 / 33.4 | 59.92 / 59.83 |

#### 読み取れたこと

1. **毎秒更新は成立する。** baseline を除く15証跡で fps 59.5〜60.0（baseline 5証跡は58.25〜59.92・
   ウォームアップ残りのためモード比較には使わない）。全20証跡で longtask 0・`settleTimeouts` 0。
   ①③どちらの水準でも `updateFrame` は vsync 天井（16.6〜16.7ms）に収まる
2. **インデックスバッファ方式が一貫して軽い。** `apply`（更新の同期 CPU）で 5 計測すべて同じ向き
   （①2〜4倍・③3〜5倍）。①では基準線 `repaint-only`（3回とも 0.2ms で一定）と**区別できない**
   ＝カウンティングソートは測定限界以下。`naive-rebuild` だけが基準線を安定して超える
3. **ただしフレーム時間には出ない。** `updateFrame` は全モード天井、fps も差なし。方式の優劣は
   **将来の余裕**の問題であり、現行規模では素朴実装でも実害はない
4. **カスタムレイヤーの再描画は標準 circle の 1.3〜2.7 倍重い。** `repaint-only` の `settle` p50 が
   15.8〜17.9ms（項目6 の標準 circle は 6.6〜12.0ms）。FBO 二層合成（6レベル × 全画面 clear ＋
   全画面 quad 合成）の代償。**項目7（EEW 複合負荷）で効く余地があるため要注意**
5. **点数10倍でも描画は天井内。** ③の `repaint-only` は毎フレーム 17,250点 × 6レベルを描いて
   fps 59.83〜59.92。断片数はおよそ10倍（DPR1.5 で `gl_PointSize` 7.5px ≈ 56断片/点 → 約97万断片）

#### 計測上の注意（この項目で判明した指標の性質）

- **`settle` は vsync 位相に敏感で、差が小さいときのモード間比較に使えない。** ①では
  `repaint-only`（更新なし）が3回すべてで `custom-update`/`naive-rebuild` より遅く出た。原因は
  `settle` の起点 `tApply` の位置——更新モードは先に `genLevels`（計測外・0.1ms 程度）を実行するため
  `tApply` がわずかに後ろにずれ、`setInterval` が 1000ms＝ちょうど60フレーム分で位相が毎回揃うため
  系統的に効く。**フレーム自体の長さ（`updateFrame`）で見るべき**。項目6 の `setData`（356ms）のような
  桁違いの差なら `settle` でも問題ないが、ミリ秒以下の勝負では別の物差しが要る
- **`apply` の p95 は雑音を拾う。** ①では `repaint-only`（`triggerRepaint` のみ）の p95 が 0.3→0.6→1.1ms
  と動き、`naive-rebuild` の 0.9〜1.0 と重なった。①の根拠は **p50 一本**。③では基準線 0.4〜0.6 に対し
  `naive` 2.8〜3.3 で桁が違うため p95 も有効
- **`baseline` の `frame.max` はウォームアップ残りで、モード比較に使えない。** スイート最初の
  フェーズが `warmup()` 直後のタイル取得・GC の尻尾を被る。同一セッションで回を重ねるほど
  単調に下がる（283.3 → 250 → 73.1 → 17.2 → 17.6）
- **30ms台の単発スパイクは全モードに出る環境要因。** `repaint-only` 35.5・`naive-rebuild` 33.4・
  `custom-update` 33.0 と出揃った。24〜36サンプル中1件程度で、**方式に帰属できない**
- **`settle` には温まりのトレンドと素のばらつきの両方がある。** ①の `repaint-only` は 17.9→15.8→14.4 と
  単調減少したが、`custom-update` は 12.6→15.4→12.7 で非単調。項目6 の「10ms台は約2倍ばらつく」も
  一部は温まり（水準ごとにページを開き直していた）で説明できるが、それだけではない
- **負荷軸③（17,250点）は本番負荷の予測に使えない。** `loadPositions` が実観測点 4,372 を巡回複製する
  ため、**可視の塗り面積が②4,372点と同じ**（開発機実測 50,132px → 50,534px・点数3.95倍に対し +0.8%）。
  増えるのは頂点処理・断片処理・転送・更新 CPU で、可視面積は飽和する。③の結論は
  「描画負荷10倍まで余力あり」ではなく「**更新 CPU・頂点処理・断片処理を10倍にしても天井内**」。
  本番相当は①②のみ

**残る一点**: 案②（全点 1 バッファ＋level 属性＋`discard`）は PoC 未実装。採用案が実機で天井内に
収まったため比較の必要が無くなったが、頂点処理 6 倍の見積りは実測していない。

### 検証項目: モバイル実機（iOS / Android）— **一次確認済み・保留分あり**

**この計画書はここまで対象プラットフォームを明示してこなかった。** 計測はすべて Surface Go 2（Windows）で
行っており、iOS / Android の記述は本項目を書くまでゼロだった。本アプリは PWA で GitHub Pages 配信のため
モバイルで開かれうる。**対象に含めるかどうかを含めて未決**である。

#### 2026-07-26 一次確認（iPhone 15 Pro Max / Safari / 目視のみ）

`poc/subthreshold-rt.html` を LAN 経由（`vite.poc.config.ts` の `server.host: true`）で開いて目視確認した。

| 確認項目 | 結果 |
|---|---|
| 点が描画されるか（`GL_POINTS` / `gl_PointSize` / FBO 二層合成） | **OK** |
| 表示崩れ・`stat` への `ERROR:` 表示 | **なし** |
| 「custom-update 毎秒」の毎秒更新 | **OK**（更新を目視確認） |
| ホーム画面へ退避 → 数分後に復帰 | 戻した瞬間は未描画。その後は正常に見える → **判別不能（下記）** |

**これで最大の互換性リスクは消えた。** Apple の WebGL2 実装（Metal バックエンド）で `GL_POINTS` と
`gl_PointSize` が機能し、FBO 二層合成も崩れない。ここが駄目なら設計ごと作り直しだった。

**負荷の観点では iPhone の方が重い**点に注意する。iPhone 15 Pro Max は DPR 3 で描画ピクセルが
1290×2796 = 約360万px、Surface Go 2 の 1908×1152 = 約220万px の **1.6倍**。FBO 二層合成は画面面積に
比例するためフィルレート負荷はむしろ大きい（A17 Pro の余力で吸収されていると見られる）。

#### 保留（次に潰す）

**1. バックグラウンド復帰の挙動を判別する** — 目視では以下の 3 つが区別できない。実害が全く違う。

| 起きていた可能性 | 実害 |
|---|---|
| ① rAF が停止していただけ。復帰して描き直した | **無害** |
| ② WebGL `context lost` → `restored`。MapLibre が再構築した | **設計対応が要る**（カスタムレイヤーの `onAdd` が再呼び出しされ FBO・テクスチャ・シェーダが作り直されたか要確認） |
| ③ **iOS がタブごと破棄し、復帰時にページを再読み込みした** | **重大**。受信中の地震データ・EEW 表示・設定が失われる。ユーザーからは「正常に表示されている」ようにしか見えない |

③は防災アプリとして致命的である（EEW 受信中にホーム画面へ行って戻ると受信状態が消える）。

**判別方法**: Windows 開発機からは iOS Safari の Web インスペクタに接続できない（Mac が必須。
Android は `chrome://inspect` で可能）。したがって**画面に出すしかない**。`stat` パネルへ以下 3 点を出せば
目視だけで判別できる。

- **ページ読み込みからの経過時間** — リセットされていたら③
- **`webglcontextlost` / `webglcontextrestored` の発火回数** — 増えていたら②
- **カスタムレイヤーの `onAdd` 呼び出し回数** — ②のとき復帰処理が走ったか。1 のままなら FBO を失ったまま

**2. モバイルでの証跡取得** — 同じ理由でコンソールから `__runSubRtSuite` を叩けない。
PoC ページにスイート起動ボタン（または `?autorun=1`）を足せば、タップだけで走り結果は
`/__perf-report` へ POST されるため端末側に何も要らない。

**3. 対象プラットフォームの明記** — モバイルを対象に含めるなら、本計画書と README に対応環境を
書き、iOS / Android を検証項目として正式に立てる。**含めないなら、それも明示する**（現状は暗黙のうちに
Windows のみを見ている状態）。

> **注意**: 1 が片付くまで「iOS 対応 OK」と結論しないこと。③（タブ破棄）を見落としたまま
> 安心する形になる。一次確認で分かったのは**描画互換性**であって**セッションの継続性**ではない。

#### 【2026-07-27 ユーザー判断】バックグラウンド復帰判別の事前PoC検証は行わない

上記「保留（次に潰す）」1〜3（バックグラウンド復帰の判別・モバイルでの証跡取得・対象プラットフォームの明記）について、事前のPoC検証・実装は行わないと決定した。**モバイルは対象に含める**が、描画互換性は一次確認済み（iPhone 15 Pro Max / Safari で`GL_POINTS`・`gl_PointSize`・FBO二層合成・毎秒更新すべて動作）であり、バックグラウンド復帰時の③（iOSによるタブ破棄＝ページ再読み込み）が実際に問題になるかは**リリース後に問題が発覚してから対応する**方針とする。事前に`stat`パネルへの経過時間・lost/restored回数・onAdd回数の作り込みは行わない。

この判断により、本計画書における「モバイル」関連の残作業は移行判断のGO/NO-GOには含めない（描画互換性の一次確認で必要な確認は完了済みという扱いとする）。

### 測定方法

**律速の切り分け（項目1）——最初にやる**:

律速が CPU か GPU フィルレートかは、負荷の"次元"を独立に振って FPS の反応を見て分離する。以下 3 軸の反応表を作れば律速要因が一意に定まる。

- **DPR（内部解像度）を振る**: `deviceScaleFactor` を 1.0 / 1.5 / 2.0 と変える（`Emulation.setDeviceMetricsOverride`、または描画バッファ解像度）。塗る面積は DPR² に比例するので、**DPR を下げただけで FPS が大きく改善するなら GPU フィルレート律速**。
- **CPU スロットルを振る**: `Emulation.setCPUThrottlingRate` を 1×/2×/4× と変える。DPR 一定でスロットル倍率にほぼ比例して悪化するなら **CPU 律速**。
- **塗り面積 vs 頂点数を別々に振る**: 活断層の線幅・面塗り有無（＝被覆ピクセル）と、頂点密度（間引き前/後＝頂点数）を独立に変える。線幅を増やすと悪化し頂点を増やしても変わらないなら **フィルレート律速**、頂点数に比例して悪化するなら **CPU/頂点処理律速**。
- **GPU 実測**: Chrome DevTools Performance の GPU トラック、`chrome://gpu`、`chrome://tracing`（gpu カテゴリ）で GPU フレーム時間を直接確認する。

**FPS・ロングタスク**:

- Playwright + CDP。静止 / パン / ズームの 3 局面で FPS・ロングタスク（>50ms）を `PerformanceObserver` で計測。比較対象は現行実装（Leaflet）。
- マウス自動操作は穏やかに、または MapLibre のカメラ API（`panBy`/`zoomTo`/`flyTo`）で代替。CPU スロットルは rate 4（6 倍はクラッシュしたため緩和）。
- **実機ではメモリも測る**（`performance.memory`・タスクマネージャ）。RAM 4GB 構成では実行時メモリがタブキル・context lost の誘因になるため、Leaflet 版との差分を記録する。

**実機（Surface Go 2）を前倒しする**:

- CPU スロットルは CPU 性能しか模擬できず、**UHD 615 の GPU 性能・220ppi の高精細は実機でしか再現できない**。よって**律速の切り分け（項目1）は開発機では原理的に判定不能**。薄い PoC を早期に Surface Go 2 実機へ載せ、上記 3 軸を実機で振ることを **PoC 最初のマイルストーン**にする。
- PoC ビルドは GitHub Pages のブランチ、または同一 LAN の dev サーバーへ出す。実機で律速が判明して初めて下記「フィルレート律速だった場合の分岐」が引ける。ここまで通って初めて全面リライトの確信が持てる。

### フィルレート律速だった場合の分岐

律速がフィルレートと判明しても「一本化 or 別対策」の二者択一ではない。判断変数は「一本化する/しない」「間引きを廃止する/しない」「DPR を抑える/抑えない」の 3 つで、律速の正体が決めるのはこの組み合わせである。

- **間引き廃止は保留** — 間引き（Douglas-Peucker）は形状保存のため、廃止で増えるのは頂点数であり塗り面積はほぼ不変（§2.4）。フィルレート律速には効きも悪化もせず、当初目的（フル解像度化）自体が効果対象外になる。
- **DPR 抑制が本命対策** — 塗り面積は DPR² に比例するため、実効 DPR ≒ 1.5 の描画バッファを 1.0 相当へ抑えれば塗り面積は半分以下になる。見た目の精細さ（ぼやけ）とのトレードオフは実機で確認する。
- **DPR 抑制の実装手段が一本化の是非を左右する** — MapLibre はコンストラクタの `pixelRatio` オプションで描画バッファ解像度を CSS 解像度から切り離せる（API 詳細は本設計時に Context7 で裏取り）。一方、現行 Leaflet+SVG は SVG をブラウザがデバイス解像度でラスタライズするため DPR 抑制の実装余地がほぼ無い（Canvas レンダラはズームアニメ中の再合成コストで撤退済み・§2.2）。よってフィルレート律速でも一本化は自動的に NO-GO にはならず、**「`pixelRatio` 抑制の手段として全面リライトのコストに見合うか」を改めて判断する**。

### 準備

- フル解像度データ生成: `build-active-faults.mjs` の `EPSILON` を 0（または大幅に小さく）にして再生成し、**間引き前後の頂点数を記録**する。
- 段階0 の差分更新化は本体に一時的に入れて実測してよい（有効なら本採用できる小改修のため）。

### 結果記録

段階0・PoC の実測結果は本節に追記する（計測日・環境・計測スクリプトの所在・数値・判断を残す）。

#### 2026-07-25 段階0: `KyoshinPoints` 差分更新化の before/after（開発機）

- **計測環境**: 開発機（非力ではない・165Hz モニタ）、dev サーバー（DMDSS 版）、リアルタイムモニタ表示・静止 15 秒、viewport 1918×1269、DPR 1.0、観測点 1,725 点（平常時・可視点 1）。CPU スロットルは CDP `Emulation.setCPUThrottlingRate`（1× / 4×）。
- **計測スクリプト**: `scripts/perf/measure-kyoshin-static.js`（ページへ注入し `window.__measureKyoshinStatic(15000)` を実行。rAF フレーム間隔・longtask・`kyoshin-points` ペインの MutationObserver で DOM 書き込みを計測。使い方はファイル冒頭コメント参照）。
- **改修内容**: `KyoshinPoints` の毎秒更新を二経路の差分更新に変更 — ①indices は「表示レベル（不可視=0 / 可視=index 7+）」に正規化して前回値と比較し、変化した点だけ `setStyle`。②半径は震度非依存のため `iconScale` 変化時のみ全点 `setRadius`。

| 指標 | before 1× | before 4× | after 1× | after 4× |
|---|---|---|---|---|
| kyoshin ペイン属性更新/秒 | 10,366.8 | 10,365.6 | **16.4** | **16.7** |
| ノード追加+削除/秒 | 11.3 | 11.9 | 11.6 | 10.6 |
| FPS | 167.9 | 125.7 | 169.3 | 138.5 |
| フレーム時間 p95 (ms) | 6 | 7 | 6 | 6 |
| フレーム時間 max (ms) | 29.3 | 194.1 | 17.8 | 82.3 |
| longtask 件数 / 合計 (ms) | 0 / 0 | 35 / 2,437 | 0 / 0 | 38 / 2,498 |
| longtask 最大 (ms) | 0 | 142 | 0 | 80 |

- **判明したこと**:
  1. §2.1 の「527 回/秒」は桁誤りで、**実際は約 10,370 回/秒**。内訳は 1,725 点 × 6 属性/秒 — `setStyle` の `stroke`/`fill`/`fill-opacity`/`fill-rule` 4 属性 + `d` 2 回（明示 `setRadius` に加え、`CircleMarker.setStyle` が内部で `setRadius` を再実行するため。明示 `setRadius` は丸ごと冗長だった）。
  2. 差分更新化で属性更新は **99.8% 削減**（10,367 → 16/秒。残りは `KyoshinSubThreshold` の正常な差分更新分）。フレーム時間 max は半減以下（4×: 194 → 82ms）、FPS は +13（4×: 126 → 139）。
  3. **4× スロットルの longtask 合計はほぼ不変**（約 2.4 秒/15 秒）。LoAF（`long-animation-frame`）の帰属は全件 react-dom の `performWorkUntilDeadline` — **毎秒の React 再レンダー（`indices` 等の setState → App 以下の再レンダー）が層 A の第二成分**として残る。ただし dev ビルド（開発モードの React・StrictMode）での数値のため、そのまま本番の重さとは言えない。
- **判断**: 層 A の第一成分（DOM 書き込み）は解消し、差分更新化は本採用する。層 A の残差（毎秒 React 再レンダー）が実機の体感にどれだけ効くかは、**実機（Surface Go 2）× 本番ビルド**で §6 の計測を再実施してから判断する（実機前倒しの方針どおり）。第二成分の対策（メモ化・再レンダー範囲の限定など）は React 側の安価な改修であり、MapLibre 一本化の要否判断とは独立に検討できる。

#### 2026-07-25 段階0: 実機（Surface Go 2）before/after — dev ビルド・LAN 経由

- **計測環境**: Surface Go 2 実機（Edge 150）、viewport 1272×768・**DPR 1.5**（Windows スケーリング 150%。§3 の負荷特性想定どおり）、開発機の dev サーバーへ LAN 接続（before = main / after = 差分更新化ブランチの 2 サーバー並走）、リアルタイムモニタ表示・静止 15 秒。**dev ビルド**（開発モードの React）である点に留意。
- **計測方法**: `GET /__perf-script`（自動実行モード: タブ切替→観測点ロード待ち→計測）→ `POST /__perf-report` で開発機へ証跡自動保存（`scripts/perf/vite-plugin-perf-report.ts`）。証跡: `scripts/perf/results/perf-2026-07-24T19-45-59-surface-go2-before.json`・`perf-2026-07-24T19-52-06-surface-go2-after.json`（before は `19-52-53` の再計測でも p95 33.2ms・longtask 1,691ms と整合し再現性を確認。表の値は初回計測）

| 指標 | before | after | 変化 |
|---|---|---|---|
| kyoshin ペイン属性更新/秒 | 10,606.9 | **21.8** | −99.8% |
| FPS | 48.2 | **56.8** | +8.6（ほぼ 60 へ回復） |
| フレーム時間 p50 (ms) | 16.7 | 16.7 | 同（60Hz 上限） |
| フレーム時間 p95 (ms) | 33.3 | **17.1** | 半減（毎フレーム落ち → ほぼ 1 vsync） |
| フレーム時間 max (ms) | 349.9 | **149.9** | −57% |
| longtask 件数 / 合計 (ms) | 16 / 1,593 | 11 / **608** | 合計 −62% |
| longtask 最大 (ms) | 340 | **69** | −80% |

- **判明したこと**:
  1. **実機では差分更新化の効果が開発機スロットルより大きい**。開発機 4× では longtask 合計がほぼ不変だったが、実機では −62%。CPU スロットルは JS 実行しか遅くしない一方、実機では DOM 書き込み後のスタイル計算・ペイントも本当に遅く、DOM 書き込み削減がそこへ直接効いたため。**CPU スロットルでは層 A の実害を過小評価する** — 「実機前倒し」（§6）の方針の実証でもある。
  2. 静止時の体感を決める p95 は 33.3ms（毎フレーム落ち）→ 17.1ms（ほぼ 60fps 張り付き）へ改善。340ms 級の引っ掛かりも消えた。
  3. 残る longtask（毎秒 40ms 相当・最大 69ms）は開発機と同じく毎秒の React 再レンダー由来と推定。FPS が 60 に届き切らない主因もこれ。ただし dev ビルドの React は本番より重い。
- **判断**: 層 A 第一成分の解消は**実機でも有効**。差分更新化は本採用で確定。残差の扱いは (a) **本番ビルドでの実機再計測**で残差の実寸を確認 → (b) 必要なら再レンダー範囲の限定（`indices` を地図レイヤーへ届ける経路の分離など安価な改修）を層 A 第二弾として検討。層 B（フル解像度・律速切り分け）は §6 の PoC として別途進める。

#### 2026-07-25 層B（項目1）: 律速切り分け PoC — 実機（Surface Go 2）・**判定不成立（pan 飽和）**

- **計測環境**: Surface Go 2 実機（Edge）、viewport 1272×768・**DPR 1.5**・**60Hz**、開発機の `npm run dev:poc` へ LAN 接続。PoC は素の maplibre-gl（React/PWA 無し・`vite.poc.config.ts`）で 背景 GEBCO raster ＋ 活断層 line ＋ 観測点 circle のみ。
- **計測方法**: `poc/measure.ts` の `await window.__runLayerBSuite('surface-go2')`。static/pan/zoom を rAF ごとのカメラ操作で駆動し、フレーム時間・longtask を `POST /__perf-report` で開発機へ自動保存。証跡: `scripts/perf/results/perf-2026-07-25T04-0[6-8]-*-surface-go2-*.json`（8件・各10秒）。
- **描画状態**: 活断層は**間引き無しフル解像度 31,646 頂点**（`scripts/perf/build-active-faults-full.mjs` 生成。本体の間引き後は 9,874 頂点）、観測点 1,458 点。

| run | phase | p95 (ms) | fps | longtask 件/最大 | pixelRatio | 塗り面積(px) | 頂点 | 線幅 |
|---|---|---|---|---|---|---|---|---|
| baseline | static | **16.9** | 60.0 | 0 / 0 | 1.5 | 2.20M | 31,646 | 1.2 |
| baseline | pan | **16.9** | 59.9 | 0 / 0 | 1.5 | 2.20M | 31,646 | 1.2 |
| baseline | zoom | **33.3** | 54.2 | **6 / 85** | 1.5 | 2.20M | 31,646 | 1.2 |
| dpr-1.0 | pan | **16.9** | 59.7 | 0 / 0 | 1.0 | 0.98M | 31,646 | 1.2 |
| dpr-0.75 | pan | **16.9** | 59.9 | 0 / 0 | 0.75 | 0.55M | 31,646 | 1.2 |
| verts-thin | pan | **16.9** | 59.8 | 0 / 0 | 1.5 | 2.20M | 9,874 | 1.2 |
| fill-lw4 | pan | **16.9** | 59.9 | 0 / 0 | 1.5 | 2.20M（線3.3倍） | 31,646 | 4 |
| points-off | pan | **16.9** | 59.9 | 0 / 0 | 1.5 | 2.20M | 31,646 | 1.2 |

- **判明したこと**:
  1. **pan の律速切り分けは測定不能で不成立**。p95 が全軸 16.9ms 一定 — 60Hz の vsync 上限 16.7ms に baseline から張り付いており、天井より上の余裕は原理的に測れない。塗り面積を 1/4（2.20M→0.55M px）にしても、頂点を 68% 削っても、線幅を 3.3 倍にしても数値が 0.1ms も動かなかった。全軸が同値を返す以上、この p95 は軸ではなく vsync を測っている。上記「フィルレート律速だった場合の分岐」も CPU/頂点律速も**どちらも引けない**。
  2. ~~層 B の当初懸念は pan については否定された。pan が天井なら Leaflet に負ける余地も無い（最良でも同着）ため、検証項目2 は pan について Leaflet 側を測らずとも満たされる~~ → **【訂正・撤回】**（下記「2回目」）。飽和は律速の不在を証明せず検出失敗にすぎないため「否定された」は言い過ぎで、正しくは**「現行負荷では天井を超えない。headroom は未測定」**まで。Leaflet 比較の主張も誤り: Leaflet の pan は CSS transform のコンポジタ合成のみで再ペイントがほぼ走らない（§2.4・§8）のに対し MapLibre は毎フレーム 2.2M px を塗り直すため、**同じ 60fps でも仕事量は桁違い**。飽和した指標は「余裕で 60fps」と「かろうじて 60fps」を区別できず、差は熱・電力と**複合負荷（検証項目7）に回せる余裕**として現れる。**検証項目2 は Leaflet 側を実測しない限り閉じられない**。
  3. ~~劣化が出たのは zoom のみ。longtask との同時発生は CPU 側の作業を示唆~~ → **【訂正・撤回】** 2回目で**タイル初回読み込みのウォームアップ交絡**と判明（下記）。軸の反応ではなかった。
  4. ~~計測器の健全性は確認できた（zoom で劣化を検出しているので pan は本当に軽い）~~ → **【訂正・撤回】** 検出していたのはウォームアップであり、軸に対する感度の証明にはならない。
  5. **`max` は判定に使えない**。DPR を下げた run の方が max が悪化する非単調（dpr-1.0 で 42.5ms > baseline 33.3ms）が出た。単発のタイル読み込み・GC・`pixelRatio` 変更時の描画バッファ再確保が乗るため。傾向は p95 と longtask で読む。
  6. **PoC 側の設計欠陥を1つ発見・修正済み**: `measure.ts` の run 定義が baseline 以外を `phases: ['pan']` にしていたため、**劣化が見えた zoom に軸を1つも振っていなかった**（＝交絡かどうかを判別できない構成だった）。各軸を `['pan','zoom']` へ拡張した（所要 約2.3分・13計測）。この拡張によって 2回目でウォームアップ交絡を検出できた。
- **判断（この時点のもの。下記「2回目」で改訂済み）**: 「pan では律速していない」と読み、測る局面を zoom へ移して継続する方針とした。この判断は飽和の誤読（項目2・4）と zoom の交絡（項目3）を含むため、**改訂後の判断は下記「2回目」を参照する**。

#### 2026-07-25 層B（項目1）: 2回目 — zoom へ軸拡張して再計測。**1回目の zoom 解釈を撤回（ウォームアップ交絡）**

- **計測環境・方法**: 1回目と同一（Surface Go 2・Edge・viewport 1272×768・DPR 1.5・60Hz・開発機の `dev:poc` へ LAN 接続）。`measure.ts` の軸 run を `['pan','zoom']` へ拡張して `__runLayerBSuite('surface-go2')` を再実行。証跡: `scripts/perf/results/perf-2026-07-25T04-4[6-8]-*-surface-go2-*.json`（13件・各10秒）。
- **pan は1回目と完全一致**（全軸 p95 16.9ms・longtask 0）。**zoom の行**:

| run | phase | p95 (ms) | fps | max | longtask 件/最大/合計 | pixelRatio | 頂点 | 線幅 | 実行順 |
|---|---|---|---|---|---|---|---|---|---|
| baseline | zoom | **33.3** | 55.5 | 66.7 | **3 / 53 / 156** | 1.5 | 31,646 | 1.2 | 3番目 |
| dpr-1.0 | zoom | **16.9** | 58.2 | 84.1 | 2 / 91 / 180 | 1.0 | 31,646 | 1.2 | 5番目 |
| dpr-0.75 | zoom | **16.9** | 59.3 | 49.9 | 1 / 62 / 62 | 0.75 | 31,646 | 1.2 | 7番目 |
| verts-thin | zoom | **16.9** | 59.7 | 33.2 | **0** | 1.5 | 9,874 | 1.2 | 9番目 |
| fill-lw4 | zoom | **16.9** | 59.7 | 33.4 | **0** | 1.5 | 31,646 | **4** | 11番目 |
| points-off | zoom | **16.9** | 59.5 | 33.4 | **0** | 1.5 | 31,646 | 1.2 | 13番目 |

- **判明したこと**:
  1. **1回目の zoom 劣化はウォームアップ交絡だった**。決定的な矛盾は `fill-lw4`: 線幅 1.2→4 で**塗り面積を 3.3 倍に増やした**のに、baseline zoom の 33.3ms から **16.9ms へ改善**している。GPU に塗らせるピクセルを 3 倍にして速くなることは物理的にあり得ない。負荷を減らす `points-off` と負荷を増やす `fill-lw4` が**同じ 16.9ms に着地**する以上、33.3 → 16.9 の差は軸が作ったものではない。
  2. **正体は初回タイル読み込み**。longtask を実行順に並べると **3 → 2 → 1 → 0 → 0 → 0** の単調減衰（上表「実行順」列）。baseline zoom はスイート中で最初に z4〜7 を訪れる計測で、その時点で GEBCO ラスタは未キャッシュ・ジオメトリも当該ズームレベルで未タイル化。初回だけ「タイル取得＋デコード＋再タイル化＋頂点バッファ生成」を丸ごと払っている。DPR を変えた 5・7 番目に longtask が残るのも、`pixelRatio` 変更が描画バッファ再確保を強制するためで整合する。
  3. **pan・zoom とも全軸が vsync 上限に飽和**しており、律速切り分けは**両局面で不成立**。
  4. **飽和は律速の不在を証明しない**。「余裕で 60fps」と「かろうじて 60fps」を区別できないため headroom は未知のまま。§8 はフィルレート律速を「GO/NO-GO を分ける前提条件」と位置づけているが、**飽和した指標ではこの条件を否定も肯定もできない**。
  5. **計測設計が上記「測定方法」の指定から外れていた**（今回の最大の反省）:

| 「測定方法」の指定 | 今回の PoC |
|---|---|
| DPR を 1.0 / 1.5 / **2.0** で振る | 1.5 / 1.0 / **0.75** — **下げる方向のみ**。天井下での比較は何も測っていない |
| CPU スロットル 1× / 2× / 4× を軸に振る | 未実施 |
| **GPU フレーム時間を直接見る**（Performance の GPU トラック・`chrome://tracing`） | 未実施。rAF 間隔のみ＝構造的に vsync でクランプされる指標 |
| 比較対象は現行実装（Leaflet） | 未実施 |
| 実機ではメモリも測る（`performance.memory`・タスクマネージャ） | 未実施。`measure.ts` に項目が無い。なお `performance.memory` は JS ヒープのみで WebGL バッファ・テクスチャを含まないため、タスクマネージャ併用が要る（§8「実行時メモリ」） |

  6. **有効な新規知見**: 初訪問ズームレベルで 50〜90ms の longtask が出る。これは MapLibre 固有ではない（GEBCO ラスタは Leaflet 版も同じものを引く）が、WebGL 版ではジオメトリ再タイル化が上乗せされる。**EEW の `flyTo` は「初訪問ズームレベルへ飛ぶ」動作**なので、この初回コストが最悪局面で出る懸念がある → **検証項目7（EEW 複合負荷）で見るべき具体的観点**として扱う。
- **判断（1回目から改訂）**: 層 B について現時点で言えるのは**「現行の負荷では pan・zoom とも天井を超えない。ただし残り余力は未測定」**まで。「律速が無い」「Leaflet と同等以上」はいずれも**主張できない**。一本化の GO/NO-GO には中立で、根拠にも反証にもならない。次アクション: (a) 計測設計を「測定方法」の指定へ寄せる（DPR を **2.0 へ上げる**・線幅 8/12 追加・`performance.memory` 追加・**ウォームアップパス**＋末尾に対照 `baseline-warm`）→ (b) vsync にクランプされない指標を取る（GPU フレーム時間。`EXT_disjoint_timer_query_webgl2` が UHD 615 で使えるか要確認）→ (c) Leaflet 比較（本体側にフル解像度活断層を入れ同条件で測る。PoC 範囲外の別工数だが**検証項目2 はこれ無しに閉じられない**）→ (d) 検証項目5（非加算合成・最難関）。観点の詳細は [webgl-layerb-verification-points.md](webgl-layerb-verification-points.md)。

#### 2026-07-25 層B（項目1）: 3回目 — 負荷を上げる方向へ改訂して再計測。**律速に到達せず＝検証項目1 決着**

- **計測環境**: 1・2回目と同一（Surface Go 2・Edge・viewport 1272×768・DPR 1.5・60Hz・開発機の `dev:poc` へ LAN 接続）。
- **スイート改訂**（レビュー [webgl-poc-review-4b0517c.md](webgl-poc-review-4b0517c.md) の HIGH 3件対応後）: ①冒頭に**全整数ズーム（z4〜7）のウォームアップ**（初回タイル読み込み・geojson-vt 再タイル化の交絡除去）②**負荷を上げる方向**の軸（DPR 2.0・線幅 8/12・両者を重ねた `overload`）③**全計測の開始 zoom を z5 に固定**（可視ジオメトリ量を揃える）④末尾に **`baseline-warm` 対照**。19計測・各10秒・約3.5分。
- **証跡**: `scripts/perf/results/perf-2026-07-25T10-5*-surface-go2-*.json`（19件）。

**p50 は全19件が 16.7ms。p95 は 16.9〜17.0ms（振れ幅 0.1ms）。** 主要 run:

| run | phase | p95 (ms) | fps | max | longtask 件/最大 | pixelRatio | 塗り面積 | 線幅 | 頂点 | 点 |
|---|---|---|---|---|---|---|---|---|---|---|
| baseline | static / pan / zoom | 16.9 / 16.9 / **17.0** | 59.9 / 59.9 / 58.2 | 17.2 / 17.2 / 50.0 | 0 / 0 / 2·58 | 1.5 | 2.20M | 1.2 | 31,646 | 1,458 |
| **overload-dpr2-lw12** | pan / zoom | **16.9 / 17.0** | 60.0 / 58.5 | 17.5 / 50.1 | 0 / 1·58 | **2.0** | **3.91M** | **12** | 31,646 | 1,458 |
| dpr-2.0 | pan / zoom | 17.0 / 17.0 | 59.9 / 58.2 | 17.5 / 100.2 | 0 / 1·97 | 2.0 | 3.91M | 1.2 | 31,646 | 1,458 |
| fill-lw12 | pan / zoom | 16.9 / 16.9 | 59.1 / 59.6 | 69.6 / 33.3 | 1·86 / 0 | 1.5 | 2.20M | 12 | 31,646 | 1,458 |
| fill-lw8 | pan / zoom | 16.9 / 16.9 | 59.9 / 59.5 | 18.1 / 33.5 | 0 / 0 | 1.5 | 2.20M | 8 | 31,646 | 1,458 |
| verts-thin | pan / zoom | 16.9 / 16.9 | 59.9 / 59.4 | 17.1 / 49.9 | 0 / 0 | 1.5 | 2.20M | 1.2 | **9,874** | 1,458 |
| dpr-1.0 | pan / zoom | 16.9 / 16.9 | 59.9 / 59.6 | 17.1 / 33.4 | 0 / 0 | 1.0 | **0.98M** | 1.2 | 31,646 | 1,458 |
| points-off | pan / zoom | 16.9 / 16.9 | 59.9 / 59.4 | 17.3 / 33.4 | 0 / 0 | 1.5 | 2.20M | 1.2 | 31,646 | **0** |
| **baseline-warm**（対照） | **zoom** / pan | **16.9** / 16.9 | 59.5 / 59.9 | 33.3 / 17.6 | **0** / 0 | 1.5 | 2.20M | 1.2 | 31,646 | 1,458 |

- **判明したこと**:
  1. **1・2回目の zoom 劣化（33.3ms）がウォームアップ由来だと対照実験で確定**。ウォームアップ後の baseline zoom は 17.0ms、スイート最後の `baseline-warm` zoom は **16.9ms**。longtask も 6件/368ms → 3件/156ms → 2件/58ms → 0件と、ウォームアップの効きに応じて消えた。2回目の「`fill-lw4` で塗りを 3.3 倍にしたのに改善する」という矛盾から立てた交絡仮説は正しかった。
  2. **意図的に負荷を上げても vsync 天井（16.7ms）を破れなかった**。振った範囲は 塗り面積 0.98M→**3.91M px（4.0 倍）**・線幅 1.2→**12（10 倍）**・頂点 9,874→31,646（3.2 倍）・観測点 0→1,458、および両者を重ねた `overload`（3.91M px ＋ 線幅 12）。**全 19 計測が p95 16.9〜17.0ms・fps 58.2〜60.0**。
  3. **これは 1・2回目の「飽和」より強い主張**である。前 2 回は天井に張り付いた baseline から負荷を**下げて**動かなかっただけで、何も測っていなかった。今回は**上げて**動かなかったため、**「実配置（2.20M px・線幅 1.2）の 1.78 倍の塗り面積を、線幅 10 倍と同時に背負っても余力がある」という上界の言明**になる。
  4. **検証項目1（律速切り分け）は決着**。フィルレート律速でも CPU/頂点律速でもなく、**実配置の 1.8 倍以上の負荷まで律速に到達しない**。上記「フィルレート律速だった場合の分岐」は発動しない。
  5. **間引き廃止は描画コストの観点で青信号**。フル 31,646 頂点と間引き後 9,874 頂点で p95 の差が測定不能（ともに 16.9ms）。当初目的（フル解像度化）は、少なくとも Surface Go 2 の描画負荷としては無償で達成できる。
  6. **GPU フレーム時間を直接測る必要性が下がった**。rAF 間隔が vsync でクランプされるため GPU 時間が要るという判断だったが、`overload` で**余力を実測で上から抑えた**ためこれが代替になる。DPR 3・線幅 24 等でさらに詰めれば破断点は分かるが、判断に必要なのは「実配置に余裕があるか」であり、そこは答えが出た。
  7. **zoom だけ max 33〜50ms・fps 58〜59.6 が全 run で一定**に出る（pan は max 約 17ms）。p95 は 16.9ms のままなので 5% 未満のフレーム。軸依存でないため、連続スイープが整数ズームを跨ぐ際のタイルセット入れ替えが濃厚。**律速ではない**。
  8. **単発の longtask**（`dpr-2.0` zoom 97ms・max 100.2ms、`fill-lw12` pan 86ms）は軸と単調な関係が無く、`pixelRatio` 変更に伴う描画バッファ再確保が乗った公算。p50/p95 は無傷。
  9. **メモリ・安定性**: `jsHeapMB` は 38〜107MB で振動（GC）し暴走なし。**WebGL 初期化エラー・context lost は発生せず**。ただし `performance.memory` は JS ヒープのみで WebGL バッファ/テクスチャを含まないため、真の実行時メモリはタスクマネージャ併用が要る（§8）。
- **判断**: **層 B は閉じる。** 検証項目1 は「律速に到達しない」で決着し、間引き廃止も描画コストとしては無償と確認できた。ただし**一本化の GO/NO-GO はこれで決まらない**。残る論点は (a) **検証項目2 の Leaflet 比較**（MapLibre は天井にいるが、Leaflet の pan はコンポジタ合成のみで再ペイントがほぼ走らない（§2.4・§8）。両者 60fps でも仕事量が違うため、実測しない限り「同等以上に軽い」は言えない。ただし 1.8 倍の余力を実測できたことで「複合負荷に回す余裕が無い」懸念は大幅に薄まった）(b) **検証項目5 非加算合成（最難関）** (c) 毎秒更新・EEW 複合負荷 (d) 真の実行時メモリ。**次は最大の未検証リスクである検証項目5 へ進む**。観点の詳細は [webgl-layerb-verification-points.md](webgl-layerb-verification-points.md)、スイート改訂の経緯は [webgl-poc-review-4b0517c.md](webgl-poc-review-4b0517c.md)。

#### 2026-07-25 検証項目5（非加算合成・最難関）: **実機で成立。カスタムレイヤーの FBO 二層合成でクリア**

- **PoC**: `poc/subthreshold.html` / `poc/subthreshold.ts`（使い捨て）。3ペイン比較 — 左=標準 circle（①違反の見本）／中=現行 SVG 相当の `<g opacity>` 見本（正解）／右=カスタムレイヤー（FBO 二層合成）。同一のテスト点配置（index3 を80点密集・index5 を80点密集・両者を重ねたクラスタの3群）。
- **原理**: レベルごとに①オフスクリーン FBO へ**不透明**で全点を描き（同レベルの重なりは飽和＝濃くならない）→②その FBO を `subThresholdOpacity(level)` で本描画へ premultiplied over 合成（レベル間は別 FBO なので積み重なる）。現行 SVG の `<g opacity>` と等価。
- **レビュー経緯**: `3c2ddaf`（初版）→ [webgl-poc-review-3c2ddaf.md](webgl-poc-review-3c2ddaf.md) の指摘 → `3a42d36`（feedback loop 解消・GL 状態復元）。LOW 1件（点ゼロのフレームで FBO 束縛が漏れる）も対応済み（`render` 末尾の復元に `bindFramebuffer(mainFBO)` を追加）。
- **計測環境**: Surface Go 2 実機（Edge）、viewport 1272×768・DPR 1.5・60Hz、開発機の `dev:poc` へ LAN 接続。`await window.__runSubSuite('surface-go2-sub')` で 3方式 × static/pan/zoom = 9計測・各8秒。証跡: `scripts/perf/results/perf-2026-07-25T15-0[7-8]-*-surface-go2-sub-*.json`（9件）。

| 方式 | phase | p50 | p95 | fps | max | longtask 件/最大/合計 |
|---|---|---|---|---|---|---|
| std（標準 circle） | static / pan / zoom | 16.7 | 16.8 / 16.8 / 16.9 | 59.4 / 59.5 / 59.9 | 88.8 / 37.8 / 24.0 | 0 / 0 / 0 |
| svg（見本・下記留保あり） | static | 16.7 | 16.9 | 60.0 | 17.1 | 0 |
| svg | **pan** | 16.7 | **33.3** | **50.9** | **287.4** | **3 / 276 / 511** |
| svg | **zoom** | 16.7 | 16.9 | **55.3** | **179.4** | **5 / 175 / 426** |
| **custom（FBO 二層合成）** | static | 16.7 | 16.9 | 59.0 | 83.3 | **0** |
| **custom** | **pan** | 16.7 | **16.8** | **60.1** | **19.5** | **0** |
| **custom** | **zoom** | 16.7 | **16.8** | **59.9** | **21.2** | **0** |

- **判明したこと**:
  1. **FBO 経路が UHD 615 で成立した**。カスタムレイヤーは全局面で **59〜60fps・longtask 0 件**。スイート9件が完走し、**実機目視でも左下 `ERROR:` の表示なし・地図の黒画面なし・描画崩れなし**（ユーザー確認済み）。§8 の最悪障害モード（非力 GPU で MapLibre が破綻／context lost で地図消失）はこの PoC の範囲では発生しない。
  2. **合成は数値的に正しい**。開発機で解析期待値と突き合わせ済み — 背景(17,20,24)・点色(156,163,175)・`subThresholdOpacity(3)`=0.13214・同(5)=0.26500 から計算した over 合成の期待値に対し、実測は index3密集 (36,38,44) / index5密集 (53,58,64) / index3+5 (67,71,78) でいずれも±1で一致。**① 同レベル内は重なっても濃くならない・② レベル間は素直に濃くなる の二層挙動が成立**。同条件の標準 circle は (127,129,142)〜(153,160,171) と加算され①違反の負の対照として機能した。**実機でも「中=SVG 正解」と「右=カスタム」の濃さが一致**（ユーザー目視確認済み）。
  3. **FBO 往復のオーバーヘッドは測定限界以下**。`custom` と `std`（標準 circle）が pan で p95 16.8ms・fps 59.5/60.1 と同等。レベルごとの FBO 描画＋フルスクリーン quad 合成を6レベル分行っても標準 circle と差が出ない。**①の非加算合成を実質無償で得られている**。
  4. **【留保】svg 見本との差を「現行より軽い」と読んではいけない**。この PoC の SVG 見本は `map.on('move', draw)` で毎フレーム `replaceChildren()` ＋ 320個の `<circle>` 全再生成を行う簡易版で、276ms の longtask はほぼ DOM 再生成のコスト。**現行 `KyoshinSubThreshold` は差分更新でパン追従を Leaflet のペイン transform に任せており（毎フレームの再投影をしない）、実物はこれより軽い**。PoC 側コメントも「地図追従は move で再描画する（静止比較には十分）」と範囲を限定している。有効な比較は **static の行**（svg 60fps / custom 59fps ＝ 同等）。**検証項目2（現行 Leaflet 比）は依然として未解決**。
  5. **絶対値は本番負荷ではない**。`TEST_RADIUS = 9`（本番は `BASE_RADIUS 2.5 × iconScale`）で1点あたり塗り面積が約13倍、点数は 320 対 本番約1,725 と逆方向にずれる。方式間の相対比較には有効。
- **判断**: **検証項目5（WebGL 共通の最難関）はクリア**。標準 circle では原理的に不可能な①を、カスタムレイヤーで**現行と等価に・実機で 60fps・追加コストほぼゼロ**で再現できた。§8 のリスク一覧のうち「非加算 opacity 合成の再現」と「非力 GPU での MapLibre 安定性」は本項目の範囲では解消。**残る GO/NO-GO の論点**: (a) **検証項目2 の Leaflet 比較**（未実施・本番実装同等の差分更新版と比べる必要がある）(b) **検証項目6 毎秒更新**・**項目7 EEW 複合負荷**（最悪局面）(c) 項目3 カメラ操作・項目4 当たり判定 (d) 真の実行時メモリ・テキスト描画（§8）。次は **項目6・7（毎秒更新と複合負荷）** を推す — 層Bで余力が確認され項目5 も解けた今、残る最大の未知は「毎秒の全画面再描画＋EEW 同時実行」である。

#### 2026-07-26 検証項目6（毎秒更新）: **実機で問題なし。毎秒の全画面再描画はカクつかない**

- **PoC**: `poc/realtime.html` / `poc/realtime.ts`（使い捨て）。**本番相当のベースマップ**（県境 fill＋一次細分区域境界＋県境線＝**40,917 頂点**）を敷いた上に観測点 **1,725 点**（`BASE_RADIUS 2.5`＝本番半径）を置き、毎秒**全点をランダムな lvl(0..9) に更新**する（＝毎秒全点変化の最悪ケース。WebGL は変化数に依らず全画面再描画になるため上界を取る）。
- **4 モードの比較**: `baseline`（更新なしの床）／`repaint-only`（属性更新せず `triggerRepaint` のみ＝**全画面再描画コスト単独**）／`feature-state`（GPU 上の状態だけ更新）／`setData`（GeoJSON 丸ごと差し替え＝geojson-vt が worker で再タイル化）。
- **主指標は `settle`（apply → 次の `idle` までの収束時間）**。毎秒 1 回の更新は全フレームの約 1.7%（60Hz）しか占めず `p95` の外に埋もれるため、`p95` では判定できない（レビュー経緯は [webgl-poc-review-9b97a37.md](webgl-poc-review-9b97a37.md)）。
- **レビュー経緯**: `9b97a37`（初版）→ 指摘 → `3560156`（指標・負荷の是正）→ `aa7067d`（`updateFrame` の 1 フレームずれ）→ `e27552b`（`settle` 追加・FIFO 化）。LOW 1件（計測末端で最後の apply の裾が切れ `mean` が下振れ）は `p50`/`max` に影響しないため未対応。
- **計測環境**: Surface Go 2 実機（Edge）、viewport 1272×768・DPR 1.5・60Hz・canvas 1908×1152、開発機の `dev:poc` へ LAN 接続。`await window.__runRealtimeSuite(…)` で 4 モード × 12 秒。**計 3 回実施**（下記「実機のばらつき」）。証跡: ① `perf-2026-07-26T03-2[23]-*-surface-go2-rt-*`（4件・LOW8 修正前）／② `perf-2026-07-26T11-1*-surface-go2-rt-*`（4件・LOW8 修正後）／③ `perf-2026-07-26T11-2*-surface-go2-rt2-*`（4件・同）。**②③は LOW8 修正の実機確認も兼ねる** — `apply`/`settle`/`updateFrame` の n が **12/12/12** で一致（①は 12/12/11 と裾切れ）・`settleTimeouts` は 3 回とも 0（`DRAIN_MS` 1000ms が実機の 356〜378ms に足りている）。

| mode | fps | frame p50/p95/max | **settle p50/max** | updateFrame p50/max | applyMs p50 | longtask | heap |
|---|---|---|---|---|---|---|---|
| baseline | 59.83 | 16.7 / 16.8 / 35.4 | — | — | — | 0 | 44MB |
| **repaint-only** | 59.92 | 16.7 / 16.8 / 28.6 | **6.6 / 14.4** | 16.7 / 16.8 | 0.1 | 0 | 31MB |
| **feature-state** | 59.83 | 16.7 / 16.8 / 33.2 | **16.3 / 39.8** | 16.7 / 33.2 | 1.2 | 0 | 35MB |
| **setData** | 60.0 | 16.7 / 16.8 / 20.7 | **356.1 / 364.8** | 16.7 / 19.2 | 0.3 | 0 | 45MB |

`settleTimeouts` は全モード **0**（5 秒以内に必ず収束）。`longtask` も全モード **0 件**。

- **判明したこと**:
  1. **毎秒の全画面再描画は問題にならない**。`repaint-only` の `settle` が **6.6ms**（p50）— 本番相当のベースマップ 40,917 頂点＋観測点 1,725 を毎秒まるごと塗り直しても **1 フレーム弱で収束**する。fps 59.92・p95 16.8ms・longtask 0。**検証項目6 の懸念（「WebGL は 1 点の変化でも全ビューポート再描画になるため毎秒カクつかないか」）は否定された**。層 B で確認した余力（塗り面積 4 倍・線幅 10 倍でも 60fps）がここでも効いている。
  2. **`feature-state` は実用上ただ**。`settle` **16〜36ms**（3 回実測のレンジ・下記「実機のばらつき」参照）＝1〜2 vsync 相当。`applyMs` 1.2ms（`setFeatureState` を 1,725 回呼んで 1.2ms）。`repaint-only` との差が更新 CPU 分（約 10〜20ms）。
  3. **`setData` も周期内に収束した**。`settle` **356〜378ms**＝周期 1,000ms の **36〜38%**、`settleTimeouts` 0。**「素朴な `setData` は不可」という移植制約は確定しなかった**。ただし `feature-state` 比で **10〜22 倍**のコストであり、常時 4 割近くの時間は再描画状態にある。
  4. **開発機 317ms → 実機 356ms（1.1 倍）にとどまった**。事前予測は「非力 CPU で数倍に伸びる」だったが外れた。**geojson-vt の再タイル化は worker で走るためメインスレッドの速度差が効きにくい**のが理由と考えられる。
  5. **実機目視（ユーザー確認済み）**: 観測点の色が**毎秒切り替わっている**・**体感のカクつきなし**・`ERROR:` 表示なし・地図の黒画面なし・描画崩れなし。**数値（p95 16.8ms・longtask 0）と体感が一致**した。
  6. **メモリ**: `jsHeapMB` 31〜45MB で安定。毎秒更新を 12 秒 × 4 モード回しても増加傾向なし（JS ヒープのみ・§8 のとおりタスクマネージャ併用は別途必要）。
- **実機のばらつき（2026-07-26 追記・3 回計測）**: LOW8 修正後の再計測で 2 点追加し、計 3 回になった。
  **小さい値ほど相対ばらつきが大きい**ことが判明した。

| mode | ① 03-22 | ② 11-10 | ③ 11-21 | ばらつき幅 |
|---|---|---|---|---|
| `repaint-only` settle p50 | 6.6 | 12.0 | 10.2 | **約 1.8 倍** |
| `feature-state` settle p50 | 16.3 | 36.3 | 30.8 | **約 2.2 倍** |
| `setData` settle p50 | 356.1 | 361.4 | 377.9 | **約 1.06 倍** |

  ②③が近く①だけ低いため、**①が特に軽い条件を引いたと見るのが妥当**（②を外れ値とする説は消えた）。
  `baseline` の fps は 3 回とも 59.83〜59.92 で安定しており実機の不調ではない。
  **ミリ秒スケールの測定はサーマル・他プロセス・GC タイミングに埋もれる** — `setData` が 6% しか
  動かないのに 10ms 台が 2 倍動くのはそのため。**方式間の比較（10〜22 倍差）には十分だが、
  絶対値を単一実測で断定してはいけない。**
- **判断**: **検証項目6 はクリア**。毎秒更新は MapLibre でも軽く回せる。**本設計では `feature-state` を採用する**（`settle` 16〜36ms・更新 CPU 10〜20ms で、現行 Leaflet の差分更新と同等の軽さ。悪い方の実測を採っても周期 1,000ms の 3%）。`setData` も周期内に収束するため**フォールバックとして許容**できるが、22 倍のコスト差があるため常用しない。残る論点: (a) **検証項目7 EEW 複合負荷** — `setData` の 356ms や `feature-state` の毎秒更新に flyTo＋予報円アニメが重なる最悪局面。本項目で余力が確認できたとはいえ、**唯一この 356ms が効いてくる可能性がある局面**なので次に測る (b) 検証項目2 の Leaflet 比較 (c) 項目3 カメラ操作・項目4 当たり判定 (d) 真の実行時メモリ・テキスト描画（§8）。

#### 2026-07-26 検証項目7（EEW 複合負荷）: **実機で成立。最悪局面でも 60fps を維持**

- **環境**: 実機 Surface Go 2 / DPR 1.5 / canvas 1908×1152 / vsync 16.7ms（60Hz）/ ベースマップ 40,917 頂点 /
  強震点 1,725（`poc/eew-composite.ts`）
- **シナリオ**（本番 `JapanMap.tsx` / `PsWaveLayer.tsx` / `useDmdssWaves.ts` を調査して再現・各30秒）:
  EEW 領域 `setData` 追加 → `flyTo`（震源・0.8s）→ 予報円成長に伴う `fitBounds` 再フィット（0.8s）→
  強震点の毎秒全点更新 → 予報円の 100ms 更新 → `setData` 削除 → 全国 `fitBounds`（1.0s）
- **モード**: `baseline`（何も動かさない）／`flyto-only`（カメラ＋`setData` のみ）／
  `composite-pause`（全部・`flyTo` 中は予報円更新を停止＝**本番の緩和策を再現**）／
  `composite-nopause`（全部・停止しない＝最悪ケース）
- **2 スイート実行**（同一ページで連続。数値は「1回目 / 2回目」）

| mode | fps | `frame` max | dropped | **`flyTo` 区間** p50 / p95 / max | `flyTo` dropped | longtask |
|---|---|---|---|---|---|---|
| `baseline` | 59.10 / 59.67 | 250.1 / 84.5 | 6 / 5 | — | — | 2件(69ms) / **0** |
| `flyto-only` | 59.49 / 59.81 | 79.8 / 36.4 | 5 / 2 | 16.7 / 23.6・19.9 / 36.0・36.4 | 2 / 2 | 2件(73ms) / **0** |
| `composite-pause` | 59.92 / **59.97** | 31.7 / 33.3 | **0 / 0** | 16.7 / 19.5・18.5 / 31.7・33.3 | **0 / 0** | **0 / 0** |
| `composite-nopause` | 59.90 / 59.91 | 38.2 / 33.5 | 2 / 1 | 16.7 / **17.5・17.8** / 38.2・33.5 | 1 / 1 | **0 / 0** |

| mode | `kyoshinApply` p50 / p95 | `waveApply` p50 / p95（回数） | `detect-add` apply / settle | `clear-remove` apply / settle |
|---|---|---|---|---|
| `flyto-only` | — | — | 0.2・0 / **248・241.2** | 0.9・0.5 / **337・337.9** |
| `composite-pause` | 0.3・0.1 / 1.6・0.3 | 0.2 / 0.7・0.5（**264 / 264**） | 0・0 / 365.6・242.9 | 0.4・0.7 / 67.2・132.5 |
| `composite-nopause` | 0.2・0.2 / 0.5・0.4 | 0.2 / 0.5・0.5（**280 / 280**） | 0.1・0.1 / 547.2・251.6 | 0.9・0.3 / 327・128.7 |

- **読み取れたこと**:
  1. **EEW 発報中の最悪局面でも 60fps を維持する**。複合モードは 2 スイートとも **longtask 0 件**、
     `flyTo` 区間の p95 が 17.5〜19.5ms（vsync 天井の 1.05〜1.17 倍）。段階0 以降で最も重い局面を
     測ったが破綻の兆候は無い
  2. **`setData` は刺さらなかった**。検証項目6 で「唯一 356ms が効きうる局面」と名指しした EEW 領域の
     追加/削除で、実際に **67.2〜547.2ms** の収束が観測された（`areaEvents` 12件のレンジ。複合モードは
     `idle` が来にくくなる分ばらつきが大きく、67.2ms で収まることも 547.2ms まで伸びることもある）。しかし
     **`apply` は 0〜0.9ms** ——再タイル化は worker で走りメインスレッドのフレームを止めない。
     **名指しした現象が起きて、それでも無害だったという否定的結果**である
  3. **`flyTo` 中の予報円停止（本番の緩和策）は MapLibre では不要**。`pause` は `nopause` に対し
     予報円更新を毎スイート 16 回スキップしている（`waveApply` 264 対 280・2 回とも完全一致）が、
     フレーム側に差が出ない。しかも 2 指標が逆を向く（`dropped` は `pause` 優位・`flyTo` p95 は
     `nopause` 優位）ため差は雑音の域。Leaflet + Canvas 2D では `ctx.arc` の再描画がメインスレッドを
     直撃するため必要だったが、WebGL では 100ms ごとの頂点バッファ更新が 0.2ms で済む
  4. **カスタムレイヤーの再描画コスト（§8 のリスク）は複合負荷でも顕在化しなかった**。
     `composite` は `flyto-only` に対し「強震点の毎秒更新＋予報円 100ms 更新」が乗るが、
     2 スイートとも `flyto-only` より悪くない
  5. **`kyoshinApply` は 0.1〜0.3ms**（項目5×6 の `apply` 0.1〜0.2ms と整合）。`waveApply` も 0.2ms で、
     予報円の幾何再構築（リング 48 頂点＋扇形 50 頂点）は無視できる

- **計測上の注意（この項目で判明した指標の性質）**:
  - **`dropped`（33.4ms 超のフレーム数）は負荷指標として機能しない。** 描画頻度が上がるほど**減る**
    （`baseline` 5〜6 → `flyto-only` 2〜5 → `composite` 0〜2）。`triggerRepaint` を打たない区間では
    ブラウザが描画のないフレームで rAF を間引くため、**静止しているモードほどコマ落ちが多く見える**。
    比較するなら常時描画しているモード同士に限ること
  - **`areaEvents` の `settle` は `flyto-only` では極めて安定**（`detect-add` 248 / 241.2、
    `clear-remove` 337 / 337.9）。`setData` の再タイル化は worker 実行のためメインスレッドの
    温まりに左右されない。**複合モードのばらつきが大きい（67.2〜547.2ms）のは `idle` が来にくく
    なるため**で、`setData` 自体が重くなったのではない
  - **フェーズ順序の効果は 1 スイート目に強く出る**（`frame` max が 250.1 → 79.8 → 31.7 と単調減少）。
    2 スイート目では全フェーズが温まりモード間の比較が可能になる。**モード差を見るなら 2 回目以降を使う**

- **判断**: **検証項目7 はクリア**。EEW 発報という最悪局面で、`flyTo` 多段＋予報円 100ms 更新＋
  強震点毎秒更新＋`setData` を同時に走らせても 60fps・longtask 0 を維持する。**§8 の「`setData` の 356ms が
  複合負荷で効く」懸念は解消**。また **本番の緩和策（`flyTo` 中の予報円停止）は MapLibre 版では
  実装不要**という設計上の結論も得た（維持しても害はないが根拠は無い）。
  残る論点（**当時のメモ。現在の状態は冒頭の状態一覧表を正とする**）: (a) 検証項目2 の Leaflet 比較
  → **実機計測済み・決着（一本化GO）**(b) 項目3 カメラ操作 → 実機計測済み・**判定は保留**（Leaflet側カメラ
  計測が無く優劣未判定）・項目4 当たり判定（開発機検証で決着・bbox方式r=8採用）
  (c) 真の実行時メモリ（§8）→ WebGL推定のみ・**未完**、テキスト描画（§8）→ 描画可だが実機でコスト問題が
  顕在化し**要対処**（「大部分解消」は訂正済み・§8末尾）。
  **モバイル**（§6「検証項目: モバイル実機」）はバックグラウンド復帰判別の事前検証を行わず、
  リリース後に問題が発覚してから対応する方針にユーザー判断で確定（GO/NO-GOには含めない）。

#### 2026-07-27 検証項目2（Leaflet 比較）: **決着。MapLibre が 9 倍以上速い＝一本化は GO**

- **環境**: 実機 Surface Go 2 / DPR 1.5 / vsync 16.7ms（60Hz）。**両エンジンを同一セッションで連続計測**し、
  `meta.estimatedVsyncMs` が両側とも **16.7ms** で一致することを証跡で確認した（レビュー HIGH4 で追加した指標）
- **証跡 82 件**: Leaflet 44（`poc/leaflet-main.ts`・2 スイート × 22）／MapLibre 38（`poc/main.ts`・2 スイート × 19）
- **主指標は `zoom-forced`（Leaflet）／`zoom`（MapLibre）** — どちらも「毎フレーム `setZoom(animate:false)`」で、
  MapLibre が常時やっている仕事を Leaflet にもさせる唯一の対等な条件。`static`/`pan` は両者とも天井に飽和して
  情報を持たない（下記「指標の性質」）

##### 直接比較（同一軸・同一セッション）

| 軸 | Leaflet `zoom-forced` | MapLibre `zoom` |
|---|---|---|
| `baseline`（`full` 31,646頂点・lw1.2） | **150.0ms（9F）** | **16.7ms（1F）** |
| 線幅 8 | 150.1（9F） | 16.7（1F） |
| 線幅 12 | 150.1（9F） | 16.7（1F） |
| **`verts-thin`（頂点 9,874）** | **116.7（7F）** | **16.7（1F）** |
| **`points-off`（観測点 0）** | **133.4（8F）** | **16.7（1F）** |
| DPR 1.0 / 2.0 / 2.0×lw12 | 軸が無い（対応 API 無し） | 16.7（1F）× 3 |
| fps | 6.3〜9.1 | **57.1〜59.7** |
| longtask | **60〜78 件**（表の5軸の最大 213〜258ms。末尾の `baseline-warm` 対照は最大 293ms） | **0〜2 件** |

**Leaflet は軸に応じて 7〜9 フレーム、MapLibre は全 16 計測で例外なく 1 フレーム。**
MapLibre 側は飽和しているため**言えるのは下限**であり、真の差は 9 倍より大きい可能性がある。

##### Leaflet 側の構造分解（3 条件が 2 スイートで完全一致）

`zoom-forced` の p50 は **vsync の整数倍に量子化される**（rAF が vsync に同期するため）。フレーム単位で読むと:

| 成分 | フレーム | 概算 |
|---|---|---|
| **固定費**（ジオメトリに依らない） | **6F** | 約 100ms |
| 活断層 31,646 頂点 | 2F | 約 33ms |
| 観測点 1,725 個 | 1F | 約 17ms |
| **合計** | **9F** | **150ms** |

`full`＋点 = 9F・`thin`＋点 = 7F・`full`−点 = 8F の 3 条件が、2 スイートとも 0.0ms 差で再現した。

> **【この表の読み方・重要】** 実測したのは上記 3 条件（9F / 7F / 8F）だけである。**「固定費 6F」は
> 3 点からの外挿であって実測値ではない**——ジオメトリを全て取り除いた条件は一度も測っていない
> （`points-off` すら `fillOpacity: 0` にしただけで DOM の `<circle>` 1,725 個は残っている）。
> 各点が ±0.5F の量子化幅を持つため、外挿値の幅は **5〜7F** 程度と見るべき。
> 「Leaflet には構造的な下限がある」は**この外挿に基づく推定**であり、確定した実測ではない。
> 確定させるなら、活断層・観測点の両レイヤーを DOM から削除した条件を測る必要がある。

##### 移行判断への答え

1. **9 倍以上の余力**（150.0ms 対 16.7ms・同一条件）
2. **間引き廃止のコストが、Leaflet では +2 フレーム・MapLibre では測定限界以下** — 現行 `thin`
   （9,874頂点。本番が読むのは `active-faults.json` ＝ この間引き版。[activeFaults.ts](../src/utils/activeFaults.ts)）
   → 間引き廃止 `full`（31,646頂点）で **Leaflet は 7F → 9F（+33ms）、MapLibre は 1F → 1F（変化なし）**。
   ただし **MapLibre 側は天井に飽和しているため「増加が 16.7ms 未満」までしか言えない**——
   「ゼロ」と断定はできない。本プロジェクトの出発点だった「フル解像度化」の代償が、
   Leaflet では既存の 150ms にさらに 33ms 上乗せされるのに対し、MapLibre では観測できない水準に留まる
3. **ジオメトリ量への反応が両者で正反対** — **観測事実**: 線幅 10 倍では**両者とも動かない**が、
   **頂点数を 1/3 にすると Leaflet だけ 2 フレーム軽くなり、MapLibre は 1 フレームのまま動かない**。
   つまり **Leaflet はジオメトリ量がメインスレッドのフレーム時間に直接現れ、MapLibre は現れない**。
   間引き廃止の可否を分けるのはこの一点である
   > **【機序は未測定・以下は推定】** Leaflet 側は `moveend` → `Renderer._updatePaths()`（再クリップ・
   > `L.LineUtil.simplify`・DOM 属性書き換え）の経路が**コード上は**存在し、これがメインスレッドを
   > 占めていると考えるのが自然だが、**プロファイラで実行時間の内訳を測ってはいない**。
   > MapLibre 側も「GPU に逃がしている」「worker で処理している」「単に速い」を**区別できていない**
   > （`EXT_disjoint_timer_query_webgl2` による GPU 時間計測は未実施）。
   > **機序の特定は本設計の最適化には効くが、GO/NO-GO の判断には不要**（観測事実だけで足りる）

##### 移行で失うもの（正直に記録する）

**メインスレッドが止まる総量は大差なく、止まる場所が違う。**

| | 動作中 | 確定時 |
|---|---|---|
| Leaflet `pan` | longtask **0 件**（全 10 計測） | `moveendMs` **中央値 24ms**（10 サンプル・14〜45ms） |
| MapLibre `pan` | longtask **0〜2 件・54〜78ms** | 対応する測定値なし（`moveend` 相当の一括処理が無い） |

Leaflet は「動作中は静かで、離した瞬間に 1 回払う」、MapLibre は「動作中に散発、離しても何も起きない」。
MapLibre 側の longtask は 2 スイート目でほぼ消えた（**原因は未測定**。タイルキャッシュの影響と推定されるが
確かめていない）。同じく **Leaflet 側の longtask が 0 件である機序も未測定**（実装上は CSS transform の
コンポジタ合成のみで再ペイントが走らないためと考えられる）。
なお Leaflet の `zoom-native`（本番のズーム操作に近い駆動）は **longtask 8〜24 件・最大 90〜279ms**で、
ここは MapLibre が明確に優る。

##### 指標の性質（この項目で判明したこと）

- **`zoom-forced`/`zoom` の p50 は vsync の整数倍に量子化される。** 116.7 = 7F・133.4 = 8F・150.0 = 9F・
  166.6 = 10F。**分解能は 16.7ms 刻み**で、それ以下の差は原理的に読めない。線形モデルの係数を
  小数点以下まで信じてはいけない（当初「固定費 85.0ms」と算出したが、正しくは「6F ± 0.5F」）
- **9 サンプル中 8 が 9F、1 が 10F**（観測事実）。この頻度から**真値は 9F の上限（158ms）寄りにあると
  推定される**が、量子化された観測から真値の位置を確定はできない。
  **「決定論的」と断定したのは早計だった**（5 点続けて同値でも疑うべき）
- **`moveendMs` は n=1 でばらつき 3 倍**（14.1〜44.4・同一条件内でも 21.2 と 44.4）。桁を知る用途に限る
- **`zoom-native` は fps なら軸の効果を検出できるが、longtask 件数（8〜24）と max（90〜279ms）はばらつく。**
  同じデータでも、離散イベントを数えると不安定・全フレームを平均すると安定という差が出る
- **総括: 天井を大きく超える連続負荷が最も安定した物差しになる。** 層B の「飽和した指標は情報を持たない」の
  裏返しで、`zoom-forced` だけが交絡（線幅・温まり・スイート間）に反応せず狙った軸にだけ応えた

- **判断**: **検証項目2 はクリア。一本化は GO。** 唯一残っていた GO/NO-GO 論点——「MapLibre は Leaflet より
  速いか」——に、同一セッション・同一天井の直接比較で **9 倍以上**という答えが出た。§2.4 の
  「同じ 60fps でも仕事量は桁違い」という仮説は正しく、**向きは MapLibre 有利**だった。
  **これで移行判断の全項目（律速・非加算合成・毎秒更新・交差点・複合負荷・Leaflet 比較）が通った。**
  残る作業は GO/NO-GO ではなく実装上の詰め: (a) 項目3 カメラ操作（開発機PoC検証済み・実機計測は
  未実施）・項目4 当たり判定（開発機検証で決着・bbox方式r=8採用）
  (b) 真の実行時メモリ（§8。テキスト描画は`poc/label.ts`で開発機検証済み・大部分解消）
  (c) 本番ビルドでの実機再計測（層 A 残差の実寸）→**【2026-07-28 決着】**
  [結果記録](webgl-production-build-measure-result-2026-07-27.md)参照。
  **モバイル**（§6「検証項目: モバイル実機」）はバックグラウンド復帰判別の事前検証を行わず、
  リリース後に問題が発覚してから対応する方針にユーザー判断で確定（GO/NO-GOには含めない）。

## 7. PoC 後の本設計・移行戦略

- PoC が通れば architect で全面リライトの本設計を起こす。
- **移行の難所は「地図基盤の置換は部分適用できない」こと**。現実的な進め方の候補:
  - MapLibre 版の新地図コンポーネントを並行実装し、機能を 1 つずつ移して検証（`JapanMap` の各レイヤー相当を順次移植）。
  - レイヤー移植順（案）: 背景タイル・県境/区域 → 海岸線・活断層 → 強震点 4 レイヤー → EEW/津波/予報円/ヒートマップ/検知/自動フィット。
- **間引きスクリプトの扱い**: 移植済みレイヤーは `EPSILON` を緩める / 廃止する。
- **両バリアント**（standard / DMDSS）・GitHub Pages（サブパス配信）での動作確認。
- 現行 Leaflet 実装は、全機能の移植・回帰確認が完了するまで削除しない。

## 8. リスクと未確定事項

- **全面リライトの回帰リスク** — 地図連携の全機能（付録）を再実装・再検証する必要がある。最大のリスク。
- ~~**非加算 opacity 合成の再現**（`KyoshinSubThreshold`）— WebGL 共通の最難関。カスタムレイヤーが必要になる可能性。PoC 項目 5。~~ → **【2026-07-25 解消】** 予想どおりカスタムレイヤーが必要だったが、オフスクリーン FBO 二層合成で**現行と等価に再現でき、実機で追加コストほぼゼロ**（§6 結果記録・検証項目5）。本設計では「レベルごとに FBO へ不透明描画 → opacity で over 合成」を前提にできる。
- **非力 GPU での MapLibre 安定性** — 目的（非力機の軽量化）と逆行しないか。フォールバックの要否を検討。
  - **【2026-07-25 一部解消】** 層B（塗り面積 4 倍・線幅 10 倍まで 60fps 維持）と検証項目5（FBO 二層合成で 59〜60fps・`ERROR:` なし・描画崩れなし）の実機計測で、**UHD 615 でも MapLibre は安定動作**することを確認した。ただし毎秒更新（項目6）・EEW 複合負荷（項目7）・長時間稼働での context lost は未検証。
- **WebGL コンテキストロストからの復帰** — GPU リセット・ドライバ再起動・メモリ逼迫（RAM 4〜8GB）でコンテキストが失われると、SVG/DOM と異なり WebGL 地図は描画ごと消える。常時表示の防災用途では「地震発生の瞬間に地図が黒画面」が最悪の障害モード。MapLibre の復帰挙動（自動リストアの可否・イベント）を PoC で確認し、復帰失敗時のフォールバック（再初期化・リロード誘導）を本設計に含める。
- **【最重要 / PoC 項目1 へ昇格】高精細 × 弱 GPU のフィルレート律速**（Surface Go 2 の 220ppi × UHD 615）— GPU 化しても「画面全体を塗る速度」が壁になりうる。**この場合、間引き廃止（フル解像度化）は効果対象外になり一本化の本来の動機が消える**うえ、WebGL 地図はカメラ操作中に毎フレーム全ビューポートを描き直すため、コンポジタ合成で済む Leaflet のパンより塗りが増える局面もある。単なるリスクではなく、全面リライトの GO/NO-GO を分ける前提条件。§6「フィルレート律速だった場合の分岐」に従い実機 PoC で最優先に見極める。
- **【2026-07-26 追加 → 同日解消】カスタムレイヤーの毎秒更新** — `KyoshinSubThreshold` は検証項目5 の結論により**カスタムレイヤー**（FBO 二層合成）になるが、検証項目6 の解決手段 `feature-state` は **paint 式を経由する仕組みのためカスタムレイヤーには効かない**。項目5 の PoC は `STATIC_DRAW` で更新経路を持たず、項目6 の PoC は標準 circle レイヤーで測ったため、**「カスタムレイヤーを毎秒更新する」だけが両 PoC の間に落ちていた**。→ **`poc/subthreshold-rt.ts` で PoC 化し実機計測して解消（インデックスバッファ方式を採用）。①1,725点・③17,250点ともに vsync 天井内（更新モードは fps 59.5〜60.0。baseline のみウォームアップ残りで58.25〜59.92）・longtask 0。§6「検証項目5×6 の交差点」に結果記録。**
- **【2026-07-26 追加】カスタムレイヤーの再描画コストは標準レイヤーより高い** — `KyoshinSubThreshold` の FBO 二層合成は「6レベル × (全画面 clear ＋ 点描画 ＋ 全画面 quad 合成)」を毎フレーム走らせるため、**更新をしない `repaint-only` でも収束が 15.8〜17.9ms**（項目6 の標準 circle は 6.6〜12.0ms）＝ **1.3〜2.7 倍**。単独では vsync 天井内に収まるが、**検証項目7（EEW 複合負荷）で `flyTo`・予報円アニメと重なったときの余裕がその分薄い**。項目7 ではこのレイヤーを有効にした状態で測ること。→ **【2026-07-26 解消】検証項目7 の実機計測（このレイヤーを有効にした状態）で顕在化しなかった**。複合モードは `flyto-only` より悪くならず、2 スイートとも longtask 0・`flyTo` 区間 p95 17.5〜19.5ms。単独の再描画が重いのは事実だが、EEW 複合負荷でも天井内に収まる。§6 検証項目7 に結果記録。
- ~~**対象プラットフォームが未定義／モバイルのセッション継続性が未検証**~~ → **【2026-07-27 ユーザー判断で決着】** 本計画書は対象環境を明示しておらず、計測はすべて Surface Go 2（Windows）で行ってきた。本アプリは PWA・GitHub Pages 配信でモバイルから開かれうる。**描画互換性は iPhone 15 Pro Max / Safari で一次確認済み**（`GL_POINTS`・`gl_PointSize`・FBO 二層合成・毎秒更新すべて動作。§6「検証項目: モバイル実機」）。バックグラウンド復帰時の挙動（①rAF 停止 ②`context lost` → `restored` ③**iOS によるタブ破棄＝ページ再読み込み**、の3つが目視で区別できていない問題）は、③なら受信中の地震データ・EEW 表示・設定が失われ防災アプリとして致命的なリスクではあるものの、**事前のPoC検証・実装は行わず、リリース後に問題が発覚してから対応する方針にユーザー判断で確定した**。モバイルは対象に含めるが、この論点は移行判断のGO/NO-GOには含めない。
- **層 A と層 B の混同**（§2.4）— 「静止時の重さ」（層 A・安価に解消可）と「フル解像度の重さ」（層 B・GPU 化が要る）を分けずに測ると、全面リライトの効果を過大評価する。段階0 で層 A を先に潰し、残差として層 B を測る。
- **バンドルサイズ / PWA 初回ロード** — MapLibre は Leaflet より重い（実行時は GPU で軽くなる想定だが、初回ロードは要計測）。
- **実行時メモリ（RAM 4GB 実機）** — バンドルサイズ（転送量）とは別軸で、WebGL バッファ・グリフアトラス等の実行時メモリが Leaflet 比で増える。4GB 構成ではタブキル・context lost の誘因になるため、実機 PoC で計測する（§6 測定方法）。
  - **【2026-07-27 実機で WebGL 推定値は取得済み（`surface-go2-memory` 1件）・ただし本番比較は未完】** 実機で WebGL 推定 36.1MB・JS ヒープ 77.3MB・タブフットプリント約327MB を得たが、**VRAM は本手法では観測できず**（GPU テクスチャはプロセスのメモリフットプリントに現れない）、`performance.memory` とタスクマネージャが1.7倍食い違う（同時刻採取要）。「移行でメモリが増えるか」に答えるには本番 Leaflet 版を同一機体・同一時点で測る必要があり未実施＝**未完**（状態は冒頭の状態一覧表を正とする）。**【2026-07-27 ユーザー判断で計測方針決定＝「実機で安定性を長時間観測」】** VRAM のバイト厳密計測は原理的に不可能なので追わない。問い（＝移行で 4GB 機が不安定化するか）に直答するため、**実機（RAM 4GB 相当）で MapLibre 版を長時間稼働させ、タブ kill / context lost / OOM が起きるかという結果を観測**する方針に決定。`webglMemoryTracker` の推定値（36.1MB 等）は概算の補助に留める。バイト差分（案）は VRAM を含まず過小評価するため主指標にしない。実行は実機セッション担当。**【2026-07-27 観測ハーネスを実装（資材作成済み・実機計測待ち）】** `poc/measure.ts` の `installMeasure` に `__runStabilitySuite(labelBase, minutes, sampleEverySec)` を追加。実利用（常時表示＋時々 flyTo）を模した軽い活動を長時間走らせ、sampleEverySec ごとに jsHeap・webglMemory 推定・`contextLost`/`contextRestored` 回数・longtask をスナップショットして `/__perf-report` へ送信、末尾に first/last/min/max とデルタの要約を送る。判定は人が読む（`contextLost>0` は不安定・デルタの持続的増加はリーク疑い・タブ kill は最後のサンプルの時刻で判断）。開発機で短縮版動作確認済み（6サンプル・contextLost0・trend算出・コンソールエラー0）。実機呼び出し: `await window.__runStabilitySuite('surface-go2-stability', 30)`。
    - **【運用仕様・合否条件（2026-07-27 レビュー(8e8df3e)の指摘「長時間の具体と判定条件を実施前に決めよ」に対応）】**
      - **時間**: 最低30分連続（ハーネス既定 `minutes=30`）。余力があれば60分。`sampleEverySec=30`（30秒ごとにサンプル）。
      - **操作**: ハーネスが実利用を模した軽い活動（30秒ごとに flyTo/fitBounds）を自動で継続する。ユーザーは開始後は放置でよい。
      - **FAIL 条件（いずれか1つでも該当したら移行に赤信号）**: ①`contextLost > 0`（通常操作中に WebGL コンテキスト喪失）②サンプルが意図した時間より前で途切れる（＝タブ kill / reload。最後のサンプルの `elapsedSec` で判定）③`jsHeapMB` または `webglTotalMB` の `deltaFirstToLast` が頭打ちにならず単調に増え続ける（リーク）。
      - **PASS 条件**: `contextLost === 0` かつ タブ生存（意図時間まで完走）かつ メモリが**初期ウォームアップ後に頭打ち（プラトー）**。**初期の立ち上がり増加（working set がタイル/テクスチャで埋まる分）は正常なので無視し、後半で持続的・単調に増え続けるかだけを見る**（min/max/first/last の並びで判断）。
    - **【2026-07-28 決着・PASS】** 実機（Surface Go 2）で `__runStabilitySuite('surface-go2-stability', 30)` を実行。
      1回目（15:27〜）は別環境からの `git pull` が PoC dev サーバー（`:5173`）を自動再起動させ42サンプルで中断
      （[[feedback-no-pull-during-long-measurement]]）、2回目（15:59〜16:26）で **30分04秒（1804秒・61サンプル）完走**。
      `contextLost:0`・`contextRestored:0`。`jsHeapMB` は27.9〜38.8MBで終始振動。`webglTotalMB` は最初の約3.5分で
      6.98MB→39.4MBと立ち上がった後、残り約26.5分は41〜44MB帯で振動するのみでプラトー（単調増加の継続なし）。
      **PASS 条件を全て満たし FAIL 条件は該当なし。§8 実行時メモリの安定性観測はこれで決着**（結果記録:
      [webgl-poc-surface-go2-stability-2026-07-27.md](webgl-poc-surface-go2-stability-2026-07-27.md)）。
    - `performance.memory` は JS ヒープのみで WebGL バッファ・テクスチャ・レンダーバッファを含まない。ブラウザに WebGL メモリ使用量の直接取得 API は無いため、`poc/webglMemoryTracker.ts` を新設し、`HTMLCanvasElement.prototype.getContext` をモンキーパッチして MapLibre が内部生成する WebGL2 コンテキストを捕捉、`bufferData`/`texImage2D`/`texStorage2D`/`renderbufferStorage` 等の呼び出しを横取りして確保・解放バイト数を積算する方式で推定値を得る構成にした。ブラウザに WebGL メモリ使用量の直接取得 API は無いため、`poc/webglMemoryTracker.ts` を新設し、`HTMLCanvasElement.prototype.getContext` をモンキーパッチして MapLibre が内部生成する WebGL2 コンテキストを捕捉、`bufferData`/`texImage2D`/`texStorage2D`/`renderbufferStorage` 等の呼び出しを横取りして確保・解放バイト数を積算する方式で推定値を得る構成にした。
  - **つまずき1（実機投入前に解消）**: `HTMLCanvasElement.prototype.getContext` を `bind(proto)` して差し替えたところ、実際の canvas 要素から呼ばれた際に `TypeError: Illegal invocation` が発生した。`bind` すると `this` がプロトタイプ自体に固定されるため。`bind` せず元関数を保持し、呼び出し時の実際の `this`（本物の canvas 要素）で `call` するよう修正した。
  - **つまずき2（実機投入前に解消）**: 当初 `texImage2D` のみをフックしたところ、実際に運用してみるとテクスチャ推定値が常時 0MB のままだった。`node_modules/maplibre-gl` のソースを確認したところ、MapLibre はタイル用テクスチャ等の主要テクスチャを `texImage2D` ではなく `texStorage2D`（不変ストレージ確保）＋`texSubImage2D`（データ転送のみ）で確保しており、`texImage2D` はほぼ使っていなかった。`texStorage2D` も同様にフックし解消した。
  - `poc/main.ts`（層B PoC）に組み込み、stat パネルへ「WebGL推定(buf/tex/rb)」「推定実行時メモリ（JS+WebGL）」を表示。計測レポート（`installMeasure` の `snapshot()`）にも `webglMemory` として含めた。開発機で DPR 軸（1.0→2.0）を振ると、テクスチャ推定値が **11.3MB→21.3MB（約1.89倍・DPR¹に近い線形の反応。塗り面積の DPR² 則とは別軸で、DPR 変更は主に可視タイル枚数の増加として効いていると見られる）**で単調に反応することを確認した（バッファ・JSヒープも軸に応じて妥当な範囲で変動）。型チェック・Playwright確認（コンソールエラー0件）済み。
  - **セルフレビュー対応**: (a) context lost/restored 時に旧コンテキストのオブジェクトが `delete*()` されないまま失効し、カウンタが二重計上される問題（RAM4GB機の実機計測で起こりうる）を、`webglcontextlost` イベントでカウンタ・WeakMap・bind状態を丸ごとリセットする処理を追加して解消。(b) `bufferData`のWebGL2版5引数（`length`指定の部分アップロード）が未対応（現状のmaplibre-gl v6.0.0は3引数版のみ使用のため実害なしとレビューで確認済み・将来のバージョンアップでの静かな不正確化リスクとしてコメント明記）。(c) `texImage2D`の6引数分岐（img/canvasソース版）は現状のMapLibre利用では実行されない未検証コードパスである旨を明記。
  - **限界・残作業**: 本推定はミップレベルを考慮しない・不明フォーマットは安全側に丸める等の近似であり、絶対値の精度はタスクマネージャ実測に劣る。**実機（RAM 4GB相当機）でのタスクマネージャ実測との突き合わせ、および長時間稼働でのメモリ増加傾向（リーク有無）の確認は未実施**で残作業とする。
  - **【2026-07-27 レビュー(8c75e49) LOW 対応】** `getContext`パッチが`type==='webgl2'`にしか反応しないため、webgl1環境では捕捉できず`snapshotWebglMemory()`が全項目0を返す。0が「WebGLを使っていない」のか「計測できていない」のか区別できない問題があった（MapLibre v6はwebgl2前提のため現行対象環境では起きない）。`WebglMemorySnapshot`に`tracked: boolean`を追加し、statパネルにも未計測時は`[未計測]`を表示するようにした。
- **投影・座標の差異** — Web メルカトルで一致する見込みだが、`[lng,lat]` 並びや境界の扱いを PoC で確認。
- **背景タイル** — 現行タイル（`TileTintLayer` 含む色調整）の MapLibre での再現。
- ~~**テキスト描画（ラベル・ツールチップ）** — 現状のラベルは全て DOM（地方/県/区域名 = `L.marker`+`divIcon`、ヒートマップの `Tooltip` 等）。MapLibre で GPU 描画するには symbol レイヤー＋**日本語グリフ（フォント PBF）の自前生成・ホスト**（GitHub Pages）が要り、地図描画とは別種の準備物になる。~~ → **【2026-07-27 描画は可能と判明。ただし「解消」ではない——実機でコスト問題が顕在化（この項目末尾の実機計測を参照）】** 事前の code-explorer 調査で、テキストを伴うラベルは実は最大192件（一次細分区域集約ラベル）＋固定248件（地方9/県47/区域192）に留まり、「数百〜数千規模の点にDOM妥協が効かない」という当初の懸念自体が該当しないと判明した（`IntensityPoints`/`KyoshinPoints` 等の大量点群はそもそもテキストを持たない設計）。さらに Context7 で MapLibre 公式リポジトリ（`/maplibre/maplibre-gl-js`）を裏取りしたところ、`style` に `glyphs` を一切設定せず `text-font` に OS 標準の日本語フォント名（`text-font: ["Hiragino Mincho ProN", "Noto Serif CJK JP", "MS Mincho"]` 等）を並べるだけで日本語ラベルを描く公式サンプルが存在した（実際に日本の地名「安曇野市」「松本市」で検証済みのサンプル）。**`poc/label.ts`／`poc/label.html` で glyphs 未設定・`text-font: ['Yu Gothic UI','Meiryo','MS Gothic','Noto Sans CJK JP']` の構成を実装し、地方名（9件・2〜3文字）・県名（47件）・区域名（192件・多くは5〜7文字だが最短2文字〜最長17文字まで分布する漢字）・震度ラベル（`src/utils/intensity.ts` の `getIntensityLabel()` が実際に画面へ出す文字列そのもの＝"1"〜"7"に加え"5弱"/"5強"/"6弱"/"6強"という漢字混じり2文字を含む。本番 `JapanMap.tsx` の「2文字ラベルだけフォントサイズを縮小する」分岐も `text-size` の data-driven expression で再現）の全パターンで実際に文字が正しく描画されることを、本プロジェクトが実際に使用する MapLibre v6.0.0（`maplibre-gl: ^6.0.0`）上で開発機確認した**（Playwrightスクリーンショットで目視、コンソールエラー0件。セルフレビュー: 当初 ASCII記号"5+"等の代替データで検証していたが、本番は画面にその表記を一切出さないため実表示文字列に差し替えた）。**日本語グリフ PBF の自前生成・ホストは不要という結論**。数百〜数千規模の点は元々非テキストのため、symbol レイヤー必須という当初の結論も撤回する。
    - **【2026-07-27 実機計測（Surface Go 2）でコスト問題が顕在化・「大部分解消」を訂正】** 詳細は [webgl-poc-surface-go2-2026-07-27.md](webgl-poc-surface-go2-2026-07-27.md)。「`glyphs` 不要で描ける」は正しいが「コストは無視できる」は成り立たなかった。`glyphs` 未設定＝全文字を TinySDF でクライアント生成するため、区域名192件（ユニーク漢字200種超）の初回生成が集中する。**実機 `blockMaxMs` は prefectures 75.6〜194.1ms・subregions 113.2〜262.7ms で、longtask が常発（両段とも全4回発火。longtask 最大は prefectures 66〜184ms・subregions 110〜259ms）。** 開発機（~20ms）比で 6〜10 倍——`webgl-poc-review-3598d26.md` の「2〜3倍」外挿（推測と明記済み）は3倍前後過小だった（非力機のシングルスレッド性能差は単純倍率で見積もれない）。`blockMaxMs` 自体は vsync 非依存の性質を保っており（開発機と実機の差は vsync でなく機体性能）、計測器としては正しく機能した。
    - **なぜ実務問題か**: 現行（Leaflet）はラベルが全て DOM（`L.marker`+`divIcon`）でこのコストが存在しない＝**移行で新規に発生するコスト**。かつラベルはズーム帯で切替（本番 z5.5/7.5/9）＋本アプリは地震検知時に自動ズーム・フィットするため、**区域名初出＝地震発生の瞬間に 100〜260ms 画面停止**という最悪の重なりがありうる（キャッシュされるため初回のみだが、その初回が最悪局面）。
    - **対処案（実装は本設計フェーズ）**: A=ラベルだけ DOM 維持（但し高速パン/ズーム中の追従ズレ・衝突回避の自前実装という新規劣化を伴う）／**B=`glyphs` PBF を自前生成・ホスト**／C=起動直後アイドル時に事前生成（コストは消えないが支払うタイミングを地震検知時から起動時へ移せる。レビュー推奨だったが起動時competitionと起動直後EEWの残リスクが消えない）／D=受け入れる。**この問題は移行判断（GO）を覆すものではない**（方式選択で回避可能）。
    - **【2026-07-27 ユーザー判断で B（グリフPBF自前生成・ホスト）に決定】** 理由: 全面リライトで MapLibre に移行する以上ビルド時グリフパイプラインの工数は見合い、B は生成スパイクを**恒久的に消し**、PWA の Service Worker 事前キャッシュで**オフラインでも即座**＝防災アプリで「肝心の瞬間に固まらない」保証が最も堅い。C（レビュー推奨）は最小工数だが起動時の初期化競合と起動直後 EEW の残リスクが消えないため非採用。**実装時の要確認事項**: OS フォント（Yu Gothic 等）は再配布不可のため、再配布可能な **Noto Sans CJK JP（SIL OFL）等へ変更**する必要がある（見た目の微変＋ライセンス確認）。ラベル文字種は 地方9＋県47＋区域192＋震度9 の**既知・有限集合（ユニーク漢字は数百）**のため、必要なグリフ範囲だけの PBF を生成すれば軽量。生成ツール（fontnik / maplibre font-maker 等）は本設計着手時に Context7 で最新手順を確認する。
  - **【2026-07-27 レビュー(8c75e49) MEDIUM 対応 → レビュー(4ecdb6e)で新規HIGH指摘 → 解消】** `glyphs`不要の機序は「`glyphs` URLが無いためASCII数字も含め全文字をクライアント側でTinySDF（48px SDF、Canvas2Dラスタライズ）生成する」というもので（MapLibre実装の`!this.url`分岐でレビュー側が裏付け済み）、「描ける」ことは確認済みだが「安い」かは未確認だった。当初`poc/label.ts`に`measureLabelZoomTransition`/`__runLabelZoomSuite`（ズーム帯を`z6→z8→z11`と遷移させてlongtask計測）を追加したが、**レビューが`map.style.glyphManager.entries`を直接観測したところ、全ズーム帯を通じて生成グリフ数が40種のまま一度も増えていないと判明**——ズームインすると視野が狭まり県47件・区域192件の対象点が画面外に出るため、「多数の文字種が同時に初出現する状態」が原理的に発生しない構成だった（層B・項目4に続く「差の出ない条件で差が出なかった」の3度目）。`longtask 0`は「安い」ではなく「ほとんど何も生成していない」ことの誤読だった。
  - **対処**: 日本全体が映るビュー（`fitBounds(JAPAN_BOUNDS)`）を固定したまま、各レイヤーの`minzoom`/`maxzoom`制約を`setLayerZoomRange`で計測時だけ外し、`visibility`の出し入れだけでレイヤーを順に表示する方式に作り直した。結果に`renderedSymbols`（実描画数）・`glyphsGenerated`（累積グリフ生成数）を追加し、「安い」か「生成していない」かを後から判別できるようにした（レビューがLOWで指摘した`tracked`フラグと同じ処方箋）。
  - **セルフレビューで追加発見・解消**: `intensity-label`が`load`時点で常時表示（`visibility`未指定＝既定visible）だったため、`__runLabelZoomSuite`実行前に既にそのグリフ（"1"〜"7"・"5弱"等）が生成済みになっており、4番目のデータ点（intensity）だけ「suite開始前に済んでいた」というアーティファクトで汚染されていた——これも本件（ズーム切替で視野が狭まり計測が成立しない）と同型の失敗。初期状態を`visibility:'none'`に変更し、目視確認は手動トグルボタンに分離して解消。あわせて`map.style.glyphManager`への不要な`unknown`キャストも除去（公開型にそのまま乗るため。将来のバージョン変更で構造が変わった際に「コンパイルエラーで気づける」ようにする）。
  - 開発機で実行したところ、グリフ生成数が`regions:15→prefectures:84→subregions:209→intensity:218`と**全4段で単調増加**することを確認——狙った文字種（県名・区域名・震度ラベル）が実際に生成されていることを実証できた。
  - **【2026-07-27 レビュー(f5d8de5) 新規HIGH対応】** 上記は「狙った負荷を踏んだか」の確認だったが、「その負荷が計測している指標に現れるか」の確認が不足していた。実際に結果へ含めていた`elapsedMs`（idleまでの所要時間）は、生成量が2→125（60倍以上）と変わっても差0.7ms・約612ms一定で、**生成コストの指標として無情報**だった（一度きりの生成コストは平均には埋もれず該当フレーム1点にだけ跳ねる性質のため）。`measureLabelLayer`にrAFベースのフレーム時間計測（`poc/measure.ts`の`summarizeFrames`をexportして共有）を追加し、結果に`frame`（p50/p95/max）を含めるよう修正。レビュー環境（実Chrome、vsync6.1ms）ではsubregions段のみ`frame.max`が17.6msに跳ね生成コストを検出できたが、本セッションの動作確認（Playwright/Chromiumのheadless、vsync16.7ms固定）では同種のスパイクは再現しなかった。**副産物として開発機の実vsyncが16.7ms(60Hz)ではなく6.1ms(≒164Hz)と判明**（レビュー`b5d8bae`の「170Hz」と整合。§6検証項目3の記述訂正）。 →**【2026-07-27 訂正・下記バレット参照】** この「レビュー環境=実Chrome / 本セッション=headless」という区別は誤り。両セッションとも Playwright 経由の headed（`navigator.userAgent`実測で確認）。本セッションの開発機を直接計測すると vsync は 16.7ms(60Hz)・Intel Iris Xe。**双方 headed にも関わらず noise floor が 6.1ms 対 16.7ms で一致しない原因は未特定**（別モニタか別実行コンテキストか他要因かは未検証。「物理モニタのリフレッシュレート差にすぎない」「セッションごとに別の物理機」と断定していたのは、確かめずに原因を名付けた誤り）。上記「開発機の実vsyncが6.1ms(164Hz)」は別セッションのレビュー機の値で、本セッションの機の値ではない、という点のみは確か。`blockMaxMs`（`poc/label.ts`、下記バレット参照）は両環境でほぼ同値を返し vsync 非依存と実証済みのため、この原因を追う実務上の必要はない。
  - **【動作確認を追加で多角化】** 「フレーム計測ロジックが動くこと」の確認だけでは不十分だったため、以下を追加検証した: (a) 何もしていない静止状態でrAF間隔のノイズフロアを単独計測したところ**最大18.7ms**あり、これがレビューが検出した生成コストの増分（約11.4ms）より粗いため、この開発機ではノイズに埋もれて検出できないという具体的な機序を確認。(b) `__runLabelZoomSuite()`を同一ページで複数回実行すると、2回目以降は`glyphsGenerated`が全段で変化しなくなることを発見——**グリフキャッシュ（`glyphManager`）はページを閉じるまで消えないため、2回目以降は既に生成済みのグリフを再描画するだけで計測が無効化される**。ページをリロードしてから1回だけ実行する必要がある旨を`poc/label.ts`・`poc/README.md`にコメントで明記した。(c) リロード後のフレッシュな1回実行では生成量が正しく15→84→209→218と増加する一方、`frame.max`は全段16.9〜17.0msで無反応——(a)のノイズフロア分析と整合し、「開発機で反応が見えない」ことは「生成コストが無い」ことの証明にはならないと確認できた。**実機（Surface Go 2、生成コストがノイズフロアを超えやすい非力機）での計測は未実施で残作業とする**。 →**【2026-07-27 訂正・下記バレット参照】** (a)(c) の「開発機では frame.max がノイズに埋もれて検出できない・実機を待つしかない」は誤り。計測器を vsync 非依存の `blockMaxMs`（MessageChannel 連続ブロック検出）に替えれば、同じ 60Hz 開発機で subregions 生成が `blockMaxMs`≈20〜24ms として σ~0.5ms で検出できる。frame.max が動かなかったのは vsync 量子化による計測器の限界であって、生成コストが検出不能なわけではなかった。
  - **【2026-07-27 追加検証・上記（f5d8de5対応／多角化）の結論を訂正】** レビュー(3598d26)の2つの HIGH 指摘（「レビュー環境＝実Chrome」「開発機では構造的に検出できない」はいずれも事実誤認）を受けて再検証し、機序を特定した。
    - **この開発機は headed（`navigator.userAgent` に Headless なし・`webdriver` false）で vsync 16.7ms(60Hz)・Intel Iris Xe（rAF 実測）。** レビュー機も headed 申告で vsync 5.9〜6.1ms(≒164Hz)。両者の noise floor 差は headed/headless でも Playwright/実Chrome でもない——ここまでは実証済み。**ただし「物理モニタのリフレッシュレート差」と決めつけたのは訂正する**（2026-07-27 レビュー(61776b0)指摘）: 双方 headed にも関わらず vsync が一致しない原因（別モニタ／別実行コンテキスト／他要因）は実際には未検証だった。`blockMaxMs` が両環境でほぼ同値（16.7ms環境≈20ms・5.9ms環境18.6ms）を返し vsync 非依存と実証できているため、この原因を追う実務上の必要はない、という結論だけが正しい。
    - **glyph 生成コストは vsync 非依存の計測器なら 60Hz 開発機でも再現性高く検出できる。** `poc/label.ts` の `measureLabelLayer` に MessageChannel ping-pong によるメインスレッド連続ブロック検出（`blockMaxMs`/`blockTop3Ms`）を主指標として追加。区域192件＝196グリフの初回生成で **3回とも `blockMaxMs`≈20ms（20.6/19.6/20.2、σ~0.5ms）**、cached 再表示・生成0の対照では≈6ms（生成コストを描画コストからクリーンに分離できた）。suite 実行では生成量に単調反応: regions 15→4.5ms・prefectures 84→9.5ms・**subregions 209→24.2ms**・intensity 218→5.8ms。
    - **一方 `frame.max` は同 suite で全段16.9msのまま無反応。** 最長20ms級のブロックが 16.7ms budget を僅かに超えるだけで、vsync 位相との整列次第で 17ms に吸収されたり 33/50ms に跳ねたりする量子化アーティファクトのため（前セッションで「rAF ループと visibility 切替の順序を変えたら49.9msが1回だけ出た」のもこれ。統計ノイズでも順序依存でもなかった）。60Hz では frame.max は生成コストの計測器として不適で、`blockMaxMs` を主指標に据えた。
    - **Surface Go 2 外挿の再定式化**: 判断に効くのは合計時間ではなく最長連続ブロック（開発機 ≈20〜24ms）。longtask は開発機では全段0（50ms 閾値未満）だが、2〜3倍遅い実機なら `blockMaxMs` が ≈40〜60ms に達し閾値を跨ぐ可能性がある。「§8 テキスト描画は無視できるとは言い切れない」という結論は維持するが、実機計測の価値は「開発機で測れないから」ではなく「非力機での生成コスト絶対値を知るため」に訂正する。
  - **【2026-07-28 B案（グリフPBF自前生成）PoC裏取り・開発機】** `scripts/build-glyphs.mjs`（`@mapbox/tiny-sdf`＋`@napi-rs/canvas`＋`pbf`でMapLibre公式glyphs.protoに準拠したPBFをビルド時生成）と`poc/label.ts`（`?glyphs=1`でA/B切替、`localIdeographFontFamily:false`必須の罠を回避）を実装。開発機実測で `blockMaxMs` 区域45.8→7.4ms（6.2倍）・県38.2→6.6ms（5.8倍）と生成スパイクが消えることを確認（詳細: [webgl-glyph-pipeline-b-2026-07-28.md](webgl-glyph-pipeline-b-2026-07-28.md)）。本番実装で潰すべき障害（スタック名の単一情報源化・codepoint集合のドリフト検知等）も自己レビューで先回りして記録済み。
  - **【2026-07-28 B案 実機決着（Surface Go 2）】** [結果記録](webgl-glyph-pipeline-b-surface-go2-2026-07-27.md)。**B案は実機でも有効**（区域192で`blockMaxMs` 206.8→43.8ms・4.7倍、longtask 1件→0件）。**ただし開発機PoCの「生成スパイクが恒久的に消える」は実機では言い過ぎ**——**県47はB案適用後も`blockMaxMs` 122.1ms・longtask 2件（最大73ms）が残る**（実機の絶対値は開発機の3〜18倍重く、PBFのパース・テクスチャアップロード自体が非力機ではまだ軽くないため）。**§8 テキスト描画の実機再計測はこれで完了**。本番実装では県47段階のlongtask残存への対処（プリフェッチ・表示タイミング遅延等）を追加TODOとする。
  - **【2026-07-28 県47残存への緩和策「起動時ウォーム」実機検証】** [結果記録](webgl-glyph-warm-mitigation-surface-go2-2026-07-27.md)（開発機PoC: [webgl-glyph-warm-mitigation-2026-07-28.md](webgl-glyph-warm-mitigation-2026-07-28.md)）。**県47は起動時ウォームで劇的に解決**（`blockMaxMs` 136.1→10.7ms・12.7倍、longtask 1件→0件）——本番実装で強く推奨。**区域192はlongtaskは0件まで消えるが`blockMaxMs`は63.8ms残存**し、ウォームだけでは不十分な可能性がある。**【未解決の論点】** 区域192のcold値が本検証（95ms）と`__runLabelZoomSuite`内の値（43.8ms）で2倍以上食い違う——後者は県の描画で共有漢字が部分的にウォーム済みだった疑いがあり、どちらが実際の自動ズームのシナリオに近いかは本番のズーム挙動確認が必要な未解決事項として残す。
- **React 統合方式・MapLibre 最新 API** — 本設計時に Context7 で裏取り（未確定）。
- **学習コスト・工数** — Leaflet 資産を捨てるため相応の工数。
- **2 バリアントの地図描画は共通** — standard / DMDSS の差はデータソース側で、地図描画レイヤー自体はバリアント非依存の見込み。PoC・移行は実質 1 系統で足りる（要確認）。§5 の「双方で成立」は主にデータ連携部分の話。

## 9. 付録: 移行対象レイヤーと参照コード

**移行対象（Leaflet 依存で MapLibre に載せ替える機能）**:

- ベースマップ: `BaseMap.tsx`, `JapanMap.tsx`, `TileTintLayer.tsx`
- 強震点: `KyoshinPoints.tsx`, `KyoshinSubThreshold.tsx`, `KyoshinDetectedPoints.tsx`, `KyoshinMaxEffect.tsx`
- 活断層: `ActiveFaultsLayer.tsx`, `src/utils/activeFaults.ts`
- プレート境界: `PlateBoundariesLayer.tsx`
- 地震・EEW: `IntensityPoints.tsx`, `LpgmPoints.tsx`, 予報円 `PsWaveLayer.tsx`, `useDmdssWaves.ts`
- 津波・ヒートマップ・カメラ: 津波海岸線, `QuakeHeatmapLayer.tsx`, `flyToLite.ts`, 自動フィット（`FitToDetection`/`FitToCandidate`）
- 取得・状態: `useKyoshinRealtime.ts`, `src/App.tsx`（地図モード・連携の中枢）

**間引きスクリプト**: `scripts/build-active-faults.mjs`, `build-prefectures.mjs`, `build-subregions.mjs`, `build-tsunami-zones.mjs`

**設計参考**: `docs/kyoshin-detection-v3-design.md`
