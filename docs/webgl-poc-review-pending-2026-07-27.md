# 保留分まとめレビュー（`779a8bb` / `13b2a29` / `339f29d` / `f900a43`）— LOW 1件のみ

> 対象: 影響が低いと判断して保留していた4コミット（いずれもドキュメント／コメント変更）
> 保留の方針: 文言修正・決着済み軸の精度改善・レビュー側の LOW への訂正は即レビューせず溜め、
> まとめて1本の綴りで処理する（2026-07-27 ユーザー指示）
> レビュー日: 2026-07-27
>
> **ステータス: 4件中3件は指摘なし。LOW 1件。**

---

## 1. `779a8bb` — 実機計測結果の計画書反映

**指摘なし。**

§6 検証項目3 に実機結果を追記して**判定保留**と明記し、残作業を「Leaflet 側に計測フックを仕込む」へ更新。
§8 テキスト描画の「大部分解消」を訂正し、実機コスト（100〜260ms）・地震検知時と重なる構造・
対処3案（A/B/C）を記載。数値（fps 28.2〜52.7・`frame.max` 183.3・`p50` 全32計測 16.7ms）も
証跡と一致していた。両文書へのリンクも入っている。

## 2. `13b2a29` — 不整合解消・検証項目 状態一覧表の追加

**LOW 1件。それ以外は指摘なし。**

レビュー側の提案（冒頭に状態一覧表を置き、そこだけ更新する）が採用され、
さらに**「状態はこの表を唯一の正とする」と明記**された。加えて各所の古い記述を削除せず
「（当時のメモ。現在の状態は冒頭の状態一覧表を正とする）」と注記して**権威だけ下げた**——
提案より良い形である。経緯が失われない。

### 【LOW】項目6 の証跡件数が実際と合わない

| 表の記載 | 実際 |
|---|---|
| `6 毎秒更新 ｜ 済（**`rt-` 28**）` | **12件** |

`ls scripts/perf/results/ \| grep -c "surface-go2-rt"` → **12**
（内訳: `rt` 4 / `rt2` 2 / `rt-setdata` 2 / `rt-fs` 2 / `rt2-setdata` 1 / `rt2-fs` 1）。
28 の出所は不明で、他のラベルを足しても 28 にはならない。

**他の8項目の件数はすべて実際と一致していた**（`leafletB` 44 / `layerB-vs` 38 / `camera` 34 /
`sub-` 9 / `subrt` 20 / `eew` 8 / `label` 4 / `memory` 1）。

**結論への影響はない**（項目6 は 12件でも十分な証跡で「決着」のまま）。ただし
**この表は「唯一の正」と宣言されている**ため、中の数字が誤っていると今後これを見た人が
「28件ある」と信じてしまう。**権威を上げた分、正確さの要求も上がっている。**

## 3. `339f29d` — 項目3 再計測の形をガイダンス化

**指摘なし。**

レビュー側が `ec74bef` で示した再計測の形（旧証跡との混同回避のため新ラベル `-v2` を使う）を
計画書に1行で記録したもの。**この記録が実際に機能した**——本日の再計測は
`surface-go2-camera-v2` / `surface-go2-leaflet-camera-v2` のラベルで実施され、
旧証跡（各34件）と新証跡（各34件）が混ざらずに集計できた。

## 4. `f900a43` — 段階0 静止計測器の実測割り化・誤コメントの訂正

**指摘なし。**

`measure-kyoshin-static.js` の `fps` / `perSec` を実測 `elapsedMs` 割りに統一。
誤っていた「fps 名目割りの最後の1箇所」コメントを訂正し、**残る3箇所を正確に列挙**
（`realtime.ts` / `subthreshold-rt.ts` / `subthreshold.ts`）したうえで、
据え置く理由（結論が fps 非依存・既存証跡との比較のため）を明記。

**`before`/`after` の物差しの違いに関する注記**も入っている——
「before は改修前コードが要り測り直せない（比較時は『before/旧after=名目・新規=実測』を明示すること）」。
レビュー側が指摘した非対称性がそのまま反映された。

**この修正は本日の計測で実際に活きた**: 静止時を新定義で測り直し（`surface-go2-static-v2` 2件）、
移動中と同一物差しでの比較が成立した（`meta.elapsedMs` 15008.9 対 名目 15000＝ズレ 8.9ms）。

---

## 保留運用の振り返り

**4件中3件が指摘なしだった**——溜める判断は妥当だったと言える。
即レビューしていれば往復が3回増え、いずれも「確認しました」で終わっていた。

一方、`13b2a29` の LOW は**溜めたことで報告が遅れた**。ただし結論に影響せず、
かつ本日の実機計測を優先する判断のほうが得られたものは大きい。

**判定基準（実装コードのロジック変更は即レビュー／文言修正は溜める）は今後も維持してよい。**
ただし**「唯一の正」と宣言された表のような、後続の判断の土台になる記述**は、
文言変更であっても数値の照合だけは早めに行うほうが安全である。
