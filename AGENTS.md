# AGENTS.md

realtime-earthquake-viewer（リアルタイム地震ビューアー）で作業するときの手順とルール。

## プロジェクト概要

- React 18 + TypeScript + Vite 6 製の PWA。MapLibre GL JS（WebGL）地図で地震情報・緊急地震速報・津波情報・リアルタイム震度を表示する。
- データ: DMDATA.JP API（WebSocket + REST）／ Yahoo リアルタイム震度（強震モニタ・HTTPS JSON）（DMDSS 版）、P2PQuake API v2（標準版）。
- GitHub Pages（サブパス配信 `/realtime-earthquake-viewer/`）へ GitHub Actions で自動デプロイ。

## ビルドバリアントと URL（重要）

このプロジェクトには **2 つのビルドバリアント** があり、**配信パスが異なる**。

| バリアント | dev 起動コマンド | ビルド出力 | 配信パス（URL） |
|---|---|---|---|
| standard | `npm run dev` | `dist/` | `/realtime-earthquake-viewer/` |
| DMDSS | `npm run dev:dmdss` | `dist-dmdss/` | `/realtime-earthquake-viewer/dmdss/` |

- **DMDSS 版の検証時は `npm run dev:dmdss` で起動し、必ず `/dmdss/` サブパスにアクセスすること**
  - dev URL 例: `http://localhost:5173/realtime-earthquake-viewer/dmdss/`（ポートはフォールバックで変わる）
- `npm run dev`（standard 版）では DMDSS 設定セクションが表示されず、P2PQuake 接続になる
- Windows で `cross-env` が動かない場合は PowerShell から `$env:VITE_VARIANT="dmdss"; npx vite` で代替起動する

## 変更時の基本フロー（必ずこの順で行う）

> **このフローは「ワークツリー内でコミットし、main に追随させ、ブランチを `ready/` へ改名する」（手順 11）ところで実質的に完了する**（手順 12 は後片付け）。
> main へのマージ・バージョン更新・プッシュは**このフローに含まれない**。複数セッションが並行して作業するため、
> main を書き換える操作はすべて下記「リリース」の手続きに集約している。**実装フローの途中では行わない**
> （理由は「リリース」節の「原則」を参照）。

1. **状況整理・修正方針の確認**（実装に着手する前に必ず行う。自明な軽微修正でも省略しない）
   - 対象のコード・挙動を調査し、現状と問題点（原因・影響範囲）を整理する
   - **被害の見積もりを、方針を提示する前に裏取りする。** 実装から確かめずに方針へ載せると、規模も優先度も対策の形も見当違いになる。とくに**並行処理・タイミングに関わる問題は、実測するか全経路を列挙する**まで断定しない（「同時に呼ばれる」と書く前に呼び出し元を全部数える、「こう壊れる」と書く前に壊してみる）
   - **網羅性を主張する調査（点検・棚卸し・突き合わせ）を根拠に範囲を決めたなら、方針を提示する前に「調査レビュー」を行う**（下記「調査レビュー」。エージェントへ渡すのは調査の手順・入力・出力であって変更差分ではない）。**ここでしか掛からない** —— 実装が終わってから回しても、間違った範囲のまま仕上がったものを確認するだけになる
   - **調査で外部 API を叩くなら、走らせる前にリクエスト数を見積もる**（下記「調査で外部 API を叩くとき」。配信元からの利用量の指摘が発端）
   - **「なぜ直すのか」の説明も裏取りの対象。** 直す必要があること自体は正しくても、理由の説明が間違っていれば直す範囲も誤る。2026-09-15 に「アーカイブの目録には翌日ぶんが現れないので 00:00〜09:00 JST の窓では電文を取りこぼす」と報告したが、旧実装の右端は UTC で +1 日していたため**取りこぼしは起きていなかった**（実際の効きは無駄な取得が減ることだけ）。報告する前に、その説明が実装で成り立つかを追い直すこと
   - 整理した内容と修正方針（どこを・なぜ・どう直すか）をユーザーに提示する
   - ユーザーの確認を得てから次のステップに進む。指摘や方針変更があれば整理からやり直す
   - **機能を落とす・制限する提案をするなら、その前に「落とさずに済む方法」を実装から調べる**（下記「機能を落とす提案の前に調べる」）
   - **直し方を提案するなら、設計として正しいものを出す**（下記「直し方の提案は、設計として正しいものを出す」）。触る範囲が大きくなることを理由に、筋の悪い案を勧めない
2. **ワークツリー作成**（修正・機能追加時はワークツリーを作成してから作業する。ブランチ名は必ず `worktree/<type>/<name>` 形式にする: `worktree/fix/〇〇`・`worktree/feat/〇〇`・`worktree/refactor/〇〇`・`worktree/docs/〇〇`・`worktree/chore/〇〇` など）
   - `worktree/` という接頭辞は**作業中**であることを表す。完了時に手順 11 で `ready/` へ付け替える
   - **作成前に必ず `git pull`（メインの checkout 側で）し、その時点の最新 `main` から派生させる。** ローカルの `main` が `origin/main` に遅れたまま気づかずワークツリーを切ると、大きく後退した地点から作業を始めることになり、後で `origin/main` へ作り直す手戻りが生じる（過去に実際に発生）
   - **前の作業の続きを別のワークツリーで始めるときは、`main` から切って後でそのブランチを取り込むのではなく、その `ready/*`（または `worktree/*`）から直接切る。** リリース待ちのブランチはまだ `main` に無いため、`main` から切ると前段の成果を持たないところから始めることになり、必要になった時点で `git merge` する形になる。**取り込みは、そのとき手元に未コミットの変更があると実行できず、待ち行列が伸びるほどコンフリクトも増える**（2026-09-09 にこの手順で実際に詰まった）。派生元を最新のブランチにしておけば、取り込み自体が要らなくなる
     ```bash
     git worktree add -b worktree/<type>/<name> .codex/worktrees/<name> ready/<type>/<前段>
     ```
     - 派生元のブランチはリリース時に `main` へ入るので、こちらのブランチはその後 `main` の履歴に含まれる。**リリースの順序は派生元が先**になる点だけ、`/release` で候補を並べるときに気をつける
   - `git worktree` でブランチ名と保存先を明示して作成し、以下の手順で作業を開始する:
     ```bash
     # Step 0: メインの checkout 側でローカル main を最新へ（ワークツリー内からは実行できない）
     git pull
     # Step 1: 正しいブランチ名でワークツリーを作成
     git worktree add -b worktree/<type>/<name> .codex/worktrees/<name>
     # Step 2: 作成したワークツリーに入る
     作成した worktree のディレクトリへ移動
     ```
3. **実装**（**ワークツリー内では `package.json` の `version` フィールドを変更しない**。バージョンはリリース時に一括で決める。理由は下記「リリース」参照）
   - **電文から読む項目・表示・読み上げを足したり変えたりしたら、テストボタンのデータ（`src/utils/testData.ts`）も同じコミットで追随させる。** テストボタンは**本物の電文を再現するもの**で、そこに無い形は実機で一度も確かめられない（下記「テストボタンは実機確認の唯一の入口」）
4. **検証**（下記「検証」を実施。型チェック必須＋**実行確認（ブラウザ確認）必須**）
   - **電文の読み取り（パーサー）を変えたときは、テストボタンではなくリプレイで確かめる。** テストボタンは内部型を直接組み立てるためパーサーを通らない（下記「読み取りの変更はリプレイで確かめる」）
   - **本番ビルド確認（`npm run preview` でのブラウザ確認）まで行った場合は、その旨をコミットメッセージに記録する。** リリース時に検証範囲を判断する材料になる（`/release` スキル手順 4 の軽量化条件）
5. **敵対的レビュー**（下記「敵対的レビュー」の手順で必ず実施。**全変更が対象。小規模修正でも省略しない**。指摘があれば実装に戻る。**ただし無条件に繰り返さない**——収束しない場合の手当ては下記「巡回が収束しないとき」）
6. **ドキュメント更新**（下記「ドキュメント更新」の条件に該当する場合。関連する仕様書（`docs/spec/`）・README.md・AGENTS.md・コード内コメントを**同一コミットで**更新する）
7. **ドキュメント客観レビュー**（下記「ドキュメント客観レビュー」の手順で必ず実施。**ドキュメントを更新した場合のみ**・技術的すぎないか・初見の読み手に伝わるかをエージェントで客観的に検証。指摘があればドキュメント修正に戻る）
8. **コミット前確認**（実装・検証・敵対的レビュー・ドキュメント更新が終わったら、コミットする前に必ずユーザーに確認を取る。**省略しない**）
   - 何を・なぜ・どう直したかを簡潔に提示し、コミットしてよいか確認する
   - 確認が取れるまで次のステップに進まない
9. **コミット**（確認が取れたら、下記「コミット」の規約に従いコミットする。バージョンはまだ変更しない）
10. **main の取り込み**（コミット完了後に必ず実施する。**省略しない**）
    - ワークツリー内で `git merge main` を実行し、ローカル `main` の先行分を取り込む
      - ここで取り込むのは**ローカルの `main`** でよい。`origin/main` がさらに先行していることもあるが（同一 `.git` を共有しない別クローン・別環境でリリースが完了した場合）、**その差はリリース手続きの着手前チェックで解消されるため、実装セッションが追いかけない**（`main` を進める操作をしないこと）
    - コンフリクトはここで解消する。解消したら型チェックを再実行し、必要なら再検証する。**解消が実質的なコード変更を伴った場合は敵対的レビューをもう 1 度回す**（どちら側のハンクを採るかの判断はバグを生みうるため）
    - コンフリクトを解消した場合は**マージコミットのメッセージにその旨を明記する**。リリース時の検証範囲の判断材料になる（`/release` スキル手順 4 の軽量化条件）
    - **理由**: リリースまで待つ間にも main は他セッションのマージで進む。取り込みを後回しにすると乖離が積み上がり、リリース時にまとめて衝突する
    - この取り込みは 1 回きりの緩和策であり、待ち行列に長く留まればまた乖離する。リリース待ちが長引いたら再度取り込んでよい
11. **リリース待ちへの登録**（ブランチ名の接頭辞を `ready/` に付け替える。**省略するとそのブランチは永久にリリースされない**）
    - **改名前に必ずユーザーに確認を取る。省略しない**。確認は **通常の対話** で次の 4 択を提示する
      - **ラベルは短く保ち、動作の詳細は選択肢の説明文へ回す。** 一覧を一瞥して 1 と 2 の違いが分かることが大事

      | # | ラベル | 動作 |
      |---|---|---|
      | 1 | リリース待ちのみ | 改名だけ行い、ワークツリーは残す |
      | 2 | リリース待ち＋片付け | 改名後、ワークツリーのディレクトリも削除する（**ブランチは残す**。手順 12 参照） |
      | 3 | 続けてリリース | 改名後、そのまま「リリース」の手続きへ進む |
      | 4 | まだ改名しない | `worktree/*` のまま留める（追加で直す予定がある場合）。待ち行列に現れないため手順 12 へは進まず、後日あらためてこの手順に戻る |

    - **確認の出し方**は状況で決める

      | 状況 | 出し方 |
      |---|---|
      | 発言がどの選択肢か一意に指している（「リリース待ちに入れて」＝1。ワークツリーの片付けまで求めていれば 2。「リリースして」＝3、改名前の「まだ直したい」＝4） | 4 択の提示を省略してそれを実行する。改名の可否はその発言で確認が取れている |
      | 発言から一意に決まらず（「コミットして」等）、かつ**追加のフェーズ・修正が続く見込みがある**（ユーザーが完了を明言していない・スコープの残りが分かっている等） | **4 択を出さない。** 手順 9〜10 だけで止め、ready/ の話は持ち出さない。作業が全て完了したと判断できるまで、コミットのたびに聞き直さない |
      | 発言から一意に決まらないが、**自分（Codex）が作業を完了したと判断できる** | 4 択を出す。判断の主体は自分であって、ユーザーが完了を明言したかどうかではない。ユーザー側の意向を先読みして「まだ直したいかもしれないから」と自主的に省略しない——聞くコストは低く、続きがあるならユーザーが選択肢 4（まだ改名しない）を選べば済むだけ |
      | 手順 10 でコンフリクト解消を伴った、または**伴ったか判然としない** | 発言が選択肢を指していても省略せず、**差分を提示してから**確認する |

      - コンフリクト解消の有無は、このセッションで手順 10 を実行していなければ対象ブランチの最後のマージコミット（`git log --merges -1 <branch>`）のメッセージで判定する。手順 10 はコンフリクト解消時にその旨を明記する規定のため
    ```bash
    git branch -m worktree/<type>/<name> ready/<type>/<name>
    ```
    - **`worktree/*` = 作業中、`ready/*` = 完成・リリース待ち。** この改名だけが「完了」の印になる
    - **理由**: 未マージのブランチには「実装途中で中間コミットしただけのもの」と「完成したもの」が混在し、コミット履歴や更新日時からは区別できない。名前で分けておかないと、リリース時に未完成のブランチを本番へ出しうる
    - ワークツリーのディレクトリ名（`.codex/worktrees/<name>`）は変わらない。リネームしてもワークツリーはそのまま使える
    - 同名の `ready/<type>/<name>` が既にあるとリネームは失敗する（過去のリリース中断などで残っている場合）。**勝手に別名を付けず**、状況をユーザーに報告して対象を確認する
    - **改名後に追加変更が必要になった場合**は、まず `git branch -m ready/<type>/<name> worktree/<type>/<name>` で作業中へ戻してから直す。検証・敵対的レビューを通し、あらためて `ready/` へ改名する。`ready/*` は「いつリリースされてもよい状態」を意味するため、そのまま触らない
      - 選択肢 2 でワークツリーを消していた場合は、**改名を戻した後に** `git worktree add .codex/worktrees/<name> worktree/<type>/<name>` → `作成した worktree のディレクトリへ移動` で作業を再開する。ブランチが残っているため巻き戻せる
    - 新ルール導入前から残っている `worktree/*` のブランチは、次に触るセッションがこの手順で改名すればよい。一括での移行は不要
    - **なぜ手順 8 のコミット前確認と分けるのか**
      - コミットの承認は「この変更を記録してよいか」であって「完成したか」ではない。改名は**この内容でリリースしてよい**という宣言であり、他セッションや監視モードが候補として拾い得る状態にする**外向きの操作**
      - 手順 10 でコンフリクト解消を伴った場合は、コミット前確認で見せた内容と実際の中身が変わっている（差分提示が必要になるのはこのため）
12. **ここで実装セッションは終了**（選択肢 1・2 の場合。ブランチはリリース待ち行列に入る。選択肢 3 はそのまま下記「リリース」へ進む）
    - ユーザーに「コミット済み・リリース待ち」であることを伝える。**マージ・バージョン更新・プッシュを自発的に行わない**
    - **選択肢 2 を選んだ場合は、この場でワークツリーのディレクトリを削除する**
      - メインのチェックアウトへ移動してから `git worktree remove .codex/worktrees/<name>`。**ブランチ（`ready/*`）は消さない**（消すとリリース候補の列挙に現れず、作業内容を指す ref も失われる）
      - リリース側はワークツリーが無ければ削除を飛ばし、ブランチの削除は通常どおり行う。マージ前に消しても手続きは成立する
      - 削除に失敗したら（未コミットの変更が残っている・別セッションやツールがそのパスを掴んでいる等）**`--force` で押し通さず**、状況をユーザーに提示して指示を仰ぐ
      - **この場合、下記の自己クリーンアップ監視は行わない**（消すものが既に無い）
    - **選択肢 3 を選んだ場合**、またはユーザーがそのままリリースを指示した場合は、メインのチェックアウトへ移動 でワークツリーを抜けて「リリース」へ進む（出す候補が自分の 1 本だけなら、セッションを分ける必要はない）
    - **選択肢 1 でセッションを開いたまま待つ場合は、マージを監視して自分で後片付けしてもよい**（任意。詳細は下記「マージ後の自己クリーンアップ」）
    - セッションを終える際は、検証用に起動した dev サーバー（Vite）を停止する（セッション中は停止せず再利用する。**手順は下記「検証」を見ること** —— **Git Bash の `kill` はこの環境では効かない**ので、そこに書いた方法で止める）

### 機能を落とす提案の前に調べる

**機能を落とす・制限する提案をするなら、その前に「落とさずに済む方法」を実装から調べること。**
労力が大きそうという印象だけで削りにかからない。**上限値を置く提案も同じ**（「上限は N まで」）。

落とす提案は、こちらの見積もりが甘いだけで相手の要求を削る。しかも「調べた上での判断」の顔をして
出てくるため、受け取る側は反論の材料を持たない。

- **提案文には、根拠にした実装のファイル:行を最低 1 つ添える。** 添えられないなら、まだ調べていない
- **制限値を置くときは、その数字が「破綻する境界」なのか「なんとなく不安な水準」なのかを区別する。**
  境界が分からないなら、先に境界を測る
- **調べる範囲は、その機能が依存するデータの生成側まで伸ばす。** 実行時のコードだけ読んで
  「1 箇所直せば済む」と結論すると、生成データに焼き込まれた前提を見落とす（実例:
  地名ラベルの退避余地は `gl/labelOverlap.ts` が画面座標で扱うが、その値は
  `scripts/lib/labelAnchor.mjs` の `shiftRoom` が**経度線に沿って真北・真南へ**測って
  焼いたもの。実行時のコードだけでは、この方位の固定が見えない）

これは 2026-08-27 の「作業範囲を縮小するなら実行前に報告する」の**前段**にあたる。あちらは
縮小を決めた後の報告義務、こちらは縮小を提案として口に出す前の調査義務。

### 直し方の提案は、設計として正しいものを出す

**修正方針を出すときは、設計として正しいものを必ず含め、それを推すこと。** 触るファイルが増える・
他セッションと競合して順番待ちになる・工数が伸びる —— こうした事情は**選択肢の説明に書くのはよいが、
推す理由にしてはならない**。

こちらの都合で選択肢を狭めると、受け取る側は並んだものの中に最善があると読む。**正しい案が欠けて
いることに気づけるのは、調べた側だけ。**

- **事実が手元にあるのに代理値で代用する案を、範囲が狭いという理由で勧めない**（代理値そのものの
  害は `rules/common/code-review.md`「指標・条件が本体からずれていないか」）
- **範囲が大きすぎるかどうかを決めるのはユーザー。** 大きいと思うなら、正しい案を提示したうえで
  「ここまで要るか」を聞く。黙って下げない
- **根本的にありえない・過剰だと判断したときは下げてよい。** そのときも正しい案を先に示し、
  下げる理由を書く
- 直す範囲が広がったこと自体は報告する。**ただし 2026-08-27 の「作業範囲を縮小するなら実行前に
  報告する」とは向きが逆**で、こちらは広げる側。止めるかどうかの判断材料として出す

> 2026-09-12: #71（リプレイの時間軸を切り替えると行動チェックリストの帯が旧い軸のまま残る）の
> 直し方として、「検知結果のオブジェクトの**参照が変わったか**」で時間軸の切替を判定する案を勧めた。
> 知りたい事実（検知結果のデータ時刻）は `KyoshinDetectorV2Result.dataTime` として**既に手元に
> あった**のに、`src/App.tsx` が他セッションと競合するのを避けたいというこちらの都合で代理値の案を
> 選んでいた。ユーザーの「一番正しい修正は」で撤回した。**競合も工数も、正しさを曲げる理由にならない。**

### ユーザーからの合図

ユーザーが特別な言い回しを覚える必要はないが、次の意図は取り違えないこと。

| ユーザーの言葉 | 意味 |
|---|---|
| 「コミットして」 | 手順 9〜10 を実行する。**自分（Codex）が作業を完了したと判断できない限り、手順 11（`ready/` 改名）の話は持ち出さない**（続きの作業がある間はコミットのたびに聞き直さない。完了と判断した時点で、ユーザーに確認せず自主的に 4 択を省略しない） |
| 「リリース待ちに入れて」 | 手順 11 の選択肢 1（ワークツリーも片付けるなら 2）を実行し、実装セッションを終える。**4 択の提示は省略してよい**（コンフリクト解消を伴った場合を除く。下記） |
| 「リリースして」 | 手順 11 の選択肢 3。続けて「リリース」の手続きへ進む。**4 択の提示は省略してよい**（同上） |
| 「これで十分」 | レビューの反復（敵対的レビュー・ドキュメント客観レビューのいずれも）を打ち切り、その時点の内容で確定する |
| 「まだ直したい」 | `ready/*` を `worktree/*` へ戻して作業を再開する（push 前ならいつでも巻き戻せる）。**改名前に言われた場合は選択肢 4** —— 改名しないだけで、巻き戻し操作は要らない |

**発言から選択肢が一意に決まっても、手順 10 でコンフリクト解消を伴った（または伴ったか判然としない）場合は 4 択を省略せず、差分を提示してから確認する。** 発言した時点と実際の中身が食い違っているため。

**push するまでは、いずれの操作も引き返せる。** コミットも `ready/` 改名もローカルの操作にすぎない。「もう戻れない」と誤解させないこと。

### マージ後の自己クリーンアップ（任意）

実装セッションを開いたまま待つ場合、自分のブランチがマージされるのを監視して、自分でワークツリーを片付けてよい。**自分のワークツリーは自分で消すのが最も確実**（他セッションのロックに阻まれないため）。

**ただし条件が 1 つある。**

> **監視に入る前に必ず メインのチェックアウトへ移動すること。**
> 居座ったままだとリリースセッションが `git worktree remove` に失敗する。**片付けを速くするつもりが、かえって妨げることになる。**

監視を始める前に `git branch --show-current` が `main` を返すことを確認する（ワークツリーを出られているかの機械的な確認）。そのうえで `Monitor` を `persistent: true` で起動する。

```bash
cd <リポジトリのトップレベル>
fails=0
while true; do
  sleep 120
  # ready/ が消えた場合、理由は 2 つある。取り違えると作業場を消す
  git rev-parse --verify --quiet ready/<type>/<name> >/dev/null || {
    if git rev-parse --verify --quiet worktree/<type>/<name> >/dev/null; then
      echo "REVERTED-TO-WORKTREE: 作業中へ戻されました。何もせず監視を終了します"
    else
      echo "ALREADY-CLEANED: ready/<type>/<name>"
    fi
    exit 0
  }
  if git fetch --quiet 2>/dev/null; then
    fails=0
  else
    fails=$((fails + 1))
    [ "$fails" -ge 5 ] && {
      echo "WATCH-ERROR: git fetch が5回連続で失敗しました。監視を終了します"
      exit 1
    }
    continue
  fi
  git merge-base --is-ancestor ready/<type>/<name> origin/main 2>/dev/null && {
    echo "MERGED-AND-PUSHED: ready/<type>/<name>"
    exit 0
  }
done
```

ポーリング間隔が「監視モード」（60 秒）より緩いのは、待つ対象が違うため。あちらはブランチの出現、こちらは push の完了を待つ。

**ループが黙って回り続ける状態を作らないこと。** 上の 2 つの脱出口がそれを担っている。

- **ブランチが先に消された場合** — リリース側が手順 7 まで済ませた。`ALREADY-CLEANED` で正常終了する。これが無いと `merge-base` が「そんなオブジェクトは無い」で失敗し続け、仕事が終わったのに監視だけが気づかず回り続ける
- **`git fetch` が恒久的に失敗する場合**（資格情報の失効・ネットワーク断） — 5 回連続で `WATCH-ERROR` を出して終了する。これが無いと永久に無音で回る

> この検知は**リリース手続きのマージが `--no-ff` であること**に依存している（ブランチの tip が履歴上の祖先として残るため）。マージ方式を変える場合はこの節も見直すこと。

> **見るのは `main` ではなく `origin/main`。ここを間違えると作業が消える。**
> ローカルの `main` はリリース手続きの**マージ**の時点で更新される。しかしその後の**統合後の検証**で不具合が見つかれば、`git reset --hard origin/main` でマージごと破棄されうる。`main` を見て発火すると、**破棄される前にブランチとワークツリーを消してしまい、作業内容を指す ref が一つも残らない**。`origin/main` は push 成功後にしか進まないため、これを条件にすれば確定後にのみ発火する。

通知の種類ごとに動きが変わる。

| 通知 | 意味 | すること |
|---|---|---|
| `MERGED-AND-PUSHED` | リリースが確定した | 下記の片付けへ進む |
| `REVERTED-TO-WORKTREE` | ユーザーが「まだ直したい」で作業中へ戻した | **何もしない。** 作業を再開する場所なので消してはならない |
| `ALREADY-CLEANED` | リリース側が既に片付けた | 何もしない |
| `WATCH-ERROR` | `git fetch` が繰り返し失敗した | ユーザーに報告する |

`MERGED-AND-PUSHED` の場合の片付け:

1. `git worktree list` で**自分のワークツリーがまだ残っているか確認する**
2. **通常の対話 で削除してよいか確認する。** 他の削除経路（リリース手順 7・取りこぼし検出）はすべて確認を課している。ここだけ無確認にする理由はない
3. 承認後、`git worktree remove` と `git branch -d` で片付ける
4. **既に無ければ何もしない。** リリースセッションが先に片付けただけであり、エラーではない
5. 確認した直後にリリースセッションが消すこともある。**削除コマンドが「対象が無い」と言って失敗しても、それも正常**として扱う

**これは「うまくいけば早く片付く」という上積みにすぎない。** 監視が働かなくても、次のリリースの取りこぼし検出（`/release` スキル手順 1）で必ず回収される。先着順でよく、競合を調停する仕組みは要らない。

> ユーザーから「修正前に状況を整理し問題点をまとめ、修正内容を確認してから作業する」方針の指示済み。
> ユーザーから「今後は必要に応じて README を更新して、コミットまで自動で行う」方針の指示済み（ただし後述の「コミット前確認」が優先。実装・検証・敵対的レビュー・ドキュメント更新の後に必ずユーザー確認を取ってからコミットする。「自動で行う」はワークフロー全体を止めないという意味であって、コミット前の一時停止をスキップする意味ではない）。
> ユーザーから「リリース時は毎回バージョン種別（メジャー/マイナー/パッチ/なし）を確認する」方針の指示済み。
> ユーザーから「main へのマージはユーザーに確認してから行う」方針の指示済み。
> ユーザーから「リリース後はワークツリー削除を、セッション終了時は dev サーバー停止をクリーンアップとして実施する」方針の指示済み。
> ユーザーから「ソース修正後は全変更で敵対的レビューを行う」方針の指示済み（2026-08-10）。
> ユーザーから「修正時は関連仕様書・他ドキュメント類も合わせて修正する」方針の指示済み（2026-08-10）。
> ユーザーから「ドキュメント更新後は技術的すぎないか客観レビューを行う」方針の指示済み（2026-08-10）。
> ユーザーから「ユーザーが明示的に指定した作業範囲・深さ・粒度（『全て』等）を、実装の途中で縮小・簡略化する場合は、実行前に必ず報告し確認を得る」方針の指示済み（2026-08-27）。**確認前に縮小した状態を先に成果物として出さない。** 縮小に気づいた時点で一旦手を止め、何をどこまで削るつもりかを提示してから続きに進む。
> ユーザーから「リリース待ちに入れるか（`ready/` 改名）の確認は、コミットのたびに聞くのではなく、作業が全て完了した時点で一度だけ聞く」方針の指示済み（2026-08-27）。追加のフェーズ・修正が続く見込みがある間は、コミットが完了しても手順 11 の確認へは進まない（下記「ユーザーからの合図」表を参照）。
> ユーザーから「この会話（セッション）由来の不要なコンテキストを、利用者向け・外部消費者向けの成果物に書き込んではならない」方針の指示済み（2026-08-27）。ここでいう不要なコンテキストとは、作業フェーズ名（「Phase 2として」）・スコープ縮小の経緯・「概算」「代表値」「〜のみ収録」といった実装上の言い訳など、読者がその会話を見ていなければ意味を持たない記述を指す。**対象は「ドキュメント」に限らない**——データファイルのメタデータフィールド（`description` 等）・UI 文言も同じ穴に落ちる（実例と詳細は下記「ドキュメント客観レビュー」参照）。
> ユーザーから「`ready/` 改名を聞くかどうかは、ユーザー側の意向ではなく、自分（Codex）が作業完了だと判断したかどうかで決める」方針の指示済み（2026-08-28）。大規模な作業の後、「まだ直したい点があるかもしれないから」とこちらの判断で 4 択の提示を控えたところ、ユーザーから「私がどうかじゃなくて、あなたが作業完了だと思ったら改名を聞くようにしてください。私の方で何かあっても私が拒否したりするだけなので」と修正された。**聞くコストは低く、続きがあるならユーザーが選択肢 4 を選ぶだけで済む**——ユーザーの意向を先読みして自主的に確認を省略しないこと。
> ユーザーから「機能を落とす・制限する提案をする前に、それ相応の調査を行う」方針の指示済み（2026-08-29）。詳細は下記「機能を落とす提案の前に調べる」。
> ユーザーから「テストボタンは本物の再現をする必要がある。修正に応じてテストボタンの内容も直すこと」方針の指示済み（2026-09-06）。あわせて「電文の読み取りを変えたときはリプレイで確かめる」ことを検証手順に加えた。詳細は下記「テストボタンは実機確認の唯一の入口」「読み取りの変更はリプレイで確かめる」。
> ユーザーから「修正方針を提案するときは、設計として正しいものを出す。触る範囲が大きくなることより、そちらを優先する」方針の指示済み（2026-09-12）。詳細は下記「直し方の提案は、設計として正しいものを出す」。
> ユーザーから「作業手順・判断基準はメモリではなく、リポジトリにコミットする資材へ書く」方針の指示済み（2026-08-29）。**メモリ（`~/.codex/projects/<プロジェクト>/memory/`）は端末ごとに閉じており、別の PC で作業するときに引き継がれない。** 手順として守らせたいものは AGENTS.md か `docs/spec/` 配下へ置き、コミットに含めること。メモリに残してよいのは、その端末固有の事情（接続先の IP・インストール状況・環境の癖など、他の PC では成り立たない事実）に限る。
> 2026-07-12: 複数ワークツリーを並行して進めている場合に、各セッションがマージ前の古いバージョンを見て同じ番号（例: パッチ +1）を選んでしまい、結果的にバージョンが正しく積み上がらない事故が発生。対策として、バージョン確定を「ワークツリー内でのコミット時」から「main へのマージ直後・main 上」に移した（現在はさらに下記「リリース」へ集約している）。
> 2026-07-12: 上記の変更で「バージョン確認」がコミット前の一時停止ゲートを兼ねていたことが判明。バージョン確認をマージ後に移した結果、コミット前に立ち止まる仕組みが失われていたため、コミット前確認を独立したステップとして追加した（バージョンとは無関係に、コミットしてよいかを毎回確認する）。
> 2026-08-15: 2026-07-12 の変更でバージョン番号の積み上げ事故は防げたが、**main を書き換える権利の奪い合い**は残っていた。実装セッションが各自でマージ・版上げ・プッシュまで行う建て付けだったため、並行セッションの順番をユーザーが手作業で裁く必要があり、待たされたブランチは main から離され続けていた（実測: 同時刻に完成した 3 本が待機中に behind 13〜16 まで拡大）。またリリースが機能 1 本ごとに発生するため本番デプロイが 1 日 9 回に達していた。対策として、**main を書き換える操作をすべて実装フローから切り離し、下記「リリース」に集約**した。これにより**実装セッション間の**順番調整は不要になる（リリースセッション自身は、他にリリースが走っていないことを着手時に確認する）。
> 2026-08-15: 上記の集約にあたり、リリース候補を「未マージのブランチ」で判定する案には穴があった。未マージのブランチには「実装途中で中間コミットしただけのもの」と「完成したもの」が混在し、git からは区別できない（並行本数が増えるほど人間の記憶にも頼れない）。未完成のブランチを本番へ出す事故を防ぐため、**完了したブランチを `ready/` 接頭辞にリネームする**規約を追加し、待ち行列を `ready/*` に限定した。

## 検証

> **コードを修正した場合は、型チェックだけでなく必ずアプリを起動して実行確認（ブラウザ確認）まで行う。**
> 型チェックのみで完了とせず、**特に指定がない場合は `npm run dev:dmdss`（DMDSS 版）で起動し**、Playwright MCP で実際の表示・挙動を確認してからコミットする。

- **型チェック（必須）**: `npx tsc -b`。エラー0を確認する。
  - `npm run build` でも型チェックは走るが、その前段に**地名ラベル用グリフの検証**（`build-glyphs.mjs --check`）が入る。ここで落ちた場合は型エラーではなく「グリフの焼き直し忘れ」なので、メッセージを読んで切り分けること（下記「補助コマンド」参照）。
- **ユニットテスト**: テストを持つコード（`src/utils/` / `src/hooks/` / `src/services/` / コンポーネント配下の `*.test.ts` が対応する箇所）を変更した場合は `npm test`。**main への push 時に CI（`.github/workflows/deploy.yml`）がビルドより先に同じテストを走らせ、失敗するとビルドもデプロイも実行されない**ため、`ready/` へ改名する前に通しておく。
  - **挙動を変えるときは、正・対照・安全弁の 3 種を対にして固定する**（**正**＝変更が効くこと／**対照**＝境界の手前では効かないこと／**安全弁**＝併せて緩めなかったものが残っていること）。**これはリポジトリ共通の書き方で、強震モニタの検知エンジンに限らない**（例: [`src/utils/alertSound.test.ts`](src/utils/alertSound.test.ts) の「通知音のあとに声を出すまでの間」）。1 つでは足りない ―― 「効くこと」だけを書くと、境界を動かす次の変更で静かに壊れる。検知エンジンでの具体例は下記「特殊ケース: 強震モニタ検知エンジンを変更したとき」。
  - **探索・走査の順序を変える修正では、順序に依存していたテストが先に壊れる。** 応答を「何番目の呼び出しに何を返すか」で並べた fixture は探し方と結合している（線形探索を二分探索へ替えたとき 2 件が落ちた）。**落ちたものは「意図的に覆した」のか「壊した」のかを分ける**（下記「特殊ケース」と同じ規律）。順序に依らない形へ書き直せるならそちらへ倒す —— 「最初の応答で決着する」fixture は、窓の広さや探し方が変わっても意味を保つ。
  - **後から足すモックが、初期化処理に食われていないか確かめる。** 1 回だけ返す指定（`mockReturnValueOnce` 等）をセットアップの前に置くと、そのモックは起動時の取得に消費され、テスト本体は既定値を見る。**テストは通るので気づけない**（実際に書いた）。置くのはセットアップの後で、なぜそこなのかをコメントに残す（→ `rules/common/testing.md`「後から足す回帰テスト」）。
  - **大きく削るときは、削除対象でないものを巻き込んでいないか `git show HEAD:<path>` と突き合わせる。** 旧経路の撤去でテストを 267 行削ったとき、残すべき `describe` を 1 つ巻き込んでいた（型検査も残りのテストも通る）。
  - **生成スクリプトを他のモジュールから import するなら（テストかどうかを問わない）、`main()` を「直接実行のときだけ走らせる」門で包むこと**（`process.argv[1]` と `import.meta.url` の突き合わせ。実物は [`scripts/build-epicenter-accents.ts`](scripts/build-epicenter-accents.ts) の末尾）。包み忘れると、定数を 1 つ読むためだけの import で生成処理まで走る —— **そのスクリプトが要る外部資源（`build-epicenter-accents` なら VOICEVOX）を持つ端末では `npm test` が生成物を黙って書き換え、持たない CI では `process.exit(1)` でテスト実行ごと落ちる**。しかも落ち方が一定しないので気づきにくい（2026-09-11 の連続する 2 回の CI で、`process.exit` とワーカーのハングの両方を観測した。後者は exit 0 で通ってしまう）。[`scripts/scriptEntrypoints.test.ts`](scripts/scriptEntrypoints.test.ts) が `npm test` で検査する。**字面の共起では足りない** —— 門の条件と `main()` の呼び出しが同じブロックに入っていることまで見ること（`__dirname` の計算で `import.meta.url` を既に使っているファイルでは、門を消しても字面だけなら残る）。
  - **テスト本体の中で対象モジュールを初めて読まないこと。** そのファイルで初回の解決・変換（全ファイル並列実行では他ワーカーとの順番待ちで数秒に伸びる）が **1 件目の所要時間に丸ごと乗り**、既定の 5 秒を超えて時間切れになる。トップレベルで一度 `import './対象'` しておけば待ちはファイル読み込み時へ移り、`testTimeout` の対象から外れる。上限を延ばす対処（`{ timeout: 15_000 }`）を採っているファイルもあるが、待ちを消せるならそちらを先に検討する。
    - **当てはまる形は 2 つある。片方だけを覚えて「自分は該当しない」と判断しないこと。**
      - **テストが動的 import を書いている**（`vi.resetModules()` ＋動的 import で毎回作り直す）。手順・実測値・つまずきどころは [`src/services/akamaiClock.test.ts`](src/services/akamaiClock.test.ts) 冒頭
      - **実装が遅延読込していて、テストがそれを待つ**（`utils/testDataLoader.ts` の `import()` で読む実データ 6 本・約 1.4 MB を、`simulate*` を呼ぶテストが最初に待つ）。**テストの書き方だけを見ても気づけない。** 手当ては同じくトップレベルで一度読む（実例は [`src/hooks/useEarthquakes.wiring.test.ts`](src/hooks/useEarthquakes.wiring.test.ts) 冒頭の `await import('../utils/testData')`）
    - **症状は「1 件の時間切れ」ではなく「そのファイルのほぼ全件が落ちる」形で出る**（後続のテストが `h.current` を null で掴んで連鎖する）。実測で 117 件中 102 件が巻き添えになった。件数の多さに驚いて実装の不具合を疑う前に、落ちた 1 件目が時間切れかどうかを見ること。**並列実行のときだけ起きる**ので、そのファイルを単独で回すと通ってしまう（実測: 1 件目だけが単体実行 118ms・全体実行 907ms。同じファイルの他のテストは 3ms 前後）
  - **多数のファイルをテスト本体で直列に読まないこと。** `readFileSync` を 1 件ずつ回すと待ちが件数だけ積み上がる。**並列実行のときだけ既定の 5 秒を超え、単独で回すと通る**——ここは上の項と同じだが、原因も手当ても違う（上は 1 件目にモジュール解決が乗る話で、こちらはファイル I/O そのもの。`fs/promises` の `readFile` を `Promise.all` でまとめて投げる）。**症状の出方も違う**——テストが 1 件しか無いファイルでは上の項が言う「全件が落ちる」形にはならず、単発の時間切れとして出る。実例は 2 つ——[`scripts/noRawControlChars.test.ts`](scripts/noRawControlChars.test.ts)（`src` / `scripts` / `docs` 配下を走査。全バイトを `Set.has` で判定していた分も 256 要素の表引きへ変えた）と、[`scripts/specRequirementCount.test.ts`](scripts/specRequirementCount.test.ts)（`src` 配下だけで、走査量はあちらの半分以下）。**量が少ないほうも同じ形で落ちた**ので、件数の少なさを理由に自分を対象外と判断しないこと。**件数・バイト数・実測値は各テストのコメントが持つ**——走査対象は増え続けるので、ここへ写すと古くなる。**余裕を測るなら `npm test` 全件のなかでの所要を見る**（走査部分だけを単体で測った値は他ワーカーとの競合が乗らないぶん桁ひとつ小さく出ることがあり、根拠にならない）。
- **アプリ起動（デフォルト: DMDSS 版）**: **特にバリアントの指定がない場合は `npm run dev:dmdss` を使用する**。
  - DMDSS 版 URL: `http://localhost:5173/realtime-earthquake-viewer/dmdss/`
  - standard 版が明示的に必要な場合のみ `npm run dev` → `http://localhost:5173/realtime-earthquake-viewer/`
  - （5173 が使用中なら 5174 等にフォールバックするため、起動ログで実ポートを確認する）
  - **必ず `run_in_background: true` でバックグラウンドタスクとして起動する**（ユーザー指示）。フォアグラウンドで起動するとプロセスが応答を返さずハングするため。
  - **検証用に起動した dev サーバーは Codex のセッション中は停止せず起動したままにする**（ユーザー指示）。次の検証では新規起動せず、稼働中のサーバー（既定 5173）へ Playwright で接続して再利用する。セッションをまたぐ必要は無い。
  - **起動したサーバーは覚えておく。** 種別（dev / preview）・実ポート・**どのワークツリーで起動したか**を控える。再利用する規約が成り立つのはこれが分かっている間だけで、同じセッションで複数のワークツリーやバリアントを立てると、どれがどれか分からなくなる（実例: リリース時に dev をワークツリーの 5173・preview をメインの 4173 で同時に動かした）。段・フェーズに分かれる作業では、計画ファイルにも記録する（→ 下記「作業の記憶と切り出し」）。**会話や一時的なタスク一覧だけには依存しない**
  - 停止するときは**ポートから PID を引いて PID 指定で止める**（`netstat -ano` で PID を取り、PowerShell の `Stop-Process -Id <PID>`）。抽象的なタスク停止だけでは node の子プロセスが残り、ワークツリーの削除が `Permission denied` になる
  - **DMDSS 版の dev サーバーでは DMDATA の APIキーが自動で入る**。リポジトリ直下の `.env.local` に `DMDATA_API_KEY` があれば、設定タブへ貼り直さずに接続できる（条件・例外・`.env.local` の引き継ぎは [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §6「dev サーバーでの API キー自動投入」）。`--host` を付けた起動では意図的に投入しない（dev サーバーは認証を持たないため）。
  - **`.codex/` 配下は dev サーバーの監視対象から外してある**（`scripts/dev-watch-ignore.ts` の `isInsideClaudeDir`）。効くのは**メインリポジトリ直下で起動したとき**で、並行して動く別セッションがワークツリー（`.codex/worktrees/`）のファイルを触ってもこちらのページがリロードされない。**この除外を glob（`**/.codex/**`）で書き直さないこと** —— 今度はワークツリー内で起動したときに HMR が丸ごと死ぬ（理由はそのファイルのコメント）。
- `__APP_VERSION__` はビルド時に `vite.config.ts` の `define` が `package.json` の `version` から注入する定数のため、dev サーバーは `package.json` の `version` を変更したあと**再起動しないと新しい値を反映しない**（HMR では拾えない）。
- **本番ビルド確認（大きめの変更時は必須）**: `npm run build` でビルドが通ることを確認するだけでなく、**`npm run preview`（本番ビルドのサブパス配信）を起動し Playwright MCP でブラウザ確認まで行う**。
  - preview URL: standard は `http://localhost:4173/realtime-earthquake-viewer/`／DMDSS は `npm run build:dmdss` → `npm run preview:dmdss`。
  - **preview は PWA の Service Worker が前回のビルドをキャッシュから配信する。** リビルドしてリロードしても古いバンドルが動き続けるため、**「確認したつもり」で通ってしまう**。確認の前に必ずキャッシュを捨てること（`registration.unregister()` ＋ `caches.delete()` を実行してからリロード）。読み込まれた実体が期待どおりかは、`dist/index.html` が参照するバンドル名と、ブラウザの `document.querySelectorAll('script[src]')` を突き合わせて確かめる。
    ```js
    // Playwright の browser_evaluate で実行する
    for (const r of await navigator.serviceWorker.getRegistrations()) await r.unregister()
    for (const n of await caches.keys()) await caches.delete(n)
    ```
    > 2026-08-20: この罠を踏み、削除済みのはずの旧実装のログが preview に出ていた。バンドル名を突き合わせて初めて別物を見ていたと分かった。dev サーバー（HMR）とは別の経路で「古い実装を見てしまう」事故が起きる。
  - 地図系の変更では `map.isSourceLoaded(id)`・`queryRenderedFeatures` で **geojson ソースが実際にタイル化・描画されているか**まで確認する（ラスタ地形だけ出ていても油断しない）。
  - **理由（2026-07-28 v4.0.0→v4.0.1 の本番限定バグ）**: dev サーバー（`vite dev`）は未バンドルでモジュールを解決するため、**本番ビルド（rollup バンドル）でのみ壊れる不具合が dev では再現しない**。実例＝MapLibre GL v6 の `setWorkerUrl()` 欠落で geojson ワーカーのパスが本番だけ解決できず、地図のベクタ（県境・一次細分区域・震度点・活断層等）が全滅した（全ソース `isSourceLoaded=false`・ラスタ地形だけ描画）。dev のみで検証し preview を飛ばしたため見逃し、GitHub Pages で初めて発覚した。**worker/asset のパス解決・コード分割・minify 起因の壊れは dev では出ない**ため、大きな変更は必ず本番ビルドをブラウザで確認する。
- **コマ落ち・引っかかりを疑ったとき**: 体感や見た目で当てにいかず `window.__frameProfiler` で測る（`__frameProfiler.reset(); __frameProfiler.start()` → 操作 → `__frameProfiler.dump()`）。落ちたフレームを詰まらせた処理が名前で出る。読み方・囲ってある区間の一覧・区間を足すときの決まりは [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §11「フレーム時間の記録」。
- **ブラウザ確認（修正時は必須）**: **Playwright MCP**（`mcp__playwright__*` ツール群）で上記 URL を開き、`browser_take_screenshot`・`browser_evaluate` で表示や DOM を確認する。**`preview_start` / `preview_*` ツール（Codex Preview MCP）は使用しない。**
  - **起動確認時は必ず DMDSS 版（`npm run dev:dmdss`）を起動する**。ユーザーも実機確認を行うため、DMDSS 版が起動していることを必ず確認してからブラウザ検証を行う。
  - **DMDSS 版のブラウザ確認は配信元へのリクエストを伴う。** 確認のためにタブを増やす前に見合うかを考えること。**種類ごとに控えの効き方が違う**（控えの詳細は [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §2）。
    - **電文本体**（`data.api/v1/:id`）: 初回は数十件（実測 85 件）。**2 回目以降は控えが効いて 0 件**（IndexedDB に永続）
    - **アーカイブ本体**（`data.api/v1/archive/:id`）: 初回は 7 日ぶん（6〜7 件）。**控えは二層で、端末（IndexedDB）にも gzip のまま残る**ので、**再生を開始し直しても、ページを再読込しても 0 件**（同じ日を初めて触ったときだけ落とす。→ 同 §2「アーカイブ本体の控え」）
    - **一覧・目録**（`api.dmdata.jp` の `/v2/archive`・`/v2/telegram`・`/v2/gd/eew`）: **控えが効かず、起動のたびに数件飛ぶ**。「もっと見る」を押すたびにも増える
    - つまり**「2 回目以降は 0 件」は種類と操作によって意味が違う** —— 電文本体はリロードをまたいで 0 件、アーカイブ本体は再生の開始をまたいでだけ 0 件、一覧・目録は毎回飛ぶ
  - **契約の同時接続枠は 3 本**（`/v2/contract` の `connectionCounts` の合計）で、dev サーバー・実機・検証用ブラウザが並行すると 4 本目が 409 になる。枠の空きは `GET /v2/socket?status=open` で確かめられる（1 リクエスト）。**409 を受けるとアプリは自分の古い枠を返そうとする**（要件 E4。→ 同 §2「WebSocket 接続」）
  - **リクエスト数を測るときは `StrictMode` の二重実行を差し引く。** dev では React が effect を 2 回走らせるため、起動時の取得が 2 セット飛ぶ（本番ビルドは 1 回）。`browser_network_requests` の件数をそのまま本番の値として読まないこと。**`performance.getEntriesByType('resource')` は既定のバッファ上限（250 件）で古い分が落ちる**ので、件数を数える用途には使わない
- **修正後の確認は徹底する**。型チェック・ブラウザ動作など複数の手段で確実に修正されたことを確認する。「たぶん直っているだろう」でコミットしない。
  - コンソールエラーが0件であることを確認する（以下は良性で無視してよい）。
    - リロード時の P2P WebSocket 再接続 warning。
    - 強震モニタのクロック同期のフォールバック経路が出す `RealTimeData/...?_=...` への 403（未登録秒を叩いて 403→200 境界を捉える較正の正常動作。ネットワーク層の 403 は JS から抑制不可）。**ただし通常は出ない。** 較正の主経路は外部の時刻サービス（`akamaiClock.ts`）で、そちらが取れている間は 1 周期 1 リクエストで済む。**この 403 が 30 秒ごとに出ていたら「主経路が失敗し続けている」印**なので、良性として流さず原因を見ること。主経路の失敗理由（`[time] サーバー時刻を取得できず: ...`）は**5 分間隔に間引かれる**ため 403 と同じ頻度では出ない。403 が続いている間、直近 5 分をさかのぼれば 1 回は見つかる。
  - 自動解除や時間経過で発火する挙動（自動タブ切替・アイドル復帰など）は、`localStorage` の書き換え＋リロードや DOM 検査で確認する。
  - **確認後も開発サーバーは停止しない**（セッション中は起動したまま残す）。`Stop-Process -Name node` のような一括停止は MCP サーバーまで巻き込むため使わない。
  - 検証用スクリーンショットはリポジトリ直下に出力されるが**一時ファイル。コミット前に必ず削除する**（コミットしない）。`.playwright-mcp/` の出力も同様に Git 管理対象外（`.gitignore` 済み）。
- **テスト機能の活用**: 設定タブのテストボタンで動作確認できる。各ボタンの詳細（対象電文・音・自動タブ切替・自動解除タイミング等）と DOM 検証手法は [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §7、実データから P2PQuake 経由でシナリオを作る手順は同 §6 を参照する。

### テストボタンは実機確認の唯一の入口

**電文から読む項目・表示・読み上げを足したり変えたりしたら、テストボタンのデータ（`src/utils/testData.ts`）も同じコミットで追随させる。**

テストボタンは**本物の電文を再現するもの**で、実機で挙動を確かめられる唯一の手段。**そこに無い形は一度も画面に出ない**——「実装した」「ユニットテストが通った」で終わると、実際にどう見えるか誰も見ていない状態になる。

- 新しいフィールド・新しい値（上限を定めない予想の `over`・観測状態の新しい語など）を足したら、それを持つテストデータを用意する
- **値が続報で変わる形なら、その遷移まで再現する**（初報は上限が定まらず、続報で確定して上がる等）。言い直し・引き上げに追随する経路はそこでしか通らない
- **時間を置いて次の電文を流すボタンを足したら、その「待ち」を ref で追い、リセット（リプレイの開始・停止）とアンマウントで落とす。** 落とし先は `clearTestSimulationTimers` の 1 箇所（→ [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §7「待ちはリセットとアンマウントで落とす」）。**津波と EEW のテストはキューを通らず `handleEvent` を直接呼ぶ**ので、キューを空にしても止まらない。取り残すと消したはずの画面へ電文が 1 通だけ届き、例外もログも出ない
- 実電文の形から外れさせない（報番号・発表時刻・区域コードの規約は [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §7「実電文の形に合わせる」）

> 2026-09-06: 「程度以上」の表示・読み上げを足したときテストデータが追随しておらず、実機で確かめる手段が無いまま完了しかけた。ユーザーの「テストボタンはちゃんと修正に合わせていますか？」で発覚した。

### 読み取りの変更はリプレイで確かめる

**電文の読み取り（パーサー）を変えたときは、テストボタンではなくリプレイ（設定タブ「テスト時刻設定」）で確かめる。**

```
テストボタン:  testData.ts → 内部型 → 画面      （パーサーを通らない）
リプレイ:      実電文 → パーサー → 内部型 → 画面（実運用と同じ経路）
```

**テストボタンが正常に見えても、電文を正しく読めている証拠にはならない。** 内部型を直接組み立てるため、パーサーが実電文を 1 件も拾えなくてもテストボタンでは動いて見える。

- パーサーの確認は**実電文から起こした fixture のユニットテスト**か**リプレイ**で取る
- リプレイの窓は DMDATA アーカイブから探せる（`/v2/archive`。要 API キー。`datetime` は両端より広く取らないと目的の日が返らない）
- **標準版（P2PQuake）は EEW を再生できない**ので、EEW の読み取りは DMDSS 版で確かめる（[`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §6「テスト時刻設定」）

> 2026-09-06: 「震度5弱以上未入電」の値を推測で `!5-` と書いており、**実電文を 1 件も拾えていなかった**。テストボタンでは表示されていたため気づけず、ユーザーが「リプレイで確認したいので、いつの電文に未入電があるか教えて」と言って実電文を調べたことで初めて判明した。同じ形の誤りが同時期に 3 件あった（未入電の値・予想波高の属性・EEW の固定付加文の要素名）。

### 環境による制約

- 一部の外部ホストへ到達できない環境がある（例: 防災科研 kmoni の HTTPS）。Yahoo 強震モニタ（`weather-kyoshin.*.storage-yahoo.jp`）・DMDATA.JP API は到達可能。
- 予報円は自前計算（`usePsWaveCalc`）に統一済みのためテストボタンでも確認できるが、気象庁の実電文が発表初期に載せる「仮定震源要素」（震源未確定）の判別は、情報源によって精度が異なる（DMDATA/P2PQuakeは`condition`フィールドで判別可能、Yahoo hypoInfoには相当フィールドが無い）。この非対称性そのものの実地震での挙動は、実発報時の確認をユーザーに委ねる。

## 調査で外部 API を叩くとき

**調査は実装より多くリクエストを出す。** 実装は「必要なものを 1 回」取るが、調査は
「全期間を何度も」走査する。このリポジトリが配信元から利用量の指摘を受けた原因は
アプリ本体ではなく**調査スクリプトの再走査**だった（2026-09-13 に 2099 日分のアーカイブ走査を
1 時間半で 3 回。→ [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §11）。

### 走らせる前に

- **リクエスト数を見積もる。** 「走査する単位の数 × 1 単位あたりのリクエスト」で出る。
  桁が分からないなら、**まず一覧だけ取って単位の数を数える**（アーカイブなら日数）
- **配信元が定める上限を読む。** 明記があるのは 2 つ —— DMDATA は
  [`data-sources-spec.md`](docs/spec/data-sources-spec.md) §2「リクエスト数を抑える」、
  **P2PQuake は同 §3**（`/history` が 60req/分・`/jma` が 10req/分。どちらも IP ごと）。
  それ以外（気象庁・NIED・NII・Yahoo）は明記された上限が無いので、**無いことを
  「いくらでもよい」と読まない** —— 既存の生成スクリプトが採っている間隔・直列実行に倣う
  （`build-hypocenter-catalog` の日別ページ取得・`build-station-coords` の履歴走査）
- **前回の結果が残っていないか探す。** 同じ走査を 2 度やる前に、控え
  （`.codex/dmdata-archive-cache/` ほか）と作業ディレクトリの出力を見る。
  **集計の切り口を変えたいだけなら、抽出と集計を分けて集計側だけ回す**
- **見積もりが上限を超えるなら、範囲を狭めるか間隔を空ける。** ただし**ユーザーが範囲を
  指定した調査では、狭める前に手順 1 の方針提示の場で確認を取る**（2026-08-27 の方針。
  「全て」と指定されたものを、上限超過を理由に黙って狭めない）。自分で決めた調査計画なら
  判断してよい。**どちらの場合も、狭めたことを結論と一緒に書く**
  （「N 日分を見た」と「全期間を見た」は別の主張）

### 投げる前に「余計なものが飛ばない」ことを確かめる

> ユーザーからの指示（2026-09-15）: 「APIを叩くときは検証を行って不必要なリクエストが
> 確実に飛ばないことを検証してから飛ばしてください。それができないなら絶対にリクエストは
> 送らないでください。」**見積もり（上記）だけでは足りない。渡す値と経路を実際に確かめる。**

- **呼ぶ前に引数の型と意味を実装で読む。** 推測で渡さない。**`window.__replay.start()` に
  オフセットのミリ秒を渡して再生時刻が 1969 年になり、DMDATA の電文一覧へ 399 件・強震モニタへ
  177 件が飛んだ**（2026-09-15）。あれは ISO 文字列を取る口で、`App.tsx` を見れば
  `start: (iso: string)` と書いてあった
- **スクリプトは先に空振りで回す。** `fetch` を「URL を記録するだけ」の関数へ差し替えて実行し、
  **投げようとした URL の一覧と件数**を見る。想定どおり・件数が有界だと確かめてから本物へ繋ぐ
- **ブラウザでの検証も同じ。** 先にページ内で `window.fetch` を記録・拒否する形へ差し替え、
  操作してから「飛ぼうとした先」を読む。件数が想定どおりなら差し替えを外して本番の確認へ進む
- **確かめられないなら投げない。** 「たぶん大丈夫」で叩かない。件数を事前に見られない操作は、
  見られる形に作り替えてから行う

### ページを辿るループには必ず上限を置く

**`nextToken` が尽きるまで辿る形は、範囲指定が効かなくなった瞬間に数百リクエストへ化ける。**
上記の 399 件はこれ（配信元は 1969 年の範囲指定を無視したかのようにページを返し続け、
1 日あたり 42〜63 ページを全部辿った）。

- 上限に達したら**記録を残す**。黙って切ると取りこぼしが「静かな時間帯」に化ける
- 規約と実測値は [`data-sources-spec.md`](docs/spec/data-sources-spec.md) §2
  「ページを辿るループには上限を置く」が単一情報源
- 現在の上限は 6 箇所（アプリ側）＋生成・調査スクリプト側の 2 つ。**ページ数が揃っていない**ので
  値は上記の仕様書の表を見ること（`MAX_PAGES_PER_DAY` は 5 ページ・`ACTIVE_EEW_MAX_PAGES` は 10 ページ）
- **1 ページの件数（`limit`）は明示して渡す。** 既定に任せると少ない件数で返り、同じ範囲を読むのに
  余計なページを辿る（アーカイブの目録が実際にそうなっていた）。**`cursorToken` で 2 ページ目を
  辿るときも同じパラメータを渡し続ける**（配信元がそう定めている。落とすと途中から既定へ戻る）

### 失敗したら間隔を空ける（回数ではなく経過時間で測る）

**失敗しても同じ間隔で撃ち続ける形は、配信が止まっている間ずっと上限近くを使う。**
強震モニタのフレーム取得は 200ms 間隔・エッジ 2 つで**毎秒 10 件**を投げ続けていた。

- 連続失敗が続いたら間隔を倍々にし、上限で頭打ちにする（`stalledRetryDelayMs`）
- **異常の判定は経過時間で行う**（`STALLED_AFTER_MS`）。間隔が伸びる作りで回数を数えると、
  **伸びたぶんだけ判定も遅れる**（→ `rules/common/code-review.md`
  「指標・条件が本体からずれていないか」）。従来は 101 リクエスト・約 20 秒かかっていた
- **データが 1 件も無い範囲では打ち切る**（`REPLAY_MAX_CONSECUTIVE_GIVEUPS`）。
  1 回あたりの再試行上限は、範囲全体の総量を抑えない
- **同じ位置を線形に探さない。** 単調な条件（時刻順に増える・登録済みかどうか）は二分探索で
  足りる。強震モニタのクロック較正（フォールバック）は 1 秒ずつ下げていて **1 回の較正で最大
  121 件**投げる形だった。二分探索で 9 件になった（`findFrontier`）。**この経路が走るのは主経路が
  失敗した周期だけ**なので、「30 秒ごとに走るので毎分 240 件超」は失敗が続いた場合の値 ——
  常時そうなっていたわけではない（2026-09-16 に実測して取り下げた）

### 範囲外の指定は入口で弾く

**下流へ流すと、取得元の数だけ空振りのリクエストが出る。** リプレイは
`replayTargetProblem` が取得を 1 件も投げる前に判定する（下限・未来・日時として読めない値）。
**新しく「時刻や範囲を受け取って取得へ渡す入口」を作るときは、同じ判定を置くこと。**

### 走らせるとき

- **控えを通す。** 取得した不変のデータはローカルへ残し、2 回目以降は読むだけにする
  （アーカイブの控えの仕組みと「素の `fetch` を足さない」規約は
  [`data-sources-spec.md`](docs/spec/data-sources-spec.md) §2「リクエスト数を抑える」）
- **並列にする前に、レート制御が本当に効くかを確かめる。** 待つ前に枠を予約しない実装は
  並列呼び出しを直列化できず、**並列数の分だけ上限を超える**（実際にこの不備を作り込んだ）
- **進行と件数が見える形にする。** 何件取ったか・何件を控えで済ませたかを出す。
  出ないと、止めるべきかの判断ができない
- **検証で開くブラウザも同じ配信元へリクエストを出す。** 見積もりに含めること
  （枠の本数と実測値は上記「検証」）

### 結論を出すとき

- **走査できなかった範囲を結論と一緒に出す。** 「0 件だった」と「取得できなかった」を
  区別できない形で残さない
- **印は手で引き継がない。** 調査スクリプトが何段にも連なるときは
  [`scripts/lib/incompleteness.mjs`](scripts/lib/incompleteness.mjs) を通す ——
  中間成果物を `readArtifact` で読めば、そのファイルの印は読んだ時点で引き継がれ、
  `writeArtifact` / `markResult` で自動的に出ていく。**個別の `if` で引き継ぐ形は、
  ホップが 1 つ増えるたびに書き忘れる**（実際に 3 巡続けて 1 段ずつ下流で同じ穴が開いた）。
  迂回（素の `fs` で読み書きする）は `scripts/incompletenessPropagation.test.ts` が落とす。
  規約は [`docs/spec/telegram-coverage-audit.md`](docs/spec/telegram-coverage-audit.md)
  §2「取りこぼしは印として下流へ運ぶ」
- **成果物がファイルの山（JSON でないもの）なら、札を別に置く。** 印を載せる場所が無いと、
  収集が途中で失敗しても下流には「0 件」としか見えない
- 網羅性を主張するなら下記「調査レビュー」へ掛ける

## 調査レビュー（網羅性を主張する調査を根拠に範囲を決めたとき）

**点検・棚卸し・突き合わせの結果を根拠に作業範囲を決めたら、その調査自体をレビューに掛ける。**
実施タイミングは**変更時の基本フローの手順 1 の中**——調査結果を方針としてユーザーへ提示する前
（実装より手前）。**実装・検証の後に回さないこと。** そこで指摘が出ても、間違った範囲のまま
仕上がったものを確認するだけになる。エージェントは
`adversarial-reviewer` 1 本でよく、渡すのは**調査の手順・入力・出力**（スクリプト、範囲の定義、
集計結果）であって変更差分ではない。

**変更のレビューはこれを代替しない。** 敵対的レビューが見るのは「直したものが正しいか」で、
**「何を直すべきと判断したか」は誰も見ていない**。調査が間違っていれば、そこから先の
レビューが何巡回っても間違った範囲を丁寧に仕上げるだけになる。

### 見る観点

- **網羅性の根拠は何か。** 「資料を読んで気づいた」で終わっていないか。**機械的に数え上げられる
  ものを目視で済ませていないか**（数え上げるスクリプトは数分で書ける。着手時に書けば初日に見える）
- **入力そのものが正しいか。** 範囲・境界・分類の定義が手書きなら、それ自体が誤りうる。
  資料の中に機械的な手掛かり（見出し・ページの柱・目次）があるなら、そちらから導く
- **測定が対象を正しく捉えているか。** 代理指標で測っていないか。とくに
  **測定対象を変更すると測定結果が変わる作りになっていないか**
- **「見つからなかった」と「見ていない」を区別できるか。** 区別できないなら、それは
  網羅性の主張になっていない。**「探したつもりで、対象の入れ物を外していた」形もここに含む**
  ——「N 件走査して 0 件だった」は走査先が対象を含む場合にしか意味を持たないので、
  取得元が分類・名前空間・種別で分かれているなら、**対象がどれに属するかを先に確かめる**
  （実例は下記 2026-09-11）
- 出た数字を独立に再現できるか（手順とスクリプトが残っているか）

> 2026-09-07: 電文の総点検（#13）で 2 回続けて踏んだ。①章の範囲を手書きの行番号で渡していて、
> Ⅱ.33 の範囲が Ⅱ.34 へはみ出し、**アプリが扱っていない種別の要素を「未読」と誤認**した
> （`Naming`【命名地震】。Ⅱ.34「地震情報（地震の活動状況等に関する情報）」の要素で、アプリは
> この種別を受信していない）。②ある種別の要素を読むコードを足した結果、**別の種別の同名要素が「参照済み」に
> 見えるようになった**（突き合わせが文字列の有無しか見ていないため）。どちらも変更のレビューでは
> 原理的に出ない —— レビュアーには差分しか渡していないため。ユーザーの指摘で手順に加えた。
>
> 2026-09-11: **走査先そのものが対象を含んでいなかった。** `/v2/archive` を 130 日ぶん走査して
> 「訓練・試験の EEW は 1 通も無い」と結論し、その旨を `dmdataReplayLive.ts` のコメントへ書いて
> リリースまでした。**`/v2/archive` は分類ごとに分かれていて、見ていたのは `telegram.earthquake`
> だけ** —— EEW は `eew.forecast`・`eew.warning` にある。分類を直して数え直すと、2026-05-01〜09-12 に
> 生成されていた `eew.forecast` の 132 日で `head.test` の立った電文が 6,346 件（実測。1 日あたり
> 約 48 件で、毎正時の配信テスト VXSE42 が XML と JSON の 2 版で載る）。`eew.warning` は 10 日分で
> 0 件だったので、**標本が無いのは訓練の EEW だけ**だと分かった。日数も件数も本物だったぶん、
> 数字の多さがかえって結論を確からしく見せていた。**「N 件見た」は、その N 件が対象を含む場合に
> しか意味を持たない。**

## 敵対的レビュー

**全変更（コード・ドキュメント問わず）でエージェントによる敵対的レビューを実施する**。目的:
- 実装から裏取りしたバグ・エッジケース・機能間競合を検出する
- コミット前に「レビュー観点の抜け」を客観的に潰す
- 既存コメント・ドキュメントの誤解を疑う（実コードを信じる）

### 実施タイミング

**検証（型チェック＋ブラウザ確認）が完了した後、ドキュメント更新の前**。指摘があれば実装に戻り、修正後に敵対的レビューをもう 1 度回す。
**ただし無条件に繰り返すわけではない**（下記「巡回が収束しないとき」）。

### 巡回が収束しないとき

**「指摘が出たら直してもう 1 巡」を無条件に続けると終わらない。** 修正が次の指摘を産む連鎖が起き、重要度も下がらないまま巡数だけが伸びる（この規約の運用中に、1 変更で 8 巡・5 巡に達した例がある）。

**収束しないのは「直し方が下手」なのではない。直している対象の設計が、指摘を産み続けている。**だから次の一手は**設計を変えること**であって、巡回を止めることではない。

#### 収束しているかの判定

**「1 巡」= レビューを 1 回実行してから、指摘を反映して次のレビューを実行する直前まで。**下記「エージェント構成」にある**並列のエージェントは何本でも合わせて 1 巡**（領域別に 3 本走らせても 1 巡）。メタレビューも同じ巡に含める。エージェント 1 本ごとに数えない。

**比較するのは各巡の「最高重要度」だけ。** 件数の増減や MEDIUM / LOW の増減は判定に含めない（1 巡目 HIGH 2 件 → 2 巡目 HIGH 1 件＋MEDIUM 3 件は「下がっていない」）。**曖昧な基準にすると「もう十分」と言いたい方向へ判断が寄る。**

| 状況 | 判断 |
|---|---|
| 最高重要度が巡ごとに下がっている（HIGH → MEDIUM → LOW） | 収束している。そのまま続けてよい |
| **3 巡を超えても最高重要度が下がらない**（4 巡目に HIGH が出る等） | **収束していない。** 下記の手を打つ |
| **同じ箇所を 3 巡以上書き換えている** | その箇所の設計が問題。**巡数が 3 に達していなくても手 1（作り直し）へ移る** |
| 指摘が「前の巡で入れた修正の文面・doc」だけになった | 実装の指摘が 1 件でも混じっているならまだ続ける。文面だけなら、まとめて直して締める |

複数の行に該当する場合は重ねて適用してよい（推奨する行動が食い違うことはない）。

**判断は Codex 側の義務。** ユーザーが「これで十分」と言うのを待つのは、収束していないことに気づく責任をユーザーへ押し付けている（ユーザーが自分から「これで十分」と言った場合は別。それは確定の指示であって、Codex 側が収束を判定した結果ではない）。

#### 収束していないと判定したら、手を打つ

**見るのは 2 つ**——**設計を変える範囲が今回の変更（diff）に含まれるファイルへ収まるか**と、**その指摘を今直さねばならないか**。

| 範囲 | 今直すか | 打つ手 |
|---|---|---|
| 今回の変更に**収まる** | 重要度を問わない | **1. 設計を変える**——継ぎ足しをやめて、その箇所を作り直す。指摘を 1 件ずつ潰す形から、**同じ問題が構造的に起きない形**へ変える。**これが第一手** |
| **収まらない** | **直さねばならない**（CRITICAL、およびユーザーが「直す」と判断した HIGH） | **2. 範囲の拡大をユーザーへ諮る**——今の変更を広げるか、前提のほうを先に作り直すかを決める。**「今は直さない」ではなく「どう直すか」の相談** |
| **収まらない** | 今は直さなくてよい（HIGH 以下） | **3. 別タスクへ切り出す**——**残りが今の変更と競合しない場合だけ**（競合するなら 2 へ）。下記「切り出すときに残すもの」 |

**「巡回を打ち切る」は手ではない。** 巡回は 1〜3 のどれかを実行した結果として終わるものであって、それ自体を目的にすると**同じ設計のまま先へ進む**ことになる。「もう十分」と言いたくなったときほど、1 を検討したか自問すること。

**迷ったら 1 を試みる。** 広がると分かった時点で 2 か 3 へ移してよい。**ただし 1 を無限に繰り返さない**——**作り直しを 2 度試しても同じ形の指摘が出るなら、範囲が収まっていない**。2 か 3 へ移ること。

**この「2 度」は上の「3 巡」とは別に数える。** あちらはレビュー全体の巡数、こちらは収束していないと判定してから手 1 を試みた回数。

#### CRITICAL は 3 へ回せない

**CRITICAL は巡数によらず解消する。** 承認基準（`rules/common/code-review.md`）が「CRITICAL があればブロック」と定めており、この節はそれを緩めない。**切り出しは「今は直さない」という判断**なので、ブロック基準と両立しない。

**1 で収まらない CRITICAL は 2 へ回す**（範囲の拡大をユーザーへ諮る）。**3 ではない。**CRITICAL が 4 巡目・5 巡目に出たなら、それは**1 か 2 が要るという合図**。巡回を終える口実にはしない。

**HIGH を 3 へ回す場合は、残す判断をユーザーに確認する**（勝手に格下げしない）。ユーザーが「直す」と判断したなら、それは 3 ではなく 2 の側へ移る。

#### 切り出すときに残すもの

3 を選んだら、**何を・なぜ今直さないかをユーザーへ明示する**。黙って終わらせない。

**切り出す前に、残りが今の変更と競合しないことを確かめる**（判定条件は下記「作業の記憶と切り出し」の「作業中に別の課題を見つけたとき」）。**競合するなら、それは 3 ではない**——同じファイルを触る残りをチップにすると、別セッションが並行して同じ場所を直すことになる。その場合は 2（範囲の拡大をユーザーへ諮る）へ移る。

- **チップとして記録する**（運用は下記「作業の記憶と切り出し」）
- **消えては困るならリポジトリ内の資材にも書く。** チップもタスクリストもセッション用で、永続する保証がない
- **リポジトリの外にある課題管理の番号だけを残さない。** リポジトリを読んでいる人には辿れない

**「設計を変えるために切り出した」と「指摘が無くなった」は別の状態。** 混ぜると次に触る人が誤解する。

### エージェント構成(変更規模で可変)

| 変更規模 | 例 | エージェント構成 |
|---|---|---|
| 小規模 | 1〜2 ファイルの typo/スタイル修正・軽微なパラメータ調整 | `adversarial-reviewer` 1 エージェント |
| 中規模 | 機能追加・複数ファイル・条件分岐の変更 | `adversarial-reviewer` + `verifier` の並列 2 エージェント |
| 大規模 | アーキテクチャ変更・複数機能横断 | 領域別に `adversarial-reviewer` を複数並列 ＋ **機能間競合レビュー**（EEW×津波・地図×EEW 等）＋ **メタレビュー**（`code-reviewer` で CRITICAL/HIGH の実装からの再検証） |

`adversarial-reviewer` / `code-reviewer` の定義は [`.codex/agents/`](.codex/agents/) を参照（プロジェクトローカル・共通利用のため Git 管理）。

### レビュー観点（全エージェント共通）

- **実装から裏取り**: 既存コメント・仕様書・AGENTS.md を鵜呑みにせず、実コードを読んで挙動を確認する
- **修正はしない**: レビュアーは変更をコミットしない。指摘のみ返す
- **前の巡の修正を、元のコードと同じ厳しさで見る**: 指摘を潰すために足した抑制・ガード・早期 return が、**指摘の範囲より広い範囲を止めていないか**を確かめる。過剰補正は元の問題より重くなりうる（実例: リクエストを抑えるために足した打ち切りが、次の予約を置かずに `return` する形になっていて、**配信が回復してもそのセッションではリプレイの強震モニタを二度と取りに行かない**に化けた。止めたかったのは「無駄なリクエスト」なのに、止まった範囲は「機能そのもの」だった。2026-09-15）
- **矛盾しているのは実装か文面かを見分ける**: 記録・通知の文面が事実以上を主張しているだけなら、**直すのは文面**。ロジックを止めて黙らせると、その検出機能ごと失う
- **分類**: CRITICAL / HIGH / MEDIUM / LOW で分類する
- **統一フォーマット**:
  ```
  [レベル] [ファイル:行] 見出し
  - 事実（実装からの引用）
  - 問題
  - 影響（どの機能・どのバリアント・どの経路）
  - 修正方針の候補（実施はしない）
  ```

### メタレビュー（大規模変更時）

CRITICAL / HIGH の指摘は **`code-reviewer` エージェント**で実装から再検証する。目的:
- 過大評価（CRITICAL が実は HIGH 相当）を検出
- 修正方針の副作用（別バグを生む可能性）を検出
- 事実誤認・根拠の誤りを検出

判定は「確認済み / 過大評価 / 過小評価 / 誤検出 / 修正方針要再検討」のいずれか。

### 参考

過去のレビュー実施例は `.codex/review-reports/` にアーカイブされている（`.gitignore` 済み・共有はコミット外）。

## ドキュメント更新

コードの変更に応じて、関連するドキュメントを**同一コミットで**更新する。対象は以下:

- **`docs/spec/` 配下の該当機能仕様書**: 実装が仕様と 1 対 1 で対応する箇所（パラメータ・判定条件・データフロー等）を変更したら、必ず仕様書側も更新する。仕様書は冒頭で自ら「実コードを正とする」と宣言しているため、放置すると即座に嘘になる。
- **`README.md`**: 機能・技術スタック・バリアント差分・データソース・注意事項・ライセンス関連の変更のみ更新（README は概要と導線のみを残す方針。詳細はすべて `docs/spec/` 配下に集約する）
- **`AGENTS.md`**: 変更時のワークフロー・検証手順・コード整合性チェックポイント・リリース手続きなどに影響する変更のみ更新
- **コード内コメント**: 変更した処理のコメントを実装と一致させる（`common/comment-integrity.md` 参照）

スタイルの微調整・レイアウトの軽微な調整など、上記のいずれの記載にも影響しない変更ではドキュメント更新不要。

**仕様書の改訂履歴は日付の昇順で並べる（新しい項は必ず末尾へ足す）。** 主題の近い項の隣へ挿し込みたくなるが、あの節は時系列だけで並べる。崩れても型チェックでは捕まらないため、[`scripts/specRevisionHistory.test.ts`](scripts/specRevisionHistory.test.ts) が `npm test` で検査する（実際に 3 ファイルで独立に崩れていた。2026-08-31 に並べ直した）。日付が同じ項どうしの順序は見ない。

### 特殊ケース: 強震モニタ検知エンジンを変更したとき

`src/utils/kyoshinDetector.ts`（強震モニタ揺れ検知エンジン）のパラメータ・判定ロジックを変更した場合は、上記の一般則に加えて**以下 2 つのドキュメントと回帰テスト（後述）を同一コミットで**更新する:

- [`docs/spec/kyoshin-detection-spec.md`](docs/spec/kyoshin-detection-spec.md)（仕様書）: **現在の実装が何をどう処理するか**を書く文書。パラメータ一覧表（§5）と判定ロジックの記述（§4）は実装と 1 対 1 で対応させる。
- [`docs/spec/kyoshin-detection-v3-design.md`](docs/spec/kyoshin-detection-v3-design.md)（設計書）: **なぜそうしたか**の経緯・調査・検証履歴を節番号付きで追記する（§13 以降が改訂履歴。新しい変更は末尾に §N を足す）。仕様書側からは「設計書§N」の形で参照する（仕様書自身の節番号と紛らわしいので「§N」単独で書かない）。

パラメータ表と実装の一致は機械的に確認できる（`PARAMS` のキー・値を §5 の表と突き合わせる）。
2026-08-09 の高震度 fast path 追加時にこの照合を行い、37件全件一致を確認した。

> 2026-08-09: 検知ロジック修正時にこれらのドキュメントを更新すべき旨がどこにも明文化されておらず、更新するかどうかが作業者の判断に委ねられていた。次に触る人が同じ判断に至る保証が無いため、ルールとして明文化した。
> 2026-08-10: 「関連ドキュメントの更新」の一般則に格上げし、検知仕様書はその特殊ケースとして位置づけを変更した。

**回帰テストの追加**（ドキュメントと同じく同一コミットで）: 変更が意図した挙動を固定するテストを [`src/utils/kyoshinDetector.test.ts`](src/utils/kyoshinDetector.test.ts) に追加する。追加するのは**正・対照・安全弁の 3 種を対にしたもの**（分類の定義は上記「検証」節）。設計書§29 での例:

| 種類 | 例 |
|---|---|
| **正** | 震度4の単点が成分1点でも confirmed になる |
| **対照** | 震度3の単点では confirmed にならない |
| **安全弁** | 単点でも `CONFIRM_FRAMES` は免除しない |

閾値の大小関係に依存する分岐（`if` の順序で意味が決まるもの）を足したら、**その不変条件もテストで固定する**。値を 1 つ動かすだけで分岐の意味が静かに反転しうるが、型チェックでは捕まらない。

既存テストが落ちたときは、**まず「意図的に覆したもの」と「壊したもの」を分ける**。前者は反転させて更新し（何を覆したかをテスト名に書く）、後者は実装を直す。設計書§29 では 3 件落ち、うち 2 件は §18 が置いた防御テスト（離れた点が同時に高震度）で、これを壊さない形に設計を変えた。

**実データでの回帰確認も併せて行う。** ユニットテストは合成フレームなので、実地震の立ち上がり方の変化は捕まえられない。`npm run bench-kyoshin` が 793 窓を再生して分類ごとに集計し、**ベースライン（`scripts/kyoshin-bench/baseline.json`）との差分だけを出す**（比べるのは段階・初 likely・初 confirmed・**確定の継続フレーム数**・無関係な検知の件数）（下記「補助コマンド」参照）。窓の定義は `scripts/kyoshin-bench/windows.json` が単一情報源で、分類の意味と「その地震を検知したか」の判定規則は `scripts/bench-kyoshin.ts` の冒頭にある。

変更を入れる前に一度素の状態で回し、差分が 0 であることを確かめてから実装に入ること（キャッシュや窓カタログの取り違えをここで弾ける）。**差分が出たら 1 件ずつ中身を見る。** 検知の位置が震源から離れていれば、それはその地震の検知ではなく無関係なノイズで、「取りこぼしが増えた」ように見えて実は「誤検知が減った」ことがある（実例: トカラ列島近海の窓で東京の観測点が鳴っていた）。意図した変更なら `-- --update-baseline` で受け入れる。

> 2026-08-21: `HIGH_CONFIRM_POINTS` を下げるだけでは能登本震の確定が 1 秒も早くならないことが実データで判明した（設計書§29）。パラメータ単体の妥当性を論じても、**他のゲートに隠れて効かない**ことがある。ユニットテストと実データ検証を両方要求するのはこのため。
>
> 同じ作業で、敵対的レビューが**実装の判定量そのものの誤り**を複数回検出した（経緯は設計書§29 以降）。いずれもユニットテストを書いて「修正前に落ちること」を確認してから直している。**穴を再現できないテストは回帰を止められない**ので、この順序を守ること。

## ドキュメント客観レビュー

**ドキュメントを更新した場合、および利用者向け・外部消費者向けの文字列（UI 文言、データファイルの `description` 等の表示・利用されるメタデータフィールド）を新規作成・更新した場合**、対象箇所にエージェントで客観レビューを実施する。
目的: 技術的すぎないか・過剰でないか・初見の読み手に伝わるか・制作過程が漏れていないか・実装と乖離していないかを検証する。

> 2026-08-27: `public/data/historical-archives/*.json` の `description`（型定義上「概要（震源要素・被害規模・出典等）」と定めたメタデータフィールド）に、作業フェーズ名（「Phase 2として」）やスコープ縮小の経緯（「各1報の代表値」「概算」「全国70以上の予報区までは未収録」）をそのまま書いてしまい、初めて見る利用者には意味不明な文になっていた。ユーザーの指摘で発覚。**この種の漏れは `docs/spec`・README・AGENTS.md のような「ドキュメント」に限らない**——データファイルの説明フィールドや UI 文言も同じ穴に落ちるため、レビューの発動条件をそちらまで広げた。

### 実施タイミング

**ドキュメント更新・該当文字列の作成/更新の直後、コミット前確認の前**。指摘があれば該当箇所に戻る。

### エージェント構成

- 更新規模が小さい場合（1 ファイル・数行の追記）: `documentation-reviewer` 1 エージェント
- 更新規模が大きい場合（複数仕様書・README 大幅改訂・AGENTS.md 追記）: `documentation-reviewer` + `code-explorer` の並列 2 エージェント（後者は「初見の開発者視点」で追加観点を担当）

`documentation-reviewer` の定義は [`.codex/agents/`](.codex/agents/) を参照（プロジェクトローカル・共通利用のため Git 管理）。

### レビュー観点

- **読者が誰か明示されているか**: 一般利用者向け / 開発者向け / 特定機能を触る人向け 等
- **前提知識のない読み手が理解できるか**: 過度な略語・専門用語の初出説明はあるか
- **実装との乖離がないか**: パラメータ値・関数名・行番号・ファイル名の正確性
- **冗長でないか**: 同じ内容が複数箇所に重複していないか（重複は単一情報源へ集約する候補）
- **技術的すぎないか**: 「実装の内部詳細」と「読者が知るべき挙動」の粒度が分離されているか
- **過剰でないか / 情報量が適切か**: 実装の複雑度・機能の重要度に対して分量が釣り合っているか。読者が知る必要のない詳細（内部変数の一時的な状態遷移・改訂履歴の細部・過去の失敗の詳述など）を延々書いていないか。「あって困らない」を理由に膨らませていないか
- **制作過程が漏れていないか**: 「ご要望どおり」「前版と違い」「ご指定により X は使っていません」など、読者の行動ではなく**その文書がどう作られたか**を語る記述が残っていないか。制約は成り立ちとして説明せず、手順・既定値・用語選びそのものに反映する。読者に守らせる禁止指示（「X は使うな」）と、判断を再現するために置いた設計根拠（`理由:` `根拠:` の類）はこれに当たらない
- **リポジトリの外にしかない参照を書いていないか**: ハーネス側の課題ボードの番号（「→ 課題 #183」）や、会話でしか通じない呼び名は、**リポジトリを読んでいる人には辿れない**。残したい判断は番号ではなく本文へ書く
- **数えていないことを数えたように書いていないか**: 「実配信では 1 件も観測していない」は走査した範囲を示せるときだけ書ける。確かめていなければ「知られていない」「数えたことはない」と書く
- **仕様書相互の整合性**: 別の仕様書と食い違う記述はないか

### README 特有の観点

README は「一般利用者・フォーカー・貢献者への導線」であることを念頭に:
- 実装内部の詳細（プロジェクト構成ツリー・パラメータ値等）が入り込んでいないか
- 詳細は `docs/spec/` 配下の仕様書へリンク委譲されているか
- 開発者専用の情報（デプロイ手順・EEW 二次配信の規約制限等）が混入していないか

### データファイル・UI文言特有の観点

`description` 等のメタデータフィールドやUI表示文言は「その機能を初めて触る利用者」向け（ドキュメントより読者の予備知識が少ないと想定する）:
- 会話・作業経緯に由来する語（フェーズ名・スコープ縮小の弁明・「ご要望どおり」等）が残っていないか
- フィールドの用途（型定義のコメント等が定めた「何を書く場所か」）に沿っているか
- 実装上の妥協・簡略化・データの網羅範囲は、利用者向けフィールドではなく `docs/spec/` 側の開発者向け記述に置く（利用者向けフィールドに書いていいのは「何の・いつの出来事か」だけ）

### 出力形式

```
[レベル] [ファイル:節/行] 見出し
- 記述の該当箇所
- 何が伝わりにくいか / 何が実装と食い違うか
- 修正方針の候補
```

レベルは CRITICAL（誤情報・法的リスク）/ HIGH（読み手が誤解する）/ MEDIUM（冗長・重複・制作過程の漏れ）/ LOW（表現改善余地）。

## プランモード

- プランモードに入ったとき、前回のプランが**完了済み**（実装・コミット済み）の場合は、既存プランファイルを修正せず**新規プランファイルを作成**する。
- 前回プランが未完了（作業途中）の場合のみ、既存プランファイルを引き続き更新してよい。

## 作業の記憶と切り出し

コンテキストは失われる。**会話へ書いただけの計画は、失われた時点で存在しなくなる。**
この節は「思い出せる形で残す」ための決まり。

> ここでいう**チップ**とは、独立した課題を別の作業として記録すること。依存関係がある課題を別作業に分けない。

### 進める作業はタスク機能で管理する

**複数の段・フェーズに分かれる作業は、着手前に `docs/spec/` または対象作業の計画ファイルへ記録する。**

- **依存関係を宣言する**。計画ファイルに「A が終わるまで B は着手しない」と記録し、着手順を取り違えない
- 状態は `pending` → `in_progress` → `completed`。**着手時に `in_progress`、完了時に `completed`**。部分的にしか終わっていないものを `completed` にしない（テストが落ちている・実装が途中・必要なファイルが見つからない、のいずれでも完了ではない）
- **単一で些末な作業には使わない**。目安は段・フェーズに分かれるか、3 ステップ以上か
- **ここで定めているのは道具ではなく記録の要件**——依存関係を宣言し、着手と完了を区別し、消えては困るものを資材へ残すこと。
- **消えては困る計画は資材にも書く。** タスクリストはセッション用で、永続する保証がない。手順・判断基準は `AGENTS.md` か `docs/spec/` 配下へ置く（2026-08-29 の方針。メモリは端末ごとに閉じているため同じ理由で不可）

> 2026-09-03: XML パーサーの整合作業で、段 0〜3 の計画を会話へ書いただけでタスクに登録しなかった。
> セッションの再起動でコンテキストが失われ、**自分が立てた計画を自分で思い出せなくなった**。
> ユーザーが画面のスクリーンショットを見せるまで、別の会話の話だと誤認していた。

### 作業中に別の課題を見つけたとき

**まず現在の変更と競合するかを判定する。** 競合するなら同じセッションで直すことを提案し、独立なら、別の作業として計画ファイルへ切り出す。

競合と見なす条件（どちらかを満たせば競合）:

- **同じファイルを触る**（同じ関数である必要はない。対になる関数や近接する処理も含む）
- **前段の結果でその課題の方針が変わる**（直列の依存がある）

| 実例（2026-09-03） | 判定 | 扱い |
|---|---|---|
| 取消電文に `eventId` が無い（`parseEarthquake` / `parseEarthquakeFromXml`） | **同じファイルの対になる関数** → 競合 | 同じセッションで直した |
| 奈良県の区域名衝突にコメントが無い（`useQuakeLayerData.ts`） | 別ファイル・独立 | チップ |
| 段 0（配信形態の確認）・段 2（経路統一）・段 3（情報の追加） | 同じファイルを大きく触る＋直列依存 → **競合** | **1 本のチップにまとめた** |

**チップ同士が競合する場合は 1 本にまとめる。** 1 クリックで別セッションが始まるため、
依存関係のあるものを並べると同時に押せてしまう。プロンプトに「前提」と書いても、
**それを守る責任をユーザーへ押し付けるだけ**で、依存そのものは表現できない。

> 2026-09-03: 段 0・2・3 を 3 本のチップに分けて出し、ユーザーから「それぞれ別で対応して問題ないのか」と
> 指摘された。段 0 の結果で段 2 の方針が変わり、段 2 と段 3 は同じファイルを触るため、並行させれば衝突する。
> 1 本に統合した。**依存を表現したいなら、計画ファイルへ明記する**——独立していない課題を並列にしない。

### 会話の内容を思い出せないときはトランスクリプトを読む

コンテキストが失われて「前に何を決めたか」が分からなくなったら、**推測で進めず記録を読む**。

```
C:/Users/<user>/.codex/projects/<プロジェクトのパスをハイフンで繋いだ名前>/<sessionId>.jsonl
```

- 現在のセッションIDを前提にせず、対象プロジェクトのディレクトリにある `.jsonl` を更新時刻の新しい順に見て候補を絞る。**ただし「一番新しい」は「一番最近動いた」であって「自分」ではない。** 同じプロジェクトで別セッションが並行していれば、作業ディレクトリ・直近に触ったブランチ名・時刻の連なりで中身を照合する。決め手が無ければ要点をユーザーへ提示して確認を取る。`r`n- JSONL 形式（1 行 1 メッセージ）なので grep できる。再起動前の分が `.jsonl.pre-import` に残ることもある
- **`list_events` と `search_session_transcripts` は自分のセッションを対象にできない**（2026-09-03 に実測）。
  **自分の記録はファイルを直接読むしかない**。ここはハーネス側の仕様なので、動かなくなったら
  まず挙動が変わっていないかを疑うこと

> 2026-09-03: セッション再起動でコンテキストを失ったとき「復元できない」と決めつけて調査をやり直した。
> トランスクリプトは最初からファイルとして存在し、`未処理の情報を足す` で grep すれば段の表が出てきた。
> ユーザーに「自分が出力した過去の文は読めないのですか」と問われて初めて試した。
> **「無い」と判断する前に、読む手段があるかを確かめること。**

## コミット

- **Conventional Commits**（`feat` / `fix` / `refactor` / `docs` / `chore` / `perf` / `ci`）。説明は日本語。
- コミットメッセージ末尾に必ず付与する。**モデル名はその時作業している Codex モデルに合わせる**（ハーネスが指定するモデル名を使用する。特定モデルに固定しない）:
  ```
  Co-Authored-By: Codex <モデル名> <noreply@anthropic.com>
  ```
  例（Sonnet 5 で作業時）: `Co-Authored-By: Codex Sonnet 5 <noreply@anthropic.com>`
- 検証用スクリーンショット（`*.png`）はコミットに含めない。
- Windows 環境のため `LF will be replaced by CRLF` の警告が出るが正常（無視してよい）。
- ワークツリー内のコミットでは `package.json` の `version` を変更しない（下記「リリース」参照）。バージョン確定はリリース時にまとめて行う。
- **コミット前に必ずユーザーに確認を取る。省略しない**。同一セッション内で別の修正のコミット許可をもらっていても、その修正のコミット許可を個別にもらっていない場合は確認する。
- このリポジトリは `main` に直接コミットしている。

## リリース

main を書き換える唯一の手続き。**具体的な手順は [`/release` スキル](.agents/skills/release/SKILL.md) に集約している。**

- `/release` と入力するか「リリースして」と指示してスキルを起動する。**ワークツリー内にいる場合は メインのチェックアウトへ移動してから起動する**（ワークツリー内からは main を操作できない）。
- 出す候補が自分の 1 本だけなら、実装したセッションでそのまま続けてよい。リリース待ちが複数溜まっているときは、リリース専用のセッションを立てた方が実装の文脈と判断を混同しにくい。
- スキルは起動時に `ready/*` を探し、あればリリース手順へ進む。無ければ**ブランチ監視モード**に入り、`ready/*` が現れた時点でユーザーに確認する。
- 監視中に検知しても、**ユーザーの承認なしに main を触ることはない**。

### 原則

- **main を書き換える操作（マージ・バージョン更新・プッシュ・マージ済みブランチの削除）は、リリース手続きの中でのみ行う。** 実装フローの途中では行わない（背景は冒頭の 2026-08-15 の注記を参照）。
  - ワークツリー内から main は操作できない（理由はスキル側の「着手前チェック」に記載）。実装フローの途中で試みても正しく動かない。
  - **唯一の例外: 長期震源カタログの週次更新**（[`update-hypocenter-catalog.yml`](.github/workflows/update-hypocenter-catalog.yml)）。この原則はバージョン番号の競合とマージ順の混乱を防ぐためのもので、**データのみ・版上げなし・マージなし**のコミットはどちらの危険も持たない。リリース手続きは着手前チェック #4 で behind を検出し `git merge --ff-only` で追随する（bot 由来はコミットメッセージ `chore: 長期震源カタログを` で見分けられる）。**新しい自動 push をここへ足すときは、同じ 2 つの危険を持たないことを確かめてから追記すること。**
- **リリース手続きを同時に 2 つ走らせない。** 並行すると、両方が同じバージョン番号・同じタグを採番して push が衝突する。着手前に `.codex/release.lock` の有無で確認するが、**ファイルの確認と作成は不可分ではない**ため完全な排他ではない。技術的な最後の砦は push 直前の `git fetch` 再確認と non-fast-forward での拒否（詳細はスキル側の「リリースロック」）。
- **1 回のリリースで複数ブランチをまとめて出してよい。** バージョンはリリース 1 回につき 1 回だけ上げる（機能 1 本ごとではない）。
- **`package.json` の `version` を書き換えてよいのはリリース手続きの中だけ。** ワークツリー内やマージ前のブランチでは変更しない。
  - 理由: 複数のワークツリーを並行して進めていると、各セッションがワークツリー作成時点の古いバージョンを見て同じ番号を選んでしまい、正しく積み上がらない（例: 1.0.0 から 2 セッションが同時にパッチ修正すると、両方とも 1.0.1 を選んで最終的に 1.0.1 のまま残る）。リリースを 1 セッションに限れば、常に最新の値を見て +1 できる。

## 補助コマンド

| コマンド | 用途 |
|----------|------|
| `npm run dev` | 開発サーバー |
| `npm run build` | 地名ラベル用グリフの検証 + 型チェック + 本番ビルド |
| `npm run preview` | 本番ビルドのプレビュー（サブパス配信） |
| `npm test` | ユニットテスト（Vitest）を 1 回実行（CI との関係は上記「検証」節） |
| `node scripts/build-station-coords.mjs` | 観測点座標テーブル（`public/data/station-coords.json`）の再生成。**実行後は `npm test` を通すこと**（このファイルのキー順が読み上げの地域順の情報源のため、上流データの並びが変わると読み上げ順だけが静かに崩れる。`stationCoords.test.ts` がその前提を検証する）。**現行の観測点だけでなく、取得元のリビジョン履歴を遡って「現行の一覧に無い観測点」も集める**（`unlisted`）—— 観測点の統廃合は毎年続き、過去の電文を再生すると当時あって今は無い観測点が座標を引けず地図から消えるため。**区域の代表点（`areas`）には混ぜない**（代表点は観測点座標の平均なので、混ぜると震度速報の描画・カメラの寄り先・読み上げの距離選抜がまとめてずれる）。**中身を読めたリビジョンの数**（歯止めの本体。件数は「履歴を辿れたか」の代理値でしかない）・**固定しているリビジョンが上流の一覧に含まれること**・件数の幅・現行との重複・既知の 2 点の有無・新しい区域名を持ち込まないことを検査し、合わなければ exit 1 で止まる。**取得元と履歴の走査は読み仮名側（`build-station-readings`）と共有する**（`scripts/lib/stationSource.mjs`）—— 別々に持つと座標と読みが違う版から作られる。詳細は [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §6「現行の一覧に無い観測点」 |
| `npm run build-station-readings` | 観測点名の読み（`public/data/tts-station-readings.json`）の再生成。**要 VOICEVOX**（既定 `http://localhost:50021`・`--engine` / `--speaker` で変更可）。「どの観測点をエンジンが誤読するか」を実際に読ませて判定し、**誤読する点だけ**を収録する（全点を入れると正しく読める点までアクセントと句切れが崩れる）。**対象は 2 つ**＝震度観測点（「5弱以上・未入電」の地点名）と潮位観測点（津波の観測情報）。前者は震度観測点一覧、後者は気象庁の個別コード表（PointTsunami）から取る。**長い名前は市町村の境界で 2 アクセント句に割る**（割れ目と読みは同じコード表の AreaInformationCity から取り、前半の核はエンジンへ訊く）。**震度観測点は現行の一覧だけでなく「現行の一覧に無い観測点」も含める**（座標側と同じ `scripts/lib/stationSource.mjs` で集める）—— 過去の電文を再生すると当時の観測点名が声になり、上流が更新されれば後から加わった観測点がライブでも声になるため。**観測点が新設されたら実行する**。判定結果は生成物に焼き込まれるので**他の環境で作り直す必要はない**。上流の件数・既知の点のふりがな・距離部分の読み・作った読みの往復検証のいずれかが合わなければ exit 1 で止まる。ほかにも読めないふりがな・誤読の判定が 0 件や大半といった形でも失敗する（一覧は仕様書）。**句割りは「判定に掛けた点のうち割れた割合が 8 割を下回ったら」止める** —— これが無いと、判定が壊れて全件が見送りへ倒れても従来どおりの辞書が書き出され、句数の検証も一度も発火しないまま正常終了する。詳細は [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §3「震度観測点名の読み」「潮位観測点名の読み」「観測点名の句割り」 |
| `npm run build-epicenter-accents` | 震央地名の句割り（`public/data/tts-epicenter-accents.json`）の再生成。**要 VOICEVOX**。エンジンが 1 アクセント句にまとめてしまう長い震央地名（8 モーラ以上）を、前部要素と後部要素の境界で割る指定を作る。**各句の核はエンジンへ訊く**（構成要素を単独で読ませて 1 句・読みが一致したときだけ採り、採れなければ末尾核へ倒す。「〜地方」だけは実測より優先して「チ」へ置く）。**実測できた割合が半分を下回ったら止める**（`MIN_MEASURED_RATIO`）＝採れなければ末尾核へ倒れるので句数も読みも検証を通り、核の位置だけが黙って戻る。採れなかった語は名前を出す。**後部要素が 3 モーラ未満なら割らない**（`MIN_TAIL_MORAS = 3`。落ちるのは `能登半島沖` だけで、助詞を取り込むと短い句が浮くため）。**誤読はこの辞書の担当ではない**（手書きの句区切り辞書が全件手当て済み）。上流の件数・既知の点のふりがな・作った句割りの検証のいずれかが合わなければ exit 1 で止まる。詳細は [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §3「震央地名の句割り」 |
| `npm run verify-particle-phrases` | 辞書の読みへ助詞を連結しても**アクセント句の数とモーラ列が変わらない**ことを実データで確かめる。**要 VOICEVOX**（既定 `http://localhost:50021`・`--engine` / `--speaker` で変更可）。読み仮名辞書の該当語の直後に来る助詞は、読みへ文字列として足してから 1 つのアクセント句へ入れる。この足し戻しが句の数を変えないことが仕組みの前提で、**句が増えれば助詞が独立した句として浮き、直そうとした症状（1 モーラで自ら核を持つ句）が黙って戻る**。**辞書を作り直したとき**（`build-station-readings` / `build-epicenter-accents` / 手書き辞書の編集）と**助詞を足したとき**に実行する。**CI では代われない** —— 判定に音声合成エンジンが要る。3 辞書の全エントリ × 全助詞を突き合わせ、句数・モーラ列のいずれかが合わなければ exit 1 で止まる。詳細は [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §3「助詞は辞書の読みへ取り込む」 |
| `npm run audit-accent-unidic` | 読み上げに出る名前の**アクセント核**を、エンジンとは別の出典（[UniDic](https://clrd.ninjal.ac.jp/unidic/) 現代書き言葉フルパッケージの `lex_3_1.csv`・28 列目のアクセント型）と突き合わせる監査。**要 VOICEVOX**（既定 `http://localhost:50021`・`--engine` / `--speaker` で変更可）。エンジンが置く核は、語が内蔵辞書にあればその値・無ければ推定なので、**推定が外れた語は読みも句数も正しく、誤読の突き合わせにも句割りの判定にも掛からない**。**zip 全体 1.75GB のうち圧縮 25MB だけを HTTP Range で抜く**（目録を読んで該当エントリだけ取り、`.codex/unidic-cache/` に残す）。既定の対象は 3 辞書のキーと `public/data` の地名と警報対象地方で 3241 語、`--upstream` を付けると震央地名と震度観測点の上流も取って 5259 語。**`--bare-check` を付けると「1 語としてエンジンの辞書にあるか」も調べる**（観測点名まで見るには `--upstream` と併用する）＝辞書に値が無い語を読点あり／なしで読み比べ、読みが変わるものを出す（`甲信` を見つけたのはこれ）。エンジンへの問い合わせが倍になるので既定では回さない。**比べられるのは名前まるごとが 1 語として載っているものだけ**（29 語）で、**句ごとには比べない** ——複合語のアクセントは構成要素の単語アクセントと別物で偽陽性だらけになる。**食い違いが出ても exit 0** ＝ 正解を決めるのは耳で、これは候補を出すところまで。取得そのものに失敗したときだけ exit 1（列位置は既知の語で検算する）。**辞書を作り直したとき・観測点や区域が増えたとき**に実行する。詳細は [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §3「エンジンが置く核が外れているとき」 |
| `node scripts/build-tsunami-zones.mjs` | 津波予報区 海岸線データ（`public/data/tsunami-zones.json`）の再生成 |
| `node scripts/build-tsunami-obs-coords.mjs` | 津波観測点の座標（`public/data/tsunami-obs-coords.json`）の再生成。**API キー不要**。**電文は観測点の座標を持たない**ので、名前から引くこの表が無いと観測棒も到達確認の印も地図に出ない。**名前から座標へ直接引ける表はどこにも無い** —— 気象庁の名前（「岩手沖６０ｋｍＡ」）と運用機関側の施設名（「三陸沖１」）は別の体系なので、気象庁の観測点図を仲立ちにして図上の位置から施設へ突き合わせる（取得元 5 つ・仕組みと限界は [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §6「津波観測点の座標」）。**観測点が新設されたら実行する**。全 459 点に座標が付いたか・取得元ごとの件数・同じ場所の二重割り当て・防災科学技術研究所の座標との照合・名前が示す距離との釣り合い・既知 9 点との一致を検査し、合わなければ exit 1 で止まる |
| `node scripts/build-prefectures.mjs` | 都道府県境界データ（`public/data/prefectures.json`）の再生成（ベースマップ用） |
| `node scripts/build-subregions.mjs` | 一次細分区域境界データ（`public/data/subregions.json`）の再生成 |
| `node scripts/build-glyphs.mjs` | 地名ラベル用 SDF グリフ（`public/fonts/`）の再生成。**地名（地方・県・区域）が増減したら実行する**（`npm run build` が前段で `--check` を走らせ、未生成の文字があればビルドを止める。詳細は [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §5）。県名・区域名は `public/data/*.json` から読むため、**`build-prefectures.mjs`・`build-subregions.mjs` を先に実行すること** |
| `npm run capture-scenario` | 実地震テストシナリオを DMDATA archive から取得（`scripts/capture-test-scenario.ts`）。**要 DMDATA.JP API キー**（置き場所・渡し方は [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §6）。**ワークツリーでの注意**: キーを置く `.env.local` は Git 管理外のため**引き継がれない**。ワークツリー内で使うときはメインリポジトリ直下からコピーする。読めているかは引数なしで `npm run capture-scenario` を実行して確認する（`--from は必須です` なら読めている／`APIキーが必要です` なら読めていない。値をエコーせずに済む）。ただしシェルに `DMDATA_API_KEY` が残っていると `.env.local` が無くても通ってしまうため、事前に残っていないことを確かめる |
| `npm run build-test-quake` | 設定タブ「地震テスト」のデータ（`src/data/noto-honshin-2024-quake.json`）を実電文から作り直す（`scripts/build-test-quake.ts`）。**要 DMDATA.JP API キー**（`capture-scenario` と同じ置き場所・同じワークツリーの注意。**環境変数 `DMDATA_API_KEY` が `.env.local` より先に読まれる**ので、シェルに古い値が残っていないか確かめる）。`--event=<EventID>`（14 桁）で地震を選ぶ（既定は能登半島地震の本震）。同じ地震の電文が複数あるときは**観測点がいちばん多いもの**を採る。電文をパーサーへ通した結果をそのまま書き出すので、**手では作れない形（観測点と市町村の紐付けなど）が入る**。市町村に紐付いた観測点が 1 件も無ければ失敗して止まる。**アーカイブの取得は電文の棚卸しと同じ控えを通る**ので、一度取った日は取得を出さない（ワークツリーで走らせると控えもそのワークツリー内にできる。やり直したくなければ `DMDATA_ARCHIVE_CACHE` でメイン側を指す。→ [`docs/spec/telegram-coverage-audit.md`](docs/spec/telegram-coverage-audit.md) §2） |
| `npm run build-test-quake-unreceived` | 設定タブ「未入電テスト」のデータ（`src/data/hyuganada-2022-quake.json`）を実電文から作り直す。**要 DMDATA.JP API キー**（同上）。**市町村の未入電（`City/Condition`）を実機で出せる唯一の入口** —— 地震テストの能登本震は発表条件に当たらずこの形を持たない（→ [`docs/spec/quake-spec.md`](docs/spec/quake-spec.md) §5「市町村の震度」）。**引数はこの script に焼いてある**（対象の地震・出力先・未入電が 0 件なら失敗させる指定）。手で打つ形にすると検査の指定を落としたまま実行でき、上流の電文が差し替わったときに 0 件のデータが黙って書き出される |
| `npm run build-test-quake-max-unreceived` | 設定タブ「5弱以上テスト」のデータ（`src/data/ishikawa-seihooki-2024-quake.json`）を実電文から作り直す。**要 DMDATA.JP API キー**（同上）。**カードの最大震度に「以上」が付く形を実機で出せる唯一の入口** —— 未入電は下限の5弱へ寄せてあるので、電文全体の最大震度が5弱の地震でだけ階級が一致する（`isMaxScaleUnreceived`）。地震テスト（能登本震・震度7）と未入電テスト（日向灘・5強）はどちらも一致せず、この形を持たない。**引数はこの script に焼いてある**（対象の地震・出力先・階級が一致する未入電が 0 件なら失敗させる指定）。理由は 1 つ上と同じ |
| `npm run build-hypocenter-catalog` | 長期震源カタログ（`public/data/hypocenter/`）の再生成。既定で **1919 年〜今年・M2.0 以上**を年ごとの JSON にする（`--from` / `--to` / `--min-magnitude` で変更可）。**API キー不要**。取得元は確定値（年・まとめ ZIP／1919〜2023 年）と速報値（日別「震源リスト」／2024 年〜2 日前）の 2 つで、**年 ZIP を先に試し 404 なら日別へ落ちる**。日別ページは `.codex/hypocenter-cache/` に残すので、2 回目以降は新しい日だけ取る（初回は 966 日・約 70MB）。**取得経路ごとに既知の地震と照合し、合わなければ exit 1 で止まる**（7 経路・7 地震）。ほかに「読めない行が想定より多い」「J レコードがあるのに 1 件も採れない」等でも失敗する（一覧は仕様書）。週 1 回は GitHub Actions が `--from 2024` で自動実行する。詳細は [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §6「長期震源カタログ」 |
| （計測台）`src/services/dmdataCoverage.probe.test.ts` | **電文と実装の突き合わせ**（どの要素から値を取り出しているか）を実測する。実電文を本物のパーサーへ流し、`textContent` と `getAttribute` の呼び出しを記録する。**環境変数を渡さないと飛ばす**ので通常の `npm test` は汚さない。**静的解析（ソースの文字列から推し量る方法）は使わない**。**実行手順は書かない** —— この計測台と `scripts/telegram-audit/` の突き合わせは出力先を揃えて動かす必要があり、片方だけ書くと食い違う。手順とその理由、測り方・読まないと決めたもの・決まっていない判断・読み落としの型・残っている段は [`docs/spec/telegram-coverage-audit.md`](docs/spec/telegram-coverage-audit.md) |
| `npm run build-test-lpgm` | テストボタン用の長周期地震動データ（`src/data/noto-honshin-2024-lpgm.json`）を、**実電文をパーサーへ通して**作り直す（`scripts/build-test-lpgm.ts`）。**要 DMDATA.JP API キー**（置き場所は `capture-scenario` と同じ・キーの読み取り順は `build-test-quake` と同じ）。`--event=<EventID 14 桁>` で対象の地震を選ぶ（既定は能登半島地震の本震）。**電文から読む項目を足したら実行する** —— 手で組み立てると、テストボタンだけ古い形のまま残って新しい項目が実機で一度も画面に出ない。アーカイブの控えは `build-test-quake` と共通 |
| `npm run build-test-estimated-intensity` | テストボタン用の推計震度分布図データ（`src/data/test-estimated-intensity.json`）を、**実電文をパーサーと BUFR 復号器へ通して**作り直す。**要 DMDATA.JP API キー**（`capture-scenario` と同じ置き場所・同じワークツリーの注意・キーの読み取り順は `build-test-quake` と同じ）。`--arrival=<地震発現時刻 UTC・分まで>` で対象を選ぶ（既定は 2026-07-28T10:03 の熊本県熊本地方 M4.2。**推計震度分布図が出た地震のうちいちばんセルが少ない**ので軽い）。**鍵が `EventID` でないのは、この電文が識別子を持たないため** —— 地震情報と結び付けられるのは発現時刻しかなく、アプリ側の引き当てと同じ鍵で選ぶ。**地震情報と対で書き出す**（分布だけ置いても引き当てる相手のカードが無くボタンが出ない）。**手では作れない** —— BUFR は二進。アーカイブの控えは `build-test-quake` と共通 |
| `npx tsx scripts/measure-kyoshin-warmup.ts` | 強震モニタ検知エンジンの**助走**の効きを実データで測る。「その時刻から再生を始めた場合」が「ずっと再生し続けた場合」とどれだけ一致するかを、大地震 7 本・地震発生の 1〜12.5 分後の 164 点で見る。**API キー不要**。初回は `--fetch` で窓を取得する（7 本・約 6300 フレーム）。キャッシュは `bench-kyoshin` と共有する。`--warmups=0,180,300,600` で固定長を比べ、`--adaptive` で実装と同じ「遡って静穏で打ち切る」方式を測る。**助走の長さを決める定数（`utils/kyoshinWarmup.ts`）を変えたら実行する** —— `bench-kyoshin` は窓の先頭から流すので、途中から入る形は測れない |
| `npm run bench-kyoshin` | 強震モニタ検知エンジンの実データ回帰ベンチ（`scripts/bench-kyoshin.ts`）。Yahoo の過去フレームを窓ごとに再生し、**気象庁発表の地震を捉えられたか**と**地震が無いのに鳴っていないか**を分類ごとに集計してベースラインと突き合わせる。**API キー不要**。窓は `scripts/kyoshin-bench/windows.json`（単一情報源）。初回は `-- --fetch` で取得する（全 793 窓・500MB 超・数十分）。検知エンジンを変更したら実行する（上記「特殊ケース: 強震モニタ検知エンジンを変更したとき」） |
| `npx tsx scripts/build-historical-archive-<地震>.ts` | ローカル履歴アーカイブ（`public/data/historical-archives/*.json`）の再生成。対象は 4 本（`2016-kumamoto` / `2016-tottori` / `2018-iburi` / `2018-osaka`）。**API キー不要**。地震情報・津波は NII「気象庁防災情報 XML データベース」の実電文を本番と同じパーサーへ通し、EEW は気象庁の発表状況ページから取る。**電文から読む項目を足したら実行する** —— テストボタンのデータと同じ理由で、作り直さないとその項目は履歴再生で一度も画面に出ない。**`index.json` を 4 本とも書き換えるので順次実行する**。**NII からの取得だけ**は一度取ったページが `.codex/nii-cache/` に残るため、途中で落ちても続きから進む（EEW 側の取得はキャッシュを持たない）。詳細は [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §6「国立情報学研究所（NII）の実電文アーカイブ由来のデータ（地震情報・津波）」 |
| `npm run capture-kyoshin-waveform` | ローカル履歴アーカイブ用に、NIED K-NET/KiK-net の実波形から強震モニタ風のリアルタイム震度リプレイデータを生成（`scripts/capture-kyoshin-waveform.ts`）。**要 NIED 登録**（`NIED_KNET_USER`/`NIED_KNET_PASSWORD` を `.env.local` に。取得は `--origin=<地震発生時刻JST、YYYYMMDDHHMMSS> --id=<出力id>`）。`--origin` は複数回指定でき、本震＋離れた余震を1つのアーカイブへ統合する（1件の取得失敗ではバッチ全体を止めず、残りで統合する）。出力（`public/data/historical-archives-kyoshin/*.json`）は**再配布禁止のためリポジトリに含めない**（`.gitignore` 対象。生成した本人のローカル環境でのみ再生できる）。**devサーバー無しでも使いたい場合は、設定タブ「K-NETデータの取り込み」から同じZIPを直接ブラウザへ取り込める**（NIEDのダウンロードページで自分で取得したZIPを選ぶだけで、対応する地震へ自動で追加される。データは端末内にのみ保存）。詳細は [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §6「NIED K-NET/KiK-netの実波形由来のデータ（ローカル限定・リアルタイム震度）」 |
| `npm version patch\|minor\|major` | バージョン更新・コミット・git tag 作成（リリース時に 1 回だけ実行。詳細は「リリース」参照） |

> 2026-09-13: ローカル履歴アーカイブ 4 本が 2026-08-28 の生成のまま取り残され、それ以降にパーサーへ入った項目（市町村の段・`＊` の注記・都道府県ロールアップ点）が丸ごと欠けていた。**テストボタンのデータと同じ穴が、生成コマンドを表に載せていなかったぶんだけ気づきにくい形で開いていた。** 電文から読む項目を足したときに作り直す対象は、テストボタンのデータだけではない。

## 構成メモ

- `src/App.tsx`: レイアウトの中枢。地図常時表示＋アイコンナビでパネル内容を切替。地図内容・更新時刻・通知音・自動タブ切替・EEW 連携の制御もここ。
- レイアウトは画面の向き・高さで切り替わる（左右分割／上下分割）。分岐条件は `tailwind.config.js` の `screens`（`side` / `sideNarrow` / `roomy`）が単一情報源。上下分割時は境界のつまみで比率変更・折りたたみができる（`PanelResizeHandle`）。詳細は [`docs/spec/architecture-spec.md`](docs/spec/architecture-spec.md) の「画面サイズ別のレイアウト」。
- 地図は MapLibre GL JS（`JapanMapGL`）に一本化。`MapView` が App と地図実装の間の薄いラッパー。旧 Leaflet 版 JapanMap とエンジン切替フラグは F7 で撤去済み。
- 地図のモード（`JapanMapGL` の `mode`）: `quake`（地震）／`tsunami`（津波海岸線）／`kyoshin`（リアルタイム震度・予報円）／`catalog`（長期震源カタログの点群）。
- 生成データ（`public/data/*.json`）は座標が大きいため遅延読込（初回利用時に一度だけ fetch しキャッシュ）。
  地図は常時表示のため、地図が使うもの（`prefectures.json` / `subregions.json` / `station-coords.json` / `tsunami-zones.json` / `active-faults.json` / `plate-boundaries.json` / `tsunami-obs-coords.json`）は表示モードに関わらず地図の初回表示時にまとめて要求する。実地震テストシナリオの本体だけは再生時に取得する。

## コード整合性チェックポイント

新機能追加・修正時にコメントと実装が乖離しやすい項目は、以下の仕様書（`docs/spec/`）に単一情報源として集約している。変更時に該当仕様書を参照し、コード修正と同一コミットで仕様書側も更新する（詳細は上記「ドキュメント更新」節）。

### 読み上げ: 何を・どの語で読むか

**津波の読み上げ文を変えるときは、下記「津波」節も併せて見ること。** 既読の管理・観測波高の並び・等級を伝えていない電文の扱いは読み上げ文の作り方に直結するが、参照先の仕様書が `tsunami-spec.md` のため向こうに置いてある。読み上げに合わせて画面を動かす話は「自動タブ切替と画面の追従」節。

| 項目 | 単一情報源となる仕様書 |
|---|---|
| 読み上げの範囲（`ttsIntensityLevels` / `ttsAlwaysReadScale` / `ttsMaxRegions` / `ttsRegionTolerance` の組み合わせ方・階数は観測がある階級のみを数える・`ttsAlwaysReadScale` は長周期に適用しない） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4 |
| 読み上げの詳しさを項目ごとに選べること（**既定はすべて「設定を入れる前の挙動」**＝設定を足しただけで既存の利用者の耳に届く内容を変えない／**既読の鍵に設定値を含めない**＝含めると設定を触るたび全部が未読へ戻る。粒度の切り替え（地点名 ⇄ 区域名）は鍵そのものが変わるので読み直しへ倒れる（安全側）／**震源要素だけは既読の待ち方にも効く**＝`tellableHypocenterFacts` が「声にしうる事実」の単一情報源で、読む側と記録を待つ側が共有する。片方だけ設定を見ると、読まれる機会の無い事実を待ってその地震だけ差分の経路へ入れなくなる／**未入電の「地名を読まない」でも件数と未入電であることは読む**＝地名の列挙だけを落とす選択肢で、危険の告知まで消すものではない。**代替文（地域名を 1 件も作れなかったときの文）に任せない**＝観測値のある区域が 1 つでもある電文ではその分岐に入らず、告知ごと消える（観測値と未入電が混ざる形は強い地震ほど起きやすい）。件数として伝えた分は既読にする。区域名へ丸めるときは述語に「一部の地点で」を挟む（区域全体が未入電に聞こえるため）／**津波の観測点の件数は選抜と文の生成の両方へ同じ値を渡す**＝片方が既定のままだと、読まれていない観測点が既読になる。`0` は「無制限」ではなく「1 件も読まない」／設定の変更は次の電文から効く（読み上げ文は受信時に組み立てる）） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「読み上げの詳しさ」 |
| 読み上げの地域名の粒度（一次細分区域・区域の点を持たない電文は観測点から逆引きする・引くのは**地図と同じ** `lookupStationRegion` で都道府県名と観測点名の組でしか引かない・`pref` が空の経路は観測点名から県を先に補う（この補完は初出優先なので同名観測点があれば誤る）・区域が引けない観測点だけ都道府県名へ落とし同じ県で区域が取れていれば県名は捨てる・区域の点がある電文では都道府県ロールアップ点を見ず、観測点経路でだけ観測点を持たない県の救済に使う） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「地域名の粒度」 |
| 階級をまたぐ県名まとめの抑止（上位の階級で区域名を出した県は下位の階級でまとめない・地震と長周期の両方に同じ規則を置く・数えるのは**続報で差分に落とす前**の区域（落とした後だと既出の区域が消えて県の全区域が揃って見える。**差分を持つのは地震の経路だけ**で長周期は毎回全区域を読み直す）・区域名と県名の見分けは区域名の索引に頼るので「多区域の県に県名と同名の区域が無い」ことが前提） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「地域名の粒度」 |
| 「5弱以上・未入電」を断定形で読まないこと（**別の文で伝える**＝「〇〇では、震度5弱以上と推定されますが、未入電です。」／地域名を含む文の述語は「観測しました」なので混ぜると嘘になる／**地点名で読む**＝未入電は観測点ごとの事実で、区域名へ丸めると同じ区域に観測値がある電文で矛盾して聞こえる（強い地震ほど起きやすい）／**地点で覆えない区域・県は区域名で補う**＝`partitionUnreceivedPoints`。地点の有無を電文全体で 1 つのフラグにして倒すと、区域単位だけで未入電を伝えてきた県が痕跡も無く消える。無条件に並べると二重に言う／省略と見出しの単位は中身に合わせる（地点／地域／混在なら件）／**推定の理由まで言う**＝理由が無いと音声だけの利用者に伝わらない。語は気象庁の「未入電」をそのまま使い、画面の印「未入電あり」と揃える／観測値と混在すれば文を分けて両方読む／区域を作れないときは「最大震度〇〇以上と推定されますが、未入電です。」／**既読の鍵にも未入電の別を含める**＝同じ階級でも別の内容で、推定として読んだ区域に観測値が届いたら読み直す（「新たに」は付けない）／**未入電を含む県は県名へまとめない**＝カードと同じ規則／**続報の差分でも出す**＝震源要素が既出のまま未入電の区域が加わる続報があり、落とすと一度も声にならない／**選抜と上限は観測値の文と共有する**＝`selectRegionNames`。独自に切ると `maxRegions` の 0（無制限）を取り違えるか、省いた件数を伝えない） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「地域名の粒度」 |
| 読み上げで地域名を 1 件も作れなかったときの扱い（最大震度だけを伝える・**震度も判らなければ何も足さない**（`intensityText` は震度不明で空を返すので確かめずに埋めると助詞だけの文になる）・地震情報と震度速報で同じ一文（`maxScaleOnlySentence`）を使う・地域が消えたことは間引いて記録する） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「地域名の粒度」 |
| 読み上げの地域名の並び順（**地震・長周期のみ**。気象庁の標準順・順序の実体は `station-coords.json` の区域キー順・上限で切るときの選抜だけは震源距離で行う・震源を持たない電文（震度速報・長周期）は距離で選ばない） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4 |
| 津波の区域の並び順（読み上げとカードで**同じ並びを使う**こと・実体は `tsunami.ts` の `sortAreasForCardDisplay`（**1 つの等級の中だけ**）と `sortAreasAcrossGradesForCardDisplay`（等級をまたぐ通し順。上位数件だけ採る通知・スクロールはこちら）・予想最大波高の文もカードの波高見出しと同じ区切りで読む。片方だけ変えると追従スクロールが上下へ往復する） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「津波の区域の並び順」 |
| 津波観測情報で新規と更新を言い分けること（境界は**前に声にした波高があるか**だけで、名前を聞いたことがあるかでは判定しない・到達確認だけ読んだ観測点に初値が付いたら「新たに」側・渡すのは読み上げ用の記憶・深刻な波高を含む群を先に読み「また、」で継ぐ・群に割っても件数上限は合計） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「新規と更新を言い分ける」 |
| 話題が変わるところを「また、」で継ぐこと（**観測点の群は 4 つ**＝波高更新 → 警報相当 → 到達確認 → 欠測の順に継ぐ。確定した事実を先に、観測できていないものを後に置く／挟む系統は 2 つ＝**観測情報の続報**と**等級の発表・引き下げ／区域単位の等級変化**・実体は `joinWithAlso`・**両方が非空のときだけ**挟む・新規と更新の群分けが使う「また、」と同居したら**群の数だけ**要る・**等級を語れない電文（全区域が `Unknown`）では到達確認を継がず既読にもしない**） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「話題が変わるところは「また、」で継ぐ」 |
| 読み上げから外した地点数の句を述語に貼り付けないこと（`omittedPointsSentence`。新規と更新で述語が変わるため、貼り付けると外した地点が更新扱いになる。地点数の言い方は到達確認と共有する） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「観測点は「選抜」と「並び」を分ける」 |
| 津波の読み上げの組み立て（**区域名は 1 回だけ**・等級ごとに「区域と波高」を 1 文で言い切る・等級と行動を先に言う・下位等級の波高も読む・等級をまたいで波高をまとめない・波高が付いていない区域は別の文で挙げる・数値で表せない波高（「巨大」「高い」）は語を補う） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「区域名は 1 回だけ。等級ごとに「区域と波高」を 1 文で言い切る」 |
| 読み上げ文で名前を並べるときの書き方（区切りは読点・中黒は音にならず合成の区切りにもならない・場所の助詞「で」は文末のみ） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4 |
| 地震情報の続報は差分だけ読むこと（初出の区域と震度が上がった区域・変わった震源要素／津波区分だけ／記録は地震ごとで**種別を跨いで共有**（省き方は別で、震源要素はその種別の初報では通しで言う）／**まだ声にしていない事実が残る続報は差分にせず初報と同じ形で言い直す**／震度速報を震源要素・津波区分の比較に使わない／「最大」を冠せるのは最大震度に一致する階級だけ／階級の打ち切りは差分を取る前の観測で数える） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「続報は差分だけ読む」 |
| 続報の区域は**初出と上がりで群を分ける**こと（上がった分を先に読み、初出群を「また、新たに」で繋ぐ・「新たに」は初出にだけ掛かる語・**「最大」は上がりの群だけ**に冠する・上限は群ごとに数える・**群分けと「新たに」は差分のときだけ**で、判定は記録の有無ではなく「区域を一度でも声にしたか」） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「続報は差分だけ読む」 |
| その地震で**最初の確定情報**（震源・震度情報／各地の震度情報）は地域も通しで読むこと（2 通目以降は差分に戻す・記録は受信時に進める） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「続報は差分だけ読む」 |
| 変化が無い続報でも**名乗りだけは読む**こと（地震情報の読み上げ文は常に非空になり、タブ移動は読み上げ追従が担う。受信時要求へ落ちるのは読み上げが無効な端末だけ）／**ただし名乗りだけで終わらせない**＝「更新されました。」で切ると聞き手に「何が？」しか残らない。区域の差分が空なら**区域より下の階層（観測点・市町村）を見て**「観測地点が追加されましたが、地域ごとの最大震度は変わっていません。」等を足す（読み上げの地域名は一次細分区域までしか下りないため、観測点だけが増えた続報はそこだけ見ると空になる）／**「各地の震度」と言わない**＝市町村の段は実際に上がっていることがある。据え置きだと言い切ってよいのは区域の最大震度だけ／**「変化が無い」と「判らない」を混ぜない**＝まだ 1 通も声にしていない地震では言い切らず名乗りだけで終える／**区域より下の階層を持たない種別は別の言い方**＝震度速報（区域まで）・震源情報・その他（震源要素のみ。判定は `isEpicenterOnly`）は「観測した震度に」「震源の内容に」変わりはありません、と対象を示す。**突き合わせを通すのは震源・震度情報と各地の震度情報だけ**／**比べる材料は「最後に声にした報」**で、記録は声になった時点で進める（`withObservedRef`）。**観測点を運ばない報では記録を発行しない**＝空で上書きすると前に伝えた観測点が消え、次の報が「追加されました」と嘘を言う。数えるのは観測点（`isArea` が偽）と市町村で、`points` の件数では見ない（震度速報は区域の点だけを積む）／**電文の `Revise` は使わない**＝あれは「直前の報」からの差分で、鳴らなかった報があると食い違う。P2PQuake 経路も持たない | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「続報は差分だけ読む」 |
| 地震情報の既読は「声になった分」だけ進めること（判定はチャンク単位・**鳴り始めた最後のチャンクは数えない**・1 つも鳴らなければ記録しない＝全文へ倒す。受信時や文の生成時に更新すると、割り込みで鳴らなかった地域が二度と読まれない。EEW の既読値が粗くて済むのは差分を語らないため）。**「声にしうる事実」と「記録を待つ事実」は同じ述語で判定すること**（深さは震源名の句の中でしか読まれない）／**記録を進める箇所は 2 つ**＝読み上げの完了時と、**次の電文を受け取った瞬間**（`flushSpoken`）。差分は受信時に同期で組まれるので、後者が無いと前の報を読み切る前に届いた続報が「まだ何も声になっていない」古い状態を基準にし、**先頭から読み直す**／**途中で確定させるときは「完走した」とみなさない**＝完了後だけが通れる近道（最終チャンクが鳴り始めていれば完走）を途中で適用すると、鳴り終えていないチャンクを既読にして元の穴を再現する | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「既読になるのは「声になった分」だけ」 |
| 上限で「ほか○地域」へ落ちた区域も**既読にする**こと（名前は読んでいないが件数としては伝えている／**以前は記録しない設計**で、次報で「新たに」付きで読み直され地域が増えたように聞こえていた。2026-09-11 に覆した／「新たに」を外すだけでは上がりの群と同じ語形になり「震度が上がった」と聞こえる／**代償**＝上限に落ちた区域の名前は震度が上がらない限り声にならない／長周期の「ほか○地域」は差分を持たない経路なので対象外） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「続報は差分だけ読む」 |
| 地震情報の続報判定（キーは「地震を指すキー」と情報種別の組。前者は DMDATA が `eventId`／P2PQuake は地震の時刻＋震源名・**見た報は全部覚える**。直近 1 件だと種別の違う報が交互に届いたときに互いの記憶を上書きし、2 度目が初報のように読まれる） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「特殊な扱い」 |
| 震度を伝えない電文（震源情報等）でウィンドウタイトルの震度を消さないこと（判定は既存カードが震度を持つかどうかで行う。**その報が初報かどうかで判定しない**。種別ごとに初報になるため歯止めが効かない） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §5 |
| 「特別警報」を音声で読まないこと（表示・通知・通知音は 2 段階を保つ） | [`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §4 |
| 推計震度分布図の初報と続報を言い分けること（「受信しました」／「更新されました」。**判定は分布を伝えた発現時刻の台帳**で、反映の可否の理由を流用しない。電文そのものの扱いは下記「地震情報」節の推計震度分布図の行） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「推計震度分布図は地震情報の音を借りる」 |
| 震度を伝えられない報では長周期階級も読まないこと（電文では最大予測震度が必須・階級が任意なので「震度なし・階級あり」は作れない。読むと「予想震度なし。予想最大階級3。」という矛盾した発話になる）・**「単独点処理のため」と言えるのは予想震度が付いていない仮定震源要素の報だけ**であること（`condition` そのものは観測点数を意味しない） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「特殊な扱い」 |

### 読み上げ: 音声合成（読み仮名・チャンク・接続先）

| 項目 | 単一情報源となる仕様書 |
|---|---|
| 日付・時刻・分の読み（**直す場所を性質で分ける**＝読みが変わるもの（`1日`→ついたち）は**日付か期間かが曖昧**なので辞書に置けない。アプリは `d.getDate()` から作っていて日付だと確定しているので**生成側で読みへ直す**（`speakableDay`）。辞書へ入れて境界の条件を足すと、足すたびに次の抜け道が見つかる（4 巡した）／`0時` `0分` は期間の用法が無いので辞書に置き、判定は「前が数字なら弾く」だけ／**抑揚は句の割り方をエンジンに任せ核だけ動かす**＝1 句へまとめると頭高の語で頭の高さが反転する（186 通り中 95 件）／「N分頃」は連濁が落ちるので「頃」まで鍵に含める／気象庁が書いた文・津波観測情報の見出し文・**取消の理由**は、読む前に日時を半角・ゼロ埋めなしへ揃える） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §3「日付・時刻・分の読み」 |
| 読み仮名辞書に何を収録するか（誤読するものと、読みは正しいのに抑揚が崩れるもの・正しい読みは気象庁コード表のふりがなを正解にする・**誤読は後ろに続く文字で反転するので、単体ではなく読み上げ文が実際に作る形で確かめる**／**複合語の一部として頻出する字は語形を丸ごと鍵にする**＝1 字を鍵にするとその字を含む他の語が全部巻き添えになる。どの語形なら安全かは**実電文の用例を数え上げてから**決める（「方」は 2287 通の走査で「かた」と読むのが 1 種だけと確かめた）。走査は本文が入る要素を実装から導き、`/v2/archive` の日付範囲は両端より広く取る。棚卸しの段取りは [`/tts-reading-audit` スキル](.agents/skills/tts-reading-audit/SKILL.md)／**誤読でなく抑揚が崩れる形も収録する**＝「名詞＋係助詞」と述語が 1 アクセント句へ融合する「〜はありません」。**鍵は助詞から始めない**（`はありません` を鍵にすると係助詞が名詞から剥がれて元より悪くなる）。地名ではないので `_terms` へ列挙する。**値が後続語へ吸収される形も同じ癖**＝緊急地震速報の「予想最大震度4程度以上。」は丸ごと 1 句にまとまり核が値から「テエド」へ移る。**鍵は数値から始める**（`4程度以上`。「程度以上」だけだと数値が前半へ残り割れ目が値の後ろに来る）。**前半（`予想最大震度`）は鍵にしない**（上限が定まった報まで 2 句に割れる。そちらは崩れていない）） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §3「何を収録するか」 |
| 震度観測点名の読み（**辞書は 3 つ（手書き＋生成物 2 つ）あり、キーが衝突したら手で書いた句区切り辞書が勝つ**／**誤読する点だけを収録する**＝全点を入れると正しく読める点までカナ経由になりアクセントと句切れが崩れる・「町」の ちょう↔まち も誤読として扱う／**判定は読み上げ文が作る形（`、` と `では、`）で行い、名前を単体で読ませない**＝誤読は後ろに続く文字で反転する／**長音記号は母音の重ねへ開く**＝`is_kana=true` が受け付けない／**アクセント核は、位置を決められないものだけ末尾に置く**＝ふりがなは核を持たないので、エンジンへ訊けたときだけ実測を使う（→「観測点名の句割り」）／**観測点名のキーは一律で単独語キーにする**＝誤読する点だけを収録する設計上、長い側が辞書に無い組が必ずできるため「同位置なら長い方が勝つ」では守れない／**部分一致の面は 3 つ**（区域名・都道府県名／別の観測点名／句区切り辞書のキー）で全部を実データでテストする／**区域名・都道府県名にはこの仕組みを使わない**） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §3「震度観測点名の読み」 |
| 潮位観測点名の読み（津波の観測情報で声になる名前。**震度観測点と同じ辞書・同じ生成コマンド**／**沖合の誤読は末尾の識別英字で決まる**＝Ａ は 148 点すべて無事で Ｂ〜Ｇ は 96 点すべて崩れる／**後ろに続く文字で反転する**＝読点で終わるなら正しいのに「で〜」が続くと `ｋｍ` が単位として解析されず「クム」と読まれる。読み上げ文は観測点名の後ろに必ず「で〜」を置くので**実運用では 96 点すべてが崩れる**／**判定は述語まで含めた実運用の形で行う**＝エンジンの解析は文全体を見るため「で」だけを付けた形では再現しない／**沖合の値はエンジン自身の読みを写す**＝気象庁のふりがなが距離・単位・英字をカナ化していないので正解を組めない。正しく読める形（`名前、`）で読ませた `/audio_query` の `kana` を採り、**写す前にその形が本当に正しいかを確かめる**（崩れているものを採れば誤読を辞書へ焼き込む）／距離部分の読みはその部分だけを単独で読ませた実測を正解とし、**数字と識別英字のすべての値を生成側で固定する**（`DISTANCE_READING_FIXTURES`・31 件。一部だけ固定すると、覆っていない桁が崩れたときに正解ごと崩れて一致してしまう）／**ヘッダ部でのみ使う簡略名も列挙する**＝見出し文が声になる。座標が要らない `build-tsunami-obs-coords.mjs` と範囲が違うのは意図したもの／沿岸の誤読 54 点は手書きの句区切り辞書と重複するが、**生成物を手書き辞書の中身に依存させない**ために入れる／気象庁のふりがなに小書き拗音を使っていない点が 1 つある（`日向市細島`＝`ひゆうがし`。正しい読みは手書き辞書が持つ）） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §3「潮位観測点名の読み」 |
| 観測点名の句割り（**ふりがなから組む値は末尾に核を 1 つ置くだけなので、長い名前がひと息の 1 句になる**＝8 モーラ以上の 1 句が 2411 件あった／**エンジン自身は市町村の境界で割っている**＝正しく読める点 400 件のうち 380 件（95.0%）で最初の句が市町村名と一致。辞書へ入れる形だけが自然な句構成から外れていた／**割れ目は推測せず気象庁の個別コード表（シート 24）から取る**＝1 行に市町村の名前とふりがなが並ぶ。**漢字と読みの両方が前方一致したときだけ割る**（上流の命名は 3 通りにぶれる）／**前半の核はエンジンの実測**＝「市」「町」の直前に来るので末尾核では抑揚が外れる。市町村名を単独で読ませ 1 句で読みが一致したときだけ採り、採れなければ末尾核へ倒す（実測 592 / 1192 件）／**後半は割らない**＝旧町名の ちょう／まち を決める読みの出どころが無く、エンジンからも採れない（後半が 8 モーラ以上残るもの 409 件のうち、後半を正しく読めるのは 1 件）／**門は 2 つ**＝名前全体が 8 モーラ以上・割った各句が 2 モーラ以上（1 モーラの句は自ら核を持って浮く）／**沖合の潮位観測点は対象外**＝値がエンジン自身のカナ表記で既に句を含む／**句割りだけが黙って死にうる**＝判定が壊れて全件が見送りへ倒れると、句数の検証は句割りを指定した点だけを見るので一度も発火せず、従来どおりの辞書が正常終了で書き出される。割れた割合の下限で止め、見送りは理由別に件数を出す） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §3「観測点名の句割り」 |
| 区域名・津波予報区の句割り（**「1 句で長い」だけが崩れ方ではない**＝2 句に割れていても割れ目が語の境界と違うことがある（`イシカリ｜チホオホクブ` のように「地方」が後半の句へ入る）。区域名 192 件のうち `地方`・`県`・`府` を含むもの（`都` は除く）が 177 件で、**後半が独立した句になっていないのが 20 件**だった。前部要素の核は「チ」（`イシカリチ'ホオ`）・後部要素は実測（`ホ'クブ` 等＝UniDic の aType=1 と一致）／**震央地名と同じ「1 句にまとまって長い」崩れもある**＝読みは正しいので誤読の突き合わせには掛からない／**対象は 1 句で 8 モーラ以上**（震央地名の生成が使う下限と同じ）／**手書きの句区切り辞書へ入れる**＝対象が少なく核を 1 件ずつ耳で確かめられる規模なので、生成側へ寄せていない／**核は構成要素を単独で読ませた実測を写す**＝推測で置くと外す（`北海道` は核 3・`太平洋` は核 3・`沿岸` は核 4 で、いずれも「語末の 1 つ前」ではない）／**8 モーラ以上を機械的に全部割る運用にはしない**＝`沖縄本島地方`・`大東島地方`・`小笠原諸島` は 1 句のままが自然と判断した／**震央地名と同名の区域は、あちらの生成物が効く**＝`十勝地方北部` ほか 7 件） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §3「区域名・津波予報区の句割り」 |
| エンジンが置く核が外れている語（**句の割り方が正しくても核の位置だけ外れることがある**＝エンジンは内蔵辞書にある語はその核を使い、無ければ構成要素から推定する。読みも句数も正しいので誤読の突き合わせにも句割りの判定にも掛からない／**辞書にある語かは読点を外して読ませれば分かる**＝無ければ漢字を 1 字ずつ読む（`甲信` → `キノエ｜シン`）。`--upstream --bare-check` が辞書に値の無い語を総当たりする（観測点名まで見るには `--upstream` の併用が要る）。いま残るのは観測点名 7 語で、どれも実運用の文型では正しく読む／**裏取りは UniDic のアクセント型**（`npm run audit-accent-unidic`。要 VOICEVOX）＝`lex_3_1.csv` の 28 列目（0 が平板）。zip 1.75GB のうち圧縮 25MB だけを zip の目録経由の HTTP Range で抜ける／**突き合わせられるのは名前まるごとが 1 語として載るものだけ**＝5259 語中 29 語で、読みまで合って比べられたのは 28 語。**句ごとに比べない**＝複合語のアクセントは構成要素の単語アクセントと別物で偽陽性だらけになる（2273 句中 948 句）／**`名前、` の形では平板と尾高を区別できない**＝0 とモーラ数を同値として扱う／**平板はこの記法では書けない**ので末尾核で近似する／**辞書どうしの食い違いもここで出る**＝`鳥羽` は手書きが `ト'バ`・生成物が `トバ'` で、キー衝突では手書きが勝っていた） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §3「エンジンが置く核が外れているとき」 |
| 震央地名の句割り（**誤読ではなく抑揚の問題**＝エンジンは長い名前を 1 句にまとめ核を後部要素の頭へ置くので切れ目が語の途中に来る。読みは正しいので誤読の判定には掛からない／**核は 3 段で決める**＝①前部要素が「〜地方」なら「チ」（実測より優先。エンジンは語によって「ホ」へ置くので、任せると同じ「〜地方」が語ごとに違う位置で割れる）②構成要素を単独で読ませて 1 句・読み一致なら実測（74 語中 66 語）③どちらでもなければ末尾核。**末尾核で通していた頃は `ヨーロッパ＼`・`チューゴク＼`・`カントー＼` のように明らかに外れる語があった**／**後部要素が 3 モーラ未満なら割らない**＝`MIN_TAIL_MORAS = 3`（実データで落ちるのは `オキ` の 2 モーラだけ）。読み上げ文は名前の直後に助詞が付きそれを取り込むので、`能登半島沖` を割ると `オキ＼オ` という短い句になる。割らなければエンジンが「沖」の直前へ核を置き、その形は崩れていない（`〜沖` 33 件で実測・例外なし）。観測点名の下限（各句 2 モーラ以上）より 1 つ厳しい／**割るのは前部要素と後部要素の境界で 1 回だけ**・後部要素は長いものから当てる（`沖` を先に見ると `根室半島南東沖` が `根室半島南東 / 沖` に割れる）／**漢字と読みの両方が一致したときだけ割る**＝読みを見ないと `〜島` の しま／じま で前部要素の読みがずれる／対象は 1 句にまとまって 8 モーラ以上のものだけ／**震央地名の誤読は手書きの句区切り辞書が全件手当て済み**／ふりがなの出どころが観測点名と違う＝気象庁の一次情報（多言語辞書データ）のルビは漢字の一部にしか付かず読みを復元できない） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §3「震央地名の句割り」 |
| チャンク末尾の句読点に間を足すこと（VOICEVOX は後ろに何も続かない句読点に間を付けない・チャンクは隙間なく詰めて鳴らす・**最後のチャンクには足さない**・足すかどうかは合成の入口 3 つで揃える。読点で名前を並べても間が入らない症状の原因のひとつ） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §3「チャンク末尾の句読点には間を足す」 |
| 辞書での分割によって落ちた句読点・空白を戻すこと（**チャンク末尾とは別の穴**で、同じ症状に原因が 2 つある／**落ちるのは句読点だけではない**＝空白も同じように落ちる。気象庁が書いた文は電文の改行を空白へ直すので必ず現れる／**チャンクを割る文字の集合とは分ける**＝空白では割らないので、そちらへ足すと割れない位置に間だけが入る／落ちた位置に種を置き、長さは `/mora_data` の引き直しに決めさせる・引き直しは「間が無い位置に間を作る」ことはしない・種を置かない位置が 2 つある（チャンク末尾は `CHUNK_BREAK_PAUSE` の担当／掛ける先の句が無い場合。後者は現状起きない）・**置いた後にも歯止めがある**（種が句の並びの末尾に来たら引き直し値を採らない）・引き直しが間を返さなかったときは種へ倒して記録する） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §3「分割で落ちた句読点・空白を戻す」 |
| 辞書キーの直後の助詞を読みへ取り込むこと（**助詞だけのアクセント句は日本語に無い**＝辞書該当語だけをカナから合成すると助詞が次の断片の先頭になり、1 モーラで自ら核を持つ句として浮く。カナへ文字列連結して同じ句へ入れる／**取り込めた切れ目には間を置かない**＝`DICT_TRAILING_PAUSE` は取り込めない切れ目（漢字・数字が直に続く形）にだけ残す。助詞の後ろの区切り文字は引き直しで復活する／**助詞の列挙は `TRAILING_PARTICLES` が単一情報源**＝入れてよいのは読み上げ文の文型（`ttsText.ts`）に実在するものだけで、実データでの出現件数は基準にしない（文型が実在しても電文次第で 0 件になりうる）。**逆に、文型に無いものは入れない**＝`も` を入れると気象庁が書いた文の「もしくは」を割る／**表記をカタカナにするだけでは作れない読みがある**＝係助詞の「は」は `ワ`・目的格の「を」は `オ`／**最長一致で切り出す**＝`にも` を `に` より前に置く／**辞書キーの一致を助詞の切り出しより優先する**＝助詞と同じ字面で始まる辞書キーがある（実データでは `にかほ市金浦` の 1 件）。剥がすと残りは辞書に無い形になり、その名前の読みが二度と当たらない（記録も残らない）。読み上げ文の読点に頼らない ―― 気象庁が書いた文も同じ経路を通る／**キャッシュキーに助詞のカナを含める**＝含めないと同じキーの別の助詞で先に焼いたものを引く／**文字列連結で句数が変わらないことは実測で確かめ直す**＝`npm run verify-particle-phrases`（要 VOICEVOX）。辞書を作り直したときと助詞を足したときに回す（句が増えれば助詞が独立した句として浮き、直そうとした症状がそのまま戻る）。**CI では代われない**ので自動テストは実装の筋道までしか固めていない／**列挙は外部依存を持たないモジュールに置く**＝`utils/ttsTrailingParticles.ts`。辞書の読み込み側は読み込むだけで Vite の注入値を評価するため Node から import できず、同じ場所に置くと検証が列挙を実装から取れない／**辞書の組み直しで例外が出てもチャンクを落とさない**＝組み直しだけ諦めて素の読みで鳴らす。落とすとそのチャンクが無音で消える） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §3「助詞は辞書の読みへ取り込む」 |
| 読み上げを鳴らす直前の見直し（`shouldStillPlay`。渡しているのは EEW だけ・判定はチャンク単位・鳴っている途中のチャンクは切らない・第 2 フェーズは誤報取消・確定値の引き上げ・**確定していない引き上げが安定待ちに入ったとき**の 3 つで打ち切り、第 1 フェーズは誤報取消のみ・自動解除と値の引き下げでは止めない。**区分の格上げだけは別経路**で音を直接止める＝次行） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §3「鳴らす直前の見直し」・§6 |
| VOICEVOX 接続先の確認（`VOICEVOX_URL_DEBOUNCE_MS` で入力が落ち着くまで待つ・`isValidVoicevoxUrl` で通信前に検分・`apiBase` で末尾スラッシュを落とす・試聴も確認済み URL へ送る。検分は入力途中の値を弾く役割を持たない／**設定の変化で通信する経路はすべて同じ前置きを通す**——接続確認と切り出し語の作り置きの 2 つで、後者は設定タブの外にあり画面にも出ないため見落としやすい） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §3「接続先の確認」 |
| 設定タブの試聴文を実運用と同じ関数から組むこと（`voicevoxPreviewTexts`。文字列を直接書くと、文型を変えたとき試聴だけ古い形で残る／**発話ごとに分けて渡す**＝`speakSequentially`。繋げると実運用に無い間が挟まる／**前の文が止められたら降りる**＝降りないと、緊急地震速報の読み上げを試聴の続きが上書きしうる） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §3「試聴で読ませる文」 |
| チャンク末尾の間を渡す入口が 4 つあること（先行合成・切り出し語の作り置き・再生側の先頭チャンクの作り直し・再生中の次チャンクの先行合成）。**同じ句を作りうる入口どうしは必ず同じ値を渡す**（作り置きと作り直しは同じ句を焼くため、片方が渡し忘れるとキャッシュを先に埋めた側で間が変わる） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §3「チャンク末尾の句読点には間を足す」 |
| 通知音と声の間に合成を先行させること（`prewarmVoicevox`。進行中の読み上げを止めない・使われなかったものは打ち切る・失敗時は再生側で作り直す。対象は非 EEW の遅延経路） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「間を合成の時間に充てる（先行合成）」 |
| EEW の切り出し語を作り置きすること（`warmFixedPhrases`。**合成中のものは待たない**・切り出し語と `splitIntoChunks` の分割条件が噛み合わないと黙って効かなくなる・**接続先は §3「接続先の確認」の前置き（入力の落ち着き待ちと通信前の検分）を通すこと**——設定の変化で走る数少ない経路で、生の値を使うと打鍵ごとにリクエストが飛ぶ） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「間が無い経路には作り置きを用意する（切り出し語）」 |
| 間を置く読み上げの予約を追跡すること（`scheduleSpeech`。画面を閉じたときとリプレイ開始で取り消す。対象は EEW 誤報取消を含む全経路） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「間を置く読み上げの予約は追跡する」 |

### 読み上げ: 順番と割り込み

| 項目 | 単一情報源となる仕様書 |
|---|---|
| 読み上げの優先順位（EEW ＞ 津波の等級発表・南海トラフ ＞ 地震情報・長周期・**津波の観測情報**（同格）／同格は新しい方が勝つ／待ちの条件は毎周回で作り直す／上限到達時は記録を残す／EEW の予想震度の確定待ち中も EEW を最優先とする） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「読み上げの優先順位」 |
| EEW 読み上げ第 2 フェーズの発火条件（震度・長周期階級は続報のたびに直前の値と比較する**安定待ち**を経て確定する・震度の安定待ち時間は気象庁震度階級の並び順上のインデックス差（跳躍幅）で可変・階級は震度と独立に安定を待つ・安定待ちの上限（`EEW_PHASE2_STABILITY_MAX_WAIT_MS`）は値が変わるたびにリセットしない・震度が先に確定したら階級を待たず即読み、階級が先に確定したら震度の確定を待つ・区分の格上げと予想震度が付かない理由が判っているケースは安定待ちを経由せず即座に確定する・`EEW_PHASE2_MAX_WAIT_MS` は「値そのものが無い」場合専用の別の上限・**確定値が既に「予想震度なし」のときは張り直さない**（「一度でも確定したか」で判定しない）・引き上げも初報と同じ形で言い直す・「警報。」の前置きはその EEW で初めて伝えるときだけ・**合成が 1 音も鳴らなかった発話は既読にしない**＝既読は発話の直前に記録したまま、鳴らなかったと分かった時点で自分が書いた分だけ戻す（`SpeechOutcome.spoke` / `rollbackSpokenEntry`。第 1・第 1.5 フェーズも同じ）） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6 |
| 古くなっていた予想値は読まずに確定を待つこと（確定値より高い**震度**が安定待ち中なら第 2 フェーズは黙って降りる・待つのは震度だけで階級の安定待ちでは止めない・引き上げ方向だけ見る・**安定待ちサイクルを確定させずに捨てる場所を足すときは同じイベント処理の中で確定経路を張り直すこと**＝張り直しを欠くとその EEW の予想震度が無言で終わる／**同じ判定を「鳴っている途中」も続ける**＝発話の順番が来た時点だけで見ると、震度の句を鳴らし終えた後の階級の句が古い震度の文脈のまま鳴り切る（安定待ちはチャンク 1 つの再生より長い）／**降りたら、その発話が進めた既読のうち値（震度・階級）だけを戻す**＝譲った先の値が確定するとは限らず、戻さないと階級が一度も声にならない。**区分の告知は戻さない**——先頭チャンクにあり 1 音でも鳴っていれば声になっているので、戻すと前置きを二度読む） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6 |
| 予報から警報へ上がったときの伝え方（第 1 フェーズの進み具合で 3 通り・どの経路でも「緊急地震速報」の語は必ず声になる・**止めるのは音だけで発話の順番は崩さない**・**言い直しは 1 回だけ**・**進み具合の記録は自分が置いた分だけ消す**（書き換えは `updatePhase1Progress` に集約）・**合成が 1 音も鳴らなかったら区分を伝えた扱いにしない**（`rollbackSpokenEntry`。残すと以後の格上げが一度も声にならない）・震源の大幅更新と予想値の引き上げでは割り込まない。後ろの 3 つはいずれも外すと別 EEW を巻き込むか同じ文言を重ねる） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「予報から警報へ上がったとき — 進み具合で 3 通り」 |
| EEW 読み上げの直列化（全 EEW で 1 本のチェーン・既読値の更新は発話直前だけ） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「読み上げの優先順位」 |
| 内容が重ならない同格は互いに待つこと（`MUTUAL_YIELD_TOPICS`。対象は津波・津波の観測情報・南海トラフ臨時情報・後発地震注意情報／**同主題は例外**（言い換えなので割り込む）／片方が対象なら両方向で待つ／上位には切られる／待ち上限は専用の値（`MUTUAL_YIELD_SPEECH_MAX_WAIT_MS`）を使い、上位を待つ 90 秒と混ぜない／**待つ理由が変わったら計時をやり直す**（持ち越すと、始まったばかりの EEW を「もう待った」と見なして切る）／**取り下げの理由にしない**（待たせる）） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「内容が重ならない同格は互いに待つ（相互譲り）」 |
| 「何も切らない」層は待ちきれなくても割り込まないこと（解説情報のみ。待ち直しループを抜けた経路でも判定する・見送るときは予約の枠から降ろす（降ろさないと前の予約を巻き込む）） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「割り込みを許す条件」 |
| 到来順を守ること（予約より後に届いた重い読み上げに追い越されたら取り下げる・優先度に例外を設けない・既に読み始めていた場合は割り込みの側の話・**同格どうしは到来順の連番で裁き、主題が同じときだけ取り下げる**（主題は地震・長周期がイベントごと／津波は等級の発表と観測情報で分ける／南海トラフ系は種別で 1 つ・**同じ主題に別の優先度を混ぜない**）。優先度が上の相手には主題を問わず取り下げる・取り下げた予約は枠から降ろす／**判定は間が明けた時点だけでなく「声に出す直前」まで続ける**＝待ち行列に入った後の到来を見る場所が他に無い／**EEW を到来順へ載せるのは新規発報のときだけ**＝続報や第 2 フェーズで進めると、先に届いていた EEW が「後から来た」ように見えて順番どおり待っていた地震情報を取り下げる／**優先度の枠だけは鳴り始めた時点でも降ろす**＝残すと鳴り終わった読み上げが永久に「追い越した側」になり、本体より後に予約される解説情報が群発の最中ほぼ常に沈黙する（主題の枠はそこでは降ろさない。問いが違う）／**取り下げても「一回性で画面のタブを動かさない処理」は残す**＝いまの対象は推計震度分布図の分布モードを開く最後の機会だけ） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「到来順を守る — 追い越されたら取り下げる」 |
| 南海トラフ関連解説情報の読み上げ優先度（独立した層に置くこと。臨時情報と同格にすると臨時情報を追い出し、地震情報と同格にすると地震情報を切る） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6 |
| 警報の対象地方を**予想値の前に**伝えること（第 1.5 フェーズ。`ttsReadEewWarningRegions`・既定で有効／**予想値の読み上げを遅らせることが目的でもある**＝発話の順番が遅くなるほど新しい確定値で読める／**読む中身は「電文の全地方 − 声にした地方」**＝電文の `LastKind` は前報からの差分なので使わない。「新たに」を冠するかどうかも「声にしたか」で決める／**地方名の既読へ記録するのは読み切った分だけ**＝途中で降りた発話は入れない。**格上げの告知（前置き）は発話の直前に記録する**＝読み切ってからでは、この発話がチェーンの待ち上限（8 秒）より長いときに第 2 フェーズへ追い越され、記録が空のまま前置きを重ねられる（2024-06-03 06:31 の石川県能登・対象地方 6 つで実際に 2 回鳴った。**いまは音が出ている間その上限を計時しないので追い越されないが、合成が詰まって打ち切られる場合が残るため記録は前倒しのまま**）。戻すのは 1 音も鳴らなかったときだけで、**地方が増えて降りた回も誤報取消でも戻さない**（後者は取消が受信時に記録ごと消しており、重ねて消すと再発報した新しい発話の記録まで巻き込む）。**地方名は誤報取消を受けていたら記録せず、判定は書き込む直前の状態と発話中のフラグの両方で行う**（理由は仕様書）／**鳴っている最中に地方が増えたら打ち切って読み直す**＝実配信に 0.45 秒差で増えた例がある／**降りるのは誤報取消と「地方が増えた」ときだけ**で、自動解除では鳴らし続ける／**予報から警報へ上がった報ではこの文が格上げの告知を兼ねる**＝地方は警報級の報にしか入らないので必ず最初の告知になる。区分を第 2 フェーズの前置きだけに任せると、そちらは安定待ちを経るぶん後になり順序が入れ替わる。**語を譲っても予想値の読み直しは譲らない**＝記録は `spokenEEWLevelsRef`（予想値を読み直す契機）とは別に持つ。混ぜると「何の震度で警報になったか」の再確認が消える／**リプレイの復元でも既読にする**＝ここだけ漏れると、その EEW について他は何も声にしないのに地方だけ鳴る／実機で格上げの前置きを出せるのは設定タブ「単独点処理テスト」だけ／**既読は「実際に鳴ったか」まで見る**＝合成が 1 チャンクも成功しなくても `speakWithVoicevox` は例外を投げずに正常終了するので、戻り値（`SpeechOutcome.spoke`）を見ないと 1 音も出ていないのに既読が進む。第 1・第 2 フェーズは発話の直前に記録する設計なので、鳴らなかったと分かった時点で自分が書いた分だけ戻す（`rollbackSpokenEntry`）。非 EEW はチャンクの予約から数えるので元から影響しない） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「警報の対象地方を、予想値の前に伝える」 |
| 気象庁が書いた文（本文・付加文）を**電文本体とは別の発話にして最下位の層で読む**こと（本体の末尾へ足さない＝南海トラフ地震臨時情報の本文は実電文で 1055 字・読み上げ約 3 分あり、臨時情報は津波の等級発表と同じ**高**にいるので地震情報が最大 90 秒待たされる。その待ちは音が出ている間は計時しないので、**本文を読み切るまで待たされる**／**主題を本体と分ける**＝同じ主題にすると到来順の裁きが本体の予約を取り下げ、本文を読むために震度・等級を落とす／**本体より後に発火させる。順序は「予約の順」では決まらない**＝待ち合わせは「いま鳴っているものがあるか」で判定するので、本体が通知音の余韻を待っているあいだは空席に見える。間 0 で予約すると本文が先に鳴り出し、あとから本体に割り込まれて**同じ地震の情報が 2 回流れる**（実機で起きた）。間は本体の最大（`maxTtsDelay()`）＋余白／予約自体も本体の処理が済んでから（本体が抑制した電文で本文だけ声になるのを防ぐ）／**既読は「読んだ文そのもの」で持つ**＝電文やイベントを鍵にすると、P2PQuake 経路の鍵は電文の `id` を含むため続報のたびに別の鍵になり効かない（同じ「＊印は…」を報のたびに読む）／**鍵は本文まるごとではなく 1 文**＝未読の文だけを繋いで読み、全部が既読なら読まない。本文全体を鍵にすると 1 文の増減で既に読んだ分まで読み直す（津波の避難行動の固定付加文は等級が動くたびに節が増減し、能登の実電文では 812 字 → 924 字 → 798 字で 3 回読まれていた。2 通目で新しいのは 28 字、3 通目は 1 文も新しくない）。**後読みの正規表現で割らない**＝対応しない実行環境では読み込みの時点で落ち、アプリ全体が動かない。**鍵と読み上げの素材を分ける**＝本文は改行を空白へ直したものなので文のあいだに空白が残る。鍵は前後を削り、読み上げには空白ごと使って元の間を保つ／**読み上げのマスタートグルを必ず見る**＝設定タブでは読み上げが無効だとこの項目自体が見えないので、ガードを省くと「切ったつもり」の端末で声が出る／**参考情報と見出し文は読まない**＝前者は情報の種類を説明する固定文、後者は本文に無い事実を含まないので二度述べになる／**緊急地震速報の固定付加文は対象に含めない**＝秒を争うので、定型文を挟むと肝心の震度・地域が遅れる。読ませるなら「震度・地域を伝えたあとへ確実に回す」別の設計が要る／**読む前に整える**＝URL は落とし（音声では書き取れない。括弧で囲まれていれば括弧ごと）、**事象によらずほぼ同じ定型文は設定で 1 項目ずつ落とせる**（`TELEGRAM_BOILERPLATE_KEYS` が単一情報源の 4 項目＝`＊` の説明・長周期地震動階級の目安表・津波の高さの解説・緊急地震速報の発表告知）＝**既定はいずれも落とす側だが、根拠は項目ごとに違う** ——`＊` の説明はこの設定より前からの無条件の抑止をそのまま保った側で、**残る 3 項目は「設定を入れる前の挙動」から外れる新しい判断**（元は読み上げていた）。**落とす単位が 2 種類あること・判定を文字列で行う理由・落とし漏れの検出が効く範囲・テストボタンのデータを実電文の全文にする理由は仕様書 §6 を見ること**（ここへ写すと片方だけ古くなる）。長周期地震動の「階級と現象表現の対応表」には区切りを挟む＝**空白であって読点ではない**（合成エンジンが置く間は同じだが、読点はチャンクを割る文字なので割れると間が 0.11 秒へ縮む）。挟む鍵を「階級＋数字」だけにしない＝文中の「階級４を観測した」で助詞の前が切れる） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「気象庁が書いた文は最下位の層で読む」 |

### 通知音

**音を 1 つ作り変えるときは、この節の「通知音の内容と説明文の一致」と「通知音の長さと読み上げまでの間の連動」を必ず両方見ること。** 前者は音そのもの（§2）、後者は音が終わってから声を出すまでの間（§6）で、参照先の節が違う。片方だけ直すと、説明文が実際の音とずれるか、間が音の長さと合わなくなる。

| 項目 | 単一情報源となる仕様書 |
|---|---|
| 通知音の内容と説明文の一致（`PLAYERS` の周波数・`COUNTDOWN_PULSES` のパルス数と、設定タブ「通知音テスト」の説明文・仕様書の記述） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §2 |
| 震度更新音の段（`kyoshinLevel`。**8 段で気象庁の階級と 1 対 1**・まとめてよいのは震度0〜2 だけ・**音を選ぶだけでなく検知音とタブ移動と揺れフォーカスの起動条件を兼ねる**ので畳んだ範囲では悪化が伝わらない・上位 3 段は音数でも音量でも聞き分けにくいため音程で分ける・震度7 だけ 880Hz の外（1047Hz）へ出す・**1047Hz より上へは上げない**） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §2「震度更新音の段は気象庁の階級と対応させる」 |
| 通知音の音量の決め方（**種別ごとの個別の値を書かない**・`BASE_GAIN`（系統）×`SEVERITY`（深刻度・3 dB 刻みの 5 段）の積だけで決める・震度更新音は `BEEP_SEVERITY` が段を刻み `BEEP_PATTERNS` は音量を持たない・S 波カウントダウンも同じ設計に載っている・**同じ値でも音数と重なりで音圧は変わる**ので、揃えるために片方の値だけ動かさない） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §2「種別ごとの音量は「系統 × 深刻度」で決める」 |
| 津波の段階を掃引の形で聞き分けること（大津波警報だけ `peakAt` 0.88 の上昇主体・他は 0.55 の往復／**弁別を担うのは折り返し点と 1 周期の長さで、周波数ではない**（大津波は津波警報の音域を完全に含む）／どちらかを揃えるとテストで止まる） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §2「掃引の形の分け方 — 大津波警報だけ上がりっぱなし」 |
| 通知音の系統の分け方（7 つのプリミティブ＝`pianoNote`（地震そのもの）/ `darkPiano`（EEW の穏やかな報）/ `darkAlarm`（警報級）/ `sweep`（津波の等級）/ `marimba`（揺れを検知した）/ `warningBeep`（揺れが強まっている）/ `ding`（値が動いた・状況が変わった）のどれかに属させる・**分け方の軸は役割で音色の好みではない**（役割が既存のどれとも違うなら増やすほうが正しい）・**残響は全系統で持たない**（連続して鳴る音で尾が重なり濁るため廃した。豊かにする目的で戻さない）・フィルタを使うのは `marimba` と `warningBeep` だけ・減衰の終端は `decayTone` に集約し種別ごとに分岐させない・アタックに広帯域ノイズを戻さない） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §2「系統の分け方 — 7 つのプリミティブ」 |
| 地震回数の音を地震情報の音と分けること（`earthquakeCount`＝純音の同音 3 連 C5。**同じ純音の系統にいる南海トラフの 2 つとは音程の動きで分ける**＝臨時情報は上昇・解説情報は下降・こちらは動かさない／**ピアノ（地震そのもの）を借りない**＝同じ音にすると、群発の最中に地震カードが増えたのか回数が更新されただけなのかが音で判らない） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §2 |
| 通知音の長さと読み上げまでの間の連動（間は**電文の種別ではなく音の種別**で決まる＝`ttsDelayFor`。音を作り変えたら `SOUND_AUDIBLE_END_MS` を測り直す・**測るのは絶対値でピーク比ではない**（ピークの低い音ほど深く沈むまで待つ計算になり、実際に聞こえる時間から大きくずれる）・余白を足すのは掃引だけ（尾を持つ音は尾に声を重ねてよい）・音量設計を変えたら基準の絶対値も引き直す） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6 |

### 自動タブ切替と画面の追従

| 項目 | 単一情報源となる仕様書 |
|---|---|
| 自動タブ切替の 3 系統（読み上げ追従／電文の受信／保持機構）と競合の判定順序・優先度表 | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「自動タブ切替の優先順位」 |
| 気象庁が書いた文を読み上げているあいだ、画面のその表示を開くこと（**追従の枠は 3 本目として別に持つ**＝`telegramTextFollow`。津波カードの追従と未入電モードは別の参照を見る門を持っており、相乗りさせると気象庁の文を読むたびに津波カードが動く／**セッションの有無だけを見る**＝開く対象が本文まるごと 1 つなので「いまどの箇所を読んでいるか」は要らない。**タイマーは張らない**（非表示のタブで滞留して開いたまま残る）／どの電文の文かはセッションの `subject` が持つ（`telegramTextSubject`）＝参照に種別を持たせると既読の記録へ混ざる／**自分が開いた分しか閉じない**＝手で開いていたものを読み終わりで閉じると、見ようとしていた中身を奪う。読み上げ中に手で閉じたらその読み上げのあいだは開き直さない／津波の面は**等級が動いた報の「閉じる」が優先**＝あちらが先に閉じて読み上げの持ち物からも外す／**対象外は地震情報の付加文だけ**＝あれは元から畳んでいない。**長周期は畳んであるので対象**（「気象庁からの補足」）——「畳んでいるか」は種別ごとに実装を見て決める（まとめて扱って長周期を外していた）／**長周期だけ主題に地震の識別子まで載せる**＝地震カードは複数並ぶので種別だけではどのカードを開くか決まらない。「いま選ばれているカード」で代用しない／**開閉の状態は呼び出し側が持てる**＝`useAutoOpenWhileSpeakingIn`。地震カードの補足は他の行と同じ入れ物に乗っているので、判定を書き写さず状態の持ち主だけ差し替える／**地震カードへ渡す主題を任意の props にしない**＝渡し忘れても画面が動かないだけで例外もログも出ない） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「読み上げに合わせて気象庁の文を開く」 |
| 読み上げに合わせた津波カードの追従（視野内なら動かさない・送り先にこれから読む箇所を含める・等級カードの頭は「前置き」として扱い**読んでいる箇所に混ぜない**（混ぜると等級ごとにしか動かなくなる）・追わない 4 条件（非表示／折りたたみ／手動操作 30 秒／引き当て前）・現在位置は rAF で解決し `setTimeout` を張らない・通知の例外で読み上げを止めない） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「読み上げに合わせたカードの追従」 |
| 読み上げが未入電を伝えているあいだ、地震カードの「震度を入手していない地点」を自動で開くこと（**門は津波の追従と別に持つ**＝`hasUnreceivedFollowTarget`。`hasFollowTarget` へ足すと地震情報の読み上げが津波カードを動かす／**対象はセッションが持つ主題で決め、「いま選ばれている地震」で代用しない**＝選択は受信した瞬間に動き、読み上げの番とは独立しているので、代用すると A の未入電を読みながら B の一覧を開く／**開く範囲は実データから求める**＝「未入電は文の最後」という並びに頼ると、文を組み替えたときに黙ってずれる／**判定は純関数へ置き、「開かないことを選んだ」と「開くべきなのに開けない」を分ける**＝`decideUnreceivedSpeechOpen`。正常な見送り（別のタブを見ている・他の追加表示が開いている）まで記録すると診断が埋まる／**他の追加表示は奪わない**＝3 つは排他なので開くと相手が閉じ、閉じる番は元へ戻さない／**自分が開いた分しか閉じない**＝開けたときだけ覚え、閉じる相手も鍵で照合する（`closeUnreceivedOverlayFor`）／**範囲を抜けたら閉じる。読み上げの終わりを待たない**＝後ろに続く文のあいだ、関係のない話をしている間ずっと地図とカードの一覧が未入電だけの画になる／**タイマーを張らず rAF で解決する**＝非表示のタブでは**タイマーが**間引かれて後から発火する） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「読み上げに合わせた未入電モードの自動開閉」 |
| 津波カードのスクロールを動かす 3 駆動の関係（自動で見せたときの先頭復帰＝最も弱い・**タブが実際に変わったときだけ**・手操作の保持は尊重／受信時スクロールは読み上げが有効なら追従を待つが、**待ちに上限を置き、寄せ先が無い受信は待たない**。上限を外すと VOICEVOX 未起動・区域コード不一致の端末でカードが一切動かず、解除でも待つと解除前の位置に留まる） | [`docs/spec/tsunami-spec.md`](docs/spec/tsunami-spec.md) §9「スクロールの自動制御」 |
| **「寄せ先が無い」と「先頭へ戻せ」を同じ値で表さないこと**（区域の一覧が空、という 1 つの形に兼ねさせると、寄せ先を作れなかっただけの受信が先頭戻しとして扱われる。**各地の満潮時刻・津波到達予想時刻に関する情報**は区域も観測点も動かさないので必ずそこへ落ち、**沖合の津波観測に関する情報**は新しい観測点を持つのに沖合の観測点が津波予報区を持たないため寄せ先が空になる。どちらも直前の報や読み上げが合わせた位置を捨てる／先頭へ戻すのは解除・新規発報・等級が変わった報だけ／**既定の状態への復帰（アイドル復帰など）はこの判断に含めない**＝受信ではなくタブ切替なので「自動で見せたとき」の駆動が担う（上の行）／**判断に既定値を置かない**＝受信の種類ごとに決めるので、経路を足したときの決め忘れを型検査で捕まえる） | [`docs/spec/tsunami-spec.md`](docs/spec/tsunami-spec.md) §9「寄せ先が無い受信」「既定の状態への復帰」 |
| 読み上げ追従の引き当て失敗を記録する条件（**等級は数えない**。等級のカードは常に登録済みで必ず引けるため、数えると診断が二度と発火しない） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「対象の特定」 |
| EEW 続報の片方向抑制（**止めるのは受信時要求だけ**で、実際に声に出す瞬間の追従は素通りすること。両者は駆動源では見分けられないため「いま声になるか」を別の印として渡す）・**読み上げが無効な端末の受信は続報の保持を越えること**（駆動源 `receipt`。`eewUrgent` は越えない／読み上げ文が空になった電文は `hold` へ回す＝読み上げ中の EEW から画面を奪わない）・追従の最小滞留時間・アイドル復帰の重み（`idleRevertPriority`）・生の `setActiveTab` を使わないこと・**駆動源と追従の印に既定値を置かないこと**（要求を出す側（`requestAutoTab`）の話。判定する側が既定値を持っているのはテストを読みやすくするため）。**症状は一様でない**——追従の印を渡し忘れると「タブが動かない」だけだが、駆動源を取り違えると逆に「読み上げ中の EEW から画面を奪う」側にも倒れる。どちらも例外もログも出ない | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「自動タブ切替の優先順位」 |
| 既定の状態へ戻す操作の保持は読み上げ追従に譲ること（上の行の重み `idleRevertPriority` とは別の話。`forceTab` に駆動源 `idleRevert` を渡す。渡し忘れると「喋っているのに画面が動かない」が復活する） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「自動タブ切替の優先順位」 |
| 読み上げている間は既定の状態へ戻さないこと（**対象は `revertToDefaultTab` を通る 4 経路すべて**＝アイドル復帰・EEW 全解除・揺れ検知終了・揺れの可能性の失効。アイドル復帰は realtime へ留める分岐も見送る＝通すと EEW が生きている間ずっと引き戻される／判定は `shouldDeferRevertWhileSpeaking` に集約し、呼び出し元ごとに書かない／**読み上げ追従では取り返せない**＝追従が画面を要求するのは発話を投入する瞬間の 1 回だけ。症状は「声と画面が食い違う」に留まらず、未入電モードが一度も開かない／**拾い直す仕掛けを 3 つ置く**＝①自動復帰が無効な端末（`idleRevertSec` が 0 以下）では見送らない（拾う仕組みが無く、戻る契機が 1 つも残らない）②読み上げの終わりで計り直す（`onSpeechIdle`）③読み上げが 5 分を超えて終わらなければ読み上げ中の扱いを解く（`SPEECH_STALE_MS`。真へ張り付くと既定の状態へ戻る仕組みごと止まり、例外もログも出ない。**起点は読み上げが始まるたび引き直す** —— 読み上げは割り込み方式で本数が 0 を通らないため、1 本ごとに測ると群発で読み続けている最中に解ける）／**見送り・計り直し・安全弁の記録は別の文言で残し、見送りには呼び出し元の理由を添える**＝`revertToDefaultTab` は理由を必須の引数で受け取る（4 経路の記録が同じ文言だと、どれが止まったのか分からない）） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「読み上げている間は既定の状態へ戻さない」 |
| 読み上げがある経路のタブ移動（順序の判断は読み上げ側に預ける／待たされずに読めるなら通知音と同時に先出しし、その判定は待ち合わせと条件を共有する／読み上げが無効・読み上げ文が空になる場合だけ受信時要求へ落とす。落とし忘れるとそのタブへ永久に移らない／**先出しで見せた画面は揺れ検知から取り返さない**が、見送るのは揺れ検知が張った保持だけで、**追従の呼び出し自体は省かない**——省くと近接して届いた電文どうしで画面が戻らなくなる） | [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「自動タブ切替の優先順位」 |
| 特別情報の受信でパネルを一時的に開くこと（バナーはタブを持たないため）・元の状態へ戻す契機（ユーザー操作とアイドル復帰）・自動タブ移動では畳まないが記録も捨てないこと | [`docs/spec/architecture-spec.md`](docs/spec/architecture-spec.md) §4 |
| **アイドル復帰は震源カタログからは既定タブへ戻さないこと**（眺めているだけの時間は操作として数えられず、実測 1 分ほどで画面ごと持っていかれる）。**EEW・揺れ検知への移動は従来どおり**（進行中の揺れは過去の記録より優先する） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §16「眺めている間はタブを奪わない」 |

### 地震情報

| 項目 | 単一情報源となる仕様書 |
|---|---|
| `points` のバリアント差・`pref` 空の識別規則を取り違えると、経路によって観測点や区域が欠落する | [`quake-spec.md`](docs/spec/quake-spec.md) §4「points 構造（バリアント・経路差）」 |
| 観測点名の `＊` を外し忘れる・戻し忘れると、地図から点が消える、または電文と違う表記になる | [`quake-spec.md`](docs/spec/quake-spec.md) §8「気象庁以外が運用する観測点（`＊`）」 |
| 固定付加文（その他）を省くと、`＊` 印の説明が画面から消え何と対比しているか分からなくなる | [`quake-spec.md`](docs/spec/quake-spec.md) §8「固定付加文（その他）（`VarComment/Text`）はそのまま出す」 |
| 電文のコード値（都道府県・区域・市町村・観測点コード等）を画面に出すと、利用者に意味の無い気象庁内部識別子が露出する | [`quake-spec.md`](docs/spec/quake-spec.md) §8「電文のコード値は読むが、画面には出さない」 |
| 震源の補助要素・`Revise` の読み取りを 1 か所（`readHypocenterAreaLabels`）に集約しないと、経路ごとに震央補助表現や続報の変化が抜け落ちる | [`quake-spec.md`](docs/spec/quake-spec.md) §8「震源の補助要素と続報での変化は、読んで持つだけ」 |
| 震度5弱以上未入電を階級・地図・カードのどこかで一般の震度と同列に扱うと、最も強い地点や区域が画面・地図・音声のいずれかから消える | [`quake-spec.md`](docs/spec/quake-spec.md) §4「震度5弱以上未入電」 |
| 震度点が積めなかったことの記録条件（全滅時のみ）を誤ると、大多数が脱落し数件だけ残る形の障害に気付けない | [`quake-spec.md`](docs/spec/quake-spec.md) §4「地図には観測値と混ぜずに出す」内「読めなかった点は記録する」 |
| 取消の述語を種別間でそろえないと、同じ取消という事実なのに画面と読み上げ・種別ごとに言葉が食い違う | [`audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「取消は「取り消された事実」だけを伝える」 |
| 取消電文の理由（`Body/Text`）を状態更新で移し忘れると、画面にだけ理由が出ない（型検査・他のテストは通る） | [`quake-spec.md`](docs/spec/quake-spec.md) §8「取消しの理由は電文にしかない」・[`audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「取消の宣言だけの本文は読まない」 |
| 緊急地震速報の精度・最大予測値変化を資料の語のまま出さないと、確度の意味や変化理由が読者に伝わらない、または誤った評価を足すことになる | [`eew-spec.md`](docs/spec/eew-spec.md) §3「震源要素の精度」「最大予測値の変化」 |
| 市町村の震度を `points` に混在させると、カード・地図・読み上げの3種判別が一斉に狂い、未入電の意味も区別できなくなる | [`quake-spec.md`](docs/spec/quake-spec.md) §5「市町村の震度（`Pref/Area/City`）」 |
| 受信した電文種別の見出し表記を誤ると、震源情報を受け取った事実が画面から消える、または受信通数が水増し・過小に出る | [`quake-spec.md`](docs/spec/quake-spec.md) §8「見出しには受け取った種別を並べる」・[`settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §7 の一覧「種別遷移テスト」 |
| 震度一覧の4段構成・並び順・配下描画の規則を崩すと、画面（県単位）と音声（区域単位）の粒度がずれる、または一覧が数千行に膨れて操作不能になる | [`quake-spec.md`](docs/spec/quake-spec.md) §8「震度一覧は 4 段の入れ子」 |
| 押せる判定と寄り先の解決を別々に実装すると押せるのに動かない不具合になり、寄り上限に絶対上限のクランプを誤って適用すると押した観測点が区域塗りに隠れて選べなくなる | [`quake-spec.md`](docs/spec/quake-spec.md) §8「一覧の行をクリックしたときの寄り先」・[`map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §6「寄り上限と閾値の単位」 |
| 観測点の市町村・区域の紐付けを電文の並び順読み取り以外に頼ると、同名市町村での観測点混在や未入電の印が親の県まで届かない食い違いが起きる | [`quake-spec.md`](docs/spec/quake-spec.md) §5「観測点がどの市町村・区域に属するか」 |
| テストデータの静的 import を1つでも戻すとコード分割が無言で解除され、Service Worker のプリキャッシュ上限超過が本番ビルドでしか発覚しない | [`settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §7「テストデータは押されてから読む」 |
| 訓練・試験報の印付け（`Control/Status`）は2点守ること——①`EEWAlert.test` 抑制フラグと混同しない（混同すると試験報が画面に出なくなる）②全パース関数に通す（漏らすと訓練報の印が電文種別によって出たり出なかったりする） | [`quake-spec.md`](docs/spec/quake-spec.md) §5「電文の運用種別」 |
| 震源要素不明の電文を捨てる、またはセンチネル値（`-200`）の判定を `Number.isFinite` 等で代用すると、震度速報並みの重要情報が丸ごと消える | [`quake-spec.md`](docs/spec/quake-spec.md) §5「震源要素不明の電文は捨てない」 |
| `LgCategory` の分類番号をそのまま表示する、または値2・4の意味を書かないと、揺れは強くないのに高層階が大きく揺れた地域があるという事実が伝わらない | [`quake-spec.md`](docs/spec/quake-spec.md) §8「長周期地震動の「観測情報の種類」は意味を出す」 |
| 周期帯（`PeriodicBand`）の対応付けを文書順で決め打つと帯欠落の電文で以後すべてがずれる、階級0を落とすと短周期しか観測していないように誤読される | [`quake-spec.md`](docs/spec/quake-spec.md) §8「長周期地震動は「どの周期帯が強く出たか」まで出す」 |
| 長周期地震動の階級と震度は片方だけを拾わないこと——片方だけ拾うと、揺れが小さいのに高層階が大きく揺れた地域を見逃す | [`quake-spec.md`](docs/spec/quake-spec.md) §8「階級と震度は並べて出す」 |
| 長周期地震動の一覧の3段構成（県→区域→観測点、市町村段は混ぜない）を崩すと、親子関係を取り違え観測点の所属区域が消える | [`quake-spec.md`](docs/spec/quake-spec.md) §8「長周期地震動の一覧は 3 段の入れ子」 |
| 長周期地震動のテストデータを手組みすると、新項目を足しても実機で一度も画面に出ないまま気づけない | [`quake-spec.md`](docs/spec/quake-spec.md) §8「テストボタンのデータは実電文から作る」 |
| 推計震度分布図（BUFR二進電文）の読み取り・地震カードとの結び付け（発現時刻・300km判定）・初報続報の言い分け（`isNewEstimatedIntensity`）・古い報の排除規則のいずれかを崩すと、分布が別地震のものへ混ざる、または初報続報を正しく言い分けられない | [`quake-spec.md`](docs/spec/quake-spec.md) §8「推計震度分布図」・§9「震度分布モード」・[`audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「推計震度分布図は地震情報の音を借りる」・[`map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §19 |
| 地震回数に関する情報（VXSE60）で空欄と0回を混同すると、群発の最中に地震は起きていないと伝わる | [`quake-spec.md`](docs/spec/quake-spec.md) §8「地震回数に関する情報」 |
| 地震・津波に関するお知らせ（VZSE40）を音・読み上げ・`onLiveEvent` へ流すと、運用連絡が本物の情報のように扱われる | [`data-sources-spec.md`](docs/spec/data-sources-spec.md) §2「扱う電文種別」・[`architecture-spec.md`](docs/spec/architecture-spec.md) §2「副作用への入口（`onLiveEvent`）」 |
| 扱わない電文種別（VXSE56・WEPA60）の判断理由・配信終了種別の棚卸し範囲を書き残さないと、次の棚卸しで同じ調査をやり直すか配信終了種別を存在しないと誤認する | [`data-sources-spec.md`](docs/spec/data-sources-spec.md) §2「扱う電文種別」 |
| 利用者へ出す語を気象庁の公式表現から理由なく外すと、短縮が意味を狭めて誤読を招く（「警報等」を「津波警報」と書くと大津波警報の地震が一段軽く見える） | [`quake-spec.md`](docs/spec/quake-spec.md) §3「「警報等」を「津波警報」と書かない」・§8「気象庁の表現からわざと外している語」 |
| 地震の同一性判定（`eventKey`）を経路ごとに再実装すると、同じ地震が別カードへ割れるか取消済みの報が復活する | [`quake-spec.md`](docs/spec/quake-spec.md) §6.1「同一イベントの判定」・§6.2「取消のマッチング」 |
| 続報マージで震源・震度・付加文の引き継ぎ規則を取り違えると、確定した震源が消える／取り下げた値を出し続ける／確定した安全判断が定型文で巻き戻る | [`quake-spec.md`](docs/spec/quake-spec.md) §6.4「置換時の項目の引き継ぎ」 |
| 取消電文の前後判定（`isRetractedQuakeReport`）を誤ると、取り下げ済みの報を復活させる／別地震の報を握りつぶす | [`quake-spec.md`](docs/spec/quake-spec.md) §6.2「取消の後に届いた報」 |
| 据え置き判定を副作用側（音・読み上げ・タイトル・タブ切替）へ反映し損なうと、画面は据え置いたままなのに音や声だけが誤った内容を伝える | [`quake-spec.md`](docs/spec/quake-spec.md) §6.3「据え置いた電文は、音・読み上げ・タイトル・タブ移動も起こさない」 |
| 震度一覧の行の並びを電文の到着順に依存させると、気象庁側の電文書式が変わるだけで画面の並びが無言で動く | [`quake-spec.md`](docs/spec/quake-spec.md) §8「震度一覧は 4 段の入れ子」内「行の並び」 |
| 続報の更新印（欄・行）の色分け・比較対象・自動展開の規則を取り違えると、動いていない箇所が光る／本当に動いた箇所が埋もれる | [`quake-spec.md`](docs/spec/quake-spec.md) §8「カードの更新の印」・[`eew-spec.md`](docs/spec/eew-spec.md) §4「続報で動いた欄を示す（減衰）」 |
| 震度の区域集約閾値を自動フィットの寄り上限と別の値に固定すると、大きな画面でフィットしても震度塗りが出ない | [`quake-spec.md`](docs/spec/quake-spec.md) §7.1「ズームによる集約の切替」 |
| 震度の面・区域塗り・観測点ドットの排他条件を1つでも変えると、発表値と推定値が同じ画面に混ざって見分けが付かなくなる | [`quake-spec.md`](docs/spec/quake-spec.md) §9「QuakeIntensitySurfaceGL」 |
| 観測点0件の電文（震度速報等）でズームに応じて個別表示へ切り替えると、意味の無い単一ドット表示になる | [`quake-spec.md`](docs/spec/quake-spec.md) §7「地図描画の切替」 |
| VXSE61の座標をtype属性ではなく桁数で判定すると、度単位の丸め値を震源座標に採用してしまい座標も深さもずれる | [`quake-spec.md`](docs/spec/quake-spec.md) §3「震源要素更新（VXSE61）の座標」 |
| 規模が数値にならない電文の説明（`description`）を数値の有無だけで不明と出すと、M8超えの最大級の地震ほど画面が不明の一語になる | [`quake-spec.md`](docs/spec/quake-spec.md) §8「震度一覧は 4 段の入れ子」内「値が不明なときの表示」 |
| 訂正報の訂正内容を固定付加文（`VarComment/Code`）以外から推測すると、震度だけの訂正を表すコードは無いのに震源訂正と取り違える | [`quake-spec.md`](docs/spec/quake-spec.md) §6.2「取消のマッチング」内「訂正の扱い」 |
| 震度速報（VXSE51）の規模を `0` にすると、`hasMagnitude(0)` が真になり実測値のように表示・読み上げされる | [`quake-spec.md`](docs/spec/quake-spec.md) §5「震源未確定（震度速報）の扱い」 |
| 震源・津波区分を津波電文から借りる条件（`eventId` 一致限定・統合の後に置く・借り物の印を値ごとに持つ）を崩すと、別地震の震源が混ざるか印が消えて追随しなくなる | [`quake-spec.md`](docs/spec/quake-spec.md) §5「震源・津波区分を津波電文から借りる」・§8「借りた値の出どころ」・[`audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「津波が原因地震の震源を語るとき」・§6「読み上げに合わせて借りた震源のカードを見せる」 |
| 遠地地震の識別（VXSE53・`Head/Title`）を誤ると、震度を伴わない電文が国内地震と同じ扱いになり震度欄が壊れる | [`quake-spec.md`](docs/spec/quake-spec.md) §3「遠地地震に関する情報」 |
| 付加文の固定（区分が変わらない限り動かない）と自由（続報で動く）を取り違えると、区分が変わっていないのに文面が続報で書き換わる、または本文が更新されているのに画面が古いままになる | [`quake-spec.md`](docs/spec/quake-spec.md) §3「津波の付加文（固定と自由の 2 種類）」 |

### 津波

**読み上げ文の組み立て方は上記「読み上げ」の 3 節にある。** この節にあるのは、そこから参照される並び順・既読の管理・電文の判定。

| 項目 | 単一情報源となる仕様書 |
|---|---|
| 津波の観測点の並び順（カードと共有。**選抜と並びは別物**） | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §9「観測点の並び順」・[`audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「観測点は「選抜」と「並び」を分ける」 |
| 並べ替えに渡す材料はカードと同じものにする | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §9「並べ替えに渡す材料はカードと同じものにする」・[`audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「津波の区域の並び順」 |
| 読み上げた観測点の記憶を画面用と分ける（上限で読まなかった到達確認を既読にしない） | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §10「読み上げた観測点の記憶は画面用と分ける」 |
| 波高の表示文字列（`description`）は半角へ揃える | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §6「波高の表示文字列は半角へ揃える」 |
| 観測波高の深刻さの順（「◯m 以上」は値より優先） | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §6「観測波高の「以上」」・[`audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「観測点は「選抜」と「並び」を分ける」 |
| 観測波高・最大波時刻が**動いたか**を等値比較で見ない | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §6「観測波高の「以上」」・[`tsunami-spec.md`](docs/spec/tsunami-spec.md) §9「どの項目が動いたかを文字色で示す」・[`tsunami-spec.md`](docs/spec/tsunami-spec.md) §10「変化を伝えない続報」 |
| 観測点の行でどの項目が動いたかを文字色で示す | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §9「どの項目が動いたかを文字色で示す」 |
| 数値にならない予想波高（「巨大」「高い」）を落とさない | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §9「数値にならない予想波高」 |
| 「Ｍ８を超える巨大地震」を規模不明と区別する | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §4「DMDATA XML」 |
| 原因地震（`Earthquake`）は全件読む（同一性の判定は 1 件目だけ） | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §4「DMDATA XML」 |
| 沖合から沿岸への推定（`Estimation`）を実測・発表値と混ぜない | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §4「DMDATA XML」・[`tsunami-spec.md`](docs/spec/tsunami-spec.md) §9「沿岸への推定の行」 |
| 「観測中」のまま津波警報相当を落とさない | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §6「「観測中」のまま津波警報相当」・[`audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「「観測中」のまま津波警報相当は別の文で伝える」 |
| 「重要」（`MaxHeight/Condition`）の基準は電文で 4 通り違う | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §6「「重要」の基準は電文で違う」・[`tsunami-spec.md`](docs/spec/tsunami-spec.md) §4「DMDATA XML」 |
| 区域の到達状況（`Forecast/Item/FirstHeight`）の 4 形態 | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §9「区域の到達状況」 |
| 予想波高「あり／なし」の判定（`hasForecastHeight`） | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §9「予想波高の有無」 |
| 区域を読めなかった結果を「正式解除」と取り違えない | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §4「DMDATA XML」 |
| 津波電文の `Body/Text` は発表報でも読む | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §4「DMDATA XML」 |
| 津波の等級は `Kind/Code` で読む | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §4「DMDATA XML」 |
| 津波の解除経路（`cancelReason` 3 種・バリアント差） | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §3「3 経路の解除フロー」 |
| 有効期限は報ではなく津波に付く事実として扱う（引き継ぎを 1 つ漏らすと失効しない津波ができる） | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §3「有効期限は報ではなく津波に付く」 |
| 区域単位で等級が動いた報は、動いた区域だけを伝える | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §10「区域単位で等級が動いた報」・[`audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「区域単位で等級が動いた報は、動いた区域だけを読む」 |
| 解除された区域（`Kind/Code` が 00/50/60）を捨てずに分けて持つ | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §10「解除された区域」・[`audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「区域単位で等級が動いた報は、動いた区域だけを読む」 |
| 等級を伝えていない電文（区域が空の続報）は観測点更新として扱う | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §10「等級を伝えていない電文」 |
| 等級も観測波高も動かない続報でも黙らない | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §10「変化を伝えない続報」・[`audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「読み上げの優先順位」 |
| 観測状態（欠測・微弱・観測中・重要・上昇中）の見分けは `isObservationMissing` に集約する | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §6「観測状態」 |
| 欠測は到達確認と別の文・別の既読で読む | [`audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §4「欠測は到達確認と別の文で伝える」 |
| 波高がまだ出ていない観測点（「観測中」）も地図に出す | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §8「到達確認マーカー」・[`map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §6「津波追従の目標範囲」 |
| 観測棒に白フチと影を持たせる（丸バッジの家族へ揃える） | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §8「白フチと影」 |
| 観測点の行・区域名をクリックしたときの可否・当たり判定・寄り先・着地の深さ | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §9「観測点の行・区域名をクリックしたときの寄り先」 |
| 震源の位置要素（`Hypocenter/Area`）は 1 箇所で読む | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §4「DMDATA XML」 |
| 地震発現時刻（`ArrivalTime`）と地震発生時刻（`OriginTime`）を混ぜない | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §4「DMDATA XML」 |
| 最大波の観測時刻（`MaxHeight/DateTime`）は語を冠して出す | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §9「最大波の観測時刻」 |
| 実測の到達時刻を出せていない行に予報側の到達予想を添える（条件は生の値でなく整形の結果で見る） | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §9「実測の到達時刻が無い行に添える到達予想」 |
| 観測点の座標表（`tsunami-obs-coords.json`）は生成物 | [`data-sources-spec.md`](docs/spec/data-sources-spec.md) §6「津波観測点の座標」 |
| 津波の解除で観測点の記憶を落とす条件 | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §5「解除電文と表示中の津波の照合」・[`tsunami-spec.md`](docs/spec/tsunami-spec.md) §10「読み上げた観測点の記憶は画面用と分ける」 |
| 気象庁が書いた文（本文・付加文 2 種）はバナーの行動指示の行から開く | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §9「気象庁が書いた文は、行動指示の行から開く」 |
| 続報のマージで前報から引き継ぐもの（規則は `mergeTsunamiReports` の 1 箇所） | [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §5「続報で前報から引き継ぐもの」 |

### 緊急地震速報（EEW）

**EEW を地図にどう描くかは下記「地図」の 2 節にある**（震源の点滅と深さへの配置・予想の区域塗りとカメラ追従対象の一致・追従の引き上限）。この節にあるのは電文の解釈と計算。

| 項目 | 単一情報源となる仕様書 |
|---|---|
| **EEW だけは地震の時刻に発生時刻（`OriginTime`）を出していること**（地震情報・津波・長周期は発現時刻へ揃えてあるが、**気象庁が種別ごとに違う時刻を「地震の時刻」として示している**ため。揃えるのは値ではなく**その種別で気象庁が示すものに従う**という規則で、一貫して当てると 2 種別で値が違うのが正しい／**実測では全報がずれていた**＝1 日分・VXSE45 の 91 通で一致 0 通・ずれ 1〜18 秒。**画面も秒まで出す**ので食い違いは全報で見える（**測るときに VXSE44 を混ぜないこと**＝同じ内容が対で届くので母数が倍になる。アプリはパーサーへ渡さない）／**`originTime` は表示専用ではない**＝発生時刻からの経過で決まる値（予報円・区域への S 波到達・自動解除・別地点発報の距離閾値）と、「どれを最新とみなすか」の並び順、リプレイの取得候補の絞り込みもこの値を読む。**読む場所の一覧は仕様書が単一情報源で、ここへ写さない**＝別のオブジェクトへ写して運ぶ経路があり `grep` だけでは数え直せない／根拠の全文・測り方も仕様書側） | [`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §3「地震の時刻は発生時刻を出す」・`originTime` を読む場所は同節の表 |
| 起動時に発表中の緊急地震速報を復元すること（**DMDSS 版限定**＝P2PQuake に発表中のものを取る経路が無い／取得は `/v2/gd/eew`＝`/v2/telegram` は緊急地震速報を保持しない／**1 地震につき電文は 1 通だけ**取る＝要るのは「いま有効か」の判定で最新報 1 通で決まる。全報を取ると起動が目に見えて遅れる／**有効性の判定はリプレイの初期状態と共有する**＝`selectActiveEews`。二重に持つと再生と実機で挙動が食い違う／訓練・試験の報は復元しない／**音も読み上げも鳴らさない**＝`silent` を立ててキューへ積むと状態と自動解除の予約だけ通る。画面は別の口（`onStartupRestore`）で要求する／**「初回接続だけ」に絞らない**＝StrictMode の二重実行で 1 回目に消費され、開発モードでだけ画面の要求が出なくなる（本番では出るので気づけない）／1 件の詳細取得が失敗しても他を巻き込まない） | [`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §2「起動時に、発表中のものを復元する」・タブ要求は [`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6「起動時の復元が出す要求」 |
| EEW の続報で古い報を採らないこと（同じ秒に複数の報が届き、処理順が入れ替わりうる。**捨てるのは入口**——通知は状態更新の外で走るため、そこで弾かないと画面と音声が食い違う／**判定は受理した報番号の台帳で行い、表示中の状態を見ない**（同一ティックでは状態が進まない）／報番号を持たない経路では判定しない／取消・失効・テスト報は通す） | [`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §10「古い報で退行させない」 |
| EEW の区分の呼び方（電文の名称に合わせる。予報級＝地震動予報／警報級＝緊急地震速報（警報）。表示・読み上げの対応表と `eewKindLabel`。ウィンドウタイトルも対象＝外部監視への破壊的変更） | [`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §3「電文の名称と表示・読み上げ」 |
| 種別ヘッダーに添える報番号の見た目を地震情報カードと共有すること（`components/SerialBadge.tsx`。別々に書くと片方だけ変わる／**揃えるのは見た目だけ**＝こちらは電文が名乗る報番号、地震情報カードは受け取った通数） | [`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §3「報番号の見た目は地震情報カードと共有する」 |
| 上限を定めない予想（**`over` / `99` を階級値に写さない**。下限へ寄せ、上限が定まらないことはフラグで持ち越して表示・読み上げで語を補う。判定には混ぜない／**長周期地震動階級にも同じ `over` が来る**＝震度側だけ直すと `parseInt("over")` が NaN になって長周期の予測が丸ごと消える／**語は「程度以上」**＝気象庁の表現。未入電の「以上」とは別物なので関数を分ける（`getIntensityLabelWithApproxAbove` / `getLpgmClassLabelWithApproxAbove` と `getIntensityLabelWithOrAbove`）／**比較に「程度以上」を含める**＝安定待ちと既読の 2 つの門の**両方**に要る。数値だけで比べると、上限が定まらなくなった続報（安全側の変化）が無音で消える。語は確定値と一緒に持ち回り、現在の報から引き直さない） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §8（電文上の表れ）・[`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §4（使い方・語を出す箇所）・[`docs/spec/audio-tts-spec.md`](docs/spec/audio-tts-spec.md) §6（既読の覚え方＝比較にも「以上」を入れる） |
| 仮定震源要素で連動して隠す・採らない箇所の列挙（**新たに `condition` を参照したら必ず追記する**。区域への S 波到達推定の震源もここに含む）・**予想震度と長周期階級は捨てないこと**（気象庁が発表しない条件に該当すれば電文に値が入らないので受信側で潰す必要がない。潰していた頃は震度3以上の予想が「予想震度なし」に落ちていた）・**「単独観測点処理」と同義ではないこと**（定義に観測点数は含まれない） | [`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §5 |
| EEW P/S 波予報円の計算・仮定震源要素の連動箇所 | [`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §5-§6 |
| 距離をすべて球で解くこと（地球半径は `utils/geo.ts` の `EARTH_RADIUS_KM` に一本化・**距離を測る側と距離から点を作る側で同じ値**）。**`√(地表距離² + 深さ²)` を書かない**（`hypocentralDistanceKm` / `surfaceDistanceKm` を使う。深発・遠距離で 3% 効く）／**屈折波はモホ面沿いの距離を地表の弧へ換算する**（しないと直達波だけ球で混在）／予報円の頂点・外接矩形・破線の本数は**同じ `ringVertex` を通す**（別々の近似だと円と矩形がずれる）／平面投影での南北の伸びは図法の性質なので消さない | [`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §6「距離はすべて球で解く」 |
| 警報の対象地方（`Head/Headline/Information` の地方予報区ブロック）を読むこと（**`Body` には無い**＝`Body/Intensity/Forecast` は府県予報区 → 細分区域の 2 階層で地方の単位を持たない。見出しの構造化を読む唯一の箇所／**警報級の報にだけ入る**＝実電文 240 通で「ブロックがあること」と「警報が出ていること」が例外なく一致（警報 0 の 527 通は 1 通も持たない）／**`Item` の分かれ方（新規／既存）は使わない**＝前報からの差分なので取りこぼすと一緒に落ちる。何を新しく伝えるかは読み上げ側が「声にしたか」から決める／値域は 14 種／**続報で増える**＝4 年で 3 例・間隔 0.45 秒〜59 秒／**DMDSS 版限定**） | [`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §3「警報の対象地方」 |
| EEW の固定付加文（`Comments/WarningComment/Text`）を読むこと（**画面にだけ出し読み上げには載せない**＝秒を争うため定型文を挟むと震度・地域が遅れる／取消電文では出さない） | [`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §3「固定付加文」 |
| EEW の警報級判定を種別コードで行うこと（`isEewWarningKindCode` の 1 か所。**同じ 3 値を電文の解釈・地図の塗り分け・画面の県名の振り分けで書き分けない**／名前（`Kind/Name`）を見るのはコード表に無い値のときだけで、警報を取りこぼさない安全側へ倒して記録する） | [`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §4「区域の種別コード」 |
| 区域に主要動が届いたかを `isEewAreaArrived` で判定すること（**電文は同じ事実を 2 通りで伝える**＝区域の `Condition` と種別コードの下 1 桁。`Condition` を配信するのは DMDATA だけなので、`EEWRegion.arrived` を直接見ると standard 版で到達済みの区域が画面から黙って消える／到達済みの区域は到達予測時刻を持たないため、時刻の有無だけで絞ると落ちる／**片方しか無いことを異常として記録しない**＝資料が排他だと定めているのは `Condition` と `ArrivalTime` のあいだだけで、コードと `Condition` が必ず同時に出るとは書かれていない。鳴らすと正常な電文でログが埋まる） | [`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §4「主要動が届いたかどうか」 |
| 「主要動の到達（予測）」欄に**どの区域をどの順で並べるか**（**件数で打ち切らず電文の区域を全部出す**＝実電文で 6 件超が 27.7%・最大 56 件あり、6 弱以上の報では 97% が溢れる。高さは CSS `columns` の折り返しで吸収する／**並びは時間の順**＝到達済み → 未到達（残り秒数の昇順）→ PLUM 法。**PLUM を末尾へ置く**＝時間の軸に乗らないものを途中へ挟むと前後だけが時間順になる／**震度スケール外の値でも区域を落とさない**＝弾くと到達の情報ごと消える） | [`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §4「到達予測時刻は種別によって意味が変わる」 |
| 同じ欄の**各行に何を出すか**（**絶対時刻ではなく残り秒数**＝引き算を読み手にさせない。20 秒以内は色を変える（`ARRIVAL_SOON_SEC`）／**PLUM 法（種別コード 09/19）の `ArrivalTime` を「到達予想」として出さない**＝値は「その震度を初めて予測した時刻」で過去の時刻。判定は `isEewPlumKindCode`／**日時として読めない時刻を「まもなく」へ混ぜない**＝`NaN > 0` が偽なので壊れた値が「もうすぐ来る」に化ける。記録は報ごとに 1 行へまとめる（毎秒描き直すため）／**震度バッジの幅を固定しない**＝「6強程度以上」が折り返してその行だけ高さが崩れる／**内訳の「未到達」に PLUM 法の区域を数える**＝`Condition` が無い区域を到達済みとして扱えない） | [`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §4「到達予測時刻は種別によって意味が変わる」 |
| 「対象地域」欄（「警報:」「予報:」）の出し方（**警報に出した府県予報区を予報の列へ重ねない**＝種別コードは府県予報区より細かい細分区域ごとに付くので同じ地域が両方に現れる。並べると警報の地域が一段軽く見える／**件数で打ち切らない**＝能登本震の最終報は警報 21・予報 14 あり、先頭 6 件で切ると過半が「...」の中へ落ちる。すぐ下の「主要動の到達（予測）」と同じ形／**「県」を機械的に補わない**＝府県予報区は県名と一致しない） | [`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §4「「警報:」「予報:」欄の出し方」 |
| EEW レベル判定（特別警報の条件・長周期の DMDATA 限定） | [`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §4 |
| **「予想が変わった」の帯は表示の寿命を持つこと**（電文はこの変化を 1 通しか言わず**次の報は値を 0 に戻す**ので、報の状態をそのまま描くと 1 秒で消える／最低 10 秒は出し続け、新しい変化が来たら差し替えて計時し直す／**計時をやり直す契機は報番号**＝`eew` の参照は「新しい報が届いた」ことの代理値にすぎず、報の到着以外でも EEW は作り直される／取消では出さないが、**隠しているのは `cancelledAt` の明示判定**で `forecastChange` は状態更新のマージで残る／長さは**実時計**で測る＝リプレイの再生時計には乗せない／テストボタンのデータも実電文の形（第 1 報は要素なし・2 報目で立ち・3 報目で 0）にする） | [`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §3「最大予測値の変化」 |

### 強震モニタの揺れ検知

| 項目 | 単一情報源となる仕様書 |
|---|---|
| 判定の根拠開示（**数値の確信度を作らない**＝エンジンは確率モデルではなく決定的なゲート判定／`gates` を判定の入力にしない・書くのは確信度を決め終えた後の 1 箇所だけ／イベント自身が持つ値（`lastSize`・`maxIntensity`・`confirmStreak`・`everNeighborRise`）を `gates` へ複製しない／**確定したものの根拠は凍結（`confirmedBy`）から採る**＝減衰後は現フレームの内訳から復元できない／**「秒読み中か」を確信度より先に見る**＝確定の条件は周囲の裏付けを見ないが likely の条件は見るため、faint のまま秒読みに入る／併合では確定時刻の早いほうの内訳を採り、`intenseCount` はバーが揃うときだけ合算する／診断ログ側も**同じ基準**（確定時刻の早いほう）で選ぶ＝到着順にすると画面と記録が黙って食い違う／**折りたたみの中も利用者が読む文字列**として扱い、エンジンの内部用語（「フレーム」「spread」「近傍一致」）を出さない） | [`docs/spec/kyoshin-detection-spec.md`](docs/spec/kyoshin-detection-spec.md) §8「判定の根拠を見せる」 |
| `KyoshinSubThreshold` の対象範囲（index 1〜6）・慢性ノイズ床フィルタ（**床は観測点と同じ並びの配列で渡す**。座標をキーにすると同一座標の 2 つ目以降が別センサーの床で判定される／位置で使う前に、その床がこの観測点配列に対して計算されたものかを参照で照合する＝床は次にデータ時刻が進むまで更新されない／**検知結果を位置で消費する経路を新しく足すときは、同じ照合が要らないかを確かめる**） | [`docs/spec/kyoshin-detection-spec.md`](docs/spec/kyoshin-detection-spec.md)・[`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §4「取得と再生の分離」 |
| 単点のまま居座る確定の降ろし方（**発報の条件は変えない**——鳴らすこと自体は正しく、問題は取り下げられないこと・数えるのは点数ではなく**隣り合う対があるか**（併合でメンバーは和集合になるため）・**高震度では対の相手にも値を要求する**（`SOLO_PAIR_HIGH_INTENSITY` 以上のイベントに `SOLO_PAIR_MIN_INTENSITY` 以上。張り付いた点の隣で微動しているだけの点も成分になるため、座標の隣接だけで見ると降ろす契機が永久に消える／**低い震度では要求しない**——隣が追いつくのに時間がかかる本物の初動を巻き込む。実測と、この対策が塞げていない範囲は設計書§36）・**状態を書き換えず述語で覆う**（書き戻すと猶予の周期で明滅し、検知音が鳴り直す）・確信度を書く箇所は `updateEventMetrics` と `mergeAdjacentEvents` の **2 つ**あるので判定を集約する） | [`docs/spec/kyoshin-detection-spec.md`](docs/spec/kyoshin-detection-spec.md) §4 |
| 欠測の瞬断を直前値で保持する範囲（表示・カード・音は保持値／検知エンジンは生値・保持時間・保持中の見せ方） | [`docs/spec/kyoshin-detection-spec.md`](docs/spec/kyoshin-detection-spec.md) §8 |
| 検知結果を出せないフレームが続いたときの扱い（**理由（例外／観測点数と震度の件数の食い違い）でカウンタを分けない**——表示から見れば同じ「凍結」で、分けると交互に起きたとき解けない／空にすると `confirmed` が落ちるため、**復帰時に同じ地震で音と通知が鳴り直す**。検知エンジンが `stalled` を返し、発報側は**結果が戻った最初のフレームで検知が立ち、かつ場所が途絶前と揃っているか**で続きと判断する＝**経過時間で測らない**（起点がずれる・時計が別物・窓を閉じ忘れる）／**場所の照合を省かない**（途絶が短いとエンジンは状態を組み直さないので、復帰フレームで別の地震が確定しうる。強い地震ほど 1 フレームで確定する）／物差しは `MERGE_EVENT_KM`（重心どうしを 1 地震とみなす距離。`REGION_MATCH_KM` は「1 つの地震がどこまで広がるか」の値で広すぎる）／**1 つでも離れた地点が混じれば鳴らす側へ倒す**・**揺れが収まって消えた場合は抑えない**（直後の別の地震で鳴らなくなる）） | [`docs/spec/kyoshin-detection-spec.md`](docs/spec/kyoshin-detection-spec.md)「検知結果を出せないフレームが続いたとき」「発報ロジック」・[設計書§35](docs/spec/kyoshin-detection-v3-design.md) |
| 供給が始まる前のフレームを検知エンジンへ渡すこと（**助走**。立ち上がり判定は「しきい値を超えたか」ではなく**エンジン自身が持つ履歴の中での上昇量**で決まるので、始めた時点で既に揺れていると捉えられない＝実データの 164 通りの開始時刻のうち **143 通りで「検知なし」**・段階と最大震度が揃ったのは 21 通りだけだった／**リプレイの開始時とアプリの起動時の両方に効く**＝開くのは揺れた瞬間ではなく収まりつつある頃のほうが多い／**どの供給元も `prefill` を必ず 1 度は呼ぶ**＝呼ばないと受け取る側が上限まで待ち、検知が沈黙する／**助走が届くまで通常フレームを食わせない**＝先に食わせると助走が「時刻の巻き戻り」として落ちる／**待つのは時間軸が変わったときだけ**＝供給元の識別子で待つと、ローカル履歴アーカイブの範囲を跨いだだけで検知が凍結する／**待たせたフレームは観測点集合の識別子で照合する**＝長さの検証だけでは点数がたまたま一致したときに通る／**同じデータ時刻を二重に積まない**＝助走は別経路で届くので、データ時刻が変わらないまま助走だけ届くレンダーが必ず起き、待たせた最後のフレームの検知結果が画面へ出なくなる／**遡った結果は必ず記録する**＝取得の全滅と「静穏で打ち切った」正常系は戻り値が同じ空配列で区別が付かない） | [`docs/spec/kyoshin-detection-spec.md`](docs/spec/kyoshin-detection-spec.md) §7「助走」・[`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §4「助走」 |
| リプレイで時間軸が変わったときに強震モニタの値を落とすこと（**落とすのはレンダー中**＝エフェクトまで待つと、その 1 コミットのあいだ旧い軸の震度が下流へ流れ、地震と EEW が消えたことで動いた判定がそれを読む（行動チェックリストの帯が新しい軸の画面に出て、上書きする地震が届くまで消えなかった）／**落とすのは震度・データ時刻・その識別子と更新停止の表示だけ**＝観測点座標を空にすると取り直しが走らず復帰しなくなる・学習資産は観測点に紐づくので残す／**`localArchiveId` の切替では落とさない**＝供給元が替わるだけで再生時刻は続いており、落とすと同じ地震の揺れの最中に検知音が鳴り直す／**空にしたことを `stalled` で伝えない**＝あれは上流異常の語で、受け取る側が判定を保留し次の検知音を省く／**検知エンジンには専用の経路が要る**＝データ時刻が空だと `step()` の手前で抜けて結果が凍結する／別地点発報のエピソード状態も落とす（時刻を前へ飛ばす切替では自動で解けない）） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §6「強震モニタは自分で落とす」・[`docs/spec/kyoshin-detection-spec.md`](docs/spec/kyoshin-detection-spec.md) §7「供給が作り直されたとき」 |
| 欠測の保持値を捨てる条件の順序（**位置の対応が壊れたこと（観測点集合・件数の変化）はデータ時刻を見ずに判定し、時刻のガードより前へ置く**＝供給の作り直しは「件数 0・データ時刻なし」の形で届くので、後ろに置くと素通しして旧い軸の保持値が蘇る） | [`docs/spec/kyoshin-detection-spec.md`](docs/spec/kyoshin-detection-spec.md) §8「欠測の瞬断は直前値を保持して描く」 |
| 検知点マーカー（地図）と検知カード（リアルタイムタブ）が数える点集合・下限の一致（対象イベントは `weak` 以外の全件／下限は震度0以上／点列は App が用意した 1 本を共有する） | [`docs/spec/kyoshin-detection-spec.md`](docs/spec/kyoshin-detection-spec.md) §8 |
| 検知カードの震度分布バーの幅スケール（見えている間は分母を下げない・見えていない間だけ現在値へ張り直す・可視判定はタブ／パネル折りたたみ／ページ可視性の3つ） | [`docs/spec/kyoshin-detection-spec.md`](docs/spec/kyoshin-detection-spec.md) §8 |
| 「別地点で揺れ検知」の抑制条件（EEW 吸収の記憶・エピソード起点・距離の上限・震度の下限・発報済みの扱い） | [`docs/spec/kyoshin-detection-spec.md`](docs/spec/kyoshin-detection-spec.md)「別地点」判定の動的距離閾値 |
| 「別地点で揺れ検知」の 3 つの窓（持続・クールダウン・破棄猶予）を**フレーム数ではなくデータ時刻で測る**こと・持続は観測回数ではなく初検知からの経過で見ること・破棄をフレーム末尾に置くこと | [`docs/spec/kyoshin-detection-spec.md`](docs/spec/kyoshin-detection-spec.md)「別地点」判定の動的距離閾値 |

### 地図: 描画するもの

**レイヤーを 1 枚足すときは、下記「地図: カメラ・投影・表示閾値」も併せて見ること。** 表示範囲（`minzoom`/`maxzoom`）の決め方とカスタムレイヤーの投影対応はそちらにある。震度の面と区域塗りを重ねない規則は「地震情報」節。

[`map-rendering-spec.md`](docs/spec/map-rendering-spec.md) の実装列挙のうち 4 つは [`scripts/mapRenderingSpecLists.test.ts`](scripts/mapRenderingSpecLists.test.ts) が `npm test` で実装と突き合わせるので、手で数え直さなくてよい——§3 カスタムレイヤーの id・§6 投影ごとのプログラムが要るファイル・§12 コンテキストロストから載せ直すコンポーネント・§13 の `gl/` ファイル構成。見るのは**両方向**で、足したものを一覧へ書き忘れても、消したものが一覧に残っていても落ちる。
**§7「mode 別レイヤー一覧」は名前が実在するかだけで、どの mode に出るかは手作業のまま**（JSX の条件分岐から機械的に導けない）。ほかの節の列挙（§8 の毎秒更新レイヤー・§13 の `gl/` 以外の行など）も手作業。

**`MAP_LAYER_ORDER`（描画順の単一情報源）への登録漏れは型で落ちる。** 忘れるとそのレイヤーだけ最前面に積まれ、描画順が静かに崩れる。仕組みと、カスタムレイヤーを作るときに使う型は [`map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §3。

| 項目 | 単一情報源となる仕様書 |
|---|---|
| 点滅する Marker を作り直さないこと（差分更新キーに**報番号を含めない**／内容が変わらないなら `innerHTML` を触らない。片方だけでは作り直しの経路が残り、点滅が止まってポップアップも閉じる） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §10・[`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §10（キーの取り方） |
| `maplibregl.Marker` の要素に `position` を書かないこと（MapLibre が CSS で与える `absolute` を上書きすると、マーカーが**DOM の並び順に下へ積み上がる**。**ずれはピクセルで固定なので、寄れば正しく見え、引くほど南へ流れて見える**／`cssText` への代入はスタイルを丸ごと置き換えるので、既定を残したいものは個別に書く／内側の `inset: 0` は根が `absolute` なら基準になる） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §10 |
| 地図レイヤー描画順・EEW 予想レイヤーの kyoshin 限定・`maplibregl.Marker` の opacity（**中の要素の CSS アニメーションと乗算される**。点滅する描画物を弱めるときは振幅も併せて決める） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §2・§3・§7・§10 |
| 読み込んだら変わらない形は**共有ソース 2 本**に相乗りさせること（`basemap-shapes` ＝陸地塗り・県境・区域境界線・区域名の当たり判定／`overlay-lines` ＝プレート境界線・活断層線。**分けているのはレイヤーの並び順が理由**で、あいだに震度の区域塗りが挟まる。理由: タイル被覆の再計算とクリッピングマスクの描画は**レイヤーの枚数ではなくソースの数**に比例して毎フレーム走る・描き分けは `kind` の filter・**`kind` は型を通して埋め込む**（GeoJSON の属性は何でも入るので、埋め込む側と filter 側で綴りが食い違うと**エラーも出さずにそのレイヤーだけ消える**）・共有ソースから降りるときは自分のレイヤーを先に外す（`removeSource` の失敗は例外ではなくエラーイベントの形でしか現れない）・**新しく静的なレイヤーを足すときは、どちらへ相乗りできるかを先に見る**） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §9「静的な形はソースを共有する」 |
| 観測点の値を面にするときの描き方（**不等間隔な値をそのまま補間しない**（階級値は計測震度へ写す）／**外挿しない**（最寄り観測点まで一定距離を超えたら値を作らない）／**陸だけに切り、規則は `evenodd`**（`pointInRings` と揃える。既定の `nonzero` だと巻き順が揃った瞬間に穴を抜けなくなる）／**格子は Mercator 空間で等間隔**（canvas source の貼り方に合わせる）／**座標は Canvas へ渡す前に確かめる**（`moveTo`/`lineTo` は非有限値を黙って無視するのでクリップの形だけが歪む）／**走査は「配る」側で書く**（セルごとに点を探すと密な地域で遅い）／**描けない理由が生じたら必ず消す**（途中で抜けると前の視野の絵が残る）・そのうち異常で出せない場合だけ記録する（入力はあるのに 1 件も使える値が無かった場合を含む）） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §17「観測点の値を面にする」 |
| 深さを持つ点の描画（震源・長期震源カタログ）。**地下の点は 2 つの理由で消える**（平面は far が地下を含まない／球は地表のタイルが深度を書いて地面に隠される）ので、クリップ空間の z を**手前の薄い帯**へ自前で写す／帯の下限は丸めない（near より手前は従来どおり切る）／**球では地球の中心を越えさせない**（越えると点が反対側へ写る。深さ 700km × 強調 10 倍で届く）／**表示用と判定用に同じ計算を入れる**／クリック判定はカラーピッキングで、**未解決（`'pending'`）を「何も無い」と区別する**（タッチ操作は `mousemove` を伴わないため）／柄と震央の印は**傾きによる縦の伸び**で隠す（深さ・誇張率・ズームも効くので傾きだけでは決まらない。**透視の視差を含まないので「画面上の実際の長さ」ではない**——真上では必ず 0 になり、それが意図した判定）／**球の裏側へ回った点は透けたまま残す**（`projectTileFor3D` が水平線のクリッピングを掛けず、z を帯へ書き換えるのも重ねて効く。遠地地震の震源は日本と同じ視野に入らないため、そのままにしている。裏を消す方へ転じるなら**可視面の判定は震央の位置で行う**——点の座標へ当てると手前にある深い震源まで消える） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §16「深さを持つ点」（柄と震央の印を隠す判定は同節「補助の点と柄」） |
| 長期震源カタログの点群（震源カタログタブ）。**点の入口は 2 つ**（オブジェクトの配列＝震源マーカー／列指向の型付き配列＝カタログ）で、**バッファへの書き込みは 1 箇所へ集約する**（`writePointInto`。属性を足したとき片方だけ直すと静かにずれるので、単体テストで `POINT_LAYOUT` と突き合わせる）／列指向では柄・補助点・点滅を持てない／**カタログは進行中の地震より背面**／**期間を変えたらマグニチュードの下限を完全性へ合わせる**（合わせないと古い時代が「地震が少なかった」ように見える。手で下げるのは妨げず注意書きを出す）／**付け替えてよいのは自分が置いた値だけ**（`withCompleteMagnitudeFloor` に `prev` を渡して判別する。手で上げた下限まで戻すと、期間のつまみを触っただけで絞り込みが無言で解ける。手で選ばれていたなら引き上げだけ行う）／深さの色は**平方根で浅い側へ寄せる**／発生年の色の分母は**絞り込みに残った点で測る**／色の位置の丸めは**真のときだけ通す形**で書く（`t <= 0 ? 0 : ...` は NaN が素通りして色が NaN になる）／**完全性の判定は期間の最も古い年で行う**（`oldestYearOf`。「開始」と「終了」は別々に選べるので `fromMs` を直に見ると、読んでいない年を基準に「網羅している」と答える。つまみは押し合わせて逆転そのものを作らせる前に潰す。合わせ直すのは最も古い「年」が変わったときだけ）／**期間は時刻で持つ**（`fromMs`/`toMs`＝日本時間の日境界。スライダーは年ごと・日付ピッカーは日ごとで、**動かしていない側の日付は保つ**。**期間を変える経路は `periodFromYearChange` / `periodFromDateChange` / `clampPeriodToRange` のどれかを必ず経由する**——不変条件（開始が終了を越えない・両端が日境界）を守っているのはこの 3 つだけ。日境界の数え方・年ファイルの取得単位・日付ピッカーの不正値の扱いは仕様書 §16「期間は日で持つ」）／**日付の入力欄は打鍵中の値をローカルに握る**（`DateField`。`<input type="date">` は打鍵の途中で `value` が空文字になるので、読めないからと状態を更新せずにいると制御された `value` が書き戻され、**打ちかけが巻き戻って「キーボードで入力できない」状態になる**。値を一括設定する自動操作では不完全な瞬間を経由しないため素通りする——**この種の確認は 1 文字ずつ打って行う**）／**スライダーは止まってから作り直す**（全期間で組み立て 16ms ＋ 転送 52ms。つまみとラベルは待たせない）／**遅らせる先は 2 つ**（点群の作り直しと**年ファイルの取得**。後者を繋いだままだと、つまみを引きずった途中の全ての年で取得が走り、発行済みの取得は打ち切られないのでドラッグ 1 回で全ファイルを取りに行く）／**絞り込みの端は「制限なし」として扱う**（`effectiveMaxDepthKm`。端の値で切ると、より深い地震が入ったとき黙って消える。見出しも「700km 以浅」と書かない——それより深いものは見られないと読める）／**絞り込みで選べる範囲は色が飽和する範囲と別に持つ**（`MAGNITUDE_FILTER_RANGE` と `MAGNITUDE_RAMP_RANGE`・`DEPTH_FILTER_MAX_KM` と `DEPTH_RAMP_MAX_KM`。借りると色の調整で絞り込みの届く範囲まで動く。**値がたまたま同じでも定数は分ける**）／**強調表示の倍率は M が 1 増えるごとに一定倍**（線形にすると上端ほど比が縮み、M2→M3 が 1.7 倍なのに M8→M9 は 1.17 倍にしかならず最大級どうしを見分けられない）・**起点の倍率を 0 にしない**（収録の下限 M2.0 で最も件数の多い地震が消える）・**起点の M は色の飽和範囲から借りない**（`MAGNITUDE_FILTER_RANGE.min` を見る）・**上限は M10 より先に置く**／**色は上端と深い側でも分ける**（マグニチュードは赤で止めずピンクへ抜き、飽和させる値は収録の最大 M9.0 より先／深さはシアン → 青 → 紫 → マゼンタ。飽和させると M8 と M9・350km 以深がそれぞれ同じ色になる）／**絞り込みは 5 つとも二つまみのスライダー 1 本**（期間・マグニチュード・深さ・緯度・経度。期間だけスライダーの下に日付ピッカーも置く。緯度・経度・深さで直方体を切り出せる。`RangeSlider`。`range` を 2 本重ね、本物の range のままにしてキーボード操作と読み上げを残す・**押し合わせて交差そのものを作らせない**・重なったら下端を上に置く・無効時はつまみを薄くする）／説明文はホバーで出す（設定タブと同じ `DescriptionTip`。**範囲の数値だけは常に出す**——つまみを見ただけでは値が読めない）／**隠すときは点を持たせたまま止める**（`setVisible`。空を渡すと戻るたびに詰め直す）／**離れている間は作り直しもしない**（`catalogCloudRef`。つまみを動かした直後に別タブへ移ると、待ちが明けたところで組み立てと転送が走り、移った先のフレームが飛ぶ）／**1 年の失敗で期間全体を捨てない**（`Promise.allSettled`・欠けた年は名指しで伝える・**まるごと取れなかったときは言い方を変える**＝`requestedYears` と突き合わせる。「そのぶん少なく」では 1 件も出ていない状態が「少し欠けている」と読める）／**取得に失敗したら前の期間の点を残さない**（残すと期間の表示と中身が食い違ったまま固定され、しかも読み込みが終わったように見える）／ポップアップの深さに `formatDepth` を使わない（整数の深さ向けで丸めを持たず、単精度の誤差が露出する） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §16「長期震源カタログの点群」 |
| 描画物の不調を画面に出すこと（`utils/renderHealth.ts`。**件数は JS 側だけで数えるので、描けていなくても正しく出る**——「275,554 件」と出したまま地図が真っ白になりうる）／**鍵はレイヤー ID と種別の組**（同じレイヤーが「描けない」と「掴めない」を同時に抱えうる。描けない側は毎フレーム報告するので ID だけだと掴めない側が必ず消える）／**表示名は報告する側が持つ**（集約側に対応表を置くと更新を忘れて ID が画面に出る）／**隠したら取り下げる**（`render()` は不可視だと手前で抜ける。**アンマウントに頼れない**——震源カタログは常時マウントで `visible` だけ切り替えるため、タブを離れた瞬間にバナーが固定される）／**直ったら取り下げる**（ログの 1 度きりとは別のフラグで持つ）／**`pick` の失敗も同じ枠で拾い、投げたソースだけ飛ばす**（全ソースを回す唯一の場所なので、1 つが投げるとどのレイヤーもクリックに応じなくなる。**守るのは `pick` だけでよい**——吹き出しの本文を組む処理は 1 件を確定させた後に呼ばれるので判定のループに波及しない）・成功に転じたら取り下げ・登録を外すときも取り下げる・**報告済みかどうかをストアの外に覚えない**（二重に持つと片方だけ消えたときに 2 度目以降が黙る）／**レイヤーを載せられなかったときも報告する**（載せられなければ `render()` が一度も呼ばれず、描画側の検出に永久に到達しない）／**`onAdd` は投げない**＝用意できなくても null を持って `render()` で判定する。投げると MapLibre がレイヤーを登録したまま残し（`Style.addLayer` は `_layers[id]` へ入れてから `onAdd` を呼ぶ）、載せ直す側の `if (map.getLayer(id)) return` に弾かれて**二度と作り直されない**（再読み込み以外に直す手が無いのに、症状は「そのレイヤーだけ出ない」で他の一時的な不調と見分けが付かない）／**取り下げは 2 系統**＝直った・隠したときは `clearRenderFailure`（その ID・その種別の 1 件だけ）、画面から外すときは `clearRenderFailuresFor`（種別も派生鍵もまとめて）。混ぜると**レイヤー自身の取り下げが `gl/guardRender.ts` の未解決の例外報告を巻き込み**、ガードは「自分が報告したか」をクロージャの中だけで覚えているので消されたことに気づかず黙り続ける（例外が続いていても画面は「描けている」と言い続ける） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §16「描けているかを画面に出す」 |
| EEW の震源も地震情報の震源と同じ仕組みで深さへ置くこと（**仮定震源要素だけは深さを採らない**＝確定していない数値を立体で断定しない）。点滅は**明・暗の 2 値と全点共通の位相**で作り、**電文の到着で位相を作り直さない**（続報の間隔が周期より短く、点滅が止まって見える）／**判定は明滅を見ない**（暗い位相でも掴めること）／**再描画は位相が変わる瞬間にだけ予約する**（毎フレーム要求すると 60fps で回り続ける） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §16「EEW の震源」「点滅」 |
| 同じ階級のバッジを画面の手前から並べること（`symbol-sort-key` と `symbol-z-order: 'viewport-y'` は**排他**なので、二段の並びは合成キーでしか作れない）。手前らしさは**方位と座標だけ**で決まるので再計算は回転時のみ／値の範囲は **±√2**（±1 と誤ると斜めの方位で階級を追い越す）／`styledata` で再適用するときは**中身が変わらないなら書かない**（`setLayoutProperty` が `styledata` を発火して無限ループになる）／対象レイヤーの点には `mercatorProps` を必ず持たせる | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §15「同じ階級なら画面の手前から」 |
| 夜の側（**濃さは画素ごとに太陽高度から引く**＝全球を覆う固定の格子を 1 枚描き、頂点が運ぶ経度緯度をフラグメントシェーダーへ渡す。太陽の位置は uniform なので**時刻が進んでも作り直すものが無い**／**式は JS と GLSL の 2 つの形で持つ**＝照合のため。画面に出た濃さが理論どおりかを確かめる手段が他にない／**残る段差はディザで散らす**＝連続にしても出力色の 8bit 量子化は残る。振幅は「結果色で 1 段ぶん」を目安に決め、**昼側を `discard` してからディザを掛ける**（順序を逆にすると昼側の境界に点が散る）／**覆う範囲は極まで伸ばす**＝MapLibre は球でも最上端のタイル行を極まで引き延ばすため、Mercator の限界（±85.05°）で切ると極の周りだけ夜が掛からない。緯度 90° は y が発散するので 89.99° で止め、**この区間の y は 0〜1 の外へ出るのが正しい**／**投影ごとにプログラムを持つ**（§6「地図の投影」）／**WebGL の文脈が復旧したら載せ直す**＝MapLibre はカスタムレイヤーを戻さないので、手当てが無いと夜の側だけ無音で消える。**プログラムも `onAdd` で捨てて作り直す**（無効なプログラムへの `useProgram` は例外を投げない。同じ手当てはキャッシュを持つ全レイヤーに置いてある＝§6）／**描き直す間隔は寄り具合から決める**＝太陽が画面上で何 px 動いたかで逆算し、下限 1 秒・上限 60 秒／**濃さの上限を 1 にしない**／段は高度ではなく**明るさが等間隔**になるよう置く／外側の端は地平線 0° ではなく**実際の日の入り**／**掛けるのは地形だけ**＝境界線と震度の面より背面に置く／時刻は `serverNow()` から取り、**有限でない値では太陽の位置を変えない**。**進められなかったときは描き直しの時刻も進めない**／**シェーダーを用意できないこととレイヤーを載せられないことは画面へ出す**（§16）） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §18「夜の側」 |
| ラベルのフォントスタック名の一致・グリフ収録文字の網羅性・フォント適用範囲（ラベル限定） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §5 |
| 地名ラベルとバッジの重なり回避（**退避は重なったときだけ**・平常時は代表点の真上・逃がせる上限は生成データの `room`・避けきれなければ薄くする（消さない）／**画面の外にあるラベルは問い合わせる前に外す**（判定は 1 件につき `queryRenderedFeatures` を 1〜3 回呼び、対象は常に全国 250 件前後。**縦の余白にだけ退避量を含める**——左右へは逃がさないため。左右への退避を足すならこの余白も直す。投影が壊れている・ペインの実寸が取れないときは「画面内」へ倒す）／**代表点の判定で自区域を除外しないこと**（除外すると避けたい相手が消えて退避が発火しない。除外は退避後の判定だけ）／座標と `room` は投影の前に形を確かめること（**1 件の破損が全ラベルの判定を止める**）・`room` は非負まで見ること／判定結果は `feature-state` ではなく `properties` で渡すこと（`text-offset` が layout プロパティで feature-state 式を受け付けない）） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §5「バッジとの重なりを避ける」 |
| 地名ラベルの重なり判定を走らせる間隔（再評価の**最小間隔 1 秒**はデバウンスとは別物・「前の判定が終わった時刻」の初期値を 0 にしない（`performance.now()` は経過 ms なので初回が 1 秒遅れる）・**1 巡を 1 フレームに収めない**（予算 5ms・書き戻しは全件そろってから 1 回だけ）・**`movestart` で進行中の巡回を捨てる**（分割して初めて生じる危険。予約を落とすだけでは足りず世代の番号で無効にする）・録画モードで止めるのは `overlapSignature` による再評価だけ（`moveend` とアイコン倍率の変更では走る。「`moveend` のときだけ」と書かない）） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §5「判定をいつ走らせるか」 |
| symbol レイヤーの出入りが即時であること（`fadeDuration: 0`。paint トランジションとは別系統で、地図全体にしか設定できない）と、対象レイヤーの列挙。**symbol レイヤーを新設・削除したら列挙を見直す** | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §8「symbol の配置フェード」 |
| 地震活動ヒートマップの色ランプ・拡散半径・不透明度の決め方（対数配置・高ズームは地理的距離に追従・寄るほど薄く） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §14 |
| バッジの文字色（塗り色から白/黒を自動選択・気象庁配色は変更不可）と地図バッジの半径テーブル・アイコン倍率とぼやけの関係 | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §15 |
| 地図がスタイルを失った後の後始末を、異常として記録しないこと（`gl/mapStyleGone.ts` の `isMapStyleGone`。**`getStyle()` / `getSource()` / `getLayer()` は `this.style` の有無を共有する**ので、スタイルが無ければそろって undefined を返す＝「ソースが見当たらない」「canvas / source / layer が揃っていない」が正常な経路で成立する（開発中は HMR のたびに通る）／**スタイルが無くなる経路は 2 つ**＝`Map.remove()` と WebGL コンテキストロスト。片方だけを理由として書かない／**呼ぶのは引けなかったと分かった後だけ**＝`getStyle()` はスタイル全体を直列化する。`isStyleLoaded()` は使えない（スタイルが無いと MapLibre 自身が警告を出すので、ノイズを消すために別のノイズを生む）／**スタイルが生きているのに引けない場合は従来どおり記録する**＝こちらは本物の異常／**canvas source で面を敷くものを足したら同じ判定を置く**＝作りが同じなら同じ経路で同じ文言が出る（推計震度分布図はカスタムレイヤーへ移ったので対象外）／**登録する側（`putOverlayLines`）には要らない**＝cleanup は古い地図に対して走るが effect の本体は常に新しい地図で走る／MapLibre を上げたらこの前提を目視で点検する＝崩れても型検査では捕まらない | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §9「静的な形はソースを共有する」「MapLibre を上げたときに確かめるもの」・§17「描けないときは必ず消す」 |
| 地図を作れなかったときに画面全体を落とさないこと（`new maplibregl.Map()` は WebGL2 の文脈を作れないと**同期的に投げる**。**境界へ渡さないために握る**＝地図には ErrorBoundary が置いてあるので握り損ねても差し替わるのは地図領域だけだが、**原因が判っているぶん自分で捕まえたほうが伝えられることが多い**（境界は何が起きたかを知らないので作り直しか再読み込みしか案内できない）／**例外の型で選り分けない**＝`GPUInitializationError` に絞ると `getContext` 自体が投げる端末を取りこぼし、いちばん救いたい相手で白画面のままになる／**代わりに画面では原因を断定しない**＝オプション値の不整合も同じ場所から投げられ、「WebGL が無い」と言い切れば嘘になる。実際の例外はログへ残す／覆うのは地図領域だけ） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §12 |
| 凡例に出すものと並べる順（**中身は各レイヤーの表示条件から組む**＝`MapLegendSources`。地図モードで決め打たない／**色は描いている側と同じ定数から採る**／**半透明で添えるだけの描画物は主役のモードでだけ出す**＝予報円と EEW 震源の×印／**並べる順と畳んだときの代表はモードで決める**＝`LEGEND_PRIMARY_BY_MODE`／**器を中身より大きくしない**＝専用の見出し行を持たず、狭い画面ではひと段詰める） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §20・[`docs/spec/share-card-spec.md`](docs/spec/share-card-spec.md)（画像への焼き込み） |

### 地図: カメラ・投影・表示閾値

| 項目 | 単一情報源となる仕様書 |
|---|---|
| 表示閾値の単位の使い分け（対象がどれだけ画に収まるかは**視野の実距離**／文字と点の混み具合は**ズーム値**／タイルは**タイル z**）・ズーム値の基準（MapLibre 512px タイル vs Leaflet 256px タイル）・寄り上限に揃える閾値群 | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §4・§6 |
| カメラ更新の空振り省略（`gl/skipNoopCameraUpdate.ts`）。**MapLibre を上げたら `Camera.applyUpdatedTransform` と `_elevateCameraIfInsideTerrain` の実装差分を目で確かめる**（照合できるのは transform の観測できる状態だけで、それ以外への副作用が足された場合は自己確認をすり抜ける）／条件は呼び出しごとに見る・**確認は起動直後だけでなく一定間隔で続ける**（依存指定が `^6.10.0` でマイナー更新が自動的に入る）／照合に回転行列を入れない（地図を回すまで作られず、確認が永久に始まらない）／効いているかは画面に出ないので `window.__cameraUpdateSkip()` で読む | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §9「カメラ更新の空振りを省く」・§11 |
| EEW 予想の区域塗りとカメラ追従対象の一致（`useEewLayerData` の `eewFitPositions` と `JapanMapGL` の塗り分けが、同じ「予想長周期を優先する」判定を使っていること） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §6 |
| 揺れフォーカス（揺れが強まった点・別地点へ一時的に寄る）の発火条件と寄り先（**寄るのは最大震度を記録した観測点でメンバー重心ではない**・音と同じ判定で要求を出す・likely では寄せない・担当は EEW の有無で入れ替わる（発報中は EEW 追従の側が寄せ、連番は共有して排他に消費する）・新規 EEW の発報から 10 秒（第一報の引き直し抑制 3 秒とは別の値）とユーザー操作中と古い要求は見送る・見送っても連番は消費する（担当しない側は消費しない）・戻す経路は成長フォローに任せる・消費済み連番は常時マウント側で覚える） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §6 |
| EEW 追従の引き上限（**上限は円の半径にかける**。矩形の辺を枠で切り詰めると箱の中心が震源から外れる・検知点と区域塗りには上限をかけない・値を上げるときは地方名ラベルの閾値と併せて見る） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §6 |
| 第一報の抑制を破る条件（**震源訂正フォロー**。判定は「寄り直したら中心がペイン短辺の 2 割以上動くか」で、**画からはみ出したかでは測らない**（訂正後の震源が縁の内側に残ると発火しない）・ズームの利得は見ない・寄り先には抑制中に見た震源の位置をすべて含める（現在の震源だけだと報のたびに寄り直す）・材料は第一報と同じ（自身の波円と自身の震源だけ）・見るのは第一報のフォーカスを与えた EEW だけ・**破ってよいのは第一報が張った抑制だけ**で揺れフォーカスの抑制は破らない（成り立つのは `SHAKE_FOCUS_YIELD_AFTER_EEW_MS` > `GROWTH_FOLLOW_SUPPRESS_MS` だから。片方だけ動かすときは見直す）） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §6 |
| EEW 追従にも収め直しフォローがあること（成長フォローは引くだけなので、画を狭め直すのはこちら。閾値・保持時間は揺れ検知側と共有する・**保持時間 8 秒を EEW 向けに縮めない**——実電文で震源要素は 30〜65 秒にわたって 13〜53 回変わり、短くすると全部を追いかける） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §6 |
| 視野基準の閾値が可変であること（寄り上限は 0.5 刻みへ丸める・大画面には絶対上限を置く・上限が固定の帯では可変の下限を追い越させない。`minzoom > maxzoom` は MapLibre が検証せず、どのズームでも描かれないレイヤーが黙って出来上がる） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §6「寄り上限と閾値の単位」 |
| レイヤーの表示境界（`minzoom`/`maxzoom`）をカメラの着地段（0.5 刻み）に乗せないこと（`flyTo` は開始と目標が同じズームでも移動中に必ず下回るため、同値だとその段へ着地する操作のたびにレイヤーがまるごと消え、着地で戻る。落ち込み幅は視野に対する移動量で決まり、引いた画ほど小さい） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §6「閾値を触るときに併せて見直すもの」 |
| 地図の傾き（pitch）・回転（bearing）が常態であることに依存する箇所（**予報円は地面に貼る**＝画面座標で半径を測らない／**カメラフィットは `bearing` を渡す**＝渡さないと回転が無断リセットされる／**収まり判定は目標の四隅を `map.project()` で画面座標へ投影して見る**＝地理座標の矩形どうしで比べると回転時にはみ出しを見逃す／**ラベルの退避は符号で判定**＝`room` は真北・真南固定の生成データなので絶対値では読み違える／**`circle` レイヤーは `circle-pitch-scale: viewport`**＝既定の `map` だと奥で縮み、固定サイズの震度0以下の点と食い違う／**操作検知に `rotatestart`・`pitchstart`**）。`maxPitch` の値の意味。**新たに傾き・回転を前提にする箇所を足したらここへ追記する** | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §6「地図の傾きと回転」 |
| 地図の投影が球と平面を行き来すること（寄ると MapLibre が自動で Mercator へ切り替える・境目は zoom 11 と 12 のあいだ）。**カスタムレイヤーは行列 1 本では描けない**——MapLibre が配る投影ごとの GLSL 断片を頂点シェーダーへ貼り、**プログラムを投影ごとに持つ**／属性の番号はリンク前に固定して VAO を使い回す／**リンクに失敗したプログラムを返さない**（未リンクへの `useProgram` は例外を投げず別レイヤーのシェーダーが残る）／**WebGL の文脈が復旧したら載せ直し、`onAdd` で投影プログラムのキャッシュも捨てる**＝MapLibre はカスタムレイヤーを戻さず（自身が「手動で再追加せよ」と警告する）、載せ直さなければ `render()` は二度と呼ばれない。キャッシュを捨てないと無効なプログラムを返し続け、`useProgram` は例外を投げないので `guardRender` にも `renderHealth` の自己申告にも掛からず**そのレイヤーだけが無音で描かれなくなる**。**キャッシュを持つレイヤーはすべて `onAdd` で捨てている**（`scripts/mapRenderingSpecLists.test.ts` が検査する。レイヤーを足すときも同じ形にする）／**`onRemove` だけに任せない**＝MapLibre 6.9.0 は文脈喪失時に `Style.destroy()` からレイヤーの `onRemove` を呼ぶので**いまはそこでも捨てられている**（実測）。ただしあのループはレイヤーごとに try/catch を持たず、1 枚の例外で以降が呼ばれない／**`onAdd` は「新しい文脈で作り直す」を自分で完結させる**＝キャッシュのほかに 4 つ落とす（判定用フレームバッファの寸法・点滅の予約・診断の「一度きり」の抑制＝`gl/depthPointLayer.ts`、オフスクリーンテクスチャの寸法＝`gl/subThresholdLayer.ts`。診断の抑制は `gl/dayNightLayer.ts` にもある）。どれも「同じ寸法なら作り直さない」「点が変わったら張り直す」作りなので、覚えが残ると新しい文脈で素通りし、**不完全なフレームバッファへの描画は例外にならない**。**`onRemove` が同じことをしているかは項目ごとに違う**ので、揃っていると思って `onAdd` 側を消さないこと（内訳は §6）。**点の数についての警告（`warnedPointLimit`）は戻さない**＝文脈と無関係／**リセットは資源を作る前に置く**＝`onAdd` が投げても MapLibre はレイヤーを登録したまま `render()` を回す（`Style.addLayer` は `_layers[id]` へ入れてから `onAdd` を呼ぶ）／`projectTileFor3D` の elevation は**球はメートル・平面は Mercator の z** で単位が違う／**球のときは `projectTileFor3D` を使えない**＝2 つの投影が混ざる帯（zoom 11 と 12 のあいだ）では MapLibre が同じ 1 つの値を球側にはメートル・平面側には Mercator の z として使うため、1 つの値で両方を満たせない。混ぜ合わせを自前で書き、球側と平面側へそれぞれ正しい単位を渡す（`gl/depthPointLayer.ts` が実例。**閾値は MapLibre の内部実装から写した値なので、MapLibre を上げたら目で確かめる**）／**`projectTileFor3D` は水平線のクリッピングを掛けない**（裏側の点を捨てるのは `projectTile` 側だけ）／**`map.project()` は地球の裏側にも画面内の座標を返す**ので、見えているかを画面座標で判定する箇所は投影して戻す往復で弾く | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §6「地図の投影」 |
| カメラ追従の抑制時間・津波の俯瞰帰還の猶予が `INTERACTION_HOLD_SEC` 固定であること（設定「自動復帰までの時間」＝ `idleRevertSec` と結合していないこと。結合すると「無効」の端末で追従・帰還が永久に止まる） | [`docs/spec/map-rendering-spec.md`](docs/spec/map-rendering-spec.md) §6「ユーザー操作の尊重」「津波追従の目標範囲」 |

### データの取得と時刻

| 項目 | 単一情報源となる仕様書 |
|---|---|
| 配信元へのリクエストを増やさないこと（**制限は 3 段**＝ドメイン×IP で 10 分 2000／電文本体とアーカイブ本体が各 50req/5min。規約が「**同じ `id` に対して短期間にリクエストを繰り返さない**」と明記／**仕掛けの一覧と数え方は仕様書が正**（ここに書くのは触るときの注意。どれも外すと制限に触れる）＝⓪**大量に取るならアーカイブを使う**（`/v1/archive/:id` は 1 日分が 1 ファイル・実測 gzip 10KB。**履歴の取得は `fetchDmdataQuakeHistory` の 1 本で、起動時・「もっと見る」・リプレイ開始時の復元が同じものを通る** —— 別実装を足すと「片方だけがアーカイブを使う」形に戻る／1 件ずつ `/v1/:id` を叩いていた頃は起動あたり 110 件超、寄せた後は当日分の 3 件だけ／**「もっと見る」はカーソル方式で、窓 1 つ（`HISTORY_WINDOW_DAYS`＝7 日）ずつ遡る**＝前回読み切った最古の日の手前が次の上端で、窓どうしは重ならない。**遡り幅に上限は置かない**（在庫の端＝保存開始の 2020-11-18 まで。**「目録が空だから端」で止めない** —— 一時的な障害でも空になり、読めるはずの期間を閉じてしまう。止める理由は日付だけ。**`sources` で数えない**＝当日経路の日を含むので端でも真を返し続ける）。かつては当日経路の日付列挙の暴走防止（60 日）から導いた 59 日が上限として振る舞っていた／**カーソルは取得に失敗した日を跨がない**＝跨ぐとその日は二度と要求されない（429 は待てば取れるので特に重い。**当日経路で 429 を受けた日も止める**）。採るのは連続して読み切れた範囲だけ／**どの担当にもならなかった日（`uncovered`）でも止める**＝目録にも当日経路にも当たらない日は取得の一覧に現れないので、一覧を辿るだけでは素通りしてさらに古い日まで進む。**その日は `uncovered:<日>` として「読めなかった取得元」に出す**（出さないと押しても増えないボタンが理由不明のまま残る）が、**積むのは返す直前で全滅判定の分子には入れない**。**恒久的には詰まらない**＝次の窓の上端が欠落日になり当日経路の担当（上端から 2 日）に入るので、1 回につき 2 日ずつは進む／**`head` を持たない目録エントリは履歴側でも取りこぼしに数える**＝リプレイ本編は数えていたのに履歴だけ黙って落としており、目録が壊れている日ほど「静かな日」に見えた／**カードが増えなかったことを打ち切りの理由にしない**＝静かな 1 週間は普通に起き、在庫の有無を何も意味しない（在庫は保存開始＝2020-11-18 まで残る）／**旧経路の 8 本は撤去済み**＝`services/dmdata.ts` に 1 件ずつ取る経路を足し直さないこと）①**電文本体は控えから読む**（入口は `services/telegramBody.ts` の 2 つ＝テキストが `fetchTelegramText`・二進電文が `fetchTelegramBytes`。**取り出し方だけが違い、門と 429 の窓は内部で共有する**（`fetchThroughGate`）。**二進側は永続の控えを持たない**（セッション内だけ）。**素の `fetch` を書き足すとその経路だけ控えに載らず、起動のたびに同じ `id` を取り直す形へ戻る**／控えるのは **XML の原文**でパース結果ではない＝読む項目を足したとき控えだけ古い形で残り、型を付け直して使うので**コンパイルは通り画面に出るまで気づけない**／**控えは「取り終わってから」効くので同時要求には間に合わない**＝同じ `id` への同時要求は 1 本にまとめる。`StrictMode` の二重実行で実測 170 件になっていた）②**窓ごとの上限を守る**（`services/dmdataRequestGates.ts` が**ホストごとに枠を分ける** ＝ 本体が 50req/5min ＋ 2000req/10min・一覧や目録が 2000req/10min。門の実体は `utils/requestGate.ts`。**上限に達するまでは 1 件も待たせない** ＝ 配信元が定めているのは窓ごとの上限で、配り方ではない。**均等割りへ戻さない**（戻すと `requestGate.test.ts` の「窓ごとの上限」が落ちる）。**掛け忘れは文面からは見えない**＝仕様書に「一覧・目録も対象」と書いたのに実際は EEW 詳細 1 箇所だけで、ページを辿るループ 5 経路が素通ししていた。**共通の口へ置く**（`getJson` と `fetchThroughGate`）／**件数を減らしても投げ方を変えなければ瞬間のレートは変わらない** —— 起動直後に 110 件超が同時に飛んでいた／**待つ前に枠を予約する**／**控えから読める分は通さない**／ライブは WebSocket に本文が同梱されるのでこの門に並ばず、**地震の報せは遅れない**／**履歴は取れた分から順に反映する**（本体の取得はループへ入る前にまとめて投げ、処理だけ日付順に行う＝`prefetchedBodies`）／**待たせたくないものだけ順番を先にする**＝`urgent`。渡すのは起動時に発表中の緊急地震速報を復元する経路だけで、**枠は増やさない**（全部が urgent なら優先度は意味を失う）。**枠が空いているときは同期的に通す**＝`setTimeout` を必ず通る形にすると偽のタイマーを使うテストが進まない）③**一覧で窓を決めてから本体を取る**（種別ごとに発表頻度が違うので、全件取ってから捨てると大半が無駄）④**409（同時接続の上限）は停止させず待ちを伸ばす**（こちら側の異常ではなく、枠が空けば繋がる）⑤**アーカイブは控えから読み、直列に間隔を空けて取る**（`scripts/telegram-audit/archive-cache.mjs` の 1 本に集約し、**素の `fetch` を足さない**／**レート制御は `scripts/lib/rateGate.mjs` の `gate` に集約**＝取得元ごとに `kind` を分ける汎用の門で、P2PQuake の履歴走査・観測点リビジョンの走査も同じものを通る／**待つ前に枠を予約する** —— `await` の後に書き込むと並列呼び出しが一斉に発火する）⑥**429 を受けたものはしばらく取りに行かない**（同じファイル。60 秒から倍々で 30 分まで。**鍵は電文本体とアーカイブ本体で名前空間を分ける**＝同じ id でも別のもので、混ぜると片方の 429 が無関係なもう片方を最長 30 分止める／**窓による見送りは取得の失敗と別の枠で数える**＝単位ごとに分ける（取得元・電文）。混ぜると表示の「再読み込みで取得し直します」が嘘になるうえ、**全滅判定の分母に混ざって取れていた分ごと捨てる**）⑦**ページを辿るループには上限を置き、達したら投げる**（DMDATA の一覧と目録はどちらも 20 ページ。範囲指定が効かなくなったときの歯止めで、黙って切ると取りこぼしが「静かな時間帯」に化ける。**ページ数も 1 ページの件数も経路ごとに違う**ので値は仕様書の表を見る）⑧**アーカイブ本体も控えから読み、その控えは二層で持つ**（メモリ＝`utils/archiveBodyCache.ts`・端末＝`utils/archiveBodyDb.ts` に **gzip のまま**。入口は `downloadArchive` の 1 本／**端末の層はタブを開き直したときに効く**＝起動・リロード・区間ごとの再生開始で毎回メモリ層は空になる（実測 2026-09-19: 起動時の履歴 7 本で 28.8 秒 → 端末に当たれば通信 0 件）。gz のまま置くのは展開比が実測 ×11.5〜×18.1 あるため／**片方だけ空にしても「控えを空にした」ことにならない**＝テストで取り直しを確かめるなら両方空にする／**偽物の `persist` だけでテストしない**＝実 gzip を通す結線は `services/archivePersistWiring.test.ts` が見る／**電文本体の控えと門はアーカイブ経路に掛かっていなかった**うえ `clearReplayCache()` が開始のたびに控えを捨てており、**過去の地震を再生する経路は保護が 1 つも効いていなかった**＝区間ごとに開始し直す使い方（8 日を跨ぐ 239 区間）で実測 1,000〜3,800 リクエストが、落とす実数（16 本）だけになった／鍵は URL のアーカイブ id で**内容に対して不変**なので捨てる正当性が無い／**バイト数は展開後の tar の長さで数える**＝`parseTar` は `subarray` のビューを返すので 1 エントリでもバッファ全体が残る／**上限（96 本・128MB）は「1 回のまとまった取得が同時に落とす量」を下回らせない**＝割ると同じ取得の中で追い出しが起き次の取得で落とし直す（控えの意味が消える）。**まとまった取得は 2 つ**＝リプレイの開始（窓 3 つの和集合 × 分類 2 ＝ 16 本）と地震カードの「もっと見る」（窓 7 日＋両端 × 分類 1 ＝ 8 本。**カーソル方式なので押した回数では増えない** —— 窓が重ならず、同時に落とすのは常に 1 窓ぶん）。**境界テストは窓の定数から導く**＝数字を写すと窓を広げたときに無反応で通り続ける。バイト数の実測は 8 日 × 2 分類で 55MB・「もっと見る」は測れた 58 日で 41MB で、**重いのは日数ではなく地震の多い日**（本震当日 1 日が展開 30MB・ふだんの日は gz 0.01MB）／**当日ぶんは控えない**＝目録に当日は現れない前提を実装で守る（崩れたら `uncacheable` が立つ）／**追い出しの順序は単調な連番で持つ**＝`Date.now()` だと 1 回の開始で全件が同値になり、読み直した印が効かず**いま使ったものから追い出す**／**目録は控えない**＝後から届いた電文が永久に見えなくなる／**`clearReplayCache()` はテスト側のリセット手段を兼ねていた**ので、本番を弱めず `clearArchiveCacheForTest` を別に足す）⑨**控えで読み切れる日は本体を落とさない**（目録と電文のパース結果は上限も期限も持たないのに本体の控えは 96 本・128MB・12 時間で落ちるため、**「解析結果は手元にあるのに本体だけ消えた」状態が普通に起きる** —— そこで落とし直した本体は 1 バイトも読まれずに捨てられる／**判定の材料は目録だけで足りる**＝本体を読む箇所は 3 つ（目録そのもの・発表時刻の補い・電文のパース）で、どれも「目録が直接持っているか、控えに残っているか」で決まる／**判定と消費で同じ述語を使う**＝絞り込みの連鎖を 2 箇所へ書き写すと食い違い、「本体を落としていないのに本体を読もうとする」形で電文が黙って取りこぼしへ回る（`ManifestPlan` へ `include` を載せ、窓・種別の判定は `isReplayTarget` / `isHistoryTarget` の 1 本を計画と消費の両方が通る）／**判定がずれたら必ず要約を出す**＝読めなかった取得元には積まない（等号で見る全滅判定を壊す）ので、件数を数えて最後に出す。出さないとその日が「静かな日」と見分けが付かない／**ダウンロードの位置は動かさない**＝エントリのループへ入る前のまま。中へ遅延させると失敗が「その日を途中まで読んだあと」に起き、読めなかった日として数えるかが決まらない（全滅判定の分母に効く）／**落とさなかった日は取得の失敗にも 429 の見送りにも数えない**＝控えから読み切れているので成功。数えると分母と等号が成立して例外になり、読めていたカードごと捨てる／**本編の再生にも同じ判定を置くが条件が違う**＝あちらはパース結果を控えないので、効くのは**窓に 1 件も入らない日**だけ）／**一覧の `head.time` は UTC・パース結果の `time` は JST** で、辞書順比較に混ぜると窓が全件を捨てる／**dev は `StrictMode` で 2 倍**になるので数を測るときは半分で読む／**429 は「同じ id の取り直し」に id 単位で返る** —— ブラウザからは CORS エラーにしか見えないので、切り分けは Node から同じ URL を叩く） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §2「リクエスト数を抑える」・[`docs/spec/telegram-coverage-audit.md`](docs/spec/telegram-coverage-audit.md) §2「アーカイブは一度取れば控えから読む」 |
| 取得が失敗したときの振る舞い（手当ては**間隔を空ける・上限を置く・入口で弾く**の 3 つ／強震モニタのフレーム取得は連続失敗が 25 回を**超えてから**倍にして 10 秒で頭打ち。**数えるのは試みて失敗した回数でフレームの数ではない**＝フレームで数えると間隔が伸びても加算が進まずバックオフが一度も効かない／**更新停止の判定は経過時間で行う**＝回数で数えると間隔が伸びたぶんだけ判定も遅れる／リプレイは連続して諦めた回数でも間隔を空けるが、**止めない**＝次の予約を置かない形にすると配信が回復してもそのセッションでは二度と取りに行かない／クロック較正の 403→200 境界は**二分探索**＝線形では 1 回の較正で最大 121 件（**主経路が失敗した周期だけ走る**ので、失敗が続かなければ 0 件）。誤答しても較正を見送るだけで時計は汚れず、判定不能が 1 つ返れば打ち切る／ページを辿るループの上限は上記「配信元へのリクエストを増やさないこと」の**「ページを辿るループには上限を置き、達したら投げる」**（**達したときに投げるのは電文の一覧・アーカイブの目録・P2PQuake リプレイの 3 つ**で、震源カタログ・発表中の緊急地震速報の一覧・P2PQuake の履歴は読めた分を返す。**丸数字で参照しないこと** —— 列挙を編集するたびに指す先が変わる）／再生する時刻は `replayTargetProblem` が取得を 1 件も投げる前に弾く。**新しく「時刻や範囲を受け取って取得へ渡す入口」を作るときは同じ判定を置く**） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §2「ページを辿るループには上限を置く」・§4「取得に失敗したら間隔を空ける」「フォールバック: Yahoo の 403→200 境界」・[`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §6「再生する時刻は入口で弾く」 |
| 目録・電文のパース結果の控え（鍵は**アーカイブの URL**と**電文の id**で、どちらも内容に対して不変／**失敗は控えない**＝再解析させて警告と取りこぼしの数え方を変えない／**控えるのは実際に解析した分だけ**＝打ち切りで読まなかった日は乗らないので、目標を増やせば読まれる／**電文のパース結果は履歴の取得だけ**＝リプレイ本編は窓を前へ進めるので同じ電文を二度解析しない（目録は共有する）／**再生を開始し直しても捨てない**＝鍵が不変なので捨てる正当性が無い。捨てるのはテスト専用の関数だけで、**テストの目録は作り物の id を使い回すため残すと別のテストの中身を引く**／**控えたオブジェクトは書き換えないこと**） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §2「大量に取るならアーカイブを使う」 |
| 電文本体は**目録の `filename` で引くこと**（**識別子から推測しない**＝ファイル名は受信時刻を 17 桁の数字で含むので、識別子の先頭を部分一致させる形だとその並びに偶然含まれる余地が構造として残る。**誤って別の本体を引いた例は観測していない** —— 直したのは実害ではなく推測に頼っていたこと／**拡張子でも絞らない**＝名前が拡張子まで持っているので呼び出し側で渡し分けない。アーカイブには XML 以外の形式で届く電文（平文の WEPA60 など）も混ざるが、扱う種別の外なので本体を引く手前で落ちる（**種別の判定はリプレイ本編と履歴で別の定数を使う**ので、`HANDLED_TYPES` だけを見て回らないこと）／**引けなかった理由を記録へ添える**＝「目録が名乗っていない」（目録の形が変わった合図）と「名乗っているがアーカイブに無い」（部分破損の疑い）は原因が違うのに、記録の主文はどちらも「本体が見つからず」。専用の行を別に立てて間引く形にしない（間引かれない主文が同じ数だけ出るので、原因だけが読めなくなる）／**発表時刻を補う経路にも添える**＝あの経路の電文は時刻が決まらない時点で落ち、本体の読み取りへ進まないので、そこでしか手掛かりが残らない。**ただし理由は 3 通り**で、本体を引けたのにファイル名から受信時刻を読めなかった形を「アーカイブに無い」と書くと嘘になる（`bodyMissReason` と `manifestTimeMissReason` を分ける理由）） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §6「DMDSS 版の取得（アーカイブの構造）」 |
| 目録の発表時刻が読めない電文を**本体のファイル名の受信時刻で補うこと**（アーカイブ経路は本体が手元にあるので**追加リクエスト 0 件**で救える。落とすのは純粋な損失／**代理値ではない**＝窓が問うのは「その時刻に存在したか」で、受信時刻は発表時刻以降なので境界では採らない側へ倒れる／**どちらも読めなければ落として取りこぼしに数える**＝`new Date(null)` は 1970-01-01 を返すので素通しさせると古い電文として吸収される／**補えたときだけ控える**＝同じ電文について同じ警告を回数だけ並べない。黙らせるのではなく二度目は警告の手前で返す／**当日経路には置かない**＝本体を取る前に判定するので補うにはリクエストが増える。**救える側だけ救う**／**実在は数えていない**＝目録の発表時刻はアーカイブ本体の中にあり、数えるには全期間約 2130 日ぶんを落とすことになる） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §2「大量に取るならアーカイブを使う」 |
| 履歴取得（`fetchDmdataQuakeHistory`）で**取り込めなかった分を画面へ出すこと**（履歴取得は一部の失敗を吸収するので全滅しない限り `error` が立たず、**取れた分のカードだけが「最新の地震情報のすべて」に見える**／**1 件でも出す**＝取得元が「日」単位／出す先は地震タブのカード一覧の上で、全画面の失敗表示は使わない（取れた分を覆わない）／**カードが 0 件のときも出す**＝ここが「静かな期間だった」との見分け／**積まずに置き換える**＝取得に失敗した日でカーソルが止まるので、次に押せば必ずその日から読み直す（表示中の損失は常に「いま再試行できる状態のもの」）。積むと同じ損失を回数だけ数え、取得が回復しても消えない。**カーソルが失敗日を跨ぐ作りへ戻すなら、ここも積む側へ変えること**。**置き換えてよいのはその入れ物がその取得の結果だけを持つときに限る**（リプレイ側は他の取得も同じ入れ物へ集めるので積む）／確定した損失と「もっと見る」のまるごとの失敗は別の文面（後者は押し直せば回復しうる）／**標準版では常に空**＝取得側が損失を返さないためで、欠けていないことの保証ではない） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §2「大量に取るならアーカイブを使う」 |
| 読めなかったものを記録すること（**3 層＝取得の失敗・電文を丸ごと捨てた理由・要素の種別ごとの全滅**／要素の層で数えるのは「値として解釈できたか」で**結果に残ったかではない**（長周期の階級 0 は読めている）／**全滅のときだけ記録する**＝部分脱落では黙るので「大多数が落ちて数件だけ残る」形は検知できない／**正常な振り分けを混ぜない**（長周期の階級 0・南海トラフの種別振り分け 2 箇所）／読めなかった値を見本で最大 3 件載せる／接頭辞 `[dmdata]` `[dmdata XML]` `[tsunami]` `[tsunami XML]` で経路を分け、**種別のラベルは経路で言い換えない**） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §2「読めなかったものは記録する」 |
| 同じ日を読み直したときに取りこぼしを二度数えないこと（**責任は取得側**＝合流する側（`addTelegramLoss`）は別々の取得を集める前提で足すので、同じ日を 2 度返すと 2 倍に数えられる／対象は**同じ日を何度も走査する 2 経路**＝リプレイ本編（`reportedReplaySkipCounts`。初期状態 24 時間・本編 1 時間・毎時の先読みが同じ日を読む）と標準版のリプレイ（`reportedSkipCounts`）／**「報告済みの日」の集合ではなく件数で持ち、増分だけ返す**＝当日ぶんは走査のたびに取り直すので件数が増えうる。集合だと増えた分を報告できず「その日はもう見た」として黙る／**増分にしてよいのは「窓を見る前に落ちた分」だけ**＝DMDSS 版は `scanSkips`（目録の構造異常・時刻が読めない。走査のたび同じ顔ぶれ）と `windowSkips`（本体が無い・パースできない。窓は重ならないので互いに素）に分け、後者はそのまま足す。混ぜると**並行して読んだ別の窓の分が丸ごと消える**（開始時は本編 `[T, T+1h)` と初期状態 `[T-24h, T)` が同じ日を並行に読む）。当日経路も 2 つに分けて返す（`LiveReplayResult.scanSkippedByDay`）／**記録（`log.warn`）には報告前の値を使う**＝数えたこと自体はその回の事実／**時間軸が変わったら捨てる**／**「もっと見る」はここを通らない**＝窓が重ならず、合流の側（`mergeHistoryLoss`）が同じ日を置き換える） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §2「同じ日を読み直したときに二度数えない」 |
| 標準版のリプレイで日ごとの控え（`dayCache`）に**当日ぶんだけ寿命を持たせる**こと（`TODAY_CACHE_TTL_MS`＝60 秒。**過去の日は不変なので永続でよい**が、当日は電文がこれからも増えるので、最初の結果を持ち続けると**後から届いた電文が二度と見えない**（再生もされない）／**判定は「いま」の日付で行う**＝再生時刻ではない。控えが古びるかは配信元に電文が増えるかで決まり、それは実時間の話／**毎回取り直さない**＝レート制限（`/jma` は 10 リクエスト/分）。1 日ぶんは 2 資源で 2 リクエスト／**取りこぼしの数え方も併せて見る**＝件数で覚える形にしていないと、後から届いた壊れた電文がここで黙る） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §3「再生の取得は日ごとに控える」 |
| 日時として読めない値を**電文の層と表示の層の両方**で確かめること（`new Date('壊れた値')` は例外を投げず `getHours()` 以下がそろって `NaN` を返す＝画面に `NaN:N` と出るのに**例外も記録も残らない**／**時間帯を明示していない値は表示の層では原理的に捕まえられない**（`Date` がローカル時刻として解釈し有効な値を返す）ので電文の層で弾く／日時を整形する関数はすべて `formatters.ts` の `readDateTime` を通し、**画面へ出す整形の戻り値は `string | null`**（**書き出すファイル名の時刻印だけは例外**で、`null` を返さず現在時刻で作る）。**型検査が漏れを止めるが、テンプレートリテラルと JSX へ埋めた形だけは通ってしまう**ので目で確かめる／**捨てるか記録だけ残すかは用途で決める**＝同一性の判定に使う時刻（地震情報の `earthquake.time`・津波の原因地震の `originTime`）・必須要素（長周期の `OriginTime`）・**落としたときの影響を確かめていない時刻**（全種別の発表時刻。`readReportDateTime` は `Head/ReportDateTime` が在れば素通しし、検証が掛かるのは `Control/DateTime` への受け皿だけ）は捨てない。空へ倒すと別々の電文が同じキーで束ねられ、読めない時刻を残すより重い。**どれにも当てはまらないときは残す側へ倒す**／**記録の文面で落とし先を決めつけない**＝残す理由は呼び出し元ごとに違う／**記録は種別ごとに間引く**＝1 個を共有すると、ある種別のノイズで別の種別の異常が黙る。発表時刻は 9 種別から呼ばれるので種別名を渡す／**上限を超えても黙らせない**＝超えたあとは時間で間引いて出し続ける／**`Date`・数値の入力は値では間引けない**＝`String()` が `"Invalid Date"` / `"NaN"` へ丸めるので時間で間引く） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §2「日時は 2 つの層で確かめる」 |
| P2PQuake レスポンスの検証規則（不正値の扱い・破棄条件・震度値の正規化 `46`→`45` / `scaleTo:99`→`scaleFrom`） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §3 |
| APIキーに使える文字の判定（印字可能 ASCII のみ・キーの書式は当てにいかない）と、通信前に弾く 3 経路（履歴取得・ヒートマップ・WebSocket）で判定を揃えること（事前に弾かない経路もある）・**未設定と「使えない文字」を言い分けること**（文言の選び方は 1 箇所に集約する。事前に弾かない経路はリプレイと履歴の追加読み込みの 2 つで、未設定の文言がユーザーに見えるのはリプレイだけ） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §2（認証） |
| 巨大地震に関する情報（南海トラフ 3 種別・後発地震）の共通要素は 1 箇所で読むこと（`readEarthquakeInfoMeta` / `EarthquakeInfoMeta` / `EarthquakeInfoDetail`。見出し文＝要約・次回発表予定・参考情報・情報の種別・**経路ごとに書き分けない**＝かつて解説情報だけが要約を読んでいた／**要約だけを出して本文を省かない**＝本文には規模・評価の根拠・過去の統計が入る／参考情報は固定文で長いので折りたたむ） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §2「3 種別に共通する要素は 1 箇所で読む」 |
| 見出し文（`Head/Headline/Text`）は読むが地震情報では出さないこと（**どの種別でも本文や既読の要素に無い事実は含まない**＝実電文で 1 種別ずつ確かめた・震源要素更新の文中の時刻は `ReportDateTime` と同一・**見出しの構造化（`Headline/Information`）は読まない**＝`Body` の部分集合・ただし緊急地震速報の警報報だけは「新たに警報対象となった区域」を名指ししうるので判断を保留） | [`docs/spec/quake-spec.md`](docs/spec/quake-spec.md) §8「見出し文」 |
| 南海トラフ電文 3 種別の役割（VYSE50=臨時情報／VYSE51=臨時解説／VYSE52=定例解説）・**段階判定は `Body/EarthquakeInfo/InfoSerial/Code`**（`Head/Title` は落とし先で、読めなかったら記録を残す。`Head/InfoKind` は段階に関わらず固定で使えない）・**臨時情報と解説情報の名乗り分けは `Body/EarthquakeInfo/InfoKind`**＝段階のコードと解説情報のコードは同じ番号体系なので、先に分けないと解説情報を段階として読む・**実電文で裏が取れているのは `111` と `130` だけ**で残りは公開コード表からの写し。表と電文の名乗りが食い違ったら記録を残す・`kindCode` は空文字になりうるので判定は `kindName` で行う・電文の識別は `EventID` と `Serial` の組で行うこと（臨時解説は `EventID` が固定で `Serial` が号数） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §2 |
| 長期震源カタログの読み取り（**仕様書の「96 バイト固定長」を鵜呑みにしない**＝実ファイルは改行区切り／**数値欄の小数部の桁数が年代で変わる**＝桁が 1 つ少ない年があり、素朴に読むと緯度が 78km ずれる（**年の範囲では区切れない**ので、年で判定せず末尾の空白の有無で読む。1983・1990・1993 年は全件が該当）／仕様外の深さは値を推測せず捨てる（`   0 ` は 0〜9 すべてが現れるので「0km の別表記」ではない）／採るのはレコード種別 `J` だけ／時刻は JST なので `Date.UTC` で組み立ててから 9 時間引く（CI は UTC で回る）／**取得経路ごとに既知の地震と照合する**＝7 経路・7 地震。範囲を絞ったときは照合していない経路を名指しで出す／**検知した異常は必ず exit 1 に繋げる**＝`console.warn` で終わらせると CI は終了コードしか見ないので壊れたデータが本番へ出る／**M 欄だけが壊れると「読めない行」は増えず件数 0 という形でしか現れない**ので、そこも失敗条件に入れる／格納単位・時刻の起点・データの終端・確からしさは**年ファイル自身に持たせる**（索引だけに置くと、読み損ねたとき桁がずれた値や速報値を確定値として黙って使う）／**格納単位は元データの刻みに乗る値を選ぶ**（緯度経度の 0.01 分＝1/6000 度を 10 進で切ると格子から外れて丸め誤差が乗る。基準は「決定精度より細かい」ではなく「元の値を復元できる」。小数部の桁数は取得元と年代で変わり**年の途中で変わることもある**ので、年の範囲で区切らず**最も細かい箇所**に合わせる）／**単位で表せない値は 1 件でも exit 1**（丸めは例外も NaN も出さず「読めない行」としても現れないので、`hypocenterScale.ts` の検査で突き合わせるしかない）／**単位を変えたら全年を作り直す**（週次更新は 2024 年以降しか作らない。**読み取り側は年ファイルの単位を信じて読むので混在を検知しない** —— 捕まえられるのは生成が索引を組み直すときだけ）／**索引は出力ディレクトリの実態から作る**（今回生成した年だけで書くと、範囲を絞った再生成で他の年が索引から消える）／**M2.0 の完全性は 1997 年以降だけ**なので、件数を数える機能は `completeMinMagnitude()` で閾値を上げる／直近 30 日のヒートマップとは母集団が 10 倍以上違うので統計に混ぜない） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §6「長期震源カタログ」 |
| DMDATA 震源カタログの欠測項目の扱い（震源未決定の項目を 1 件ずつ除外・件数は警告に出す・全滅は例外）・座標が 0.1 度刻みである前提 | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §2（震源カタログ） |
| 強震モニタの取得と再生の分離（供給元 → 時刻順キュー → 反映の 3 段・放出は到来分の最新 1 件のみ・反映はデータ時刻順に限る・新しい供給元を足すときの入口） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §4「取得と再生の分離」 |
| 強震モニタの秒フレームを取り直さないこと（**再生のときだけ控える**＝`utils/kyoshinFrameCache.ts`。ライブは毎秒新しい時刻を取るので当たらない／効くのは**助走**で、控えが無いと**開始し直すたびに同じ範囲を丸ごと取り直す**（区間ごとに開始し直す使い方では区間の数だけ掛かる）／**`cache` は省略できない引数**＝既定値を置くと新しい呼び出し経路が判断しないまま通り、渡し忘れても型検査が通る／**観測点の値は文字列のまま控え、展開は読むたびに作り直す**＝控えた配列を返すと読み手どうしで共有され、書き換えられたときに控えの中身が化ける（例外もログも出ない）。**複製は `indices` と `hypoInfo` の両方に要る**が、**入れる側と返す側のどちらか片方でよい**（要るのは「呼び出し元の配列と控えの配列が別物であること」）／**失敗は控えない**＝未登録の秒（403）は待てば現れる／**鍵にエッジを含めない**＝west と east は同じ秒の同じ中身（**実測で確かめた**。同じファイルに「west は east より登録が速い」と書いてあるので前提にはできなかった）／**上限は件数で置き、バイト数を観測点数から逆算して固定しない**＝観測点は増えていく／**追い出しは `Map` の挿入順で行い並べ替えない**＝アーカイブ本体の控えとはここだけ設計を違えている（あちらは高々 96 本だが、こちらは秒あたり百件を超える勢いで数千件を入れる）／**時間軸が変わっても捨てない**＝フレームは時刻ごとに不変（これも実測）。**実測値は仕様書が単一情報源**——ここへ写すと片方だけ古くなる） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §4「秒フレームの控え」 |
| クロック同期の主経路・フォールバックの順序（**主経路が失敗したときだけ Yahoo 経路を走らせる**。両方を `feedServerSample` へ供給すると精度の良い側にバイアスが混ざる。「見送り」は失敗ではないのでフォールバックへ落とさない） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §4「クロック同期」 |
| 外部時刻サービスの換算基準（返る時刻は応答生成時刻。**中点で換算すると -RTT/2 の系統誤差が乗る**）・`?ms` を落とすと精度が秒単位に静かに劣化すること | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §4「主経路: 外部の時刻サービス」 |
| 生成データ（`public/data/*.json`）の取得タイムアウト値・失敗時の扱い（TTS 辞書のみ別値） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §6 |
| 生成データの中身の検分（200 でも空・想定外の形なら失敗として扱う）を**取得の中**（`fetchJsonWithTimeout` の `validate`）で行うこと。取得後に呼び出し側で確かめると、その時点で成功として数え終わっているため「データN件を取り込めず」に計上されず、`console` にしか残らない。**新しいローダーを足すときも同じ場所へ渡す** | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §6 |
| 出典の書き方を README と仕様書で揃えないこと（**食い違うのは一次細分区域の呼び名だけ**。README「ライセンス」節は気象庁 予報区等 GIS データ側のレイヤー名「地震情報／細分区域」＝`build-subregions.mjs` が読む `AreaForecastLocalE`、仕様書はアプリが作る生成物「一次細分区域の境界」。同じものを別の観点で書いているので、片方に寄せない——配信元の表記を言い換えると出典表示として成り立たなくなる。「都道府県」「津波予報区」は両文書とも同じ語なので対象外） | [`docs/spec/data-sources-spec.md`](docs/spec/data-sources-spec.md) §1・§6（出典） |
| 発表時刻の受け皿でタイムゾーン表記を混ぜないこと（`Head/ReportDateTime` は常に JST・`Control/DateTime` は常に UTC で、後者へ落ちる経路が**XML を読むパーサーのすべて**にある。続報判定は **`Date` を経由しない文字列の辞書順**なので、混ざるとその報だけ永久に「古い」と判定されて捨てられる——例外もログも出ない／**読み取りは `readReportDateTime` に集約し、経路ごとに書き分けない**＝かつて緊急地震速報だけが素読みで、受け皿も検証も持っていなかった／日時として読めない値は空にして据え置かせる／**空になったときの扱いは種別ごとに決める**＝緊急地震速報は自動解除の時刻が決まらなくなる（→ [`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §7「時刻が読めないとき」）／**発表時刻は分単位のままで秒精度へ上げていない**。資料 Ⅰ.（ⅱ）8 が `Control/DateTime` の参照を求めているが、辞書順比較と「同時刻の続報は受け入れる」設計がそれを支えていない。**ただし緊急地震速報だけは電文の側が分へ丸めておらず、2 つの時刻が秒まで一致する**） | [`docs/spec/quake-spec.md`](docs/spec/quake-spec.md) §6.3 |
| 電文が名乗る情報名（`Head/Title`）と種別の固定名（`Control/Title`）を取り違えないこと（**読み取りは `readInfoName` に集約する**——`Head` 直下に限らないと `Control/Title` を先に拾う／地震情報の遠地地震判別も同じヘルパーを通る／**画面に出すのは津波だけ**で、緊急地震速報は見出しが区分から組み立てられていて語が二重になり、長周期は 1 種類しか無いので情報が増えない） | [`docs/spec/tsunami-spec.md`](docs/spec/tsunami-spec.md) §4 |
| 観測状況を確定した時刻（`Head/TargetDateTime`）を観測情報でだけ読むこと（種別で意味が変わる・VTSE51/52 のみ・実電文で最大 6 分さかのぼる／**発表時刻と同じ分なら画面に出さない**／続報では前報の値を残す——**引き継がないと**等級の発表が挟まった瞬間に消えるため／引き継いだ結果、名乗り（`infoName`）と観測時点の出所が別の報になることがあるが**それは正常**） | [`docs/spec/tsunami-spec.md`](docs/spec/tsunami-spec.md) §4 |
| 電文の読み取りは **XML 経路だけ**にすること（DMDATA は JSON 変換版も配るが採らない。変換は「利用頻度の高いデータ」に絞った独自スキーマで無損失を謳っておらず、実際に津波の注意文と予想波高の「未満」が落ちていた・取得元はライブ／アーカイブ／当日経路の 3 つあるが、どれも `buildXmlPayload` の 1 本へ集約する・目録や一覧に同じ電文が 2 版で載る取得元では **XML 版（`originalId` を持たない側）**を採る。選ばないと同じ電文を二重に取り込む・`dmdataParser.test.ts` の「XML 経路が落としてはいけない項目（…）」describe が、経路を 1 本にする前に JSON 版と突き合わせて確かめた値を固定している） | [`docs/spec/quake-spec.md`](docs/spec/quake-spec.md) §4・[`docs/spec/tsunami-spec.md`](docs/spec/tsunami-spec.md) §4・[`docs/spec/eew-spec.md`](docs/spec/eew-spec.md) §3 |
| 電文を音・読み上げ・自動タブ切替へ流す口（`onLiveEvent`）は**判別可能ユニオンで受けること**（`LiveEvent = AppEvent | ExtraLiveEvent`。**型を潰して渡さない**＝`as unknown as AppEvent` で渡していた頃は、種別ごとにしか無い項目を付け忘れても型検査が通った（推計震度分布図の「初報か続報か」の印がその形）／追加種別の処理は `handleExtraLiveEvent` へ置き `handleLiveEvent` の入口で振り分ける＝以降 `event` は `AppEvent` に絞られる／**`handleExtraLiveEvent` の末尾は `never` で受ける**＝種別を足して分岐を書き忘れたときに止まるのはここだけ／**「地震・津波に関するお知らせ」は流さない**） | [`docs/spec/architecture-spec.md`](docs/spec/architecture-spec.md) §2「副作用への入口（`onLiveEvent`）」 |

### 画面構成と設定

| 項目 | 単一情報源となる仕様書 |
|---|---|
| 設定タブのセクション構成・並び順の方針（`Section` の出現順・重大度は軽い順・カテゴリ順は両テストセクションと通知設定の種別トグルで共通） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §2 |
| 特別情報バナー 5 枚の並び順（重さの順ではなく**同じ事象を隣り合わせる**順）・色の意味・消え方（解説情報・**地震回数**・**お知らせ**は 7 日失効＋閉じるボタン・閉じた id は永続化し、**突き合わせる前に書式を揃える**＝`normalizeDmdataTelegramId`。揃えないと電文 id の書式を変えた版で閉じた帯が復活する）・下端の余白は `:last-child` に任せること | [`docs/spec/architecture-spec.md`](docs/spec/architecture-spec.md) §4 |
| 「自動復帰までの時間」（`idleRevertSec`）を計り直す契機が 3 つあること（ユーザー操作・**自動のタブ切替・電文の受信**）。後ろ 2 つがあるため、この設定は「自動で見せたものの最低表示時間」を兼ねる。操作だけで測る形に変えると、据え置きの端末で津波警報が一瞬映って消える | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §2 |
| 倍率の適用範囲（UI は rem で書く／地図の描画物には `iconScale` を渡す。枠線・影・アウトラインは倍率に連動させない。**ただし下限側では縮むことがある**＝塗りが小さくなるとフチが主役になるため、津波の観測棒と到達確認マーカーは芯の取り分を保つ分だけ細くする） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §2「主な項目の補足」 |
| 画面サイズ別レイアウトの分岐条件（`side` / `sideNarrow` / `roomy`）・パネル比率・折りたたみ | [`docs/spec/architecture-spec.md`](docs/spec/architecture-spec.md) §4「画面サイズ別のレイアウト」 |
| 録画モード（`recordingMode`）の原則（**ON のとき OFF より劣化させない**。逆向き＝OFF が ON より劣るのは**許容する**ので、録画中に見えるものを損なわない限り OFF 側で落として構わない／この原則のもとで**止めるもの**＝地名ラベルの重なり判定の再評価契機・揺れ検知の診断ログの保存（**保存しなかったことを `console` へ出す**。永続はしないので「後から追える」とは書かない。理由の説明を担うのは設定欄の文面／止めるのは保存だけで前後の切り出しは通す＝止めると解いた直後の検知で前側が欠ける）と、**有効にするもの**＝海底地形タイルの先読み（OFF では自動フィット直後に数秒暗転するが、陸地塗り・県境・区域・震度はすべて生成データなので**地震情報の読み取りには影響しない**。紐づけた理由は 1 巡 1,911 枚・1 時間ごとという配信元へのリクエスト量）／**追加で復元するもの**＝リプレイの窓の手前で伝えた内容を「もう伝えた」として扱う（`restorePreWindowTracking`）。対象は 7 つ＝気象庁が書いた文・地震の地域と震源要素・確定情報を通しで読んだかどうか・緊急地震速報の誤報取消・緊急地震速報で最後に告知した震源・津波の取消・**津波が語った原因地震の震源**。**通常の再生で復元しないのは「窓から聞き始めた人は一度も聞いていない」から**で、録画は区間を繋ぐのでその前提が成り立たない（区間の境目で同じ内容を読み直す）。**地震の主題は `mergeQuakeInto` でカードを組み立てて決める**＝生の電文へ `quakeEventKey` を直に当てると識別子を持たない経路で報ごとに別の鍵になる。カードの育て方を部分的に真似ると、震源名が空の報が別の地震を吸い込んだり、訂正報での名前の再変更に追随できなかったりする（どちらも実際に作り込んだ）。**震源の告知だけは読み上げを増やす側**＝比較対象が無いと震源が 50km 超動いても言い直さない。**原則との関係**＝読み上げが減るのは前の区間で鳴らしているからで、通しで見れば失われる情報はない）） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §2「主な項目の補足」 |
| dev サーバーでの APIキー自動投入の条件（dev・DMDSS 版・`--host` なし・未入力のときだけ／注入値は localStorage に保存しない／値を渡す側と受け取る側で条件を重ねる） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §6「dev サーバーでの API キー自動投入」 |
| 設定の書き出しと読み込み（**APIキーは書き出しに含めない**＝ファイルはディスクに残り人へ渡りうる。`stripDevApiKey` と同じ思想／**読み込みでも今のAPIキーを消さない**＝書き出しが空である以上、素直に上書きすると読み込むたびに消える。手で書き足された値だけ使い、**それが dev の注入値と同じ文字列でも保存する**（`keepApiKey`。既定の `stripDevApiKey` 任せだと書いたはずのキーが黙って消える）／**検証は既存の `sanitize()` に任せる**＝読み込み専用の検証を別に持つと、項目を足したときに片方だけ古くなり新しい項目が既定値に戻らない／**ただし `sanitize()` は黙って落とすので、値が入っていたのに読めなかった項目は名前を集めて画面へ出す**（`rejectedKeys`）。項目が丸ごと欠けているだけのものは数えない（欠損補完であって失敗ではない）／**形式の番号が無いファイルと、この版より新しいファイルは別の文面で断る**＝同じにすると壊れたファイルにも「アプリを更新してください」と案内することになり、更新しても直らない。生の `undefined` を画面へ出さない／バリアント違いは知らせるが拒否しない／**APIキーに触れる文言は DMDSS 版でだけ出す**＝通常版は接続設定ごと隠れており、この節で初めてその語に触れることになる／**全項目はまとめて置き換える**＝`replaceSettings`。`updateSetting` を項目の数だけ呼ぶと途中の状態が描画へ流れ、保存も数だけ走る／**保存できたかを返す**＝丸ごと入れ替える操作で、失敗を伝えないと画面は「読み込みました」のまま次回起動で元へ戻る） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §4.5 |
| リプレイを外から操る `window.__replay`（`now` / `offset` / `fetching` / `error` / `speaking` / `start` / `stop`。録画の自動化が**設定タブの UI を叩かずに済ませる**ための口で、UI 経由にすると文言やレイアウトを変えるたびに自動化が壊れる／`speaking()` は区間を閉じる合図＝読み上げの長さは読む地域の数で変わるので固定秒では足りたり余ったりする／**`error()` が無いと外から見て成功と失敗が同じ形になる**＝`fetching()` は成否によらず最後は `false` へ戻る／**`start()` の戻りを捨てない**＝`useReplayController` の `start` は取得処理こそ内部で捕まえるが、その手前の同期処理（セッションの採番・状態のリセット・時刻オフセットの適用）は `try` の外にあり、そこで投げると拾い手のない拒否になってログにも画面にも残らない） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §4.6 |
| 例外の受け止め方（**3 層**＝根・地図・各タブ／**`App` 自身のレンダー例外は根でしか拾えない**＝内側へ境界を置いても、例外は React が子の照合へ入る前に `App()` の中で起きる／**地図で包むのは `MapView` だけ**＝同じ親にいる左上の情報ブロック・行動チェックリスト・特別情報バナー・共有カードボタンを中へ入れると地図と一緒に消える／**自動リロードはしない**＝進行中の EEW は起動時の履歴取得に含まれず消える／**読み上げは止めない**／**繰り返し落ちたら再表示ボタンを引っ込める**＝押しても直らないボタンは無いより悪い／**守備範囲は 3 つに分かれる**＝`components/ErrorBoundary.tsx` はレンダーとライフサイクル、`Map/gl/guardRender.ts` は MapLibre の描画ループ（境界が原理的に届かない）、`utils/globalErrorLog.ts` はそれ以外で**記録だけ**（ブラウザ拡張や外部要因でも発火するため画面に出さない）／**入力を確かめるガードは境界ができても外さない**＝描画ループの中へ境界は届かず、境界が受け止めるのは「その範囲を表示できない」結果でしかない） | [`docs/spec/architecture-spec.md`](docs/spec/architecture-spec.md) §4.6 |
| 取得の失敗・制限を知らせる通知の文の形（**`{状態}（{行動または内訳}）`・主節は言い切り**。対象は 7 本＝地震タブの 4 本・地図に重なる 2 本（生成データの取得と描画）・再生側の 1 本／**言い切りで止めるのは狭いパネルに最大 4 本が縦に並ぶため**——述語を丁寧に書くと 1 本ずつ折り返し、何本出ているか一目で読めない／**形を揃えたぶん意味の差は語だけが担う**＝「取り込めず」は取りに行って失敗した確定の損失・「未取得」は上限で取りに行かなかった分。**2 つは同じ画面に並びうる**ので片方の文へもう片方の語を混ぜない（色分けは補助で、色は文を読み替える材料にならない）／**内訳の語（`取得元N件` / `電文N件`）は `utils/telegramLoss.ts` の `countParts` が単一情報源**で、確定した損失と見送りで共有する） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §5.5「通知の文の形」 |

### 行動チェックリスト

| 項目 | 単一情報源となる仕様書 |
|---|---|
| 行動チェックリストの発火（3 経路＝EEW・強震モニタ・地震情報／**同時に成立したら緊急度の順**で最初の 1 つ（順序を決めないと EEW 発報中に「ありました」へ落ちる）／**EEW が複数なら最も深刻なもの**（受け取った順で打ち切ると後発のより強い地震を見落とす）／**設定で「出さない」に切り替えたら出ているものも引っ込める**（判定側の早期 return だけでは開いた帯が残る）／返すのは真偽ではなく**判定に使った範囲の最大震度**（真偽だと表示する震度が判定範囲とずれる）／地点があれば半径 30km で絞る／**「近くに観測点が無い」と「近くの観測点が揺れていない」を分ける**（前者だけ全国基準へ倒す。後者まで倒すと遠方の地震で毎回出て地点を登録した意味が消える。見分けは「電文の点を索引で 1 件でも引けるか」と「半径内に確定メンバーがあるか」）／**「載っていない」から言えることは電文の粒度で決まる**（観測点を持つ電文なら震度1未満と言えるが、区域だけの震度速報は震度3未満までなので閾値がそれより低ければ全国基準へ倒す。保留すると別の地震に先頭を奪われて二度と評価されない）／強震モニタが見るのは**検知エンジンが確定したメンバー**の保持値（生値だと 1 点の跳ね上がりがそのまま表示する震度になる）／**エンジンが詰まった（`stalled`）間は判定を保留し、強震モニタの帯を出している間は地震情報へも落とさない**（揺れの最中に過去形へ差し替わる）／半径内の確定メンバーが**全欠測**なら全国基準へ倒す／**リアルタイム震度は発表震度より高く振れる**ので表示側で指標名を明示する／**震度0 は閾値によらず対象外**（震度0 と震度1 は階級値が同値なので、閾値を震度1 まで下げると平常時のノイズで鳴りっぱなしになる）／観測点名に**県名を含めない**（DMDATA は `pref` が常に空）／区域の点も見る（震度速報は区域しか持たない）／区域を持たない EEW は `eewMaxScale` で全国基準／**階級表に無い震度は判定にも表示にも使わない**（型検査を通らない経路から来る。`eewMaxScaleInfo` と同じ防御）） | [`docs/spec/action-checklist-spec.md`](docs/spec/action-checklist-spec.md) §2・§3・§5 |
| 行動チェックリストを畳んだままにする条件（**3 つとも満たすときだけ**＝いま畳んでいる・期限内・震度が上がっていない／畳んでいる間の揺れで**基準の震度を上書きしない**（余震で下がる値に追随すると小さな揺れで開く）／**リロード時は記録があれば畳んだ状態から始める**（記録の永続化だけでは足りない）／記録の震度も**階級表で検証する**（階級外の値が入ると開き直しが永久に発火しない）／開いている間は期限で消さない／**寿命の延長は表示の更新より先に決める**（「中身が同じなら触らない」で先に抜けると、同じ震度が続く揺れで延長が走らない）／**消したものは出し直さない**（地震情報は最新の 1 件が長く居座るため、覚えていないと消した次の再描画で出し直す。裏返しとして、消えた後に届く同じ地震の遅れた続報も出ない）／強震モニタだけで検知した揺れの識別子は**時刻を丸めない**（丸めると、EEW も地震情報も無い窓で続けて起きた別の地震が同じものとして扱われる）） | [`docs/spec/action-checklist-spec.md`](docs/spec/action-checklist-spec.md) §4 |

### 共有カード

| 項目 | 単一情報源となる仕様書 |
|---|---|
| 共有カードの撮影と出典（WebGL のキャンバスは `render` の同期実行を抜けると空になるので**写し取りは同期の窓で行う**・`preserveDrawingBuffer` は初期化オプションなので実行時に切り替えられない・**撮影はカメラに触らない**（動かす経路を足すなら `camera.ts` の fit 系を通し `beginProgrammaticFlight` の印を渡す。落とすと地図の自動追従が 30 秒止まる）・**撮影中は地図コンテナの親を切り抜く**（出力寸法は画面より大きく、隣のパネルへはみ出す）・**後始末は 1 つずつ囲う**（静止画の除去に届かないと、地図が覆われて変化が画面に現れなくなる）・**出典は描いているものだけを挙げる**（プレート境界の PB2002 は帰属表示が利用条件・レイヤーを足したら `ATTRIBUTION_SOURCES` も見直す）・緊急地震速報の注意文の要否は**モードではなく発報の有無**で決める（予報円は全モードで描かれる）・**仮定震源要素では規模を出さない**（画像は訂正できない）・津波の観測棒は DOM マーカーなので 2D で描き直す・**本文は見出しと同じ材料で作る**（画像と違うことを述べない）・**注意文と出典は本文に入れない**（画像に焼いてあり、重ねると X の全角 140 字に触れて利用者が削ることになる）・URL は常に standard 版（DMDSS 版は API キーが要る）・共有シートが無ければ本文をクリップボードへ置き、**置けたか確かめてから表示を変える**・その判定に `textCopied` だけを使わない（共有シートが応じた正常系も偽になり、「置けなかった」と同じ扱いになる）） | [`docs/spec/share-card-spec.md`](docs/spec/share-card-spec.md) |

### テストとリプレイ

| 項目 | 単一情報源となる仕様書 |
|---|---|
| テストデータと UI 説明文（テストボタンの `scaleTo` 値等） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §7 |
| テストデータが実電文の形に沿っていること（報番号・発表時刻・`id` は報ごとに進める／**震源時刻も続報でずらす**＝実電文は震源推定が更新されるたび動く（1 日分・VXSE45 の実測で 15 地震のうち 11 件・振れ幅 1〜6 秒）。**逆に地震発現時刻は動かさない**＝同じ実測で 15 地震すべてが不動。発現時刻が震源時刻から遅れる秒数は震源の深さに合う実測値を置く（アプリは最寄り観測点までの距離を知らないので計算では出せない。**深さ 450km の深発地震テストだけは実測が無く Pn 走時からの外挿**）／取消・解除は対象地域を空にする／区域コードは気象庁コード表 12／経路に無い項目を作らない／**津波は「地震 → 到達 → 観測 → 発表」の順に時刻を並べ、識別子は原因地震の発現時刻から作る**＝押した時刻から作ると地震より前に津波が到達した形になり、同じ秒に押した地震テストと識別子が一致して原因地震リンクが無関係なカードを指す／**実測は「観測状況を確定した時刻」より後にしない**＝最大波の観測時刻が発表より後だとまだ来ていない波を観測したことになる／**区域と地点の到達予想も未来にする**＝実測が届いた地点の到達予想は消える。区域の到達状況と実測の組み合わせは → [`tsunami-spec.md`](docs/spec/tsunami-spec.md) §9「区域の到達状況」） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §7「実電文の形に合わせる」 |
| 初期状態（24 時間）では足りないものを、地震カードの履歴（最大 7 日）から補うこと（対象は長周期地震動と、地震カードの履歴が読むアーカイブに入っている帯＝地震回数・お知らせ・南海トラフ臨時情報・南海トラフ解説情報・後発地震注意情報（実装は `HISTORY_EXTRA_TYPES` が単一情報源）／**同じアーカイブを既に読んでいるので追加の通信は要らない**／**初期状態で流した種別は補わない**＝あちらが新しいので古い報で上書きしてしまう。注入も初期状態より後に置く／**種別ごとに最新 1 通へ畳む**（長周期は地震ごと・帯は種別だけ）／**地震カードの打ち切りを帯と長周期には適用しない**＝地震が多い期間ほど早く打ち切られ、いちばん復元したい群発の最中に復元できなくなる／**推計震度分布図は対象にしない**＝最新 1 通しか持たない設計で過去のカードには紐づかず、ライブの初回履歴でも復元していない。**見たうえで外している**／**ライブの初回履歴にも同じ穴があった**＝地震回数とお知らせを復元しておらず、7 日表示される帯がリロードで消えていた） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §6「初期状態（24 時間）では足りないものを、履歴の遡り（7 日）から補う」 |
| リプレイ開始時の地震カードの厚み（一覧は**件数基準**で別途復元し、初期状態の 24 時間とは目的も遡り幅も分ける・再生中は「もっと見る」を出さない（押すとライブの最新履歴が混ざる）・履歴の取得が失敗しても再生は始める） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §6「地震カードの履歴は件数で遡る」 |
| 実地震テストシナリオの時刻シフト・ID 再採番・利用規約制約（**電文から新しく時刻を読むようにしたらシフトの対象へ足すこと**＝フィールドを 1 つずつ書き並べる形なので、足し忘れても型検査もテストも通り、**その時刻だけが収録当時の絶対値のまま画面に出る**。エラーは出ず、隣のシフト済みの時刻と食い違って見えるだけ） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §6 |
| テスト時刻設定のバリアント差（standard は P2PQuake の日付クエリ、DMDSS は DMDATA アーカイブ＋当日ぶんの別経路／ただし EEW はどちらも取得元が異なり、standard では強震モニタ側の検知に頼る） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §6「テスト時刻設定」 |
| archive リプレイの重複排除（目録に載る XML 版／JSON 版のどちらを採用するか）・失敗の封じ込め範囲（アーカイブが全滅しても強震モニタの再生は止めない） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §6「テスト時刻設定」配下 |
| アーカイブが無い日を埋める当日経路（**日付の基準が API ごとに違う**＝アーカイブの `date` は JST 日／`/v2/telegram`・`/v2/gd/eew` の `datetime` は UTC の半開区間・担当日の排他は `resolveLiveDates` の 1 箇所に集約・EEW だけ `/v2/gd/eew` を辿るのは電文一覧が EEW を保持しないため・**VXSE43 はライブを含むどの経路でも取り込まない**（対応する VXSE45 の警報報と同一内容の複製が遅れて届き、`eventId` で束ねる EEW を古い内容で上書きして区域塗りを削るため。報番号の系列も独立している）） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §6「当日ぶんの取得元」 |
| リプレイの取りこぼしの数え方（取得元と電文単位を分ける・取得元は識別子で数える・**「すべて読めない」の分母は取得元の日数**で取る（アーカイブの本数で数えると、取得元が当日経路 1 本しか無い窓で電文 0 件のまま「再生中」になる）・**当日経路の 3 本の一覧は互いに隔離する**（隔離しないと EEW の一覧の一時障害で地震も津波も消え、上の分母と噛み合って再生ごと止まる）） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §6「取りこぼしの扱い」 |
| `ConnectionStatus` の `replay`（再生中はライブ接続を切るため専用表示にする。`disconnected` と混ぜると地図に切断警告が出る） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §6「再生中はライブ接続を止める（接続状態の表示）」 |
| 再生中の予約の時刻軸（EEW の自動解除・津波の失効を再生時計の「いま」へ潰さない。潰すと最終報の直後に EEW が消え、猶予が無くなる） | [`docs/spec/settings-pwa-spec.md`](docs/spec/settings-pwa-spec.md) §6「再生中も予約は発火時刻を待つ」 |

各仕様書は冒頭で「食い違う場合は実コードを正とする」と宣言している。放置すると即座に嘘になる文書なので、実装変更時に必ず追従させる。


## dotagents 共通Codex基準

この節は `dotagents` から配布する共通規約であり、インポート時に生成された古い指示と矛盾する場合はこちらを優先する。

- 合意済みの対象範囲・深さ・完了条件を、実装側の判断だけで狭めない。変更が必要になったら、根拠と影響を示して確認を取る。
- 機能の削除・代替・未使用判定の前に、呼び出し元・設定・利用者向け導線を調べ、裏付けを残す。
- 変更に対応するテストを追加または更新し、利用者向け挙動が変わる場合は、ビルドや型検査だけで済ませず安全に実行可能な動作確認も行う。
- レビューは変更の重要度に見合う回数で収束させる。収束しないときは直し方ではなく設計を疑い、まず設計を変える。
- 設計変更が対象範囲を超える場合、直さねばならないものは範囲の拡大を相談し、そうでないものは別タスクへ切り出す。
- 未解決の問題や実施できなかった検証は、何をなぜ残すかを明記して引き継ぐ。
- カスタムエージェントは `.codex/agents/`、再利用手順は `.agents/skills/` を使う。移植元に固有の、利用可否や形式を保証できない操作には依存しない。worktreeはGitで管理し、確認が必要な場面では通常の対話でユーザーに尋ねる。
- コミット、push、マージ、リリース、外部サービスへの変更、依存関係追加、破壊的操作は、ユーザーの明示的な承認なしに実行しない。